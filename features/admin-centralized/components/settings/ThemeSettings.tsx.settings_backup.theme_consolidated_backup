import { createTheme } from '@mui/material/styles';

export const ThemeSettings = () => {
  return createTheme({
    palette: {
      mode: 'light',
      primary: {
        main: '#6B21A8',
      },
      secondary: {
        main: '#FFD700',
      },
    },
  });
};