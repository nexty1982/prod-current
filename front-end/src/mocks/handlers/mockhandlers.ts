// Mock handlers - currently empty since we're using real backend APIs
// Add specific mock handlers here if needed for development/testing

export const mockHandlers = [
  // No mock handlers needed - using real backend APIs
];
