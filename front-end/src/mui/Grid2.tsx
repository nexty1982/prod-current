/**
 * Grid2 Re-export
 * 
 * Re-exports Grid2 from the compat version for convenience.
 * This allows importing from @/mui/Grid2 instead of the full path.
 */

export { default } from '@/components/compat/Grid2';
export { default as Grid2 } from '@/components/compat/Grid2';
