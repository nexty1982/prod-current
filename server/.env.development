# Development Environment Configuration for OrthodoxMetrics
# Set NODE_ENV first (this tells the app to load .env.development)
export NODE_ENV=development

# Verify it's set
echo "NODE_ENV: $NODE_ENV"
pkill -f "node.*app.js"
node app.js

DB_HOST=localhost
DB_NAME=orthodmetrics_dev
DB_USER=orthodapps
DB_PASSWORD=Summerof1982@!

# Root Database Access (for database creation)
ROOT_DB_USER=root
ROOT_DB_PASSWORD=Summerof1982@!

# Session Configuration (CHANGE THESE IN PRODUCTION!)
SESSION_SECRET=9d7f4d9b84f74d28a693cbe843f928efad7e3bbd6f25f901a3c0e6f9f91e99e7
SESSION_KEY=orthodmetrics.sid

# Server Configuration
PORT=3002
HOST=0.0.0.0
HTTPS=false
SSL_ENABLED=false

# API Configuration
API_BASE_URL=http://192.168.1.240

# CORS Configuration
CORS_ORIGIN=http://localhost


# Email Configuration
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@orthodoxmetrics.com

# File Upload Configuration
UPLOAD_MAX_SIZE=10485760
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/gif,application/pdf

# Logging
LOG_LEVEL=info
LOG_FILE=logs/development.log
