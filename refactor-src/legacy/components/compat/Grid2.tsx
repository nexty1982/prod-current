import Grid2 from '@mui/material/Grid2';

// Grid2 compatibility wrapper for MUI v7
// This provides a Grid2 component that uses the regular Grid with compatible props
const Grid2 = Grid;

export default Grid2;
