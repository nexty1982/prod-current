diff --git a/MenuItems.ts b/MenuItems.ts
index c15a1027..2b01507b 100755
--- a/MenuItems.ts
+++ b/MenuItems.ts
@@ -443,7 +443,7 @@ const Menuitems: MenuitemsType[] = [
       },
       {
         id: uniqueId(),
-        title: 'Baptism Record Entry',
+        title: 'Baptism Record Entry (New)',
         icon: IconForms,
         href: '/apps/records/baptism/new',
       },
diff --git a/front-end/Router.tsx b/front-end/Router.tsx
index 659fff8d..86a157a4 100755
--- a/front-end/Router.tsx
+++ b/front-end/Router.tsx
@@ -152,6 +152,7 @@ const UnifiedRecordsPage = Loadable(lazy(() => import('../views/records/UnifiedR
 const ChurchAdminList = Loadable(lazy(() => import('../views/admin/ChurchAdminList')));
 const ChurchAdminPanel = Loadable(lazy(() => import('../views/admin/ChurchAdminPanelWorking')));
 const ChurchRecordsPage = Loadable(lazy(() => import('../views/records/ChurchRecordsPage')));
+const BaptismRecordEntryPage = Loadable(lazy(() => import('../features/records/baptism/BaptismRecordEntryPage')));
 
 // New Records Management (Shop Layout)
 const RecordsManagement = Loadable(lazy(() => import('../pages/apps/records/index')));
@@ -1083,6 +1084,14 @@ const Router = [
           </ProtectedRoute>
         )
       },
+      {
+        path: '/apps/records/baptism/new',
+        element: (
+          <ProtectedRoute requiredRole={['admin', 'super_admin', 'church_admin', 'priest', 'deacon', 'editor']}>
+            <BaptismRecordEntryPage />
+          </ProtectedRoute>
+        )
+      },
       {
         path: '/apps/records/marriage',
         element: (
diff --git a/front-end/package.json b/front-end/package.json
index d0ef9481..ce71193c 100755
--- a/front-end/package.json
+++ b/front-end/package.json
@@ -8,7 +8,7 @@
     "dev": "vite --mode development --host 0.0.0.0 --port 5174 --config vite.config.dev.ts",
     "dev:host": "vite --mode development --host 0.0.0.0 --port 5174",
     "dev:test": "vite --mode development --port 5177",
-    "build": "NODE_OPTIONS=\"--max-old-space-size=4096\" vite build --mode production && node scripts/verify-build.js",
+    "build": "NODE_OPTIONS=\"--max-old-space-size=4096\" vite build --mode production && node scripts/inject-build-info.js && node scripts/verify-build.js",
     "build:clean": "rm -rf dist && NODE_OPTIONS=\"--max-old-space-size=4096\" vite build --mode production && node scripts/verify-build.js",
     "build:watch": "NODE_OPTIONS=\"--max-old-space-size=4096\" vite build --mode production --watch",
     "build:verify": "node scripts/verify-build.js",
diff --git a/front-end/public/Screenshot 2025-12-22 212219.jpg b/front-end/public/Screenshot 2025-12-22 212219.jpg
old mode 100644
new mode 100755
diff --git a/front-end/public/images/._bg-new.png b/front-end/public/images/._bg-new.png
deleted file mode 100755
index 15dfd5cc..00000000
Binary files a/front-end/public/images/._bg-new.png and /dev/null differ
diff --git a/front-end/public/images/._bgtiled.png b/front-end/public/images/._bgtiled.png
deleted file mode 100755
index e45472a8..00000000
Binary files a/front-end/public/images/._bgtiled.png and /dev/null differ
diff --git a/front-end/public/images/404-orthodox.png b/front-end/public/images/404-orthodox.png
deleted file mode 100755
index 45b999a8..00000000
Binary files a/front-end/public/images/404-orthodox.png and /dev/null differ
diff --git a/front-end/public/images/EN/H.png b/front-end/public/images/EN/H.png
deleted file mode 100644
index fcd132ef..00000000
Binary files a/front-end/public/images/EN/H.png and /dev/null differ
diff --git a/front-end/public/images/EN/baptism_form.png b/front-end/public/images/EN/baptism_form.png
deleted file mode 100644
index abe22017..00000000
Binary files a/front-end/public/images/EN/baptism_form.png and /dev/null differ
diff --git a/front-end/public/images/EN/nj.png b/front-end/public/images/EN/nj.png
deleted file mode 100644
index d33d8b3b..00000000
Binary files a/front-end/public/images/EN/nj.png and /dev/null differ
diff --git a/front-end/public/images/EN/ssppoc-h1.png b/front-end/public/images/EN/ssppoc-h1.png
deleted file mode 100644
index 466861f6..00000000
Binary files a/front-end/public/images/EN/ssppoc-h1.png and /dev/null differ
diff --git a/front-end/public/images/GE/church-s1.png b/front-end/public/images/GE/church-s1.png
deleted file mode 100755
index f5f99701..00000000
Binary files a/front-end/public/images/GE/church-s1.png and /dev/null differ
diff --git a/front-end/public/images/GE/ge-header.png b/front-end/public/images/GE/ge-header.png
deleted file mode 100755
index 7f368d0b..00000000
Binary files a/front-end/public/images/GE/ge-header.png and /dev/null differ
diff --git a/front-end/public/images/GR/funeralt1.png b/front-end/public/images/GR/funeralt1.png
deleted file mode 100755
index 975e92c5..00000000
Binary files a/front-end/public/images/GR/funeralt1.png and /dev/null differ
diff --git a/front-end/public/images/GR/gr-church-s1.png b/front-end/public/images/GR/gr-church-s1.png
deleted file mode 100755
index 90d46a2c..00000000
Binary files a/front-end/public/images/GR/gr-church-s1.png and /dev/null differ
diff --git a/front-end/public/images/GR/gr-header.png b/front-end/public/images/GR/gr-header.png
deleted file mode 100755
index 3e339407..00000000
Binary files a/front-end/public/images/GR/gr-header.png and /dev/null differ
diff --git a/front-end/public/images/GR/mtathos.png b/front-end/public/images/GR/mtathos.png
deleted file mode 100755
index c6ab9818..00000000
Binary files a/front-end/public/images/GR/mtathos.png and /dev/null differ
diff --git a/front-end/public/images/RO/ro-church-s1.png b/front-end/public/images/RO/ro-church-s1.png
deleted file mode 100755
index 82be9c29..00000000
Binary files a/front-end/public/images/RO/ro-church-s1.png and /dev/null differ
diff --git a/front-end/public/images/RU/RU-header.png b/front-end/public/images/RU/RU-header.png
deleted file mode 100755
index cf94bf6a..00000000
Binary files a/front-end/public/images/RU/RU-header.png and /dev/null differ
diff --git a/front-end/public/images/Saints/Georgian/giorgi-saba.png b/front-end/public/images/Saints/Georgian/giorgi-saba.png
deleted file mode 100755
index c54217fe..00000000
Binary files a/front-end/public/images/Saints/Georgian/giorgi-saba.png and /dev/null differ
diff --git a/front-end/public/images/Saints/Greece-Ancient-Thebe/venerable-patappius-thebes.png b/front-end/public/images/Saints/Greece-Ancient-Thebe/venerable-patappius-thebes.png
deleted file mode 100755
index 9c1041d2..00000000
Binary files a/front-end/public/images/Saints/Greece-Ancient-Thebe/venerable-patappius-thebes.png and /dev/null differ
diff --git a/front-end/public/images/Saints/Greece-Ancient-Thebe/venerable-patappius-thebes2.png b/front-end/public/images/Saints/Greece-Ancient-Thebe/venerable-patappius-thebes2.png
deleted file mode 100755
index 78b80da3..00000000
Binary files a/front-end/public/images/Saints/Greece-Ancient-Thebe/venerable-patappius-thebes2.png and /dev/null differ
diff --git a/front-end/public/images/TMP2.png b/front-end/public/images/TMP2.png
deleted file mode 100755
index d3b2c43d..00000000
Binary files a/front-end/public/images/TMP2.png and /dev/null differ
diff --git a/front-end/public/images/baptisms-post.png b/front-end/public/images/baptisms-post.png
deleted file mode 100755
index e34de6cf..00000000
Binary files a/front-end/public/images/baptisms-post.png and /dev/null differ
diff --git a/front-end/public/images/baptisms-pre.png b/front-end/public/images/baptisms-pre.png
deleted file mode 100755
index e6ad54d0..00000000
Binary files a/front-end/public/images/baptisms-pre.png and /dev/null differ
diff --git a/front-end/public/images/bg-new.png b/front-end/public/images/bg-new.png
deleted file mode 100755
index 7990e1ac..00000000
Binary files a/front-end/public/images/bg-new.png and /dev/null differ
diff --git a/front-end/public/images/bgtiled1.png b/front-end/public/images/bgtiled1.png
deleted file mode 100755
index 24a5e494..00000000
Binary files a/front-end/public/images/bgtiled1.png and /dev/null differ
diff --git a/front-end/public/images/bgtiled2.png b/front-end/public/images/bgtiled2.png
deleted file mode 100755
index 76d74b21..00000000
Binary files a/front-end/public/images/bgtiled2.png and /dev/null differ
diff --git a/front-end/public/images/bgtiled3.png b/front-end/public/images/bgtiled3.png
deleted file mode 100755
index 24a5e494..00000000
Binary files a/front-end/public/images/bgtiled3.png and /dev/null differ
diff --git a/front-end/public/images/bgtiled6.png b/front-end/public/images/bgtiled6.png
deleted file mode 100755
index 5d7152ca..00000000
Binary files a/front-end/public/images/bgtiled6.png and /dev/null differ
diff --git a/front-end/public/images/big/orthodox-colors.png b/front-end/public/images/big/orthodox-colors.png
deleted file mode 100755
index ccd0e841..00000000
Binary files a/front-end/public/images/big/orthodox-colors.png and /dev/null differ
diff --git a/front-end/public/images/big/sample-image-2.png b/front-end/public/images/big/sample-image-2.png
deleted file mode 100755
index 6603b464..00000000
Binary files a/front-end/public/images/big/sample-image-2.png and /dev/null differ
diff --git a/front-end/public/images/big/sample-marriage-1.png b/front-end/public/images/big/sample-marriage-1.png
deleted file mode 100755
index d3069feb..00000000
Binary files a/front-end/public/images/big/sample-marriage-1.png and /dev/null differ
diff --git a/front-end/public/images/big/ssppoc-h2.png b/front-end/public/images/big/ssppoc-h2.png
deleted file mode 100755
index 670e230e..00000000
Binary files a/front-end/public/images/big/ssppoc-h2.png and /dev/null differ
diff --git a/front-end/public/images/big/ssppoc-logo.png b/front-end/public/images/big/ssppoc-logo.png
deleted file mode 100755
index da09e9cc..00000000
Binary files a/front-end/public/images/big/ssppoc-logo.png and /dev/null differ
diff --git a/front-end/public/images/big/stpaul.png b/front-end/public/images/big/stpaul.png
deleted file mode 100755
index f46ed459..00000000
Binary files a/front-end/public/images/big/stpaul.png and /dev/null differ
diff --git a/front-end/public/images/big/stpeter.png b/front-end/public/images/big/stpeter.png
deleted file mode 100755
index 0578fa92..00000000
Binary files a/front-end/public/images/big/stpeter.png and /dev/null differ
diff --git a/front-end/public/images/big/test-records.png b/front-end/public/images/big/test-records.png
deleted file mode 100755
index d4c88084..00000000
Binary files a/front-end/public/images/big/test-records.png and /dev/null differ
diff --git a/front-end/public/images/buildings/ge_church1.png b/front-end/public/images/buildings/ge_church1.png
deleted file mode 100755
index d8287940..00000000
Binary files a/front-end/public/images/buildings/ge_church1.png and /dev/null differ
diff --git a/front-end/public/images/buildings/ge_map1.png b/front-end/public/images/buildings/ge_map1.png
deleted file mode 100755
index 4ec1b828..00000000
Binary files a/front-end/public/images/buildings/ge_map1.png and /dev/null differ
diff --git a/front-end/public/images/buildings/gr-map1.png b/front-end/public/images/buildings/gr-map1.png
deleted file mode 100755
index 1b25946e..00000000
Binary files a/front-end/public/images/buildings/gr-map1.png and /dev/null differ
diff --git a/front-end/public/images/buildings/gr_church1.png b/front-end/public/images/buildings/gr_church1.png
deleted file mode 100755
index 42a31cde..00000000
Binary files a/front-end/public/images/buildings/gr_church1.png and /dev/null differ
diff --git a/front-end/public/images/buildings/greece.png b/front-end/public/images/buildings/greece.png
deleted file mode 100755
index 4315929b..00000000
Binary files a/front-end/public/images/buildings/greece.png and /dev/null differ
diff --git a/front-end/public/images/buildings/nj_map1.png b/front-end/public/images/buildings/nj_map1.png
deleted file mode 100755
index e510acec..00000000
Binary files a/front-end/public/images/buildings/nj_map1.png and /dev/null differ
diff --git a/front-end/public/images/buildings/om_hq.png b/front-end/public/images/buildings/om_hq.png
deleted file mode 100755
index a379318b..00000000
Binary files a/front-end/public/images/buildings/om_hq.png and /dev/null differ
diff --git a/front-end/public/images/buildings/ro-church1.png b/front-end/public/images/buildings/ro-church1.png
deleted file mode 100755
index fe92ee7e..00000000
Binary files a/front-end/public/images/buildings/ro-church1.png and /dev/null differ
diff --git a/front-end/public/images/buildings/ro_map1.png b/front-end/public/images/buildings/ro_map1.png
deleted file mode 100755
index 1adc7fb3..00000000
Binary files a/front-end/public/images/buildings/ro_map1.png and /dev/null differ
diff --git a/front-end/public/images/buildings/ru-church2.png b/front-end/public/images/buildings/ru-church2.png
deleted file mode 100755
index cf815404..00000000
Binary files a/front-end/public/images/buildings/ru-church2.png and /dev/null differ
diff --git a/front-end/public/images/buildings/ru_church1.png b/front-end/public/images/buildings/ru_church1.png
deleted file mode 100755
index 2f7fc859..00000000
Binary files a/front-end/public/images/buildings/ru_church1.png and /dev/null differ
diff --git a/front-end/public/images/buildings/ru_map1.png b/front-end/public/images/buildings/ru_map1.png
deleted file mode 100755
index 11c63ab1..00000000
Binary files a/front-end/public/images/buildings/ru_map1.png and /dev/null differ
diff --git a/front-end/public/images/buildings/ssppoc-small.png b/front-end/public/images/buildings/ssppoc-small.png
deleted file mode 100755
index a47d55bf..00000000
Binary files a/front-end/public/images/buildings/ssppoc-small.png and /dev/null differ
diff --git a/front-end/public/images/buildings/ssppoc_church.png b/front-end/public/images/buildings/ssppoc_church.png
deleted file mode 100755
index 6a6f7948..00000000
Binary files a/front-end/public/images/buildings/ssppoc_church.png and /dev/null differ
diff --git a/front-end/public/images/buttons/._4buttons.png b/front-end/public/images/buttons/._4buttons.png
deleted file mode 100755
index ebad79ce..00000000
Binary files a/front-end/public/images/buttons/._4buttons.png and /dev/null differ
diff --git a/front-end/public/images/buttons/4buttons.png b/front-end/public/images/buttons/4buttons.png
deleted file mode 100755
index 47671895..00000000
Binary files a/front-end/public/images/buttons/4buttons.png and /dev/null differ
diff --git a/front-end/public/images/buttons/GE-buttons-1.png b/front-end/public/images/buttons/GE-buttons-1.png
deleted file mode 100755
index d05cbfda..00000000
Binary files a/front-end/public/images/buttons/GE-buttons-1.png and /dev/null differ
diff --git a/front-end/public/images/buttons/GR-add-record.png b/front-end/public/images/buttons/GR-add-record.png
deleted file mode 100755
index 86d843ce..00000000
Binary files a/front-end/public/images/buttons/GR-add-record.png and /dev/null differ
diff --git a/front-end/public/images/buttons/GR-add-record1.png b/front-end/public/images/buttons/GR-add-record1.png
deleted file mode 100755
index 46f9ced8..00000000
Binary files a/front-end/public/images/buttons/GR-add-record1.png and /dev/null differ
diff --git a/front-end/public/images/buttons/GR-light-blue.png b/front-end/public/images/buttons/GR-light-blue.png
deleted file mode 100755
index 1ab57172..00000000
Binary files a/front-end/public/images/buttons/GR-light-blue.png and /dev/null differ
diff --git a/front-end/public/images/buttons/RO-button-1.png b/front-end/public/images/buttons/RO-button-1.png
deleted file mode 100755
index f3c6c673..00000000
Binary files a/front-end/public/images/buttons/RO-button-1.png and /dev/null differ
diff --git a/front-end/public/images/buttons/RU-buttons-1.png b/front-end/public/images/buttons/RU-buttons-1.png
deleted file mode 100755
index 7cef5e5b..00000000
Binary files a/front-end/public/images/buttons/RU-buttons-1.png and /dev/null differ
diff --git a/front-end/public/images/buttons/RU-buttons-2.png b/front-end/public/images/buttons/RU-buttons-2.png
deleted file mode 100755
index b5bb3011..00000000
Binary files a/front-end/public/images/buttons/RU-buttons-2.png and /dev/null differ
diff --git a/front-end/public/images/buttons/add-record-base.png b/front-end/public/images/buttons/add-record-base.png
deleted file mode 100755
index aba1dbe0..00000000
Binary files a/front-end/public/images/buttons/add-record-base.png and /dev/null differ
diff --git a/front-end/public/images/buttons/church-login.png b/front-end/public/images/buttons/church-login.png
deleted file mode 100755
index 1406b61d..00000000
Binary files a/front-end/public/images/buttons/church-login.png and /dev/null differ
diff --git a/front-end/public/images/buttons/faq.png b/front-end/public/images/buttons/faq.png
deleted file mode 100755
index 7378bb15..00000000
Binary files a/front-end/public/images/buttons/faq.png and /dev/null differ
diff --git a/front-end/public/images/buttons/get-started.png b/front-end/public/images/buttons/get-started.png
deleted file mode 100755
index c19d5a2b..00000000
Binary files a/front-end/public/images/buttons/get-started.png and /dev/null differ
diff --git a/front-end/public/images/buttons/home.png b/front-end/public/images/buttons/home.png
deleted file mode 100755
index 4c48749d..00000000
Binary files a/front-end/public/images/buttons/home.png and /dev/null differ
diff --git a/front-end/public/images/buttons/register.png b/front-end/public/images/buttons/register.png
deleted file mode 100755
index 38b8c6c5..00000000
Binary files a/front-end/public/images/buttons/register.png and /dev/null differ
diff --git a/front-end/public/images/cemetery-cross.png b/front-end/public/images/cemetery-cross.png
deleted file mode 100755
index 25bb17bc..00000000
Binary files a/front-end/public/images/cemetery-cross.png and /dev/null differ
diff --git a/front-end/public/images/circ-graphic.png b/front-end/public/images/circ-graphic.png
deleted file mode 100755
index 2cf5b01b..00000000
Binary files a/front-end/public/images/circ-graphic.png and /dev/null differ
diff --git a/front-end/public/images/cross-buttons.png b/front-end/public/images/cross-buttons.png
deleted file mode 100755
index 47671895..00000000
Binary files a/front-end/public/images/cross-buttons.png and /dev/null differ
diff --git a/front-end/public/images/crosses/b_records.png b/front-end/public/images/crosses/b_records.png
deleted file mode 100755
index ed913a4c..00000000
Binary files a/front-end/public/images/crosses/b_records.png and /dev/null differ
diff --git a/front-end/public/images/crosses/cross-header.jpg b/front-end/public/images/crosses/cross-header.jpg
deleted file mode 100755
index 520d2ccf..00000000
Binary files a/front-end/public/images/crosses/cross-header.jpg and /dev/null differ
diff --git a/front-end/public/images/crosses/cross.png b/front-end/public/images/crosses/cross.png
deleted file mode 100755
index f59fc51e..00000000
Binary files a/front-end/public/images/crosses/cross.png and /dev/null differ
diff --git a/front-end/public/images/crosses/en_baptism.png b/front-end/public/images/crosses/en_baptism.png
deleted file mode 100755
index 48b299d3..00000000
Binary files a/front-end/public/images/crosses/en_baptism.png and /dev/null differ
diff --git a/front-end/public/images/crosses/en_cross.png b/front-end/public/images/crosses/en_cross.png
deleted file mode 100755
index 703e9e50..00000000
Binary files a/front-end/public/images/crosses/en_cross.png and /dev/null differ
diff --git a/front-end/public/images/crosses/en_funeral.png b/front-end/public/images/crosses/en_funeral.png
deleted file mode 100755
index 131d6ec0..00000000
Binary files a/front-end/public/images/crosses/en_funeral.png and /dev/null differ
diff --git a/front-end/public/images/crosses/en_marriage.png b/front-end/public/images/crosses/en_marriage.png
deleted file mode 100755
index 41f9904f..00000000
Binary files a/front-end/public/images/crosses/en_marriage.png and /dev/null differ
diff --git a/front-end/public/images/crosses/f_records.png b/front-end/public/images/crosses/f_records.png
deleted file mode 100755
index 1a31a91b..00000000
Binary files a/front-end/public/images/crosses/f_records.png and /dev/null differ
diff --git a/front-end/public/images/crosses/gr_baptism.png b/front-end/public/images/crosses/gr_baptism.png
deleted file mode 100755
index 3ad6384c..00000000
Binary files a/front-end/public/images/crosses/gr_baptism.png and /dev/null differ
diff --git a/front-end/public/images/crosses/gr_cross.png b/front-end/public/images/crosses/gr_cross.png
deleted file mode 100755
index 8f8576d8..00000000
Binary files a/front-end/public/images/crosses/gr_cross.png and /dev/null differ
diff --git a/front-end/public/images/crosses/gr_funeral.png b/front-end/public/images/crosses/gr_funeral.png
deleted file mode 100755
index 652e111a..00000000
Binary files a/front-end/public/images/crosses/gr_funeral.png and /dev/null differ
diff --git a/front-end/public/images/crosses/gr_marriage.png b/front-end/public/images/crosses/gr_marriage.png
deleted file mode 100755
index e94bd37b..00000000
Binary files a/front-end/public/images/crosses/gr_marriage.png and /dev/null differ
diff --git a/front-end/public/images/crosses/greek-blue-stone.png b/front-end/public/images/crosses/greek-blue-stone.png
deleted file mode 100755
index b0da42d4..00000000
Binary files a/front-end/public/images/crosses/greek-blue-stone.png and /dev/null differ
diff --git a/front-end/public/images/crosses/m_records.png b/front-end/public/images/crosses/m_records.png
deleted file mode 100755
index 3395437e..00000000
Binary files a/front-end/public/images/crosses/m_records.png and /dev/null differ
diff --git a/front-end/public/images/crosses/ro_baptism.png b/front-end/public/images/crosses/ro_baptism.png
deleted file mode 100755
index ffb9705b..00000000
Binary files a/front-end/public/images/crosses/ro_baptism.png and /dev/null differ
diff --git a/front-end/public/images/crosses/ro_cross.png b/front-end/public/images/crosses/ro_cross.png
deleted file mode 100755
index 28b627a6..00000000
Binary files a/front-end/public/images/crosses/ro_cross.png and /dev/null differ
diff --git a/front-end/public/images/crosses/ro_flag.png b/front-end/public/images/crosses/ro_flag.png
deleted file mode 100755
index 4ccdfa42..00000000
Binary files a/front-end/public/images/crosses/ro_flag.png and /dev/null differ
diff --git a/front-end/public/images/crosses/ro_funeral.png b/front-end/public/images/crosses/ro_funeral.png
deleted file mode 100755
index c6d52bb0..00000000
Binary files a/front-end/public/images/crosses/ro_funeral.png and /dev/null differ
diff --git a/front-end/public/images/crosses/ro_marriage.png b/front-end/public/images/crosses/ro_marriage.png
deleted file mode 100755
index 208670ad..00000000
Binary files a/front-end/public/images/crosses/ro_marriage.png and /dev/null differ
diff --git a/front-end/public/images/crosses/ru_baptism.png b/front-end/public/images/crosses/ru_baptism.png
deleted file mode 100755
index 4e05a0a5..00000000
Binary files a/front-end/public/images/crosses/ru_baptism.png and /dev/null differ
diff --git a/front-end/public/images/crosses/ru_cross.png b/front-end/public/images/crosses/ru_cross.png
deleted file mode 100755
index a738680f..00000000
Binary files a/front-end/public/images/crosses/ru_cross.png and /dev/null differ
diff --git a/front-end/public/images/crosses/ru_funeral.png b/front-end/public/images/crosses/ru_funeral.png
deleted file mode 100755
index 900c42ee..00000000
Binary files a/front-end/public/images/crosses/ru_funeral.png and /dev/null differ
diff --git a/front-end/public/images/crosses/ru_marriage.png b/front-end/public/images/crosses/ru_marriage.png
deleted file mode 100755
index 98c668bf..00000000
Binary files a/front-end/public/images/crosses/ru_marriage.png and /dev/null differ
diff --git a/front-end/public/images/crosses/sand-dune1.png b/front-end/public/images/crosses/sand-dune1.png
deleted file mode 100755
index 0ad86d6e..00000000
Binary files a/front-end/public/images/crosses/sand-dune1.png and /dev/null differ
diff --git a/front-end/public/images/crosses/scircle-logo.png b/front-end/public/images/crosses/scircle-logo.png
deleted file mode 100755
index ae97e72a..00000000
Binary files a/front-end/public/images/crosses/scircle-logo.png and /dev/null differ
diff --git a/front-end/public/images/fb/fallback-ag-logo-bg.png b/front-end/public/images/fb/fallback-ag-logo-bg.png
deleted file mode 100755
index fb75a146..00000000
Binary files a/front-end/public/images/fb/fallback-ag-logo-bg.png and /dev/null differ
diff --git a/front-end/public/images/funerals.png b/front-end/public/images/funerals.png
deleted file mode 100755
index 144cabf8..00000000
Binary files a/front-end/public/images/funerals.png and /dev/null differ
diff --git a/front-end/public/images/gallery/1.png b/front-end/public/images/gallery/1.png
deleted file mode 100755
index 49998c3b..00000000
Binary files a/front-end/public/images/gallery/1.png and /dev/null differ
diff --git a/front-end/public/images/gallery/4buttons.png b/front-end/public/images/gallery/4buttons.png
deleted file mode 100755
index 47671895..00000000
Binary files a/front-end/public/images/gallery/4buttons.png and /dev/null differ
diff --git a/front-end/public/images/gallery/5.png b/front-end/public/images/gallery/5.png
deleted file mode 100755
index 0b755c64..00000000
Binary files a/front-end/public/images/gallery/5.png and /dev/null differ
diff --git a/front-end/public/images/gallery/6.png b/front-end/public/images/gallery/6.png
deleted file mode 100755
index c24d45b5..00000000
Binary files a/front-end/public/images/gallery/6.png and /dev/null differ
diff --git a/front-end/public/images/gallery/7.png b/front-end/public/images/gallery/7.png
deleted file mode 100755
index 5b9288a6..00000000
Binary files a/front-end/public/images/gallery/7.png and /dev/null differ
diff --git a/front-end/public/images/gallery/8.png b/front-end/public/images/gallery/8.png
deleted file mode 100755
index 1bbf8c45..00000000
Binary files a/front-end/public/images/gallery/8.png and /dev/null differ
diff --git a/front-end/public/images/gallery/9.png b/front-end/public/images/gallery/9.png
deleted file mode 100755
index b5a0494c..00000000
Binary files a/front-end/public/images/gallery/9.png and /dev/null differ
diff --git a/front-end/public/images/gallery/Rectangle.png b/front-end/public/images/gallery/Rectangle.png
deleted file mode 100755
index c943e95f..00000000
Binary files a/front-end/public/images/gallery/Rectangle.png and /dev/null differ
diff --git a/front-end/public/images/gallery/Screenshot_2025-12-06_215016.png b/front-end/public/images/gallery/Screenshot_2025-12-06_215016.png
deleted file mode 100755
index 7d68b828..00000000
Binary files a/front-end/public/images/gallery/Screenshot_2025-12-06_215016.png and /dev/null differ
diff --git a/front-end/public/images/gallery/b_records.png b/front-end/public/images/gallery/b_records.png
deleted file mode 100755
index ed913a4c..00000000
Binary files a/front-end/public/images/gallery/b_records.png and /dev/null differ
diff --git a/front-end/public/images/gallery/baptism-header.png b/front-end/public/images/gallery/baptism-header.png
deleted file mode 100755
index e6ad54d0..00000000
Binary files a/front-end/public/images/gallery/baptism-header.png and /dev/null differ
diff --git a/front-end/public/images/gallery/baptism.png b/front-end/public/images/gallery/baptism.png
deleted file mode 100755
index 8985f192..00000000
Binary files a/front-end/public/images/gallery/baptism.png and /dev/null differ
diff --git a/front-end/public/images/gallery/baptisms.png b/front-end/public/images/gallery/baptisms.png
deleted file mode 100755
index e6ad54d0..00000000
Binary files a/front-end/public/images/gallery/baptisms.png and /dev/null differ
diff --git a/front-end/public/images/gallery/bg-new.png b/front-end/public/images/gallery/bg-new.png
deleted file mode 100755
index 7990e1ac..00000000
Binary files a/front-end/public/images/gallery/bg-new.png and /dev/null differ
diff --git a/front-end/public/images/gallery/bgtiled1.png b/front-end/public/images/gallery/bgtiled1.png
deleted file mode 100755
index ed5e27d4..00000000
Binary files a/front-end/public/images/gallery/bgtiled1.png and /dev/null differ
diff --git a/front-end/public/images/gallery/bgtiled2.png b/front-end/public/images/gallery/bgtiled2.png
deleted file mode 100755
index d3814d1f..00000000
Binary files a/front-end/public/images/gallery/bgtiled2.png and /dev/null differ
diff --git a/front-end/public/images/gallery/buttons_bg.png b/front-end/public/images/gallery/buttons_bg.png
deleted file mode 100755
index 7cbe0577..00000000
Binary files a/front-end/public/images/gallery/buttons_bg.png and /dev/null differ
diff --git a/front-end/public/images/gallery/cemetery-cross.png b/front-end/public/images/gallery/cemetery-cross.png
deleted file mode 100755
index 25bb17bc..00000000
Binary files a/front-end/public/images/gallery/cemetery-cross.png and /dev/null differ
diff --git a/front-end/public/images/gallery/church-login.png b/front-end/public/images/gallery/church-login.png
deleted file mode 100755
index 1406b61d..00000000
Binary files a/front-end/public/images/gallery/church-login.png and /dev/null differ
diff --git a/front-end/public/images/gallery/cross-buttons.png b/front-end/public/images/gallery/cross-buttons.png
deleted file mode 100755
index 47671895..00000000
Binary files a/front-end/public/images/gallery/cross-buttons.png and /dev/null differ
diff --git a/front-end/public/images/gallery/cross-gold.png b/front-end/public/images/gallery/cross-gold.png
deleted file mode 100755
index 8b079725..00000000
Binary files a/front-end/public/images/gallery/cross-gold.png and /dev/null differ
diff --git a/front-end/public/images/gallery/cross-transparent.png b/front-end/public/images/gallery/cross-transparent.png
deleted file mode 100755
index 9e4add1d..00000000
Binary files a/front-end/public/images/gallery/cross-transparent.png and /dev/null differ
diff --git a/front-end/public/images/gallery/custom.png b/front-end/public/images/gallery/custom.png
deleted file mode 100755
index cbf4e8e5..00000000
Binary files a/front-end/public/images/gallery/custom.png and /dev/null differ
diff --git a/front-end/public/images/gallery/dune-crosses.png b/front-end/public/images/gallery/dune-crosses.png
deleted file mode 100755
index 81bc3b65..00000000
Binary files a/front-end/public/images/gallery/dune-crosses.png and /dev/null differ
diff --git a/front-end/public/images/gallery/en_baptism.png b/front-end/public/images/gallery/en_baptism.png
deleted file mode 100755
index 48b299d3..00000000
Binary files a/front-end/public/images/gallery/en_baptism.png and /dev/null differ
diff --git a/front-end/public/images/gallery/en_cross.png b/front-end/public/images/gallery/en_cross.png
deleted file mode 100755
index 703e9e50..00000000
Binary files a/front-end/public/images/gallery/en_cross.png and /dev/null differ
diff --git a/front-end/public/images/gallery/en_flag.png b/front-end/public/images/gallery/en_flag.png
deleted file mode 100755
index 938bd1b6..00000000
Binary files a/front-end/public/images/gallery/en_flag.png and /dev/null differ
diff --git a/front-end/public/images/gallery/en_funeral.png b/front-end/public/images/gallery/en_funeral.png
deleted file mode 100755
index 131d6ec0..00000000
Binary files a/front-end/public/images/gallery/en_funeral.png and /dev/null differ
diff --git a/front-end/public/images/gallery/en_marriage.png b/front-end/public/images/gallery/en_marriage.png
deleted file mode 100755
index 41f9904f..00000000
Binary files a/front-end/public/images/gallery/en_marriage.png and /dev/null differ
diff --git a/front-end/public/images/gallery/en_marriage_s1.png b/front-end/public/images/gallery/en_marriage_s1.png
deleted file mode 100755
index b037cb84..00000000
Binary files a/front-end/public/images/gallery/en_marriage_s1.png and /dev/null differ
diff --git a/front-end/public/images/gallery/f_records.png b/front-end/public/images/gallery/f_records.png
deleted file mode 100755
index 1a31a91b..00000000
Binary files a/front-end/public/images/gallery/f_records.png and /dev/null differ
diff --git a/front-end/public/images/gallery/faq.png b/front-end/public/images/gallery/faq.png
deleted file mode 100755
index 7378bb15..00000000
Binary files a/front-end/public/images/gallery/faq.png and /dev/null differ
diff --git a/front-end/public/images/gallery/funeral-header.png b/front-end/public/images/gallery/funeral-header.png
deleted file mode 100755
index 144cabf8..00000000
Binary files a/front-end/public/images/gallery/funeral-header.png and /dev/null differ
diff --git a/front-end/public/images/gallery/funeral.png b/front-end/public/images/gallery/funeral.png
deleted file mode 100755
index 810edfa5..00000000
Binary files a/front-end/public/images/gallery/funeral.png and /dev/null differ
diff --git a/front-end/public/images/gallery/funerals.png b/front-end/public/images/gallery/funerals.png
deleted file mode 100755
index 144cabf8..00000000
Binary files a/front-end/public/images/gallery/funerals.png and /dev/null differ
diff --git a/front-end/public/images/gallery/ge_flag.png b/front-end/public/images/gallery/ge_flag.png
deleted file mode 100755
index 72c8c22a..00000000
Binary files a/front-end/public/images/gallery/ge_flag.png and /dev/null differ
diff --git a/front-end/public/images/gallery/generic-baptism.png b/front-end/public/images/gallery/generic-baptism.png
deleted file mode 100755
index e893e730..00000000
Binary files a/front-end/public/images/gallery/generic-baptism.png and /dev/null differ
diff --git a/front-end/public/images/gallery/get-started.png b/front-end/public/images/gallery/get-started.png
deleted file mode 100755
index c19d5a2b..00000000
Binary files a/front-end/public/images/gallery/get-started.png and /dev/null differ
diff --git a/front-end/public/images/gallery/globe-footer.png b/front-end/public/images/gallery/globe-footer.png
deleted file mode 100755
index 72323cde..00000000
Binary files a/front-end/public/images/gallery/globe-footer.png and /dev/null differ
diff --git a/front-end/public/images/gallery/gold.png b/front-end/public/images/gallery/gold.png
deleted file mode 100755
index e17e9133..00000000
Binary files a/front-end/public/images/gallery/gold.png and /dev/null differ
diff --git a/front-end/public/images/gallery/gr_baptim_sample.png b/front-end/public/images/gallery/gr_baptim_sample.png
deleted file mode 100755
index 95e7ec14..00000000
Binary files a/front-end/public/images/gallery/gr_baptim_sample.png and /dev/null differ
diff --git a/front-end/public/images/gallery/gr_baptism.png b/front-end/public/images/gallery/gr_baptism.png
deleted file mode 100755
index 3ad6384c..00000000
Binary files a/front-end/public/images/gallery/gr_baptism.png and /dev/null differ
diff --git a/front-end/public/images/gallery/gr_cross.png b/front-end/public/images/gallery/gr_cross.png
deleted file mode 100755
index 8f8576d8..00000000
Binary files a/front-end/public/images/gallery/gr_cross.png and /dev/null differ
diff --git a/front-end/public/images/gallery/gr_flag.png b/front-end/public/images/gallery/gr_flag.png
deleted file mode 100755
index aa6e5934..00000000
Binary files a/front-end/public/images/gallery/gr_flag.png and /dev/null differ
diff --git a/front-end/public/images/gallery/gr_funeral.png b/front-end/public/images/gallery/gr_funeral.png
deleted file mode 100755
index 652e111a..00000000
Binary files a/front-end/public/images/gallery/gr_funeral.png and /dev/null differ
diff --git a/front-end/public/images/gallery/gr_marriage.png b/front-end/public/images/gallery/gr_marriage.png
deleted file mode 100755
index e94bd37b..00000000
Binary files a/front-end/public/images/gallery/gr_marriage.png and /dev/null differ
diff --git a/front-end/public/images/gallery/home.png b/front-end/public/images/gallery/home.png
deleted file mode 100755
index 4c48749d..00000000
Binary files a/front-end/public/images/gallery/home.png and /dev/null differ
diff --git a/front-end/public/images/gallery/how.png b/front-end/public/images/gallery/how.png
deleted file mode 100755
index e330b17d..00000000
Binary files a/front-end/public/images/gallery/how.png and /dev/null differ
diff --git a/front-end/public/images/gallery/inside.png b/front-end/public/images/gallery/inside.png
deleted file mode 100755
index 2b9469f6..00000000
Binary files a/front-end/public/images/gallery/inside.png and /dev/null differ
diff --git a/front-end/public/images/gallery/logo-about.png b/front-end/public/images/gallery/logo-about.png
deleted file mode 100755
index 3dfd302f..00000000
Binary files a/front-end/public/images/gallery/logo-about.png and /dev/null differ
diff --git a/front-end/public/images/gallery/logo-addons.png b/front-end/public/images/gallery/logo-addons.png
deleted file mode 100755
index a446f483..00000000
Binary files a/front-end/public/images/gallery/logo-addons.png and /dev/null differ
diff --git a/front-end/public/images/gallery/logo-main.png b/front-end/public/images/gallery/logo-main.png
deleted file mode 100755
index 6a33f0a9..00000000
Binary files a/front-end/public/images/gallery/logo-main.png and /dev/null differ
diff --git a/front-end/public/images/gallery/logo-why.png b/front-end/public/images/gallery/logo-why.png
deleted file mode 100755
index 409deea7..00000000
Binary files a/front-end/public/images/gallery/logo-why.png and /dev/null differ
diff --git a/front-end/public/images/gallery/logo0.png b/front-end/public/images/gallery/logo0.png
deleted file mode 100755
index c0faa112..00000000
Binary files a/front-end/public/images/gallery/logo0.png and /dev/null differ
diff --git a/front-end/public/images/gallery/logo1.png b/front-end/public/images/gallery/logo1.png
deleted file mode 100755
index 54333991..00000000
Binary files a/front-end/public/images/gallery/logo1.png and /dev/null differ
diff --git a/front-end/public/images/gallery/logo2.png b/front-end/public/images/gallery/logo2.png
deleted file mode 100755
index 40daefe5..00000000
Binary files a/front-end/public/images/gallery/logo2.png and /dev/null differ
diff --git a/front-end/public/images/gallery/m_records.png b/front-end/public/images/gallery/m_records.png
deleted file mode 100755
index 3395437e..00000000
Binary files a/front-end/public/images/gallery/m_records.png and /dev/null differ
diff --git a/front-end/public/images/gallery/marriage-header.png b/front-end/public/images/gallery/marriage-header.png
deleted file mode 100755
index dd18ae6e..00000000
Binary files a/front-end/public/images/gallery/marriage-header.png and /dev/null differ
diff --git a/front-end/public/images/gallery/marriage.png b/front-end/public/images/gallery/marriage.png
deleted file mode 100755
index 8c7cdf17..00000000
Binary files a/front-end/public/images/gallery/marriage.png and /dev/null differ
diff --git a/front-end/public/images/gallery/marriages.png b/front-end/public/images/gallery/marriages.png
deleted file mode 100755
index dd18ae6e..00000000
Binary files a/front-end/public/images/gallery/marriages.png and /dev/null differ
diff --git a/front-end/public/images/gallery/new-logo10.png b/front-end/public/images/gallery/new-logo10.png
deleted file mode 100755
index 603bc521..00000000
Binary files a/front-end/public/images/gallery/new-logo10.png and /dev/null differ
diff --git a/front-end/public/images/gallery/new-logo4.png b/front-end/public/images/gallery/new-logo4.png
deleted file mode 100755
index bb6bf763..00000000
Binary files a/front-end/public/images/gallery/new-logo4.png and /dev/null differ
diff --git a/front-end/public/images/gallery/nimbus.png b/front-end/public/images/gallery/nimbus.png
deleted file mode 100755
index 8f05923b..00000000
Binary files a/front-end/public/images/gallery/nimbus.png and /dev/null differ
diff --git a/front-end/public/images/gallery/nj.png b/front-end/public/images/gallery/nj.png
deleted file mode 100755
index 261124d7..00000000
Binary files a/front-end/public/images/gallery/nj.png and /dev/null differ
diff --git a/front-end/public/images/gallery/orthodox-globe.png b/front-end/public/images/gallery/orthodox-globe.png
deleted file mode 100755
index 580de4f7..00000000
Binary files a/front-end/public/images/gallery/orthodox-globe.png and /dev/null differ
diff --git a/front-end/public/images/gallery/profile_1753057691760.png b/front-end/public/images/gallery/profile_1753057691760.png
deleted file mode 100755
index 965d3549..00000000
Binary files a/front-end/public/images/gallery/profile_1753057691760.png and /dev/null differ
diff --git a/front-end/public/images/gallery/profile_1753057691761.png b/front-end/public/images/gallery/profile_1753057691761.png
deleted file mode 100755
index 127fbdd7..00000000
Binary files a/front-end/public/images/gallery/profile_1753057691761.png and /dev/null differ
diff --git a/front-end/public/images/gallery/profile_1753057691763.png b/front-end/public/images/gallery/profile_1753057691763.png
deleted file mode 100755
index 4bc5c34f..00000000
Binary files a/front-end/public/images/gallery/profile_1753057691763.png and /dev/null differ
diff --git a/front-end/public/images/gallery/profile_1753057691765.png b/front-end/public/images/gallery/profile_1753057691765.png
deleted file mode 100755
index d188944d..00000000
Binary files a/front-end/public/images/gallery/profile_1753057691765.png and /dev/null differ
diff --git a/front-end/public/images/gallery/profile_1753057691766.png b/front-end/public/images/gallery/profile_1753057691766.png
deleted file mode 100755
index 2118da64..00000000
Binary files a/front-end/public/images/gallery/profile_1753057691766.png and /dev/null differ
diff --git a/front-end/public/images/gallery/profile_1753057691767.png b/front-end/public/images/gallery/profile_1753057691767.png
deleted file mode 100755
index 0d40f240..00000000
Binary files a/front-end/public/images/gallery/profile_1753057691767.png and /dev/null differ
diff --git a/front-end/public/images/gallery/profile_1753057691769.png b/front-end/public/images/gallery/profile_1753057691769.png
deleted file mode 100755
index f9ca8adc..00000000
Binary files a/front-end/public/images/gallery/profile_1753057691769.png and /dev/null differ
diff --git a/front-end/public/images/gallery/profile_1753057691770.png b/front-end/public/images/gallery/profile_1753057691770.png
deleted file mode 100755
index 3a5a95fa..00000000
Binary files a/front-end/public/images/gallery/profile_1753057691770.png and /dev/null differ
diff --git a/front-end/public/images/gallery/profile_1753057691771.png b/front-end/public/images/gallery/profile_1753057691771.png
deleted file mode 100755
index 072218d5..00000000
Binary files a/front-end/public/images/gallery/profile_1753057691771.png and /dev/null differ
diff --git a/front-end/public/images/gallery/profile_1754130459093.png b/front-end/public/images/gallery/profile_1754130459093.png
deleted file mode 100755
index 08942862..00000000
Binary files a/front-end/public/images/gallery/profile_1754130459093.png and /dev/null differ
diff --git a/front-end/public/images/gallery/profile_1754130459094.png b/front-end/public/images/gallery/profile_1754130459094.png
deleted file mode 100755
index b0dce905..00000000
Binary files a/front-end/public/images/gallery/profile_1754130459094.png and /dev/null differ
diff --git a/front-end/public/images/gallery/profile_1754130459099.png b/front-end/public/images/gallery/profile_1754130459099.png
deleted file mode 100755
index 5bfddf7b..00000000
Binary files a/front-end/public/images/gallery/profile_1754130459099.png and /dev/null differ
diff --git a/front-end/public/images/gallery/profile_1754130459101.png b/front-end/public/images/gallery/profile_1754130459101.png
deleted file mode 100755
index 1148e2d4..00000000
Binary files a/front-end/public/images/gallery/profile_1754130459101.png and /dev/null differ
diff --git a/front-end/public/images/gallery/provide.png b/front-end/public/images/gallery/provide.png
deleted file mode 100755
index 0224a1e2..00000000
Binary files a/front-end/public/images/gallery/provide.png and /dev/null differ
diff --git a/front-end/public/images/gallery/purple-bg.png b/front-end/public/images/gallery/purple-bg.png
deleted file mode 100755
index 81d05d2b..00000000
Binary files a/front-end/public/images/gallery/purple-bg.png and /dev/null differ
diff --git a/front-end/public/images/gallery/purple2-bg.png b/front-end/public/images/gallery/purple2-bg.png
deleted file mode 100755
index 7bbc93e7..00000000
Binary files a/front-end/public/images/gallery/purple2-bg.png and /dev/null differ
diff --git a/front-end/public/images/gallery/register.png b/front-end/public/images/gallery/register.png
deleted file mode 100755
index 38b8c6c5..00000000
Binary files a/front-end/public/images/gallery/register.png and /dev/null differ
diff --git a/front-end/public/images/gallery/ro_baptism.png b/front-end/public/images/gallery/ro_baptism.png
deleted file mode 100755
index ffb9705b..00000000
Binary files a/front-end/public/images/gallery/ro_baptism.png and /dev/null differ
diff --git a/front-end/public/images/gallery/ro_cross.png b/front-end/public/images/gallery/ro_cross.png
deleted file mode 100755
index 28b627a6..00000000
Binary files a/front-end/public/images/gallery/ro_cross.png and /dev/null differ
diff --git a/front-end/public/images/gallery/ro_flag.png b/front-end/public/images/gallery/ro_flag.png
deleted file mode 100755
index 4ccdfa42..00000000
Binary files a/front-end/public/images/gallery/ro_flag.png and /dev/null differ
diff --git a/front-end/public/images/gallery/ro_funeral.png b/front-end/public/images/gallery/ro_funeral.png
deleted file mode 100755
index c6d52bb0..00000000
Binary files a/front-end/public/images/gallery/ro_funeral.png and /dev/null differ
diff --git a/front-end/public/images/gallery/ro_marriage.png b/front-end/public/images/gallery/ro_marriage.png
deleted file mode 100755
index 208670ad..00000000
Binary files a/front-end/public/images/gallery/ro_marriage.png and /dev/null differ
diff --git a/front-end/public/images/gallery/ru_baptism.png b/front-end/public/images/gallery/ru_baptism.png
deleted file mode 100755
index 4e05a0a5..00000000
Binary files a/front-end/public/images/gallery/ru_baptism.png and /dev/null differ
diff --git a/front-end/public/images/gallery/ru_cross.png b/front-end/public/images/gallery/ru_cross.png
deleted file mode 100755
index a738680f..00000000
Binary files a/front-end/public/images/gallery/ru_cross.png and /dev/null differ
diff --git a/front-end/public/images/gallery/ru_flag.png b/front-end/public/images/gallery/ru_flag.png
deleted file mode 100755
index fde85396..00000000
Binary files a/front-end/public/images/gallery/ru_flag.png and /dev/null differ
diff --git a/front-end/public/images/gallery/ru_funeral.png b/front-end/public/images/gallery/ru_funeral.png
deleted file mode 100755
index 900c42ee..00000000
Binary files a/front-end/public/images/gallery/ru_funeral.png and /dev/null differ
diff --git a/front-end/public/images/gallery/ru_marriage.png b/front-end/public/images/gallery/ru_marriage.png
deleted file mode 100755
index 98c668bf..00000000
Binary files a/front-end/public/images/gallery/ru_marriage.png and /dev/null differ
diff --git a/front-end/public/images/gallery/sample.png b/front-end/public/images/gallery/sample.png
deleted file mode 100755
index b935adbf..00000000
Binary files a/front-end/public/images/gallery/sample.png and /dev/null differ
diff --git a/front-end/public/images/gallery/sand-dune.png b/front-end/public/images/gallery/sand-dune.png
deleted file mode 100755
index 7aa045e7..00000000
Binary files a/front-end/public/images/gallery/sand-dune.png and /dev/null differ
diff --git a/front-end/public/images/gallery/sand-dune1.png b/front-end/public/images/gallery/sand-dune1.png
deleted file mode 100755
index 0ad86d6e..00000000
Binary files a/front-end/public/images/gallery/sand-dune1.png and /dev/null differ
diff --git a/front-end/public/images/gallery/silv-purp-about.png b/front-end/public/images/gallery/silv-purp-about.png
deleted file mode 100755
index d956248d..00000000
Binary files a/front-end/public/images/gallery/silv-purp-about.png and /dev/null differ
diff --git a/front-end/public/images/gallery/ss-sspp-sand.png b/front-end/public/images/gallery/ss-sspp-sand.png
deleted file mode 100755
index 951181a9..00000000
Binary files a/front-end/public/images/gallery/ss-sspp-sand.png and /dev/null differ
diff --git a/front-end/public/images/gallery/sspp-sand.png b/front-end/public/images/gallery/sspp-sand.png
deleted file mode 100755
index 28d6b222..00000000
Binary files a/front-end/public/images/gallery/sspp-sand.png and /dev/null differ
diff --git a/front-end/public/images/gallery/support-card-1.png b/front-end/public/images/gallery/support-card-1.png
deleted file mode 100755
index 5ad2e59b..00000000
Binary files a/front-end/public/images/gallery/support-card-1.png and /dev/null differ
diff --git a/front-end/public/images/gallery/var.png b/front-end/public/images/gallery/var.png
deleted file mode 100755
index 13f6f1ca..00000000
Binary files a/front-end/public/images/gallery/var.png and /dev/null differ
diff --git a/front-end/public/images/generic-baptism.png b/front-end/public/images/generic-baptism.png
deleted file mode 100755
index e893e730..00000000
Binary files a/front-end/public/images/generic-baptism.png and /dev/null differ
diff --git a/front-end/public/images/global/saints/st-paul-1-big.png b/front-end/public/images/global/saints/st-paul-1-big.png
deleted file mode 100644
index 3db77ec3..00000000
Binary files a/front-end/public/images/global/saints/st-paul-1-big.png and /dev/null differ
diff --git a/front-end/public/images/global/saints/st-peter-1-big.png b/front-end/public/images/global/saints/st-peter-1-big.png
deleted file mode 100644
index 3e112c20..00000000
Binary files a/front-end/public/images/global/saints/st-peter-1-big.png and /dev/null differ
diff --git a/front-end/public/images/header/._logo-earth2.png b/front-end/public/images/header/._logo-earth2.png
deleted file mode 100755
index e53ffd71..00000000
Binary files a/front-end/public/images/header/._logo-earth2.png and /dev/null differ
diff --git a/front-end/public/images/header/._new-logo.png b/front-end/public/images/header/._new-logo.png
deleted file mode 100755
index 0a0daa52..00000000
Binary files a/front-end/public/images/header/._new-logo.png and /dev/null differ
diff --git a/front-end/public/images/header/._purple-bg.png b/front-end/public/images/header/._purple-bg.png
deleted file mode 100755
index 4f6c682a..00000000
Binary files a/front-end/public/images/header/._purple-bg.png and /dev/null differ
diff --git a/front-end/public/images/header/baptism-header.png b/front-end/public/images/header/baptism-header.png
deleted file mode 100755
index e6ad54d0..00000000
Binary files a/front-end/public/images/header/baptism-header.png and /dev/null differ
diff --git a/front-end/public/images/header/cross-gold.png b/front-end/public/images/header/cross-gold.png
deleted file mode 100755
index 8b079725..00000000
Binary files a/front-end/public/images/header/cross-gold.png and /dev/null differ
diff --git a/front-end/public/images/header/cross-transparent.png b/front-end/public/images/header/cross-transparent.png
deleted file mode 100755
index 9e4add1d..00000000
Binary files a/front-end/public/images/header/cross-transparent.png and /dev/null differ
diff --git a/front-end/public/images/header/en_cross.png b/front-end/public/images/header/en_cross.png
deleted file mode 100755
index 44f3d46f..00000000
Binary files a/front-end/public/images/header/en_cross.png and /dev/null differ
diff --git a/front-end/public/images/header/funeral-header.png b/front-end/public/images/header/funeral-header.png
deleted file mode 100755
index 144cabf8..00000000
Binary files a/front-end/public/images/header/funeral-header.png and /dev/null differ
diff --git a/front-end/public/images/header/globe-footer.png b/front-end/public/images/header/globe-footer.png
deleted file mode 100755
index 72323cde..00000000
Binary files a/front-end/public/images/header/globe-footer.png and /dev/null differ
diff --git a/front-end/public/images/header/logo-about.png b/front-end/public/images/header/logo-about.png
deleted file mode 100755
index 3a36a806..00000000
Binary files a/front-end/public/images/header/logo-about.png and /dev/null differ
diff --git a/front-end/public/images/header/logo-main.png b/front-end/public/images/header/logo-main.png
deleted file mode 100755
index 60868b81..00000000
Binary files a/front-end/public/images/header/logo-main.png and /dev/null differ
diff --git a/front-end/public/images/header/logo-why.png b/front-end/public/images/header/logo-why.png
deleted file mode 100755
index 685893ed..00000000
Binary files a/front-end/public/images/header/logo-why.png and /dev/null differ
diff --git a/front-end/public/images/header/marriage-header.png b/front-end/public/images/header/marriage-header.png
deleted file mode 100755
index dd18ae6e..00000000
Binary files a/front-end/public/images/header/marriage-header.png and /dev/null differ
diff --git a/front-end/public/images/header/new-head.png b/front-end/public/images/header/new-head.png
deleted file mode 100755
index ec2af49b..00000000
Binary files a/front-end/public/images/header/new-head.png and /dev/null differ
diff --git a/front-end/public/images/header/new-logo10.png b/front-end/public/images/header/new-logo10.png
deleted file mode 100755
index 603bc521..00000000
Binary files a/front-end/public/images/header/new-logo10.png and /dev/null differ
diff --git a/front-end/public/images/header/new-logo4.png b/front-end/public/images/header/new-logo4.png
deleted file mode 100755
index bb6bf763..00000000
Binary files a/front-end/public/images/header/new-logo4.png and /dev/null differ
diff --git a/front-end/public/images/header/nj.png b/front-end/public/images/header/nj.png
deleted file mode 100755
index 261124d7..00000000
Binary files a/front-end/public/images/header/nj.png and /dev/null differ
diff --git a/front-end/public/images/header/orthodox-globe.png b/front-end/public/images/header/orthodox-globe.png
deleted file mode 100755
index 580de4f7..00000000
Binary files a/front-end/public/images/header/orthodox-globe.png and /dev/null differ
diff --git a/front-end/public/images/header/purple-bg.png b/front-end/public/images/header/purple-bg.png
deleted file mode 100755
index 81d05d2b..00000000
Binary files a/front-end/public/images/header/purple-bg.png and /dev/null differ
diff --git a/front-end/public/images/header/purple2-bg.png b/front-end/public/images/header/purple2-bg.png
deleted file mode 100755
index 7bbc93e7..00000000
Binary files a/front-end/public/images/header/purple2-bg.png and /dev/null differ
diff --git a/front-end/public/images/header/quick_menu.png b/front-end/public/images/header/quick_menu.png
deleted file mode 100755
index 3753cff3..00000000
Binary files a/front-end/public/images/header/quick_menu.png and /dev/null differ
diff --git a/front-end/public/images/header/sand-dune.png b/front-end/public/images/header/sand-dune.png
deleted file mode 100755
index 285ba520..00000000
Binary files a/front-end/public/images/header/sand-dune.png and /dev/null differ
diff --git a/front-end/public/images/header/silv-purp-about.png b/front-end/public/images/header/silv-purp-about.png
deleted file mode 100755
index d956248d..00000000
Binary files a/front-end/public/images/header/silv-purp-about.png and /dev/null differ
diff --git a/front-end/public/images/header/ssppoc-header.png b/front-end/public/images/header/ssppoc-header.png
deleted file mode 100755
index 18748746..00000000
Binary files a/front-end/public/images/header/ssppoc-header.png and /dev/null differ
diff --git a/front-end/public/images/header/tagline.png b/front-end/public/images/header/tagline.png
deleted file mode 100755
index 25b36aa2..00000000
Binary files a/front-end/public/images/header/tagline.png and /dev/null differ
diff --git a/front-end/public/images/home-om.svg b/front-end/public/images/home-om.svg
deleted file mode 100755
index b52d20bd..00000000
--- a/front-end/public/images/home-om.svg
+++ /dev/null
@@ -1,6 +0,0 @@
-<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="#FFD54A" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" role="img" aria-label="Home" >
-  <title>Home</title>
-  <path d="M3 10.5L12 3l9 7.5"/>
-  <path d="M5 10.5V20a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1v-9.5"/>
-  <path d="M9 21v-6a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v6"/>
-</svg>
diff --git a/front-end/public/images/icons/H.png b/front-end/public/images/icons/H.png
old mode 100644
new mode 100755
diff --git a/front-end/public/images/icons/baptism.png b/front-end/public/images/icons/baptism.png
old mode 100644
new mode 100755
diff --git a/front-end/public/images/icons/en-baptism-icon.png b/front-end/public/images/icons/en-baptism-icon.png
deleted file mode 100755
index a01c3922..00000000
Binary files a/front-end/public/images/icons/en-baptism-icon.png and /dev/null differ
diff --git a/front-end/public/images/icons/en-funeral-icon.png b/front-end/public/images/icons/en-funeral-icon.png
deleted file mode 100755
index 9c8cbb24..00000000
Binary files a/front-end/public/images/icons/en-funeral-icon.png and /dev/null differ
diff --git a/front-end/public/images/icons/en-marriage-icon.png b/front-end/public/images/icons/en-marriage-icon.png
deleted file mode 100755
index b30d9d84..00000000
Binary files a/front-end/public/images/icons/en-marriage-icon.png and /dev/null differ
diff --git a/front-end/public/images/icons/funeral.png b/front-end/public/images/icons/funeral.png
old mode 100644
new mode 100755
diff --git a/front-end/public/images/icons/marriage.png b/front-end/public/images/icons/marriage.png
old mode 100644
new mode 100755
diff --git a/front-end/public/images/logo0.png b/front-end/public/images/logo0.png
deleted file mode 100755
index c0faa112..00000000
Binary files a/front-end/public/images/logo0.png and /dev/null differ
diff --git a/front-end/public/images/logo1.png b/front-end/public/images/logo1.png
deleted file mode 100755
index 54333991..00000000
Binary files a/front-end/public/images/logo1.png and /dev/null differ
diff --git a/front-end/public/images/logo2.png b/front-end/public/images/logo2.png
deleted file mode 100755
index 40daefe5..00000000
Binary files a/front-end/public/images/logo2.png and /dev/null differ
diff --git a/front-end/public/images/logo3.png b/front-end/public/images/logo3.png
deleted file mode 100755
index a73014a7..00000000
Binary files a/front-end/public/images/logo3.png and /dev/null differ
diff --git a/front-end/public/images/main/GE-baptism.png b/front-end/public/images/main/GE-baptism.png
deleted file mode 100755
index d1ad67ba..00000000
Binary files a/front-end/public/images/main/GE-baptism.png and /dev/null differ
diff --git a/front-end/public/images/main/GE-funeral.png b/front-end/public/images/main/GE-funeral.png
deleted file mode 100755
index e5fc3161..00000000
Binary files a/front-end/public/images/main/GE-funeral.png and /dev/null differ
diff --git a/front-end/public/images/main/GE-marriage.png b/front-end/public/images/main/GE-marriage.png
deleted file mode 100755
index 1106723e..00000000
Binary files a/front-end/public/images/main/GE-marriage.png and /dev/null differ
diff --git a/front-end/public/images/main/RO-baptism.png b/front-end/public/images/main/RO-baptism.png
deleted file mode 100755
index 7126e46e..00000000
Binary files a/front-end/public/images/main/RO-baptism.png and /dev/null differ
diff --git a/front-end/public/images/main/RO-funeral.png b/front-end/public/images/main/RO-funeral.png
deleted file mode 100755
index 4c5246a3..00000000
Binary files a/front-end/public/images/main/RO-funeral.png and /dev/null differ
diff --git a/front-end/public/images/main/RO-marriage.png b/front-end/public/images/main/RO-marriage.png
deleted file mode 100755
index 1c03d175..00000000
Binary files a/front-end/public/images/main/RO-marriage.png and /dev/null differ
diff --git a/front-end/public/images/main/baptism.png b/front-end/public/images/main/baptism.png
deleted file mode 100755
index 8985f192..00000000
Binary files a/front-end/public/images/main/baptism.png and /dev/null differ
diff --git a/front-end/public/images/main/baptism2.png b/front-end/public/images/main/baptism2.png
deleted file mode 100755
index e382bed7..00000000
Binary files a/front-end/public/images/main/baptism2.png and /dev/null differ
diff --git a/front-end/public/images/main/biz-logo.png b/front-end/public/images/main/biz-logo.png
deleted file mode 100755
index 69505615..00000000
Binary files a/front-end/public/images/main/biz-logo.png and /dev/null differ
diff --git a/front-end/public/images/main/en_flag.png b/front-end/public/images/main/en_flag.png
deleted file mode 100755
index 938bd1b6..00000000
Binary files a/front-end/public/images/main/en_flag.png and /dev/null differ
diff --git a/front-end/public/images/main/en_marriage_s1.png b/front-end/public/images/main/en_marriage_s1.png
deleted file mode 100755
index b037cb84..00000000
Binary files a/front-end/public/images/main/en_marriage_s1.png and /dev/null differ
diff --git a/front-end/public/images/main/funeral.png b/front-end/public/images/main/funeral.png
deleted file mode 100755
index 810edfa5..00000000
Binary files a/front-end/public/images/main/funeral.png and /dev/null differ
diff --git a/front-end/public/images/main/funeral2.png b/front-end/public/images/main/funeral2.png
deleted file mode 100755
index 98d2f2bf..00000000
Binary files a/front-end/public/images/main/funeral2.png and /dev/null differ
diff --git a/front-end/public/images/main/ge_flag.png b/front-end/public/images/main/ge_flag.png
deleted file mode 100755
index 72c8c22a..00000000
Binary files a/front-end/public/images/main/ge_flag.png and /dev/null differ
diff --git a/front-end/public/images/main/globe.png b/front-end/public/images/main/globe.png
deleted file mode 100755
index 1ebfbd78..00000000
Binary files a/front-end/public/images/main/globe.png and /dev/null differ
diff --git a/front-end/public/images/main/gr_baptim_sample.png b/front-end/public/images/main/gr_baptim_sample.png
deleted file mode 100755
index 95e7ec14..00000000
Binary files a/front-end/public/images/main/gr_baptim_sample.png and /dev/null differ
diff --git a/front-end/public/images/main/gr_baptism.png b/front-end/public/images/main/gr_baptism.png
deleted file mode 100755
index 77fa93d1..00000000
Binary files a/front-end/public/images/main/gr_baptism.png and /dev/null differ
diff --git a/front-end/public/images/main/gr_flag.png b/front-end/public/images/main/gr_flag.png
deleted file mode 100755
index aa6e5934..00000000
Binary files a/front-end/public/images/main/gr_flag.png and /dev/null differ
diff --git a/front-end/public/images/main/gr_funeral.png b/front-end/public/images/main/gr_funeral.png
deleted file mode 100755
index 10a90154..00000000
Binary files a/front-end/public/images/main/gr_funeral.png and /dev/null differ
diff --git a/front-end/public/images/main/gr_marriage.png b/front-end/public/images/main/gr_marriage.png
deleted file mode 100755
index e6feef99..00000000
Binary files a/front-end/public/images/main/gr_marriage.png and /dev/null differ
diff --git a/front-end/public/images/main/marriage.png b/front-end/public/images/main/marriage.png
deleted file mode 100755
index 8c7cdf17..00000000
Binary files a/front-end/public/images/main/marriage.png and /dev/null differ
diff --git a/front-end/public/images/main/marriage2.png b/front-end/public/images/main/marriage2.png
deleted file mode 100755
index f62bb126..00000000
Binary files a/front-end/public/images/main/marriage2.png and /dev/null differ
diff --git a/front-end/public/images/main/ro_flag.png b/front-end/public/images/main/ro_flag.png
deleted file mode 100755
index 73e5ebd2..00000000
Binary files a/front-end/public/images/main/ro_flag.png and /dev/null differ
diff --git a/front-end/public/images/main/ru_baptism.png b/front-end/public/images/main/ru_baptism.png
deleted file mode 100755
index 0bcffbb3..00000000
Binary files a/front-end/public/images/main/ru_baptism.png and /dev/null differ
diff --git a/front-end/public/images/main/ru_flag.png b/front-end/public/images/main/ru_flag.png
deleted file mode 100755
index fde85396..00000000
Binary files a/front-end/public/images/main/ru_flag.png and /dev/null differ
diff --git a/front-end/public/images/main/ru_funeral.png b/front-end/public/images/main/ru_funeral.png
deleted file mode 100755
index 9f544b59..00000000
Binary files a/front-end/public/images/main/ru_funeral.png and /dev/null differ
diff --git a/front-end/public/images/main/ru_marriage.png b/front-end/public/images/main/ru_marriage.png
deleted file mode 100755
index 30ea7d3f..00000000
Binary files a/front-end/public/images/main/ru_marriage.png and /dev/null differ
diff --git a/front-end/public/images/main/ss-sspp-sand.png b/front-end/public/images/main/ss-sspp-sand.png
deleted file mode 100755
index 951181a9..00000000
Binary files a/front-end/public/images/main/ss-sspp-sand.png and /dev/null differ
diff --git a/front-end/public/images/main/support-card-1.png b/front-end/public/images/main/support-card-1.png
deleted file mode 100755
index 35e07192..00000000
Binary files a/front-end/public/images/main/support-card-1.png and /dev/null differ
diff --git a/front-end/public/images/market/1.png b/front-end/public/images/market/1.png
deleted file mode 100755
index bd9d7ad9..00000000
Binary files a/front-end/public/images/market/1.png and /dev/null differ
diff --git a/front-end/public/images/marriages.png b/front-end/public/images/marriages.png
deleted file mode 100755
index dd18ae6e..00000000
Binary files a/front-end/public/images/marriages.png and /dev/null differ
diff --git a/front-end/public/images/nimbus.png b/front-end/public/images/nimbus.png
deleted file mode 100755
index 8f05923b..00000000
Binary files a/front-end/public/images/nimbus.png and /dev/null differ
diff --git a/front-end/public/images/om-ocr/image-blacked.png b/front-end/public/images/om-ocr/image-blacked.png
deleted file mode 100755
index d2266a97..00000000
Binary files a/front-end/public/images/om-ocr/image-blacked.png and /dev/null differ
diff --git a/front-end/public/images/orthodox-cross-icon.svg b/front-end/public/images/orthodox-cross-icon.svg
deleted file mode 100755
index eef66f14..00000000
--- a/front-end/public/images/orthodox-cross-icon.svg
+++ /dev/null
@@ -1,32 +0,0 @@
-<svg width="80" height="80" viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg">
-  <defs>
-    <!-- Gold gradient for the cross -->
-    <linearGradient id="goldGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#F6C90E;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#D4A80A;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Drop shadow filter -->
-    <filter id="dropShadow" x="-20%" y="-20%" width="140%" height="140%">
-      <feDropShadow dx="0" dy="4" stdDeviation="4" flood-color="#F6C90E" flood-opacity="0.3"/>
-    </filter>
-  </defs>
-  
-  <!-- Orthodox Cross with 3D effect -->
-  <!-- Vertical beam -->
-  <rect x="36" y="0" width="8" height="80" rx="4" fill="url(#goldGradient)" filter="url(#dropShadow)"/>
-  
-  <!-- Top horizontal bar -->
-  <rect x="20" y="15" width="40" height="6" rx="3" fill="url(#goldGradient)" filter="url(#dropShadow)"/>
-  
-  <!-- Main horizontal bar -->
-  <rect x="15" y="35" width="50" height="8" rx="4" fill="url(#goldGradient)" filter="url(#dropShadow)"/>
-  
-  <!-- Bottom angled bar -->
-  <rect x="25" y="55" width="30" height="6" rx="3" fill="url(#goldGradient)" filter="url(#dropShadow)" transform="rotate(-20 40 58)"/>
-  
-  <!-- 3D highlights -->
-  <rect x="36" y="0" width="4" height="80" rx="2" fill="#FFE55C" opacity="0.6"/>
-  <rect x="20" y="15" width="20" height="3" rx="1.5" fill="#FFE55C" opacity="0.6"/>
-  <rect x="15" y="35" width="25" height="4" rx="2" fill="#FFE55C" opacity="0.6"/>
-</svg>
diff --git a/front-end/public/images/orthodox-metrics-logo.svg b/front-end/public/images/orthodox-metrics-logo.svg
deleted file mode 100755
index bda9e024..00000000
--- a/front-end/public/images/orthodox-metrics-logo.svg
+++ /dev/null
@@ -1,67 +0,0 @@
-<svg width="300" height="200" viewBox="0 0 300 200" xmlns="http://www.w3.org/2000/svg">
-  <defs>
-    <!-- Background gradient -->
-    <linearGradient id="bgGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#F3F4F6;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#E5E7EB;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Gold gradient for the cross -->
-    <linearGradient id="goldGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#F6C90E;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#D4A80A;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Royal purple gradient -->
-    <linearGradient id="purpleGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#4C1D95;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#2E1065;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Drop shadow filters -->
-    <filter id="crossShadow" x="-20%" y="-20%" width="140%" height="140%">
-      <feDropShadow dx="0" dy="4" stdDeviation="4" flood-color="#F6C90E" flood-opacity="0.3"/>
-    </filter>
-    
-    <filter id="textShadow" x="-20%" y="-20%" width="140%" height="140%">
-      <feDropShadow dx="0" dy="2" stdDeviation="2" flood-color="#4C1D95" flood-opacity="0.2"/>
-    </filter>
-    
-    <filter id="goldTextShadow" x="-20%" y="-20%" width="140%" height="140%">
-      <feDropShadow dx="0" dy="2" stdDeviation="2" flood-color="#F6C90E" flood-opacity="0.3"/>
-    </filter>
-  </defs>
-  
-  <!-- Background -->
-  <rect width="300" height="200" fill="url(#bgGradient)" rx="20"/>
-  
-  <!-- Orthodox Cross (centered, top) -->
-  <g transform="translate(150, 60)">
-    <!-- Vertical beam -->
-    <rect x="-4" y="-30" width="8" height="60" rx="4" fill="url(#goldGradient)" filter="url(#crossShadow)"/>
-    
-    <!-- Top horizontal bar -->
-    <rect x="-20" y="-25" width="40" height="6" rx="3" fill="url(#goldGradient)" filter="url(#crossShadow)"/>
-    
-    <!-- Main horizontal bar -->
-    <rect x="-25" y="-5" width="50" height="8" rx="4" fill="url(#goldGradient)" filter="url(#crossShadow)"/>
-    
-    <!-- Bottom angled bar -->
-    <rect x="-15" y="15" width="30" height="6" rx="3" fill="url(#goldGradient)" filter="url(#crossShadow)" transform="rotate(-20 0 18)"/>
-    
-    <!-- 3D highlights -->
-    <rect x="-4" y="-30" width="4" height="60" rx="2" fill="#FFE55C" opacity="0.6"/>
-    <rect x="-20" y="-25" width="20" height="3" rx="1.5" fill="#FFE55C" opacity="0.6"/>
-    <rect x="-25" y="-5" width="25" height="4" rx="2" fill="#FFE55C" opacity="0.6"/>
-  </g>
-  
-  <!-- Company Name: "Orthodox Metrics LLC" -->
-  <text x="150" y="120" text-anchor="middle" font-family="'Noto Serif', 'Times New Roman', serif" font-size="24" font-weight="600" fill="url(#purpleGradient)" filter="url(#textShadow)">
-    Orthodox Metrics LLC
-  </text>
-  
-  <!-- Tagline: "Recording the Saints Among Us" -->
-  <text x="150" y="150" text-anchor="middle" font-family="'Noto Serif', 'Times New Roman', serif" font-size="16" font-style="italic" fill="url(#goldGradient)" filter="url(#goldTextShadow)">
-    Recording the Saints Among Us
-  </text>
-</svg>
diff --git a/front-end/public/images/orthodox/Screenshot 2026-01-17 211044.png b/front-end/public/images/orthodox/Screenshot 2026-01-17 211044.png
deleted file mode 100644
index 7506b89e..00000000
Binary files a/front-end/public/images/orthodox/Screenshot 2026-01-17 211044.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/add-record-en.png b/front-end/public/images/orthodox/add-record-en.png
deleted file mode 100755
index b9622770..00000000
Binary files a/front-end/public/images/orthodox/add-record-en.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/default.jpg b/front-end/public/images/orthodox/avatars/default.jpg
deleted file mode 100755
index d4482bb4..00000000
Binary files a/front-end/public/images/orthodox/avatars/default.jpg and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/default.svg b/front-end/public/images/orthodox/avatars/default.svg
deleted file mode 100755
index 1de4c4cf..00000000
--- a/front-end/public/images/orthodox/avatars/default.svg
+++ /dev/null
@@ -1 +0,0 @@
-<svg width="200" height="200" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg"><defs><linearGradient id="avatarGradient" x1="0%" y1="0%" x2="100%" y2="100%"><stop offset="0%" style="stop-color:#4C1D95;stop-opacity:1" /><stop offset="100%" style="stop-color:#7C3AED;stop-opacity:1" /></defs><circle cx="100" cy="100" r="100" fill="url(#avatarGradient)"/><g fill="#F6C90E" stroke="#FFFFFF" stroke-width="3"><rect x="85" y="60" width="30" height="80" rx="5"/><rect x="60" y="85" width="80" height="30" rx="5"/><rect x="85" y="40" width="30" height="20" rx="5" transform="rotate(45 100 50)"/><rect x="85" y="140" width="30" height="20" rx="5" transform="rotate(-45 100 150)"/></g><text x="100" y="190" text-anchor="middle" fill="#FFFFFF" font-family="Arial, sans-serif" font-size="12" font-weight="bold">Orthodox Metrics</text></svg>
diff --git a/front-end/public/images/orthodox/avatars/profile_15.png b/front-end/public/images/orthodox/avatars/profile_15.png
deleted file mode 100755
index 5bfddf7b..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_15.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1753057691760.png b/front-end/public/images/orthodox/avatars/profile_1753057691760.png
deleted file mode 100755
index 965d3549..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1753057691760.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1753057691761.png b/front-end/public/images/orthodox/avatars/profile_1753057691761.png
deleted file mode 100755
index 127fbdd7..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1753057691761.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1753057691763.png b/front-end/public/images/orthodox/avatars/profile_1753057691763.png
deleted file mode 100755
index 4bc5c34f..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1753057691763.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1753057691765.png b/front-end/public/images/orthodox/avatars/profile_1753057691765.png
deleted file mode 100755
index d188944d..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1753057691765.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1753057691766.png b/front-end/public/images/orthodox/avatars/profile_1753057691766.png
deleted file mode 100755
index 2118da64..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1753057691766.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1753057691767.png b/front-end/public/images/orthodox/avatars/profile_1753057691767.png
deleted file mode 100755
index 0d40f240..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1753057691767.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1753057691769.png b/front-end/public/images/orthodox/avatars/profile_1753057691769.png
deleted file mode 100755
index f9ca8adc..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1753057691769.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1753057691770.png b/front-end/public/images/orthodox/avatars/profile_1753057691770.png
deleted file mode 100755
index 3a5a95fa..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1753057691770.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1753057691771.png b/front-end/public/images/orthodox/avatars/profile_1753057691771.png
deleted file mode 100755
index 072218d5..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1753057691771.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1754130459091.png b/front-end/public/images/orthodox/avatars/profile_1754130459091.png
deleted file mode 100755
index 79a6c7e2..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1754130459091.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1754130459093.png b/front-end/public/images/orthodox/avatars/profile_1754130459093.png
deleted file mode 100755
index 08942862..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1754130459093.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1754130459094.png b/front-end/public/images/orthodox/avatars/profile_1754130459094.png
deleted file mode 100755
index b0dce905..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1754130459094.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1754130459096.png b/front-end/public/images/orthodox/avatars/profile_1754130459096.png
deleted file mode 100755
index 57ce84cf..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1754130459096.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1754130459098.png b/front-end/public/images/orthodox/avatars/profile_1754130459098.png
deleted file mode 100755
index c7e754de..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1754130459098.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1754130459099.png b/front-end/public/images/orthodox/avatars/profile_1754130459099.png
deleted file mode 100755
index 5bfddf7b..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1754130459099.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1754130459101.png b/front-end/public/images/orthodox/avatars/profile_1754130459101.png
deleted file mode 100755
index 1148e2d4..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1754130459101.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/avatars/profile_1754130459102.png b/front-end/public/images/orthodox/avatars/profile_1754130459102.png
deleted file mode 100755
index 8c6afe0c..00000000
Binary files a/front-end/public/images/orthodox/avatars/profile_1754130459102.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/banners/1.png b/front-end/public/images/orthodox/banners/1.png
deleted file mode 100755
index 49998c3b..00000000
Binary files a/front-end/public/images/orthodox/banners/1.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/banners/2.png b/front-end/public/images/orthodox/banners/2.png
deleted file mode 100755
index 6c0757de..00000000
Binary files a/front-end/public/images/orthodox/banners/2.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/banners/3.png b/front-end/public/images/orthodox/banners/3.png
deleted file mode 100755
index 70ce350c..00000000
Binary files a/front-end/public/images/orthodox/banners/3.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/banners/4.png b/front-end/public/images/orthodox/banners/4.png
deleted file mode 100755
index 6d9d7fae..00000000
Binary files a/front-end/public/images/orthodox/banners/4.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/banners/5.png b/front-end/public/images/orthodox/banners/5.png
deleted file mode 100755
index 0b755c64..00000000
Binary files a/front-end/public/images/orthodox/banners/5.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/banners/6.png b/front-end/public/images/orthodox/banners/6.png
deleted file mode 100755
index c24d45b5..00000000
Binary files a/front-end/public/images/orthodox/banners/6.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/banners/7.png b/front-end/public/images/orthodox/banners/7.png
deleted file mode 100755
index 5b9288a6..00000000
Binary files a/front-end/public/images/orthodox/banners/7.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/banners/8.png b/front-end/public/images/orthodox/banners/8.png
deleted file mode 100755
index 1bbf8c45..00000000
Binary files a/front-end/public/images/orthodox/banners/8.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/banners/9.png b/front-end/public/images/orthodox/banners/9.png
deleted file mode 100755
index b5a0494c..00000000
Binary files a/front-end/public/images/orthodox/banners/9.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/banners/default.jpg b/front-end/public/images/orthodox/banners/default.jpg
deleted file mode 100755
index 49998c3b..00000000
Binary files a/front-end/public/images/orthodox/banners/default.jpg and /dev/null differ
diff --git a/front-end/public/images/orthodox/banners/default.svg b/front-end/public/images/orthodox/banners/default.svg
deleted file mode 100755
index 0bec908e..00000000
--- a/front-end/public/images/orthodox/banners/default.svg
+++ /dev/null
@@ -1 +0,0 @@
-<svg width="800" height="200" viewBox="0 0 800 200" xmlns="http://www.w3.org/2000/svg"><defs><linearGradient id="bannerGradient" x1="0%" y1="0%" x2="100%" y2="0%"><stop offset="0%" style="stop-color:#4C1D95;stop-opacity:1" /><stop offset="50%" style="stop-color:#7C3AED;stop-opacity:1" /><stop offset="100%" style="stop-color:#F6C90E;stop-opacity:1" /></defs><rect width="800" height="200" fill="url(#bannerGradient)"/><g fill="#FFFFFF" stroke="#FFFFFF" stroke-width="2"><rect x="350" y="80" width="100" height="40" rx="5"/><rect x="320" y="100" width="160" height="40" rx="5"/><rect x="350" y="60" width="100" height="20" rx="5" transform="rotate(45 400 70)"/><rect x="350" y="140" width="100" height="20" rx="5" transform="rotate(-45 400 150)"/></g><text x="400" y="180" text-anchor="middle" fill="#FFFFFF" font-family="Arial, sans-serif" font-size="16" font-weight="bold">Orthodox Metrics LLC</text></svg>
diff --git a/front-end/public/images/orthodox/en-form.png b/front-end/public/images/orthodox/en-form.png
deleted file mode 100755
index 5c63e7bf..00000000
Binary files a/front-end/public/images/orthodox/en-form.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/funeral-add-record-en.png b/front-end/public/images/orthodox/funeral-add-record-en.png
deleted file mode 100755
index 3e8ceba9..00000000
Binary files a/front-end/public/images/orthodox/funeral-add-record-en.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/lord-1.jpg b/front-end/public/images/orthodox/lord-1.jpg
deleted file mode 100755
index 6173c8a5..00000000
Binary files a/front-end/public/images/orthodox/lord-1.jpg and /dev/null differ
diff --git a/front-end/public/images/orthodox/record-add-engilsh.png b/front-end/public/images/orthodox/record-add-engilsh.png
deleted file mode 100755
index d8c7c1e3..00000000
Binary files a/front-end/public/images/orthodox/record-add-engilsh.png and /dev/null differ
diff --git a/front-end/public/images/orthodox/theotokos.jpg b/front-end/public/images/orthodox/theotokos.jpg
deleted file mode 100755
index 930f44fc..00000000
Binary files a/front-end/public/images/orthodox/theotokos.jpg and /dev/null differ
diff --git a/front-end/public/images/orthodox_metrics_invoice_english_svg.svg b/front-end/public/images/orthodox_metrics_invoice_english_svg.svg
deleted file mode 100755
index ff7d65d3..00000000
--- a/front-end/public/images/orthodox_metrics_invoice_english_svg.svg
+++ /dev/null
@@ -1,141 +0,0 @@
-<svg width="800" height="1100" xmlns="http://www.w3.org/2000/svg">
-  <defs>
-    <!-- Purple to Gold Gradient -->
-    <linearGradient id="headerGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#663399;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#9966CC;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Gold Accent Gradient -->
-    <linearGradient id="goldGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#FFD700;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#FFA500;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Light Background -->
-    <linearGradient id="backgroundGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#F8F6FF;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#FFF9E6;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Table Header Gradient -->
-    <linearGradient id="tableHeaderGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#4A1A5C;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#663399;stop-opacity:1" />
-    </linearGradient>
-  </defs>
-  
-  <!-- Main Background -->
-  <rect width="800" height="1100" fill="url(#backgroundGradient)"/>
-  
-  <!-- Header Section -->
-  <rect width="800" height="200" fill="url(#headerGradient)"/>
-  
-  <!-- Logo Circle -->
-  <circle cx="60" cy="80" r="30" fill="white"/>
-  <text x="60" y="88" text-anchor="middle" font-family="Arial, sans-serif" font-size="20" font-weight="bold" fill="#663399">OM</text>
-  
-  <!-- Company Name -->
-  <text x="110" y="70" font-family="Arial, sans-serif" font-size="28" font-weight="300" fill="white">Orthodox Metrics</text>
-  <text x="110" y="95" font-family="Arial, sans-serif" font-size="14" fill="#E6E6FA">Data-Driven Solutions &amp; Analytics</text>
-  
-  <!-- Invoice Title -->
-  <text x="720" y="80" text-anchor="end" font-family="Arial, sans-serif" font-size="48" font-weight="100" fill="white" opacity="0.4">INVOICE</text>
-  
-  <!-- Company Info Box -->
-  <rect x="50" y="120" width="280" height="70" fill="rgba(255,255,255,0.1)" rx="8"/>
-  <text x="60" y="140" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">From:</text>
-  <text x="60" y="155" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Orthodox Metrics LLC</text>
-  <text x="60" y="170" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">123 Analytics Boulevard, Data City, DC 12345</text>
-  <text x="60" y="185" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">billing@orthodoxmetrics.com | (555) 123-4567</text>
-  
-  <!-- Client Info Box -->
-  <rect x="470" y="120" width="280" height="70" fill="rgba(255,255,255,0.1)" rx="8"/>
-  <text x="480" y="140" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">To:</text>
-  <text x="480" y="155" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Client Name</text>
-  <text x="480" y="170" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Client Address</text>
-  <text x="480" y="185" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">client@email.com</text>
-  
-  <!-- Invoice Details Section -->
-  <rect x="50" y="230" width="700" height="100" fill="white" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  
-  <!-- Left Column -->
-  <text x="70" y="255" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Invoice Number:</text>
-  <text x="200" y="255" font-family="Arial, sans-serif" font-size="14" fill="url(#goldGradient)" font-weight="600">#OM-2025-001</text>
-  
-  <text x="70" y="280" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Issue Date:</text>
-  <text x="200" y="280" font-family="Arial, sans-serif" font-size="14" fill="#666">July 6, 2025</text>
-  
-  <!-- Right Column -->
-  <text x="420" y="255" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Due Date:</text>
-  <text x="520" y="255" font-family="Arial, sans-serif" font-size="14" fill="url(#goldGradient)" font-weight="600">August 5, 2025</text>
-  
-  <text x="420" y="280" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Payment Terms:</text>
-  <text x="520" y="280" font-family="Arial, sans-serif" font-size="14" fill="#666">Net 30</text>
-  
-  <!-- Services Section Title -->
-  <text x="50" y="370" font-family="Arial, sans-serif" font-size="18" font-weight="600" fill="#663399">Services Provided</text>
-  <line x1="50" y1="375" x2="750" y2="375" stroke="url(#goldGradient)" stroke-width="2"/>
-  
-  <!-- Table Header -->
-  <rect x="50" y="390" width="700" height="40" fill="url(#tableHeaderGradient)" rx="8 8 0 0"/>
-  <text x="70" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Description</text>
-  <text x="550" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Qty</text>
-  <text x="620" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Rate</text>
-  <text x="700" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Amount</text>
-  
-  <!-- Empty Table Rows -->
-  <rect x="50" y="430" width="700" height="40" fill="white" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="470" width="700" height="40" fill="#F8F6FF" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="510" width="700" height="40" fill="white" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="550" width="700" height="40" fill="#F8F6FF" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="590" width="700" height="40" fill="white" stroke="#E6E6FA" stroke-width="1" rx="0 0 8 8"/>
-  
-  <!-- Totals Section -->
-  <rect x="450" y="660" width="300" height="120" fill="#F8F6FF" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  
-  <text x="470" y="685" font-family="Arial, sans-serif" font-size="14" fill="#663399">Subtotal:</text>
-  <text x="720" y="685" text-anchor="end" font-family="Arial, sans-serif" font-size="14" fill="#666">$0.00</text>
-  
-  <text x="470" y="710" font-family="Arial, sans-serif" font-size="14" fill="#663399">Tax:</text>
-  <text x="720" y="710" text-anchor="end" font-family="Arial, sans-serif" font-size="14" fill="#666">$0.00</text>
-  
-  <line x1="470" y1="720" x2="730" y2="720" stroke="url(#goldGradient)" stroke-width="2"/>
-  
-  <text x="470" y="745" font-family="Arial, sans-serif" font-size="16" font-weight="700" fill="#663399">Total Amount:</text>
-  <text x="720" y="745" text-anchor="end" font-family="Arial, sans-serif" font-size="16" font-weight="700" fill="url(#goldGradient)">$0.00</text>
-  
-  <!-- Payment Instructions -->
-  <rect x="50" y="810" width="320" height="100" fill="white" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  <text x="70" y="835" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Payment Instructions</text>
-  <text x="70" y="855" font-family="Arial, sans-serif" font-size="12" fill="#666">Bank Transfer: Orthodox Metrics Business Account</text>
-  <text x="70" y="870" font-family="Arial, sans-serif" font-size="12" fill="#666">Account Number: 1234567890</text>
-  <text x="70" y="885" font-family="Arial, sans-serif" font-size="12" fill="#666">Routing Number: 987654321</text>
-  <text x="70" y="900" font-family="Arial, sans-serif" font-size="12" fill="#666">Reference: Invoice Number</text>
-  
-  <!-- Notes Section -->
-  <rect x="430" y="810" width="320" height="100" fill="white" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  <text x="450" y="835" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Notes</text>
-  <text x="450" y="855" font-family="Arial, sans-serif" font-size="12" fill="#666">Thank you for choosing Orthodox Metrics</text>
-  <text x="450" y="870" font-family="Arial, sans-serif" font-size="12" fill="#666">for your data analytics needs.</text>
-  <text x="450" y="885" font-family="Arial, sans-serif" font-size="12" fill="#666">Please retain this invoice for your records.</text>
-  
-  <!-- Footer -->
-  <rect x="0" y="940" width="800" height="160" fill="#663399"/>
-  
-  <text x="400" y="970" text-anchor="middle" font-family="Arial, sans-serif" font-size="16" font-weight="600" fill="white">Orthodox Metrics</text>
-  <text x="400" y="990" text-anchor="middle" font-family="Arial, sans-serif" font-size="14" fill="#E6E6FA">Transforming Data into Actionable Insights</text>
-  
-  <text x="400" y="1020" text-anchor="middle" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">For questions about this invoice, please contact us at billing@orthodoxmetrics.com</text>
-  
-  <!-- Payment Terms Box -->
-  <rect x="50" y="1040" width="700" height="50" fill="rgba(255,255,255,0.1)" rx="8"/>
-  <text x="400" y="1060" text-anchor="middle" font-family="Arial, sans-serif" font-size="12" font-weight="600" fill="url(#goldGradient)">Payment Terms &amp; Conditions</text>
-  <text x="400" y="1075" text-anchor="middle" font-family="Arial, sans-serif" font-size="10" fill="#E6E6FA">Payment is due within 30 days of invoice date. Late payments may be subject to service charges.</text>
-  
-  <!-- Decorative Elements -->
-  <circle cx="750" cy="50" r="3" fill="url(#goldGradient)" opacity="0.6"/>
-  <circle cx="730" cy="30" r="2" fill="url(#goldGradient)" opacity="0.4"/>
-  <circle cx="770" cy="70" r="1.5" fill="url(#goldGradient)" opacity="0.8"/>
-  
-</svg>
\ No newline at end of file
diff --git a/front-end/public/images/orthodox_metrics_invoice_greek.svg b/front-end/public/images/orthodox_metrics_invoice_greek.svg
deleted file mode 100755
index d4cedc55..00000000
--- a/front-end/public/images/orthodox_metrics_invoice_greek.svg
+++ /dev/null
@@ -1,141 +0,0 @@
-<svg width="800" height="1100" xmlns="http://www.w3.org/2000/svg">
-  <defs>
-    <!-- Purple to Gold Gradient -->
-    <linearGradient id="headerGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#663399;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#9966CC;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Gold Accent Gradient -->
-    <linearGradient id="goldGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#FFD700;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#FFA500;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Light Background -->
-    <linearGradient id="backgroundGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#F8F6FF;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#FFF9E6;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Table Header Gradient -->
-    <linearGradient id="tableHeaderGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#4A1A5C;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#663399;stop-opacity:1" />
-    </linearGradient>
-  </defs>
-  
-  <!-- Main Background -->
-  <rect width="800" height="1100" fill="url(#backgroundGradient)"/>
-  
-  <!-- Header Section -->
-  <rect width="800" height="200" fill="url(#headerGradient)"/>
-  
-  <!-- Logo Circle -->
-  <circle cx="60" cy="80" r="30" fill="white"/>
-  <text x="60" y="88" text-anchor="middle" font-family="Arial, sans-serif" font-size="20" font-weight="bold" fill="#663399">OM</text>
-  
-  <!-- Company Name -->
-  <text x="110" y="70" font-family="Arial, sans-serif" font-size="28" font-weight="300" fill="white">Orthodox Metrics</text>
-  <text x="110" y="95" font-family="Arial, sans-serif" font-size="14" fill="#E6E6FA">Λύσεις και Αναλύσεις Βασισμένες σε Δεδομένα</text>
-  
-  <!-- Invoice Title -->
-  <text x="720" y="80" text-anchor="end" font-family="Arial, sans-serif" font-size="48" font-weight="100" fill="white" opacity="0.4">ΤΙΜΟΛΟΓΙΟ</text>
-  
-  <!-- Company Info Box -->
-  <rect x="50" y="120" width="280" height="70" fill="rgba(255,255,255,0.1)" rx="8"/>
-  <text x="60" y="140" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Από:</text>
-  <text x="60" y="155" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Orthodox Metrics ΕΠΕ</text>
-  <text x="60" y="170" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Λεωφ. Αναλυτικής 123, Αθήνα 12345</text>
-  <text x="60" y="185" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">billing@orthodoxmetrics.com | (210) 123-4567</text>
-  
-  <!-- Client Info Box -->
-  <rect x="470" y="120" width="280" height="70" fill="rgba(255,255,255,0.1)" rx="8"/>
-  <text x="480" y="140" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Προς:</text>
-  <text x="480" y="155" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Όνομα Πελάτη</text>
-  <text x="480" y="170" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Διεύθυνση Πελάτη</text>
-  <text x="480" y="185" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">client@email.com</text>
-  
-  <!-- Invoice Details Section -->
-  <rect x="50" y="230" width="700" height="100" fill="white" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  
-  <!-- Left Column -->
-  <text x="70" y="255" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Αριθμός Τιμολογίου:</text>
-  <text x="230" y="255" font-family="Arial, sans-serif" font-size="14" fill="url(#goldGradient)" font-weight="600">#OM-2025-001</text>
-  
-  <text x="70" y="280" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Ημερομηνία Έκδοσης:</text>
-  <text x="230" y="280" font-family="Arial, sans-serif" font-size="14" fill="#666">6 Ιουλίου 2025</text>
-  
-  <!-- Right Column -->
-  <text x="420" y="255" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Ημερομηνία Λήξης:</text>
-  <text x="560" y="255" font-family="Arial, sans-serif" font-size="14" fill="url(#goldGradient)" font-weight="600">5 Αυγούστου 2025</text>
-  
-  <text x="420" y="280" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Όροι Πληρωμής:</text>
-  <text x="560" y="280" font-family="Arial, sans-serif" font-size="14" fill="#666">30 ημέρες</text>
-  
-  <!-- Services Section Title -->
-  <text x="50" y="370" font-family="Arial, sans-serif" font-size="18" font-weight="600" fill="#663399">Παρεχόμενες Υπηρεσίες</text>
-  <line x1="50" y1="375" x2="750" y2="375" stroke="url(#goldGradient)" stroke-width="2"/>
-  
-  <!-- Table Header -->
-  <rect x="50" y="390" width="700" height="40" fill="url(#tableHeaderGradient)" rx="8 8 0 0"/>
-  <text x="70" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Περιγραφή</text>
-  <text x="540" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Ποσότητα</text>
-  <text x="620" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Τιμή</text>
-  <text x="700" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Ποσό</text>
-  
-  <!-- Empty Table Rows -->
-  <rect x="50" y="430" width="700" height="40" fill="white" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="470" width="700" height="40" fill="#F8F6FF" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="510" width="700" height="40" fill="white" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="550" width="700" height="40" fill="#F8F6FF" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="590" width="700" height="40" fill="white" stroke="#E6E6FA" stroke-width="1" rx="0 0 8 8"/>
-  
-  <!-- Totals Section -->
-  <rect x="450" y="660" width="300" height="120" fill="#F8F6FF" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  
-  <text x="470" y="685" font-family="Arial, sans-serif" font-size="14" fill="#663399">Υποσύνολο:</text>
-  <text x="720" y="685" text-anchor="end" font-family="Arial, sans-serif" font-size="14" fill="#666">€0,00</text>
-  
-  <text x="470" y="710" font-family="Arial, sans-serif" font-size="14" fill="#663399">ΦΠΑ:</text>
-  <text x="720" y="710" text-anchor="end" font-family="Arial, sans-serif" font-size="14" fill="#666">€0,00</text>
-  
-  <line x1="470" y1="720" x2="730" y2="720" stroke="url(#goldGradient)" stroke-width="2"/>
-  
-  <text x="470" y="745" font-family="Arial, sans-serif" font-size="16" font-weight="700" fill="#663399">Συνολικό Ποσό:</text>
-  <text x="720" y="745" text-anchor="end" font-family="Arial, sans-serif" font-size="16" font-weight="700" fill="url(#goldGradient)">€0,00</text>
-  
-  <!-- Payment Instructions -->
-  <rect x="50" y="810" width="320" height="100" fill="white" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  <text x="70" y="835" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Οδηγίες Πληρωμής</text>
-  <text x="70" y="855" font-family="Arial, sans-serif" font-size="12" fill="#666">Τραπεζικό Έμβασμα: Orthodox Metrics</text>
-  <text x="70" y="870" font-family="Arial, sans-serif" font-size="12" fill="#666">Αριθμός Λογαριασμού: 1234567890</text>
-  <text x="70" y="885" font-family="Arial, sans-serif" font-size="12" fill="#666">Κωδικός Τράπεζας: 987654321</text>
-  <text x="70" y="900" font-family="Arial, sans-serif" font-size="12" fill="#666">Αναφορά: Αριθμός Τιμολογίου</text>
-  
-  <!-- Notes Section -->
-  <rect x="430" y="810" width="320" height="100" fill="white" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  <text x="450" y="835" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Σημειώσεις</text>
-  <text x="450" y="855" font-family="Arial, sans-serif" font-size="12" fill="#666">Σας ευχαριστούμε που επιλέξατε</text>
-  <text x="450" y="870" font-family="Arial, sans-serif" font-size="12" fill="#666">την Orthodox Metrics για τις</text>
-  <text x="450" y="885" font-family="Arial, sans-serif" font-size="12" fill="#666">υπηρεσίες αναλυτικής δεδομένων.</text>
-  
-  <!-- Footer -->
-  <rect x="0" y="940" width="800" height="160" fill="#663399"/>
-  
-  <text x="400" y="970" text-anchor="middle" font-family="Arial, sans-serif" font-size="16" font-weight="600" fill="white">Orthodox Metrics</text>
-  <text x="400" y="990" text-anchor="middle" font-family="Arial, sans-serif" font-size="14" fill="#E6E6FA">Μετατρέποντας Δεδομένα σε Πρακτικές Λύσεις</text>
-  
-  <text x="400" y="1020" text-anchor="middle" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Για ερωτήσεις σχετικά με αυτό το τιμολόγιο, επικοινωνήστε μαζί μας στο billing@orthodoxmetrics.com</text>
-  
-  <!-- Payment Terms Box -->
-  <rect x="50" y="1040" width="700" height="50" fill="rgba(255,255,255,0.1)" rx="8"/>
-  <text x="400" y="1060" text-anchor="middle" font-family="Arial, sans-serif" font-size="12" font-weight="600" fill="url(#goldGradient)">Όροι και Προϋποθέσεις Πληρωμής</text>
-  <text x="400" y="1075" text-anchor="middle" font-family="Arial, sans-serif" font-size="10" fill="#E6E6FA">Η πληρωμή οφείλεται εντός 30 ημερών από την ημερομηνία του τιμολογίου.</text>
-  
-  <!-- Decorative Elements -->
-  <circle cx="750" cy="50" r="3" fill="url(#goldGradient)" opacity="0.6"/>
-  <circle cx="730" cy="30" r="2" fill="url(#goldGradient)" opacity="0.4"/>
-  <circle cx="770" cy="70" r="1.5" fill="url(#goldGradient)" opacity="0.8"/>
-  
-</svg>
\ No newline at end of file
diff --git a/front-end/public/images/orthodox_metrics_invoice_romanian.svg b/front-end/public/images/orthodox_metrics_invoice_romanian.svg
deleted file mode 100755
index 8af81abf..00000000
--- a/front-end/public/images/orthodox_metrics_invoice_romanian.svg
+++ /dev/null
@@ -1,141 +0,0 @@
-<svg width="800" height="1100" xmlns="http://www.w3.org/2000/svg">
-  <defs>
-    <!-- Purple to Gold Gradient -->
-    <linearGradient id="headerGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#663399;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#9966CC;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Gold Accent Gradient -->
-    <linearGradient id="goldGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#FFD700;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#FFA500;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Light Background -->
-    <linearGradient id="backgroundGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#F8F6FF;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#FFF9E6;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Table Header Gradient -->
-    <linearGradient id="tableHeaderGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#4A1A5C;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#663399;stop-opacity:1" />
-    </linearGradient>
-  </defs>
-  
-  <!-- Main Background -->
-  <rect width="800" height="1100" fill="url(#backgroundGradient)"/>
-  
-  <!-- Header Section -->
-  <rect width="800" height="200" fill="url(#headerGradient)"/>
-  
-  <!-- Logo Circle -->
-  <circle cx="60" cy="80" r="30" fill="white"/>
-  <text x="60" y="88" text-anchor="middle" font-family="Arial, sans-serif" font-size="20" font-weight="bold" fill="#663399">OM</text>
-  
-  <!-- Company Name -->
-  <text x="110" y="70" font-family="Arial, sans-serif" font-size="28" font-weight="300" fill="white">Orthodox Metrics</text>
-  <text x="110" y="95" font-family="Arial, sans-serif" font-size="14" fill="#E6E6FA">Soluții și Analiză Bazate pe Date</text>
-  
-  <!-- Invoice Title -->
-  <text x="720" y="80" text-anchor="end" font-family="Arial, sans-serif" font-size="48" font-weight="100" fill="white" opacity="0.4">FACTURĂ</text>
-  
-  <!-- Company Info Box -->
-  <rect x="50" y="120" width="280" height="70" fill="rgba(255,255,255,0.1)" rx="8"/>
-  <text x="60" y="140" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">De la:</text>
-  <text x="60" y="155" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Orthodox Metrics SRL</text>
-  <text x="60" y="170" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Bulevardul Analiticii 123, București 123456</text>
-  <text x="60" y="185" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">billing@orthodoxmetrics.com | (021) 123-4567</text>
-  
-  <!-- Client Info Box -->
-  <rect x="470" y="120" width="280" height="70" fill="rgba(255,255,255,0.1)" rx="8"/>
-  <text x="480" y="140" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Către:</text>
-  <text x="480" y="155" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Numele clientului</text>
-  <text x="480" y="170" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Adresa clientului</text>
-  <text x="480" y="185" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">client@email.com</text>
-  
-  <!-- Invoice Details Section -->
-  <rect x="50" y="230" width="700" height="100" fill="white" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  
-  <!-- Left Column -->
-  <text x="70" y="255" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Numărul facturii:</text>
-  <text x="210" y="255" font-family="Arial, sans-serif" font-size="14" fill="url(#goldGradient)" font-weight="600">#OM-2025-001</text>
-  
-  <text x="70" y="280" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Data emiterii:</text>
-  <text x="210" y="280" font-family="Arial, sans-serif" font-size="14" fill="#666">6 iulie 2025</text>
-  
-  <!-- Right Column -->
-  <text x="420" y="255" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Data scadenței:</text>
-  <text x="540" y="255" font-family="Arial, sans-serif" font-size="14" fill="url(#goldGradient)" font-weight="600">5 august 2025</text>
-  
-  <text x="420" y="280" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Termeni de plată:</text>
-  <text x="540" y="280" font-family="Arial, sans-serif" font-size="14" fill="#666">30 zile</text>
-  
-  <!-- Services Section Title -->
-  <text x="50" y="370" font-family="Arial, sans-serif" font-size="18" font-weight="600" fill="#663399">Servicii furnizate</text>
-  <line x1="50" y1="375" x2="750" y2="375" stroke="url(#goldGradient)" stroke-width="2"/>
-  
-  <!-- Table Header -->
-  <rect x="50" y="390" width="700" height="40" fill="url(#tableHeaderGradient)" rx="8 8 0 0"/>
-  <text x="70" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Descriere</text>
-  <text x="540" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Cant.</text>
-  <text x="620" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Tarif</text>
-  <text x="700" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Sumă</text>
-  
-  <!-- Empty Table Rows -->
-  <rect x="50" y="430" width="700" height="40" fill="white" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="470" width="700" height="40" fill="#F8F6FF" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="510" width="700" height="40" fill="white" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="550" width="700" height="40" fill="#F8F6FF" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="590" width="700" height="40" fill="white" stroke="#E6E6FA" stroke-width="1" rx="0 0 8 8"/>
-  
-  <!-- Totals Section -->
-  <rect x="450" y="660" width="300" height="120" fill="#F8F6FF" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  
-  <text x="470" y="685" font-family="Arial, sans-serif" font-size="14" fill="#663399">Subtotal:</text>
-  <text x="720" y="685" text-anchor="end" font-family="Arial, sans-serif" font-size="14" fill="#666">0,00 RON</text>
-  
-  <text x="470" y="710" font-family="Arial, sans-serif" font-size="14" fill="#663399">TVA:</text>
-  <text x="720" y="710" text-anchor="end" font-family="Arial, sans-serif" font-size="14" fill="#666">0,00 RON</text>
-  
-  <line x1="470" y1="720" x2="730" y2="720" stroke="url(#goldGradient)" stroke-width="2"/>
-  
-  <text x="470" y="745" font-family="Arial, sans-serif" font-size="16" font-weight="700" fill="#663399">Suma totală:</text>
-  <text x="720" y="745" text-anchor="end" font-family="Arial, sans-serif" font-size="16" font-weight="700" fill="url(#goldGradient)">0,00 RON</text>
-  
-  <!-- Payment Instructions -->
-  <rect x="50" y="810" width="320" height="100" fill="white" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  <text x="70" y="835" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Instrucțiuni de plată</text>
-  <text x="70" y="855" font-family="Arial, sans-serif" font-size="12" fill="#666">Transfer bancar: Orthodox Metrics</text>
-  <text x="70" y="870" font-family="Arial, sans-serif" font-size="12" fill="#666">Numărul contului: 1234567890</text>
-  <text x="70" y="885" font-family="Arial, sans-serif" font-size="12" fill="#666">Codul SWIFT: 987654321</text>
-  <text x="70" y="900" font-family="Arial, sans-serif" font-size="12" fill="#666">Referință: Numărul facturii</text>
-  
-  <!-- Notes Section -->
-  <rect x="430" y="810" width="320" height="100" fill="white" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  <text x="450" y="835" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Note</text>
-  <text x="450" y="855" font-family="Arial, sans-serif" font-size="12" fill="#666">Vă mulțumim că ați ales Orthodox</text>
-  <text x="450" y="870" font-family="Arial, sans-serif" font-size="12" fill="#666">Metrics pentru nevoile dvs. de</text>
-  <text x="450" y="885" font-family="Arial, sans-serif" font-size="12" fill="#666">analitică a datelor.</text>
-  
-  <!-- Footer -->
-  <rect x="0" y="940" width="800" height="160" fill="#663399"/>
-  
-  <text x="400" y="970" text-anchor="middle" font-family="Arial, sans-serif" font-size="16" font-weight="600" fill="white">Orthodox Metrics</text>
-  <text x="400" y="990" text-anchor="middle" font-family="Arial, sans-serif" font-size="14" fill="#E6E6FA">Transformând Datele în Soluții Aplicabile</text>
-  
-  <text x="400" y="1020" text-anchor="middle" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Pentru întrebări despre această factură, vă rugăm să ne contactați la billing@orthodoxmetrics.com</text>
-  
-  <!-- Payment Terms Box -->
-  <rect x="50" y="1040" width="700" height="50" fill="rgba(255,255,255,0.1)" rx="8"/>
-  <text x="400" y="1060" text-anchor="middle" font-family="Arial, sans-serif" font-size="12" font-weight="600" fill="url(#goldGradient)">Termeni și Condiții de Plată</text>
-  <text x="400" y="1075" text-anchor="middle" font-family="Arial, sans-serif" font-size="10" fill="#E6E6FA">Plata se efectuează în termen de 30 de zile de la data emiterii facturii.</text>
-  
-  <!-- Decorative Elements -->
-  <circle cx="750" cy="50" r="3" fill="url(#goldGradient)" opacity="0.6"/>
-  <circle cx="730" cy="30" r="2" fill="url(#goldGradient)" opacity="0.4"/>
-  <circle cx="770" cy="70" r="1.5" fill="url(#goldGradient)" opacity="0.8"/>
-  
-</svg>
\ No newline at end of file
diff --git a/front-end/public/images/orthodox_metrics_invoice_russian.svg b/front-end/public/images/orthodox_metrics_invoice_russian.svg
deleted file mode 100755
index 13373342..00000000
--- a/front-end/public/images/orthodox_metrics_invoice_russian.svg
+++ /dev/null
@@ -1,141 +0,0 @@
-<svg width="800" height="1100" xmlns="http://www.w3.org/2000/svg">
-  <defs>
-    <!-- Purple to Gold Gradient -->
-    <linearGradient id="headerGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#663399;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#9966CC;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Gold Accent Gradient -->
-    <linearGradient id="goldGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#FFD700;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#FFA500;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Light Background -->
-    <linearGradient id="backgroundGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#F8F6FF;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#FFF9E6;stop-opacity:1" />
-    </linearGradient>
-    
-    <!-- Table Header Gradient -->
-    <linearGradient id="tableHeaderGradient" x1="0%" y1="0%" x2="100%" y2="100%">
-      <stop offset="0%" style="stop-color:#4A1A5C;stop-opacity:1" />
-      <stop offset="100%" style="stop-color:#663399;stop-opacity:1" />
-    </linearGradient>
-  </defs>
-  
-  <!-- Main Background -->
-  <rect width="800" height="1100" fill="url(#backgroundGradient)"/>
-  
-  <!-- Header Section -->
-  <rect width="800" height="200" fill="url(#headerGradient)"/>
-  
-  <!-- Logo Circle -->
-  <circle cx="60" cy="80" r="30" fill="white"/>
-  <text x="60" y="88" text-anchor="middle" font-family="Arial, sans-serif" font-size="20" font-weight="bold" fill="#663399">OM</text>
-  
-  <!-- Company Name -->
-  <text x="110" y="70" font-family="Arial, sans-serif" font-size="28" font-weight="300" fill="white">Orthodox Metrics</text>
-  <text x="110" y="95" font-family="Arial, sans-serif" font-size="14" fill="#E6E6FA">Решения и Аналитика на Основе Данных</text>
-  
-  <!-- Invoice Title -->
-  <text x="720" y="80" text-anchor="end" font-family="Arial, sans-serif" font-size="48" font-weight="100" fill="white" opacity="0.4">СЧЕТ</text>
-  
-  <!-- Company Info Box -->
-  <rect x="50" y="120" width="280" height="70" fill="rgba(255,255,255,0.1)" rx="8"/>
-  <text x="60" y="140" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">От:</text>
-  <text x="60" y="155" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Orthodox Metrics ООО</text>
-  <text x="60" y="170" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Аналитический бульвар 123, Москва 123456</text>
-  <text x="60" y="185" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">billing@orthodoxmetrics.com | (495) 123-4567</text>
-  
-  <!-- Client Info Box -->
-  <rect x="470" y="120" width="280" height="70" fill="rgba(255,255,255,0.1)" rx="8"/>
-  <text x="480" y="140" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Кому:</text>
-  <text x="480" y="155" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Имя клиента</text>
-  <text x="480" y="170" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">Адрес клиента</text>
-  <text x="480" y="185" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">client@email.com</text>
-  
-  <!-- Invoice Details Section -->
-  <rect x="50" y="230" width="700" height="100" fill="white" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  
-  <!-- Left Column -->
-  <text x="70" y="255" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Номер счета:</text>
-  <text x="190" y="255" font-family="Arial, sans-serif" font-size="14" fill="url(#goldGradient)" font-weight="600">#OM-2025-001</text>
-  
-  <text x="70" y="280" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Дата выставления:</text>
-  <text x="190" y="280" font-family="Arial, sans-serif" font-size="14" fill="#666">6 июля 2025</text>
-  
-  <!-- Right Column -->
-  <text x="420" y="255" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Срок оплаты:</text>
-  <text x="530" y="255" font-family="Arial, sans-serif" font-size="14" fill="url(#goldGradient)" font-weight="600">5 августа 2025</text>
-  
-  <text x="420" y="280" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Условия оплаты:</text>
-  <text x="530" y="280" font-family="Arial, sans-serif" font-size="14" fill="#666">30 дней</text>
-  
-  <!-- Services Section Title -->
-  <text x="50" y="370" font-family="Arial, sans-serif" font-size="18" font-weight="600" fill="#663399">Оказанные услуги</text>
-  <line x1="50" y1="375" x2="750" y2="375" stroke="url(#goldGradient)" stroke-width="2"/>
-  
-  <!-- Table Header -->
-  <rect x="50" y="390" width="700" height="40" fill="url(#tableHeaderGradient)" rx="8 8 0 0"/>
-  <text x="70" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Описание</text>
-  <text x="540" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Кол-во</text>
-  <text x="620" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Цена</text>
-  <text x="700" y="413" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="white">Сумма</text>
-  
-  <!-- Empty Table Rows -->
-  <rect x="50" y="430" width="700" height="40" fill="white" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="470" width="700" height="40" fill="#F8F6FF" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="510" width="700" height="40" fill="white" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="550" width="700" height="40" fill="#F8F6FF" stroke="#E6E6FA" stroke-width="1"/>
-  <rect x="50" y="590" width="700" height="40" fill="white" stroke="#E6E6FA" stroke-width="1" rx="0 0 8 8"/>
-  
-  <!-- Totals Section -->
-  <rect x="450" y="660" width="300" height="120" fill="#F8F6FF" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  
-  <text x="470" y="685" font-family="Arial, sans-serif" font-size="14" fill="#663399">Подытог:</text>
-  <text x="720" y="685" text-anchor="end" font-family="Arial, sans-serif" font-size="14" fill="#666">₽0,00</text>
-  
-  <text x="470" y="710" font-family="Arial, sans-serif" font-size="14" fill="#663399">НДС:</text>
-  <text x="720" y="710" text-anchor="end" font-family="Arial, sans-serif" font-size="14" fill="#666">₽0,00</text>
-  
-  <line x1="470" y1="720" x2="730" y2="720" stroke="url(#goldGradient)" stroke-width="2"/>
-  
-  <text x="470" y="745" font-family="Arial, sans-serif" font-size="16" font-weight="700" fill="#663399">Общая сумма:</text>
-  <text x="720" y="745" text-anchor="end" font-family="Arial, sans-serif" font-size="16" font-weight="700" fill="url(#goldGradient)">₽0,00</text>
-  
-  <!-- Payment Instructions -->
-  <rect x="50" y="810" width="320" height="100" fill="white" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  <text x="70" y="835" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Инструкции по оплате</text>
-  <text x="70" y="855" font-family="Arial, sans-serif" font-size="12" fill="#666">Банковский перевод: Orthodox Metrics</text>
-  <text x="70" y="870" font-family="Arial, sans-serif" font-size="12" fill="#666">Номер счета: 1234567890</text>
-  <text x="70" y="885" font-family="Arial, sans-serif" font-size="12" fill="#666">БИК: 987654321</text>
-  <text x="70" y="900" font-family="Arial, sans-serif" font-size="12" fill="#666">Назначение: Номер счета</text>
-  
-  <!-- Notes Section -->
-  <rect x="430" y="810" width="320" height="100" fill="white" rx="8" stroke="#DDA0DD" stroke-width="1"/>
-  <text x="450" y="835" font-family="Arial, sans-serif" font-size="14" font-weight="600" fill="#663399">Примечания</text>
-  <text x="450" y="855" font-family="Arial, sans-serif" font-size="12" fill="#666">Спасибо за выбор Orthodox Metrics</text>
-  <text x="450" y="870" font-family="Arial, sans-serif" font-size="12" fill="#666">для ваших потребностей в</text>
-  <text x="450" y="885" font-family="Arial, sans-serif" font-size="12" fill="#666">аналитике данных.</text>
-  
-  <!-- Footer -->
-  <rect x="0" y="940" width="800" height="160" fill="#663399"/>
-  
-  <text x="400" y="970" text-anchor="middle" font-family="Arial, sans-serif" font-size="16" font-weight="600" fill="white">Orthodox Metrics</text>
-  <text x="400" y="990" text-anchor="middle" font-family="Arial, sans-serif" font-size="14" fill="#E6E6FA">Преобразование Данных в Действенные Решения</text>
-  
-  <text x="400" y="1020" text-anchor="middle" font-family="Arial, sans-serif" font-size="12" fill="#E6E6FA">По вопросам об этом счете обращайтесь к нам по адресу billing@orthodoxmetrics.com</text>
-  
-  <!-- Payment Terms Box -->
-  <rect x="50" y="1040" width="700" height="50" fill="rgba(255,255,255,0.1)" rx="8"/>
-  <text x="400" y="1060" text-anchor="middle" font-family="Arial, sans-serif" font-size="12" font-weight="600" fill="url(#goldGradient)">Условия и Положения Оплаты</text>
-  <text x="400" y="1075" text-anchor="middle" font-family="Arial, sans-serif" font-size="10" fill="#E6E6FA">Оплата должна быть произведена в течение 30 дней с даты выставления счета.</text>
-  
-  <!-- Decorative Elements -->
-  <circle cx="750" cy="50" r="3" fill="url(#goldGradient)" opacity="0.6"/>
-  <circle cx="730" cy="30" r="2" fill="url(#goldGradient)" opacity="0.4"/>
-  <circle cx="770" cy="70" r="1.5" fill="url(#goldGradient)" opacity="0.8"/>
-  
-</svg>
\ No newline at end of file
diff --git a/front-end/public/images/patterns/buttons_bg.png b/front-end/public/images/patterns/buttons_bg.png
deleted file mode 100755
index 7cbe0577..00000000
Binary files a/front-end/public/images/patterns/buttons_bg.png and /dev/null differ
diff --git a/front-end/public/images/patterns/gold.png b/front-end/public/images/patterns/gold.png
deleted file mode 100755
index e17e9133..00000000
Binary files a/front-end/public/images/patterns/gold.png and /dev/null differ
diff --git a/front-end/public/images/patterns/inside.png b/front-end/public/images/patterns/inside.png
deleted file mode 100755
index 2b9469f6..00000000
Binary files a/front-end/public/images/patterns/inside.png and /dev/null differ
diff --git a/front-end/public/images/patterns/page-bg.png b/front-end/public/images/patterns/page-bg.png
deleted file mode 100755
index 24a5e494..00000000
Binary files a/front-end/public/images/patterns/page-bg.png and /dev/null differ
diff --git a/front-end/public/images/patterns/var.png b/front-end/public/images/patterns/var.png
deleted file mode 100755
index 13f6f1ca..00000000
Binary files a/front-end/public/images/patterns/var.png and /dev/null differ
diff --git a/front-end/public/images/people/om-1.png b/front-end/public/images/people/om-1.png
deleted file mode 100644
index 326d2e66..00000000
Binary files a/front-end/public/images/people/om-1.png and /dev/null differ
diff --git a/front-end/public/images/placeholder.png b/front-end/public/images/placeholder.png
deleted file mode 100755
index 1f0e9691..00000000
Binary files a/front-end/public/images/placeholder.png and /dev/null differ
diff --git a/front-end/public/images/places/beach1.png b/front-end/public/images/places/beach1.png
deleted file mode 100755
index 84254353..00000000
Binary files a/front-end/public/images/places/beach1.png and /dev/null differ
diff --git a/front-end/public/images/places/beach2.png b/front-end/public/images/places/beach2.png
deleted file mode 100755
index 4ad487e8..00000000
Binary files a/front-end/public/images/places/beach2.png and /dev/null differ
diff --git a/front-end/public/images/places/beach3.png b/front-end/public/images/places/beach3.png
deleted file mode 100755
index b653c156..00000000
Binary files a/front-end/public/images/places/beach3.png and /dev/null differ
diff --git a/front-end/public/images/prayer-rope.png b/front-end/public/images/prayer-rope.png
deleted file mode 100755
index 239bf34a..00000000
Binary files a/front-end/public/images/prayer-rope.png and /dev/null differ
diff --git a/front-end/public/images/profile/ChatGPT Image Aug 2, 2025, 06_25_00 AM.png b/front-end/public/images/profile/ChatGPT Image Aug 2, 2025, 06_25_00 AM.png
deleted file mode 100755
index ec0b0047..00000000
Binary files a/front-end/public/images/profile/ChatGPT Image Aug 2, 2025, 06_25_00 AM.png and /dev/null differ
diff --git a/front-end/public/images/random/Five Phases of Church Record Digitization.png b/front-end/public/images/random/Five Phases of Church Record Digitization.png
deleted file mode 100755
index d1335602..00000000
Binary files a/front-end/public/images/random/Five Phases of Church Record Digitization.png and /dev/null differ
diff --git a/front-end/public/images/random/Rectangle.png b/front-end/public/images/random/Rectangle.png
deleted file mode 100755
index c943e95f..00000000
Binary files a/front-end/public/images/random/Rectangle.png and /dev/null differ
diff --git a/front-end/public/images/random/empty-icon.png b/front-end/public/images/random/empty-icon.png
deleted file mode 100755
index 52c0be98..00000000
Binary files a/front-end/public/images/random/empty-icon.png and /dev/null differ
diff --git a/front-end/public/images/random/liturgical-red.png b/front-end/public/images/random/liturgical-red.png
deleted file mode 100755
index 710b2580..00000000
Binary files a/front-end/public/images/random/liturgical-red.png and /dev/null differ
diff --git a/front-end/public/images/random/om-archives.png b/front-end/public/images/random/om-archives.png
deleted file mode 100755
index 18c7f1c3..00000000
Binary files a/front-end/public/images/random/om-archives.png and /dev/null differ
diff --git a/front-end/public/images/random/orthodox-colors.png b/front-end/public/images/random/orthodox-colors.png
deleted file mode 100755
index ccd0e841..00000000
Binary files a/front-end/public/images/random/orthodox-colors.png and /dev/null differ
diff --git a/front-end/public/images/random/red-vestment2.png b/front-end/public/images/random/red-vestment2.png
deleted file mode 100755
index ca2cf32d..00000000
Binary files a/front-end/public/images/random/red-vestment2.png and /dev/null differ
diff --git a/front-end/public/images/random/sand-engraved.png b/front-end/public/images/random/sand-engraved.png
deleted file mode 100755
index 02972f00..00000000
Binary files a/front-end/public/images/random/sand-engraved.png and /dev/null differ
diff --git a/front-end/public/images/random/x.png b/front-end/public/images/random/x.png
deleted file mode 100755
index 0d84308c..00000000
Binary files a/front-end/public/images/random/x.png and /dev/null differ
diff --git a/front-end/public/images/records/._3-day.png b/front-end/public/images/records/._3-day.png
deleted file mode 100755
index 09e3e98d..00000000
Binary files a/front-end/public/images/records/._3-day.png and /dev/null differ
diff --git a/front-end/public/images/records/._46-bg.png b/front-end/public/images/records/._46-bg.png
deleted file mode 100755
index fafeb14f..00000000
Binary files a/front-end/public/images/records/._46-bg.png and /dev/null differ
diff --git a/front-end/public/images/records/12-2025.png b/front-end/public/images/records/12-2025.png
deleted file mode 100755
index f38bdfc3..00000000
Binary files a/front-end/public/images/records/12-2025.png and /dev/null differ
diff --git a/front-end/public/images/records/3-day.png b/front-end/public/images/records/3-day.png
deleted file mode 100755
index fa6475a4..00000000
Binary files a/front-end/public/images/records/3-day.png and /dev/null differ
diff --git a/front-end/public/images/records/46-bg.png b/front-end/public/images/records/46-bg.png
deleted file mode 100755
index ca2c496f..00000000
Binary files a/front-end/public/images/records/46-bg.png and /dev/null differ
diff --git a/front-end/public/images/records/46-button1.png b/front-end/public/images/records/46-button1.png
deleted file mode 100755
index 90528682..00000000
Binary files a/front-end/public/images/records/46-button1.png and /dev/null differ
diff --git a/front-end/public/images/records/46-logo.png b/front-end/public/images/records/46-logo.png
deleted file mode 100755
index 58fa9056..00000000
Binary files a/front-end/public/images/records/46-logo.png and /dev/null differ
diff --git a/front-end/public/images/records/46-parish-left.png b/front-end/public/images/records/46-parish-left.png
deleted file mode 100755
index b49eff7d..00000000
Binary files a/front-end/public/images/records/46-parish-left.png and /dev/null differ
diff --git a/front-end/public/images/records/46-parish-right.png b/front-end/public/images/records/46-parish-right.png
deleted file mode 100755
index a830aa74..00000000
Binary files a/front-end/public/images/records/46-parish-right.png and /dev/null differ
diff --git a/front-end/public/images/records/46-sample1.png b/front-end/public/images/records/46-sample1.png
deleted file mode 100755
index 57948011..00000000
Binary files a/front-end/public/images/records/46-sample1.png and /dev/null differ
diff --git a/front-end/public/images/records/FULL-BAR.png b/front-end/public/images/records/FULL-BAR.png
deleted file mode 100755
index 092fa333..00000000
Binary files a/front-end/public/images/records/FULL-BAR.png and /dev/null differ
diff --git a/front-end/public/images/records/analysis-thumb.png b/front-end/public/images/records/analysis-thumb.png
deleted file mode 100755
index 05725870..00000000
Binary files a/front-end/public/images/records/analysis-thumb.png and /dev/null differ
diff --git a/front-end/public/images/records/baptism.png b/front-end/public/images/records/baptism.png
index a0677e2e..5beb37d4 100755
Binary files a/front-end/public/images/records/baptism.png and b/front-end/public/images/records/baptism.png differ
diff --git a/front-end/public/images/records/border-gold-black.png b/front-end/public/images/records/border-gold-black.png
deleted file mode 100755
index 045ddbbf..00000000
Binary files a/front-end/public/images/records/border-gold-black.png and /dev/null differ
diff --git a/front-end/public/images/records/border-gold-white.png b/front-end/public/images/records/border-gold-white.png
deleted file mode 100755
index 0e05b025..00000000
Binary files a/front-end/public/images/records/border-gold-white.png and /dev/null differ
diff --git a/front-end/public/images/records/border-gold.png b/front-end/public/images/records/border-gold.png
deleted file mode 100755
index 0b383cbd..00000000
Binary files a/front-end/public/images/records/border-gold.png and /dev/null differ
diff --git a/front-end/public/images/records/border-gold2.png b/front-end/public/images/records/border-gold2.png
deleted file mode 100755
index f0138a12..00000000
Binary files a/front-end/public/images/records/border-gold2.png and /dev/null differ
diff --git a/front-end/public/images/records/en-bap-records.png b/front-end/public/images/records/en-bap-records.png
deleted file mode 100755
index b4b4354d..00000000
Binary files a/front-end/public/images/records/en-bap-records.png and /dev/null differ
diff --git a/front-end/public/images/records/en-funeral-records.png b/front-end/public/images/records/en-funeral-records.png
deleted file mode 100755
index 56b0fb6e..00000000
Binary files a/front-end/public/images/records/en-funeral-records.png and /dev/null differ
diff --git a/front-end/public/images/records/en-marr-records.png b/front-end/public/images/records/en-marr-records.png
deleted file mode 100755
index 8c596619..00000000
Binary files a/front-end/public/images/records/en-marr-records.png and /dev/null differ
diff --git a/front-end/public/images/records/funeral.png b/front-end/public/images/records/funeral.png
index 59c8ebe5..7e1c4f2c 100755
Binary files a/front-end/public/images/records/funeral.png and b/front-end/public/images/records/funeral.png differ
diff --git a/front-end/public/images/records/marriage.png b/front-end/public/images/records/marriage.png
index dda6e22b..eb606f67 100755
Binary files a/front-end/public/images/records/marriage.png and b/front-end/public/images/records/marriage.png differ
diff --git a/front-end/public/images/records/menu-thumb.png b/front-end/public/images/records/menu-thumb.png
deleted file mode 100755
index 4f43d829..00000000
Binary files a/front-end/public/images/records/menu-thumb.png and /dev/null differ
diff --git a/front-end/public/images/records/sample4.png b/front-end/public/images/records/sample4.png
deleted file mode 100755
index bf4ce172..00000000
Binary files a/front-end/public/images/records/sample4.png and /dev/null differ
diff --git a/front-end/public/images/records/sspeter.png b/front-end/public/images/records/sspeter.png
deleted file mode 100755
index 9d020ce8..00000000
Binary files a/front-end/public/images/records/sspeter.png and /dev/null differ
diff --git a/front-end/public/images/records/sspp-sand.png b/front-end/public/images/records/sspp-sand.png
deleted file mode 100755
index 28d6b222..00000000
Binary files a/front-end/public/images/records/sspp-sand.png and /dev/null differ
diff --git a/front-end/public/images/records/ssppoc-full-header.png b/front-end/public/images/records/ssppoc-full-header.png
deleted file mode 100755
index 7d9417f8..00000000
Binary files a/front-end/public/images/records/ssppoc-full-header.png and /dev/null differ
diff --git a/front-end/public/images/records/ssppoc-p1.png b/front-end/public/images/records/ssppoc-p1.png
deleted file mode 100755
index 57ef2c56..00000000
Binary files a/front-end/public/images/records/ssppoc-p1.png and /dev/null differ
diff --git a/front-end/public/images/records/st-peter.png b/front-end/public/images/records/st-peter.png
deleted file mode 100755
index de8575a8..00000000
Binary files a/front-end/public/images/records/st-peter.png and /dev/null differ
diff --git a/front-end/public/images/records/stpaul.png b/front-end/public/images/records/stpaul.png
deleted file mode 100755
index 9f287a9f..00000000
Binary files a/front-end/public/images/records/stpaul.png and /dev/null differ
diff --git a/front-end/public/images/records/tmp1.png b/front-end/public/images/records/tmp1.png
deleted file mode 100755
index 45d1b54e..00000000
Binary files a/front-end/public/images/records/tmp1.png and /dev/null differ
diff --git a/front-end/public/images/reference/bap-defaults.png b/front-end/public/images/reference/bap-defaults.png
deleted file mode 100755
index adcd940e..00000000
Binary files a/front-end/public/images/reference/bap-defaults.png and /dev/null differ
diff --git a/front-end/public/images/reference/en-baptism-certificate-1.png b/front-end/public/images/reference/en-baptism-certificate-1.png
deleted file mode 100755
index fec7c61a..00000000
Binary files a/front-end/public/images/reference/en-baptism-certificate-1.png and /dev/null differ
diff --git a/front-end/public/images/reference/fun-defaults.png b/front-end/public/images/reference/fun-defaults.png
deleted file mode 100755
index 56533390..00000000
Binary files a/front-end/public/images/reference/fun-defaults.png and /dev/null differ
diff --git a/front-end/public/images/reference/mar-defaults.png b/front-end/public/images/reference/mar-defaults.png
deleted file mode 100755
index 2ecc77b8..00000000
Binary files a/front-end/public/images/reference/mar-defaults.png and /dev/null differ
diff --git a/front-end/public/images/sections/._sample.png b/front-end/public/images/sections/._sample.png
deleted file mode 100755
index b11fefb0..00000000
Binary files a/front-end/public/images/sections/._sample.png and /dev/null differ
diff --git a/front-end/public/images/sections/dune-crosses.png b/front-end/public/images/sections/dune-crosses.png
deleted file mode 100755
index 81bc3b65..00000000
Binary files a/front-end/public/images/sections/dune-crosses.png and /dev/null differ
diff --git a/front-end/public/images/sections/login2.png b/front-end/public/images/sections/login2.png
deleted file mode 100755
index e9af0b18..00000000
Binary files a/front-end/public/images/sections/login2.png and /dev/null differ
diff --git a/front-end/public/images/sections/om-archives.png b/front-end/public/images/sections/om-archives.png
deleted file mode 100755
index 02786c1a..00000000
Binary files a/front-end/public/images/sections/om-archives.png and /dev/null differ
diff --git a/front-end/public/images/sections/provide.png b/front-end/public/images/sections/provide.png
deleted file mode 100755
index 0224a1e2..00000000
Binary files a/front-end/public/images/sections/provide.png and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/1.jpg b/front-end/public/images/ss/New folder/1.jpg
deleted file mode 100644
index b4fc307c..00000000
Binary files a/front-end/public/images/ss/New folder/1.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/10.jpg b/front-end/public/images/ss/New folder/10.jpg
deleted file mode 100644
index f6a5c7d6..00000000
Binary files a/front-end/public/images/ss/New folder/10.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/11.jpg b/front-end/public/images/ss/New folder/11.jpg
deleted file mode 100644
index 2531ba4b..00000000
Binary files a/front-end/public/images/ss/New folder/11.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/12.jpg b/front-end/public/images/ss/New folder/12.jpg
deleted file mode 100644
index 56a2adbb..00000000
Binary files a/front-end/public/images/ss/New folder/12.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/13.jpg b/front-end/public/images/ss/New folder/13.jpg
deleted file mode 100644
index ea02b0bc..00000000
Binary files a/front-end/public/images/ss/New folder/13.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/14.jpg b/front-end/public/images/ss/New folder/14.jpg
deleted file mode 100644
index bb6a7e06..00000000
Binary files a/front-end/public/images/ss/New folder/14.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/15.jpg b/front-end/public/images/ss/New folder/15.jpg
deleted file mode 100644
index 420b7bc4..00000000
Binary files a/front-end/public/images/ss/New folder/15.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/16-addition.png b/front-end/public/images/ss/New folder/16-addition.png
deleted file mode 100644
index 497580b2..00000000
Binary files a/front-end/public/images/ss/New folder/16-addition.png and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/16.jpg b/front-end/public/images/ss/New folder/16.jpg
deleted file mode 100644
index 7d736142..00000000
Binary files a/front-end/public/images/ss/New folder/16.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/2.jpg b/front-end/public/images/ss/New folder/2.jpg
deleted file mode 100644
index 61026c8e..00000000
Binary files a/front-end/public/images/ss/New folder/2.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/3.jpg b/front-end/public/images/ss/New folder/3.jpg
deleted file mode 100644
index f93fdf99..00000000
Binary files a/front-end/public/images/ss/New folder/3.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/4.jpg b/front-end/public/images/ss/New folder/4.jpg
deleted file mode 100644
index 14d8e1e5..00000000
Binary files a/front-end/public/images/ss/New folder/4.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/5.jpg b/front-end/public/images/ss/New folder/5.jpg
deleted file mode 100644
index 4e306521..00000000
Binary files a/front-end/public/images/ss/New folder/5.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/6.jpg b/front-end/public/images/ss/New folder/6.jpg
deleted file mode 100644
index d1929b4d..00000000
Binary files a/front-end/public/images/ss/New folder/6.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/7.jpg b/front-end/public/images/ss/New folder/7.jpg
deleted file mode 100644
index 7efeeeef..00000000
Binary files a/front-end/public/images/ss/New folder/7.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/8.jpg b/front-end/public/images/ss/New folder/8.jpg
deleted file mode 100644
index df3c7e1f..00000000
Binary files a/front-end/public/images/ss/New folder/8.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/New folder/9.jpg b/front-end/public/images/ss/New folder/9.jpg
deleted file mode 100644
index 31a8a2a3..00000000
Binary files a/front-end/public/images/ss/New folder/9.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/apps/notes1.png b/front-end/public/images/ss/apps/notes1.png
deleted file mode 100755
index 8df12ae7..00000000
Binary files a/front-end/public/images/ss/apps/notes1.png and /dev/null differ
diff --git a/front-end/public/images/ss/auth/login1.png b/front-end/public/images/ss/auth/login1.png
deleted file mode 100755
index bc7c75f0..00000000
Binary files a/front-end/public/images/ss/auth/login1.png and /dev/null differ
diff --git a/front-end/public/images/ss/church/church_operations_suite/administration1.png b/front-end/public/images/ss/church/church_operations_suite/administration1.png
deleted file mode 100755
index cd58c907..00000000
Binary files a/front-end/public/images/ss/church/church_operations_suite/administration1.png and /dev/null differ
diff --git a/front-end/public/images/ss/church/church_operations_suite/annual_reports1.png b/front-end/public/images/ss/church/church_operations_suite/annual_reports1.png
deleted file mode 100755
index 15595ff3..00000000
Binary files a/front-end/public/images/ss/church/church_operations_suite/annual_reports1.png and /dev/null differ
diff --git a/front-end/public/images/ss/church/church_operations_suite/calendar1.png b/front-end/public/images/ss/church/church_operations_suite/calendar1.png
deleted file mode 100755
index a934c86f..00000000
Binary files a/front-end/public/images/ss/church/church_operations_suite/calendar1.png and /dev/null differ
diff --git a/front-end/public/images/ss/church/church_operations_suite/dashboard1.png b/front-end/public/images/ss/church/church_operations_suite/dashboard1.png
deleted file mode 100755
index c2ba0ee4..00000000
Binary files a/front-end/public/images/ss/church/church_operations_suite/dashboard1.png and /dev/null differ
diff --git a/front-end/public/images/ss/church/church_operations_suite/inventory1.png b/front-end/public/images/ss/church/church_operations_suite/inventory1.png
deleted file mode 100755
index f6928448..00000000
Binary files a/front-end/public/images/ss/church/church_operations_suite/inventory1.png and /dev/null differ
diff --git a/front-end/public/images/ss/church/church_operations_suite/library1.png b/front-end/public/images/ss/church/church_operations_suite/library1.png
deleted file mode 100755
index fcf6c4df..00000000
Binary files a/front-end/public/images/ss/church/church_operations_suite/library1.png and /dev/null differ
diff --git a/front-end/public/images/ss/church/church_operations_suite/storefront1.png b/front-end/public/images/ss/church/church_operations_suite/storefront1.png
deleted file mode 100755
index 8777af6f..00000000
Binary files a/front-end/public/images/ss/church/church_operations_suite/storefront1.png and /dev/null differ
diff --git a/front-end/public/images/ss/church/om_church_wiz.png b/front-end/public/images/ss/church/om_church_wiz.png
deleted file mode 100755
index 28c24a19..00000000
Binary files a/front-end/public/images/ss/church/om_church_wiz.png and /dev/null differ
diff --git a/front-end/public/images/ss/en_all.png b/front-end/public/images/ss/en_all.png
deleted file mode 100755
index c7752f00..00000000
Binary files a/front-end/public/images/ss/en_all.png and /dev/null differ
diff --git a/front-end/public/images/ss/en_bap1.png b/front-end/public/images/ss/en_bap1.png
deleted file mode 100755
index ab013b18..00000000
Binary files a/front-end/public/images/ss/en_bap1.png and /dev/null differ
diff --git a/front-end/public/images/ss/en_fun1.png b/front-end/public/images/ss/en_fun1.png
deleted file mode 100755
index 6ea0838b..00000000
Binary files a/front-end/public/images/ss/en_fun1.png and /dev/null differ
diff --git a/front-end/public/images/ss/en_mar1.png b/front-end/public/images/ss/en_mar1.png
deleted file mode 100755
index 4b5d7cf7..00000000
Binary files a/front-end/public/images/ss/en_mar1.png and /dev/null differ
diff --git a/front-end/public/images/ss/en_misc/en_gr_b1.png b/front-end/public/images/ss/en_misc/en_gr_b1.png
deleted file mode 100755
index c09d2bf7..00000000
Binary files a/front-end/public/images/ss/en_misc/en_gr_b1.png and /dev/null differ
diff --git a/front-end/public/images/ss/en_misc/en_gr_f1.png b/front-end/public/images/ss/en_misc/en_gr_f1.png
deleted file mode 100755
index 255f33b5..00000000
Binary files a/front-end/public/images/ss/en_misc/en_gr_f1.png and /dev/null differ
diff --git a/front-end/public/images/ss/en_misc/en_gr_m1.png b/front-end/public/images/ss/en_misc/en_gr_m1.png
deleted file mode 100755
index 749c9a24..00000000
Binary files a/front-end/public/images/ss/en_misc/en_gr_m1.png and /dev/null differ
diff --git a/front-end/public/images/ss/en_misc/en_ro_b1.png b/front-end/public/images/ss/en_misc/en_ro_b1.png
deleted file mode 100755
index 9d698696..00000000
Binary files a/front-end/public/images/ss/en_misc/en_ro_b1.png and /dev/null differ
diff --git a/front-end/public/images/ss/en_misc/en_ro_f1.png b/front-end/public/images/ss/en_misc/en_ro_f1.png
deleted file mode 100755
index 3b06a340..00000000
Binary files a/front-end/public/images/ss/en_misc/en_ro_f1.png and /dev/null differ
diff --git a/front-end/public/images/ss/en_misc/en_ro_m1.png b/front-end/public/images/ss/en_misc/en_ro_m1.png
deleted file mode 100755
index 76d5ca0b..00000000
Binary files a/front-end/public/images/ss/en_misc/en_ro_m1.png and /dev/null differ
diff --git a/front-end/public/images/ss/en_misc/en_ru_b1.png b/front-end/public/images/ss/en_misc/en_ru_b1.png
deleted file mode 100755
index 9d502f69..00000000
Binary files a/front-end/public/images/ss/en_misc/en_ru_b1.png and /dev/null differ
diff --git a/front-end/public/images/ss/en_misc/en_ru_f1.png b/front-end/public/images/ss/en_misc/en_ru_f1.png
deleted file mode 100755
index 50fc2650..00000000
Binary files a/front-end/public/images/ss/en_misc/en_ru_f1.png and /dev/null differ
diff --git a/front-end/public/images/ss/en_misc/en_ru_w1.png b/front-end/public/images/ss/en_misc/en_ru_w1.png
deleted file mode 100755
index f63a3346..00000000
Binary files a/front-end/public/images/ss/en_misc/en_ru_w1.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/2.png b/front-end/public/images/ss/ocr-redesigned/2.png
deleted file mode 100644
index f562c318..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/2.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/3.png b/front-end/public/images/ss/ocr-redesigned/3.png
deleted file mode 100644
index 3b64c11b..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/3.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/4.png b/front-end/public/images/ss/ocr-redesigned/4.png
deleted file mode 100644
index ce0e9643..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/4.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/5.png b/front-end/public/images/ss/ocr-redesigned/5.png
deleted file mode 100644
index b438f28b..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/5.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/6.png b/front-end/public/images/ss/ocr-redesigned/6.png
deleted file mode 100644
index c5424226..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/6.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/7.png b/front-end/public/images/ss/ocr-redesigned/7.png
deleted file mode 100644
index 072bcbd6..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/7.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/8.png b/front-end/public/images/ss/ocr-redesigned/8.png
deleted file mode 100644
index 813a8638..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/8.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-1.jpg b/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-1.jpg
deleted file mode 100644
index 4508f26b..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-1.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-2.jpg b/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-2.jpg
deleted file mode 100644
index edc0d002..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-2.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-3.jpg b/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-3.jpg
deleted file mode 100644
index 5a36733d..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-3.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-4.jpg b/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-4.jpg
deleted file mode 100644
index 6cb3bb13..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-4.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-5.jpg b/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-5.jpg
deleted file mode 100644
index 76e3e8da..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-5.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-6.jpg b/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-6.jpg
deleted file mode 100644
index e0657fc3..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-6.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-7.jpg b/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-7.jpg
deleted file mode 100644
index d5f2dcfb..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/enhanced-ocr-uploader-7.jpg and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/landingpage.png b/front-end/public/images/ss/ocr-redesigned/landingpage.png
deleted file mode 100644
index b7a53862..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/landingpage.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/marriages-1.png b/front-end/public/images/ss/ocr-redesigned/marriages-1.png
deleted file mode 100644
index cd9e1a88..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/marriages-1.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/settings-2.png b/front-end/public/images/ss/ocr-redesigned/settings-2.png
deleted file mode 100644
index 9ea4be8c..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/settings-2.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/settings-3.png b/front-end/public/images/ss/ocr-redesigned/settings-3.png
deleted file mode 100644
index 6e38483e..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/settings-3.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/settings.png b/front-end/public/images/ss/ocr-redesigned/settings.png
deleted file mode 100644
index e8e50549..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/settings.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/toast-message.png b/front-end/public/images/ss/ocr-redesigned/toast-message.png
deleted file mode 100644
index 038ce19a..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/toast-message.png and /dev/null differ
diff --git a/front-end/public/images/ss/ocr-redesigned/upload.png b/front-end/public/images/ss/ocr-redesigned/upload.png
deleted file mode 100644
index dd9a325a..00000000
Binary files a/front-end/public/images/ss/ocr-redesigned/upload.png and /dev/null differ
diff --git a/front-end/public/images/ss/refund.png b/front-end/public/images/ss/refund.png
deleted file mode 100755
index 44babc12..00000000
Binary files a/front-end/public/images/ss/refund.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/1.png b/front-end/public/images/ss/russian-ssppoc/1.png
deleted file mode 100755
index 12784210..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/1.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/10.png b/front-end/public/images/ss/russian-ssppoc/10.png
deleted file mode 100755
index 4defb54d..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/10.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/11.png b/front-end/public/images/ss/russian-ssppoc/11.png
deleted file mode 100755
index b59a24d9..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/11.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/12.png b/front-end/public/images/ss/russian-ssppoc/12.png
deleted file mode 100755
index 603cdf69..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/12.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/13.png b/front-end/public/images/ss/russian-ssppoc/13.png
deleted file mode 100755
index 7a2ebd72..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/13.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/14.png b/front-end/public/images/ss/russian-ssppoc/14.png
deleted file mode 100755
index 9da8da3c..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/14.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/15.png b/front-end/public/images/ss/russian-ssppoc/15.png
deleted file mode 100755
index 9399348a..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/15.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/16.png b/front-end/public/images/ss/russian-ssppoc/16.png
deleted file mode 100755
index b474075e..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/16.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/17.png b/front-end/public/images/ss/russian-ssppoc/17.png
deleted file mode 100755
index c030b193..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/17.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/18.png b/front-end/public/images/ss/russian-ssppoc/18.png
deleted file mode 100755
index ba687b63..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/18.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/19.png b/front-end/public/images/ss/russian-ssppoc/19.png
deleted file mode 100755
index 6930e92f..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/19.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/2.png b/front-end/public/images/ss/russian-ssppoc/2.png
deleted file mode 100755
index 1306aae7..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/2.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/3.png b/front-end/public/images/ss/russian-ssppoc/3.png
deleted file mode 100755
index 15003f28..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/3.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/4.png b/front-end/public/images/ss/russian-ssppoc/4.png
deleted file mode 100755
index ebb56d14..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/4.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/5.png b/front-end/public/images/ss/russian-ssppoc/5.png
deleted file mode 100755
index 545a0e9f..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/5.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/6.png b/front-end/public/images/ss/russian-ssppoc/6.png
deleted file mode 100755
index 15a08c2b..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/6.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/7.png b/front-end/public/images/ss/russian-ssppoc/7.png
deleted file mode 100755
index f249612f..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/7.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/8.png b/front-end/public/images/ss/russian-ssppoc/8.png
deleted file mode 100755
index 516917a5..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/8.png and /dev/null differ
diff --git a/front-end/public/images/ss/russian-ssppoc/9.png b/front-end/public/images/ss/russian-ssppoc/9.png
deleted file mode 100755
index b5978227..00000000
Binary files a/front-end/public/images/ss/russian-ssppoc/9.png and /dev/null differ
diff --git a/front-end/public/images/ss/ssppoc-p1.png b/front-end/public/images/ss/ssppoc-p1.png
deleted file mode 100755
index 57ef2c56..00000000
Binary files a/front-end/public/images/ss/ssppoc-p1.png and /dev/null differ
diff --git a/front-end/public/images/ss/table_multi_views/ssppoc-alt1.png b/front-end/public/images/ss/table_multi_views/ssppoc-alt1.png
deleted file mode 100755
index 18b3cab5..00000000
Binary files a/front-end/public/images/ss/table_multi_views/ssppoc-alt1.png and /dev/null differ
diff --git a/front-end/public/images/ss/table_multi_views/ssppoc-alt2.png b/front-end/public/images/ss/table_multi_views/ssppoc-alt2.png
deleted file mode 100755
index 925b006e..00000000
Binary files a/front-end/public/images/ss/table_multi_views/ssppoc-alt2.png and /dev/null differ
diff --git a/front-end/public/images/ss/table_multi_views/suite_2_om.png b/front-end/public/images/ss/table_multi_views/suite_2_om.png
deleted file mode 100755
index 1fb984b9..00000000
Binary files a/front-end/public/images/ss/table_multi_views/suite_2_om.png and /dev/null differ
diff --git a/front-end/public/images/ss/table_multi_views/table_grid_view1.png b/front-end/public/images/ss/table_multi_views/table_grid_view1.png
deleted file mode 100755
index 912fda14..00000000
Binary files a/front-end/public/images/ss/table_multi_views/table_grid_view1.png and /dev/null differ
diff --git a/front-end/public/images/ss/table_multi_views/table_list1.png b/front-end/public/images/ss/table_multi_views/table_list1.png
deleted file mode 100755
index 891407c2..00000000
Binary files a/front-end/public/images/ss/table_multi_views/table_list1.png and /dev/null differ
diff --git a/front-end/public/images/ss/table_multi_views/table_view1.png b/front-end/public/images/ss/table_multi_views/table_view1.png
deleted file mode 100755
index 6d8e0abf..00000000
Binary files a/front-end/public/images/ss/table_multi_views/table_view1.png and /dev/null differ
diff --git a/front-end/public/images/ss/table_view/1.png b/front-end/public/images/ss/table_view/1.png
deleted file mode 100755
index 4b1e3e95..00000000
Binary files a/front-end/public/images/ss/table_view/1.png and /dev/null differ
diff --git a/front-end/public/images/tmp1.png b/front-end/public/images/tmp1.png
deleted file mode 100755
index 63532a3c..00000000
Binary files a/front-end/public/images/tmp1.png and /dev/null differ
diff --git a/front-end/src/api/admin.api.ts b/front-end/src/api/admin.api.ts
index 0c893bf2..0f14a595 100755
--- a/front-end/src/api/admin.api.ts
+++ b/front-end/src/api/admin.api.ts
@@ -94,7 +94,16 @@ import type {
    * Get table columns for a church (handles multiple backend route shapes).
    * Returns a normalized shape: { columns: string[] }
    */
-  getTableColumns: async (churchId: number, table: string): Promise<{ columns: string[]; via?: string }> => {
+      exportTemplate: (churchId: number, data: {
+        table: string;
+        language?: string;
+        template_slug?: string;
+        template_name?: string;
+        overwrite?: boolean;
+      }): Promise<{ success: boolean; slug: string; template?: any; message?: string }> =>
+        apiClient.post(`/admin/churches/${churchId}/export-template`, data),
+
+      getTableColumns: async (churchId: number, table: string): Promise<{ columns: string[]; via?: string }> => {
     const enc = encodeURIComponent;
     const qs = `?table=${enc(table)}`;
 
@@ -486,50 +495,50 @@ import type {
       retryFailedJobs: (): Promise<ApiResponse> =>
         apiClient.post('/admin/test/retry-failed-jobs'),
     };
+
+    // ===== TEMPLATE MANAGEMENT APIs =====
+    templates = {
+      getAll: (filters?: { record_type?: string; is_global?: boolean; church_id?: number }): Promise<{ success: boolean; templates: any[]; count: number }> =>
+        apiClient.get(`/admin/templates${apiClient.buildQueryString(filters)}`),
+
+      getBySlug: (slug: string): Promise<{ success: boolean; template: any }> =>
+        apiClient.get(`/admin/templates/${slug}`),
+
+      create: (template: {
+        name: string;
+        slug?: string;
+        record_type: 'baptism' | 'marriage' | 'funeral' | 'custom';
+        description?: string;
+        fields: any[];
+        grid_type?: string;
+        theme?: string;
+        layout_type?: string;
+        language_support?: any;
+        is_editable?: boolean;
+        church_id?: number | null;
+        is_global?: boolean;
+      }): Promise<{ success: boolean; message: string; template: any }> =>
+        apiClient.post('/admin/templates', template),
+
+      update: (slug: string, template: Partial<{
+        name: string;
+        record_type: 'baptism' | 'marriage' | 'funeral' | 'custom';
+        description?: string;
+        fields: any[];
+        grid_type?: string;
+        theme?: string;
+        layout_type?: string;
+        language_support?: any;
+        is_editable?: boolean;
+        church_id?: number | null;
+        is_global?: boolean;
+      }>): Promise<{ success: boolean; message: string; template: any }> =>
+        apiClient.put(`/admin/templates/${slug}`, template),
+
+      delete: (slug: string): Promise<{ success: boolean; message: string }> =>
+        apiClient.delete(`/admin/templates/${slug}`),
+    };
   }
-  
-  // ===== TEMPLATE MANAGEMENT APIs =====
-  templates = {
-    getAll: (filters?: { record_type?: string; is_global?: boolean; church_id?: number }): Promise<{ success: boolean; templates: any[]; count: number }> =>
-      apiClient.get(`/admin/templates${apiClient.buildQueryString(filters)}`),
-
-    getBySlug: (slug: string): Promise<{ success: boolean; template: any }> =>
-      apiClient.get(`/admin/templates/${slug}`),
-
-    create: (template: {
-      name: string;
-      slug?: string;
-      record_type: 'baptism' | 'marriage' | 'funeral' | 'custom';
-      description?: string;
-      fields: any[];
-      grid_type?: string;
-      theme?: string;
-      layout_type?: string;
-      language_support?: any;
-      is_editable?: boolean;
-      church_id?: number | null;
-      is_global?: boolean;
-    }): Promise<{ success: boolean; message: string; template: any }> =>
-      apiClient.post('/admin/templates', template),
-
-    update: (slug: string, template: Partial<{
-      name: string;
-      record_type: 'baptism' | 'marriage' | 'funeral' | 'custom';
-      description?: string;
-      fields: any[];
-      grid_type?: string;
-      theme?: string;
-      layout_type?: string;
-      language_support?: any;
-      is_editable?: boolean;
-      church_id?: number | null;
-      is_global?: boolean;
-    }>): Promise<{ success: boolean; message: string; template: any }> =>
-      apiClient.put(`/admin/templates/${slug}`, template),
-
-    delete: (slug: string): Promise<{ success: boolean; message: string }> =>
-      apiClient.delete(`/admin/templates/${slug}`),
-  };
 
   // Create and export the Admin API instance
   export const adminAPI = new AdminAPI();
diff --git a/front-end/src/components/apps/userprofile/profile/ProfileBanner.tsx b/front-end/src/components/apps/userprofile/profile/ProfileBanner.tsx
index e2211c8b..125b001e 100755
--- a/front-end/src/components/apps/userprofile/profile/ProfileBanner.tsx
+++ b/front-end/src/components/apps/userprofile/profile/ProfileBanner.tsx
@@ -39,7 +39,7 @@ import { useAuth } from 'src/context/AuthContext';
 import { useProfileSync } from '../../../../lib/useProfileSync';
 
 // Default images
-import profilecover from 'src/assets/images/backgrounds/profilebg.jpg';
+const profilecover = '/images/banner/profilebg.png';
 import userimg from 'src/assets/images/profile/user-1.jpg';
 
 // Import Orthodox avatars
@@ -333,7 +333,7 @@ const ProfileBanner = () => {
             sx={{
               width: "100%",
               height: "300px",
-              objectFit: "cover",
+              objectFit: "contain",
               cursor: 'pointer'
             }}
             onClick={() => setCoverUploadOpen(true)}
diff --git a/front-end/src/components/frontend-pages/shared/pricing/PricingCard.tsx b/front-end/src/components/frontend-pages/shared/pricing/PricingCard.tsx
index 492c2eb1..41fb9323 100755
--- a/front-end/src/components/frontend-pages/shared/pricing/PricingCard.tsx
+++ b/front-end/src/components/frontend-pages/shared/pricing/PricingCard.tsx
@@ -10,7 +10,7 @@ const Licenses = [
     type: 'Single record type',
     isPopular: false,
     typeText: 'Use for churches that only require one type of record .',
-    price: '49',
+    price: 'TBD',
     fullSourceCode: true,
     isDoc: true,
     isSass: false,
@@ -23,7 +23,7 @@ const Licenses = [
     type: 'Multiple records',
     isPopular: false,
     typeText: 'Use for multiple types of records, advanced data analysis.',
-    price: '89',
+    price: 'TBD',
     fullSourceCode: true,
     isDoc: true,
     isSass: false,
@@ -36,7 +36,7 @@ const Licenses = [
     type: 'High Volume Plus',
     isPopular: true,
     typeText: 'Use for churches that exceed 1000 records in one type.',
-    price: '299',
+    price: 'TBD',
     fullSourceCode: true,
     isDoc: true,
     isSass: true,
@@ -49,7 +49,7 @@ const Licenses = [
     type: 'High Volume Pro',
     isPopular: false,
     typeText: 'Use churches with high volume records who want extended features, calendar access, top tier support.',
-    price: '499',
+    price: 'TBD',
     fullSourceCode: true,
     isDoc: true,
     isSass: true,
@@ -96,11 +96,13 @@ const PricingCard = () => {
               <Divider />
               <Stack mt={4} direction="row" gap="8px" alignItems="end">
                 <Typography variant="h4" fontSize="40px" fontWeight={700}>
-                  ${license.price}
-                </Typography>
-                <Typography variant="body2" fontSize="14px">
-                  / pay over time
+                  {license.price === 'TBD' ? 'TBD' : `$${license.price}`}
                 </Typography>
+                {license.price !== 'TBD' && (
+                  <Typography variant="body2" fontSize="14px">
+                    / pay over time
+                  </Typography>
+                )}
               </Stack>
               <Stack my={4} gap="12px">
                 <Box display="flex" alignItems="center" gap="8px">
diff --git a/front-end/src/components/global/ErrorNotificationToast.tsx b/front-end/src/components/global/ErrorNotificationToast.tsx
index 95a7064c..0f312fda 100755
--- a/front-end/src/components/global/ErrorNotificationToast.tsx
+++ b/front-end/src/components/global/ErrorNotificationToast.tsx
@@ -89,15 +89,20 @@ const ErrorNotificationToast: React.FC<ErrorNotificationToastProps> = ({ onTaskC
   return (
     <Snackbar
       open={!!currentError}
-      autoHideDuration={autoHide ? 6000 : null}
+      autoHideDuration={autoHide ? 12000 : null}
       onClose={handleClose}
-      anchorOrigin={{ vertical: 'top', horizontal: 'right' }}
+      anchorOrigin={{ vertical: 'center', horizontal: 'center' }}
       sx={{
+        position: 'fixed',
+        top: '50% !important',
+        left: '50% !important',
+        transform: 'translate(-50%, -50%) !important',
+        zIndex: 10000,
         '& .MuiSnackbar-root': {
           position: 'fixed',
-          top: 80,
-          right: 24,
-          zIndex: 10000
+          top: '50% !important',
+          left: '50% !important',
+          transform: 'translate(-50%, -50%) !important',
         }
       }}
     >
@@ -105,9 +110,22 @@ const ErrorNotificationToast: React.FC<ErrorNotificationToastProps> = ({ onTaskC
         severity={getSeverityColor(currentError.severity)}
         variant="filled"
         sx={{
-          minWidth: 320,
-          maxWidth: 500,
+          minWidth: 500,
+          maxWidth: 700,
+          fontSize: '1.1rem',
+          padding: '20px 24px',
           boxShadow: '0 8px 32px rgba(0, 0, 0, 0.15)',
+          '& .MuiAlert-message': {
+            fontSize: '1.1rem',
+            fontWeight: 500,
+          },
+          '& .MuiAlert-icon': {
+            fontSize: '32px',
+          },
+          '& .MuiAlertTitle-root': {
+            fontSize: '1.2rem',
+            fontWeight: 600,
+          }
         }}
         action={
           <Box display="flex" alignItems="center" gap={0.5}>
diff --git a/front-end/src/components/records/RecordFormShell.tsx b/front-end/src/components/records/RecordFormShell.tsx
index 388ccdf3..cb2fbed4 100644
--- a/front-end/src/components/records/RecordFormShell.tsx
+++ b/front-end/src/components/records/RecordFormShell.tsx
@@ -1,5 +1,5 @@
-import React from 'react';
-import { Box, Paper, Typography, Button, Stack } from '@mui/material';
+import React, { useState, useEffect } from 'react';
+import { Box, Paper, Typography, Button, Stack, Dialog, DialogTitle, DialogContent, DialogContentText, DialogActions } from '@mui/material';
 import { styled } from '@mui/material/styles';
 
 interface RecordFormShellProps {
@@ -17,6 +17,7 @@ interface RecordFormShellProps {
     cancelLabel?: string;
     saveAndAddLabel?: string;
     loading?: boolean;
+    hasUnsavedChanges?: boolean;
   };
 }
 
@@ -131,70 +132,123 @@ export const RecordFormShell: React.FC<RecordFormShellProps> = ({
   children,
   footerActions,
 }) => {
+  const [showCancelDialog, setShowCancelDialog] = useState(false);
+  const [pendingCancel, setPendingCancel] = useState<(() => void) | null>(null);
+
+  const handleCancelClick = () => {
+    if (footerActions?.hasUnsavedChanges) {
+      setPendingCancel(() => footerActions.onCancel || (() => {}));
+      setShowCancelDialog(true);
+    } else {
+      footerActions?.onCancel?.();
+    }
+  };
+
+  const handleConfirmCancel = () => {
+    setShowCancelDialog(false);
+    pendingCancel?.();
+    setPendingCancel(null);
+  };
+
+  const handleCancelDialogClose = () => {
+    setShowCancelDialog(false);
+    setPendingCancel(null);
+  };
+
   return (
-    <PageContainer>
-      <FormCard>
-        {headerIcons.length > 0 && (
-          <HeaderRow>
-            {headerIcons.map((icon, index) => (
-              <HeaderIcon
-                key={index}
-                src={icon.src}
-                alt={icon.alt}
-              />
-            ))}
-          </HeaderRow>
-        )}
-
-        {showTitle && title && (
-          <TitleSection>
-            <StyledTitle variant="h4">{title}</StyledTitle>
-            {subtitle && (
-              <StyledSubtitle variant="body2">{subtitle}</StyledSubtitle>
-            )}
-          </TitleSection>
-        )}
-
-        <FormContent>
-          {children}
-        </FormContent>
-
-        {footerActions && (
-          <FooterBar>
-            <Stack direction="row" spacing={2}>
-              {footerActions.onCancel && (
-                <Button
-                  variant="outlined"
-                  onClick={footerActions.onCancel}
-                  disabled={footerActions.loading}
-                >
-                  {footerActions.cancelLabel || 'Cancel'}
-                </Button>
-              )}
-              {footerActions.onSave && (
-                <Button
-                  variant="contained"
-                  color="primary"
-                  onClick={footerActions.onSave}
-                  disabled={footerActions.loading}
-                >
-                  {footerActions.saveLabel || 'Save Record'}
-                </Button>
-              )}
-              {footerActions.onSaveAndAddAnother && (
-                <Button
-                  variant="contained"
-                  color="secondary"
-                  onClick={footerActions.onSaveAndAddAnother}
-                  disabled={footerActions.loading}
-                >
-                  {footerActions.saveAndAddLabel || 'Save & Add Another'}
-                </Button>
+    <>
+      <PageContainer>
+        <FormCard>
+          {headerIcons.length > 0 && (
+            <HeaderRow>
+              {headerIcons.map((icon, index) => (
+                <HeaderIcon
+                  key={index}
+                  src={icon.src}
+                  alt={icon.alt}
+                />
+              ))}
+            </HeaderRow>
+          )}
+
+          {showTitle && title && (
+            <TitleSection>
+              <StyledTitle variant="h4">{title}</StyledTitle>
+              {subtitle && (
+                <StyledSubtitle variant="body2">{subtitle}</StyledSubtitle>
               )}
-            </Stack>
-          </FooterBar>
-        )}
-      </FormCard>
-    </PageContainer>
+            </TitleSection>
+          )}
+
+          <FormContent>
+            {children}
+          </FormContent>
+
+          {footerActions && (
+            <FooterBar>
+              <Stack direction="row" spacing={2} sx={{ width: '100%', justifyContent: 'space-between' }}>
+                <Box>
+                  {footerActions.onCancel && (
+                    <Button
+                      variant="outlined"
+                      onClick={handleCancelClick}
+                      disabled={footerActions.loading}
+                    >
+                      {footerActions.cancelLabel || 'Cancel'}
+                    </Button>
+                  )}
+                </Box>
+                <Stack direction="row" spacing={2}>
+                  {footerActions.onSave && (
+                    <Button
+                      variant="contained"
+                      color="primary"
+                      onClick={footerActions.onSave}
+                      disabled={footerActions.loading}
+                    >
+                      {footerActions.loading ? 'Saving...' : (footerActions.saveLabel || 'Save Record')}
+                    </Button>
+                  )}
+                  {footerActions.onSaveAndAddAnother && (
+                    <Button
+                      variant="contained"
+                      color="secondary"
+                      onClick={footerActions.onSaveAndAddAnother}
+                      disabled={footerActions.loading}
+                    >
+                      {footerActions.loading ? 'Saving...' : (footerActions.saveAndAddLabel || 'Save & Add Another')}
+                    </Button>
+                  )}
+                </Stack>
+              </Stack>
+            </FooterBar>
+          )}
+        </FormCard>
+      </PageContainer>
+
+      <Dialog
+        open={showCancelDialog}
+        onClose={handleCancelDialogClose}
+        aria-labelledby="cancel-dialog-title"
+        aria-describedby="cancel-dialog-description"
+      >
+        <DialogTitle id="cancel-dialog-title">
+          Unsaved Changes
+        </DialogTitle>
+        <DialogContent>
+          <DialogContentText id="cancel-dialog-description">
+            You have unsaved changes. Are you sure you want to cancel? All changes will be lost.
+          </DialogContentText>
+        </DialogContent>
+        <DialogActions>
+          <Button onClick={handleCancelDialogClose} color="primary">
+            Keep Editing
+          </Button>
+          <Button onClick={handleConfirmCancel} color="error" variant="contained">
+            Discard Changes
+          </Button>
+        </DialogActions>
+      </Dialog>
+    </>
   );
 };
diff --git a/front-end/src/components/routing/SmartRedirect.tsx b/front-end/src/components/routing/SmartRedirect.tsx
index e819ae21..939f3bc1 100755
--- a/front-end/src/components/routing/SmartRedirect.tsx
+++ b/front-end/src/components/routing/SmartRedirect.tsx
@@ -69,9 +69,11 @@ const SmartRedirect: React.FC = () => {
             if (contextUser.email === 'frjames@ssppoc.org') {
               safeNavigate('/dashboards/user', { replace: true });
             }
-            // Redirect based on role: super_admin/admin to Super Dashboard, others to User Dashboard
+            // Redirect based on role: super_admin/admin to Super Dashboard, priest and others to User Dashboard
             else if (contextUser.role === 'super_admin' || contextUser.role === 'admin') {
               safeNavigate('/dashboards/super', { replace: true });
+            } else if (contextUser.role === 'priest') {
+              safeNavigate('/dashboards/user', { replace: true });
             } else {
               safeNavigate('/dashboards/user', { replace: true });
             }
@@ -113,9 +115,11 @@ const SmartRedirect: React.FC = () => {
             if (currentUser.email === 'frjames@ssppoc.org') {
               safeNavigate('/dashboards/user', { replace: true });
             }
-            // Redirect based on role: super_admin/admin to Super Dashboard, others to User Dashboard
+            // Redirect based on role: super_admin/admin to Super Dashboard, priest and others to User Dashboard
             else if (currentUser.role === 'super_admin' || currentUser.role === 'admin') {
               safeNavigate('/dashboards/super', { replace: true });
+            } else if (currentUser.role === 'priest') {
+              safeNavigate('/dashboards/user', { replace: true });
             } else {
               safeNavigate('/dashboards/user', { replace: true });
             }
@@ -132,9 +136,11 @@ const SmartRedirect: React.FC = () => {
                   if (refreshedUser.email === 'frjames@ssppoc.org') {
                     safeNavigate('/dashboards/user', { replace: true });
                   }
-                  // Redirect based on role: super_admin/admin to Super Dashboard, others to User Dashboard
+                  // Redirect based on role: super_admin/admin to Super Dashboard, priest and others to User Dashboard
                   else if (refreshedUser.role === 'super_admin' || refreshedUser.role === 'admin') {
                     safeNavigate('/dashboards/super', { replace: true });
+                  } else if (refreshedUser.role === 'priest') {
+                    safeNavigate('/dashboards/user', { replace: true });
                   } else {
                     safeNavigate('/dashboards/user', { replace: true });
                   }
diff --git a/front-end/src/config/featureFlags.ts b/front-end/src/config/featureFlags.ts
index 40438baf..ab93dd85 100755
--- a/front-end/src/config/featureFlags.ts
+++ b/front-end/src/config/featureFlags.ts
@@ -1,14 +1,38 @@
 /**
  * Feature Flags Configuration
- * Controls which features are enabled/disabled in the application
+ * Controls feature availability across the application
+ * 
+ * Uses Vite environment variables (import.meta.env) not process.env
  */
 
-export const RECORDS_LEGACY_ENABLED =
-  (import.meta.env.VITE_RECORDS_LEGACY_ENABLED ?? '0') === '1';
+import { getEnvBool } from '@/utils/env';
 
-// Log feature flag status in development
-if (import.meta.env.DEV) {
-  console.log('🏁 Feature Flags:', {
-    RECORDS_LEGACY_ENABLED,
-  });
+export interface FeatureFlags {
+  interactiveReports: {
+    enableRecipientPages: boolean;
+  };
 }
+
+// Default feature flags (can be overridden by environment variables)
+const defaultFlags: FeatureFlags = {
+  interactiveReports: {
+    enableRecipientPages: getEnvBool('ENABLE_INTERACTIVE_REPORT_RECIPIENTS', false),
+  },
+};
+
+// Get feature flag value
+export function getFeatureFlag<K extends keyof FeatureFlags>(
+  category: K,
+  flag: keyof FeatureFlags[K]
+): boolean {
+  const categoryFlags = defaultFlags[category];
+  if (!categoryFlags) return false;
+  return (categoryFlags[flag] as boolean) ?? false;
+}
+
+// Check if interactive report recipient pages are enabled
+export function isInteractiveReportRecipientsEnabled(): boolean {
+  return getFeatureFlag('interactiveReports', 'enableRecipientPages');
+}
+
+export default defaultFlags;
diff --git a/front-end/src/features/church/FieldMapperPage.tsx b/front-end/src/features/church/FieldMapperPage.tsx
index 558ae0e5..c6fa8870 100755
--- a/front-end/src/features/church/FieldMapperPage.tsx
+++ b/front-end/src/features/church/FieldMapperPage.tsx
@@ -111,6 +111,12 @@ const FieldMapperPage: React.FC = () => {
   // Church name state (for header display)
   const [churchName, setChurchName] = useState<string>('');
   
+  // Export to Template state
+  const [exportLanguage, setExportLanguage] = useState<string>('en');
+  const [exportDialogOpen, setExportDialogOpen] = useState<boolean>(false);
+  const [exporting, setExporting] = useState<boolean>(false);
+  const [exportOverwrite, setExportOverwrite] = useState<boolean>(false);
+  
   // Record Settings State
   const [recordSettings, setRecordSettings] = useState({
     logo: {
@@ -555,6 +561,49 @@ const FieldMapperPage: React.FC = () => {
     }
   };
 
+  // --- Export to Template handler ---
+  const handleExportToTemplate = async () => {
+    if (!churchId || !tableName) return;
+
+    try {
+      setExporting(true);
+      setError(null);
+      setSuccess(null);
+
+      // Generate slug and name
+      const slug = `${exportLanguage}_${tableName}`;
+      const recordType = getRecordType(tableName);
+      const langName = exportLanguage === 'en' ? 'English' : 
+                      exportLanguage === 'gr' ? 'Greek' :
+                      exportLanguage === 'ru' ? 'Russian' :
+                      exportLanguage === 'ro' ? 'Romanian' :
+                      exportLanguage === 'ka' ? 'Georgian' : exportLanguage.toUpperCase();
+      const name = `${langName} ${recordType.charAt(0).toUpperCase() + recordType.slice(1)} Records`;
+
+      const response = await adminAPI.churches.exportTemplate(churchId, {
+        table: tableName,
+        language: exportLanguage,
+        template_slug: slug,
+        template_name: name,
+        overwrite: exportOverwrite
+      });
+
+      if (response.success) {
+        setSuccess(`Exported template: ${response.slug}`);
+        setExportDialogOpen(false);
+        setExportOverwrite(false);
+        setTimeout(() => setSuccess(null), 5000);
+      } else {
+        setError(response.error || 'Failed to export template');
+      }
+    } catch (err: any) {
+      console.error('Error exporting template:', err);
+      setError(err?.response?.data?.error || err?.message || 'Failed to export template');
+    } finally {
+      setExporting(false);
+    }
+  };
+
   // --- Save mapping with a minimal dual-path fallback (/admin then /api/admin) ---
   const handleSave = async () => {
     if (!churchId) return;
@@ -1289,8 +1338,44 @@ const FieldMapperPage: React.FC = () => {
             >
               Reload Columns
             </Button>
+
+            {/* Export to Template Button */}
+            <FormControl size="small" sx={{ minWidth: 100 }}>
+              <InputLabel>Language</InputLabel>
+              <Select
+                value={exportLanguage}
+                onChange={(e) => setExportLanguage(e.target.value)}
+                label="Language"
+                disabled={loading || saving || exporting}
+              >
+                <MenuItem value="en">English (en)</MenuItem>
+                <MenuItem value="gr">Greek (gr)</MenuItem>
+                <MenuItem value="ru">Russian (ru)</MenuItem>
+                <MenuItem value="ro">Romanian (ro)</MenuItem>
+                <MenuItem value="ka">Georgian (ka)</MenuItem>
+              </Select>
+            </FormControl>
+
+            <Button
+              variant="contained"
+              color="primary"
+              startIcon={exporting ? <CircularProgress size={16} /> : <DownloadIcon />}
+              onClick={() => setExportDialogOpen(true)}
+              disabled={loading || saving || exporting || rows.length === 0}
+            >
+              Export to Template
+            </Button>
           </Stack>
 
+          {/* Warning Banner for Export */}
+          <Alert severity="warning" sx={{ mb: 2, mt: 2 }}>
+            <Typography variant="body2">
+              <strong>Export to Template:</strong> This exports the current table schema and field mapper configuration 
+              into a <strong>global template</strong> that will be available to all churches. Only use this for 
+              standardized, production-ready configurations. Church-specific customizations should not be exported.
+            </Typography>
+          </Alert>
+
           {/* Status Messages */}
           {error && <Alert severity="error" sx={{ mb: 2 }}>{error}</Alert>}
           {success && <Alert severity="success" sx={{ mb: 2 }}>{success}</Alert>}
@@ -3156,6 +3241,71 @@ const FieldMapperPage: React.FC = () => {
           )}
         </CardContent>
       </Card>
+
+      {/* Export to Template Dialog */}
+      <Dialog open={exportDialogOpen} onClose={() => !exporting && setExportDialogOpen(false)} maxWidth="sm" fullWidth>
+          <DialogTitle>Export to Template</DialogTitle>
+          <DialogContent>
+            <Box sx={{ pt: 2 }}>
+              <Typography variant="body2" color="text.secondary" gutterBottom>
+                This will create or update a global template from the current table schema and field mapper configuration.
+              </Typography>
+              
+              <Box sx={{ mt: 3, mb: 2 }}>
+                <Typography variant="subtitle2" gutterBottom>Template Details:</Typography>
+                <Typography variant="body2">
+                  <strong>Table:</strong> {tableName}
+                </Typography>
+                <Typography variant="body2">
+                  <strong>Language:</strong> {exportLanguage}
+                </Typography>
+                <Typography variant="body2">
+                  <strong>Template Slug:</strong> {exportLanguage}_{tableName}
+                </Typography>
+                <Typography variant="body2">
+                  <strong>Template Name:</strong> {
+                    exportLanguage === 'en' ? 'English' : 
+                    exportLanguage === 'gr' ? 'Greek' :
+                    exportLanguage === 'ru' ? 'Russian' :
+                    exportLanguage === 'ro' ? 'Romanian' :
+                    exportLanguage === 'ka' ? 'Georgian' : exportLanguage.toUpperCase()
+                  } {getRecordType(tableName).charAt(0).toUpperCase() + getRecordType(tableName).slice(1)} Records
+                </Typography>
+              </Box>
+
+              <FormControlLabel
+                control={
+                  <Checkbox
+                    checked={exportOverwrite}
+                    onChange={(e) => setExportOverwrite(e.target.checked)}
+                    disabled={exporting}
+                  />
+                }
+                label="Overwrite existing template if it exists"
+              />
+
+              <Alert severity="warning" sx={{ mt: 2 }}>
+                <Typography variant="body2">
+                  <strong>Warning:</strong> This will create a global template available to all churches. 
+                  Only export standardized, production-ready configurations. Church-specific customizations should not be exported.
+                </Typography>
+              </Alert>
+            </Box>
+          </DialogContent>
+          <DialogActions>
+            <Button onClick={() => setExportDialogOpen(false)} disabled={exporting}>
+              Cancel
+            </Button>
+            <Button 
+              onClick={handleExportToTemplate} 
+              variant="contained" 
+              disabled={exporting}
+              startIcon={exporting ? <CircularProgress size={16} /> : <DownloadIcon />}
+            >
+              {exporting ? 'Exporting...' : 'Export Template'}
+            </Button>
+          </DialogActions>
+        </Dialog>
     </Box>
   );
 };
diff --git a/front-end/src/features/church/apps/church-management/ChurchSetupWizard.tsx b/front-end/src/features/church/apps/church-management/ChurchSetupWizard.tsx
index a7acd13d..65fce83b 100755
--- a/front-end/src/features/church/apps/church-management/ChurchSetupWizard.tsx
+++ b/front-end/src/features/church/apps/church-management/ChurchSetupWizard.tsx
@@ -74,9 +74,9 @@ interface ChurchWizardData {
   currency: string;
   is_active: boolean;
   
-  // Template Selection
-  template_church_id: number | null;
-  selected_tables: string[];
+  // Template Selection (NEW: uses template profiles, not churches)
+  template_profile_id: string | null; // 'start_from_scratch' | 'standard_en' | 'standard_gr' | etc.
+  selected_templates: { [recordType: string]: string } | null; // { baptism: 'en_baptism_records', ... }
   
   // Custom Fields
   custom_fields: CustomField[];
@@ -116,12 +116,11 @@ interface ChurchUser {
   send_invite: boolean;
 }
 
-interface TemplateChurch {
-  id: number;
+interface TemplateProfile {
+  id: string;
   name: string;
-  city: string;
-  country: string;
-  available_tables: string[];
+  description: string;
+  templates: { [recordType: string]: string };
 }
 
 const steps = [
@@ -190,8 +189,8 @@ const ChurchSetupWizard: React.FC = () => {
   const { user, hasRole } = useAuth();
   const [activeStep, setActiveStep] = useState(0);
   const [loading, setLoading] = useState(false);
-  const [templateChurches, setTemplateChurches] = useState<TemplateChurch[]>([]);
-  const [selectedTemplate, setSelectedTemplate] = useState<TemplateChurch | null>(null);
+  const [templateProfiles, setTemplateProfiles] = useState<TemplateProfile[]>([]);
+  const [selectedProfile, setSelectedProfile] = useState<TemplateProfile | null>(null);
   const [isSubmitting, setIsSubmitting] = useState(false);
   
   // Dialog states
@@ -230,8 +229,8 @@ const ChurchSetupWizard: React.FC = () => {
       timezone: 'UTC',
       currency: 'USD',
       is_active: true,
-      template_church_id: null,
-      selected_tables: ['baptism_records', 'marriage_records', 'funeral_records'],
+      template_profile_id: null,
+      selected_templates: null,
       custom_fields: [],
       initial_users: [],
       custom_landing_page: {
@@ -249,58 +248,59 @@ const ChurchSetupWizard: React.FC = () => {
     }
   });
 
-  // Load template churches
+  // Load template profiles (NEW: from template-profiles endpoint, not churches)
   useEffect(() => {
-    const fetchTemplateChurches = async () => {
+    const fetchTemplateProfiles = async () => {
       try {
         setLoading(true);
-        const response = await fetch('/api/admin/churches?preferred_language=en', {
+        const response = await fetch('/api/admin/churches/wizard/template-profiles', {
           credentials: 'include'
         });
         
         if (response.ok) {
           const data = await response.json();
-          const templatesWithTables = await Promise.all(
-            data.churches.map(async (church: any) => {
-              // Fetch available tables for each template
-              try {
-                const tablesResponse = await fetch(`/api/admin/churches/${church.id}/tables`, {
-                  credentials: 'include'
-                });
-                const tablesData = await tablesResponse.json();
-                return {
-                  ...church,
-                  available_tables: tablesData.tables || []
-                };
-              } catch {
-                return {
-                  ...church,
-                  available_tables: []
-                };
-              }
-            })
-          );
-          setTemplateChurches(templatesWithTables);
+          if (data.success && data.profiles) {
+            setTemplateProfiles(data.profiles);
+            // Default to Standard English if available
+            const defaultProfile = data.profiles.find((p: TemplateProfile) => p.id === 'standard_en');
+            if (defaultProfile) {
+              setSelectedProfile(defaultProfile);
+              formik.setFieldValue('template_profile_id', 'standard_en');
+              formik.setFieldValue('selected_templates', defaultProfile.templates);
+            }
+          }
         }
       } catch (error) {
-        console.error('Error fetching template churches:', error);
+        console.error('Error fetching template profiles:', error);
       } finally {
         setLoading(false);
       }
     };
 
-    fetchTemplateChurches();
+    fetchTemplateProfiles();
   }, []);
 
-  // Handle template selection
-  const handleTemplateSelection = (templateId: number | null) => {
-    const template = templateChurches.find(t => t.id === templateId) || null;
-    setSelectedTemplate(template);
-    formik.setFieldValue('template_church_id', templateId);
+  // Handle template profile selection (NEW: uses profile IDs, not church IDs)
+  const handleTemplateSelection = (profileId: string | null) => {
+    const profile = templateProfiles.find(p => p.id === profileId) || null;
+    setSelectedProfile(profile);
     
-    if (template) {
-      // Pre-populate with template's available tables
-      formik.setFieldValue('selected_tables', template.available_tables);
+    if (profileId === 'start_from_scratch' || !profileId) {
+      formik.setFieldValue('template_profile_id', null);
+      formik.setFieldValue('selected_templates', null);
+      formik.setFieldValue('selected_tables', []);
+    } else if (profile) {
+      formik.setFieldValue('template_profile_id', profileId);
+      // Set selected_templates from profile
+      formik.setFieldValue('selected_templates', profile.templates);
+      // Update selected_tables based on templates
+      const tableNames = Object.keys(profile.templates).map(rt => `${rt}_records`);
+      formik.setFieldValue('selected_tables', tableNames);
+    } else {
+      // No profile selected
+      formik.setFieldValue('template_profile_id', null);
+      formik.setFieldValue('selected_templates', null);
+      formik.setFieldValue('selected_tables', []);
     }
   };
 
@@ -402,8 +402,8 @@ const ChurchSetupWizard: React.FC = () => {
         return (
           <TemplateSelectionStep 
             formik={formik}
-            templateChurches={templateChurches}
-            selectedTemplate={selectedTemplate}
+            templateProfiles={templateProfiles}
+            selectedProfile={selectedProfile}
             onTemplateSelect={handleTemplateSelection}
             loading={loading}
           />
@@ -412,7 +412,7 @@ const ChurchSetupWizard: React.FC = () => {
         return (
           <RecordTablesStep 
             formik={formik}
-            selectedTemplate={selectedTemplate}
+            selectedProfile={selectedProfile}
             onAddCustomField={() => setCustomFieldDialog(true)}
             onEditCustomField={(field) => {
               setEditingField(field);
@@ -442,7 +442,7 @@ const ChurchSetupWizard: React.FC = () => {
       case 4:
         return <LandingPageStep formik={formik} />;
       case 5:
-        return <ReviewStep formik={formik} selectedTemplate={selectedTemplate} />;
+        return <ReviewStep formik={formik} selectedProfile={selectedProfile} />;
       default:
         return null;
     }
@@ -691,75 +691,62 @@ const BasicInformationStep: React.FC<{ formik: any }> = ({ formik }) => (
 
 const TemplateSelectionStep: React.FC<{
   formik: any;
-  templateChurches: TemplateChurch[];
-  selectedTemplate: TemplateChurch | null;
-  onTemplateSelect: (templateId: number | null) => void;
+  templateProfiles: TemplateProfile[];
+  selectedProfile: TemplateProfile | null;
+  onTemplateSelect: (profileId: string | null) => void;
   loading: boolean;
-}> = ({ formik, templateChurches, selectedTemplate, onTemplateSelect, loading }) => (
+}> = ({ formik, templateProfiles, selectedProfile, onTemplateSelect, loading }) => (
   <Box>
     <Typography variant="h6" gutterBottom>
-      Choose a Template Church (Optional)
+      Template Selection
     </Typography>
     <Typography variant="body2" color="text.secondary" paragraph>
-      Select an existing English-language church to use as a template. This will copy its structure, pages, themes, and settings.
+      Choose a template profile to provision record tables. Templates define the structure of your baptism, marriage, and funeral record tables.
     </Typography>
 
     {loading ? (
       <Box display="flex" justifyContent="center" py={4}>
         <CircularProgress />
-        <Typography ml={2}>Loading template churches...</Typography>
+        <Typography ml={2}>Loading template profiles...</Typography>
       </Box>
     ) : (
       <Grid container spacing={2}>
-        <Grid item xs={12}>
-          <Paper variant="outlined" sx={{ p: 2, mb: 2 }}>
-            <FormControlLabel
-              control={
-                <Checkbox
-                  checked={formik.values.template_church_id === null}
-                  onChange={() => onTemplateSelect(null)}
-                />
-              }
-              label={
-                <Box>
-                  <Typography variant="subtitle1">Start from Scratch</Typography>
-                  <Typography variant="body2" color="text.secondary">
-                    Create a new church without using any template
-                  </Typography>
-                </Box>
-              }
-            />
-          </Paper>
-        </Grid>
-
-        {templateChurches.map((church) => (
-          <Grid item xs={12} md={6} key={church.id}>
+        {templateProfiles.map((profile) => (
+          <Grid item xs={12} md={6} key={profile.id}>
             <Paper 
               variant="outlined" 
               sx={{ 
                 p: 2, 
                 cursor: 'pointer',
-                border: selectedTemplate?.id === church.id ? 2 : 1,
-                borderColor: selectedTemplate?.id === church.id ? 'primary.main' : 'divider'
+                border: selectedProfile?.id === profile.id || (profile.id === 'start_from_scratch' && formik.values.template_profile_id === null) ? 2 : 1,
+                borderColor: selectedProfile?.id === profile.id || (profile.id === 'start_from_scratch' && formik.values.template_profile_id === null) ? 'primary.main' : 'divider',
+                backgroundColor: selectedProfile?.id === profile.id || (profile.id === 'start_from_scratch' && formik.values.template_profile_id === null) ? 'action.selected' : 'background.paper'
               }}
-              onClick={() => onTemplateSelect(church.id)}
+              onClick={() => onTemplateSelect(profile.id === 'start_from_scratch' ? null : profile.id)}
             >
               <FormControlLabel
                 control={
                   <Checkbox
-                    checked={selectedTemplate?.id === church.id}
-                    onChange={() => onTemplateSelect(church.id)}
+                    checked={selectedProfile?.id === profile.id || (profile.id === 'start_from_scratch' && formik.values.template_profile_id === null)}
+                    onChange={() => onTemplateSelect(profile.id === 'start_from_scratch' ? null : profile.id)}
                   />
                 }
                 label={
                   <Box>
-                    <Typography variant="subtitle1">{church.name}</Typography>
-                    <Typography variant="body2" color="text.secondary">
-                      {church.city}, {church.country}
+                    <Typography variant="subtitle1">
+                      {profile.name}
+                      {profile.id === 'standard_en' && (
+                        <Chip label="Default" size="small" color="primary" sx={{ ml: 1 }} />
+                      )}
                     </Typography>
-                    <Typography variant="caption" display="block">
-                      {church.available_tables.length} available table(s)
+                    <Typography variant="body2" color="text.secondary">
+                      {profile.description}
                     </Typography>
+                    {profile.templates && Object.keys(profile.templates).length > 0 && (
+                      <Typography variant="caption" display="block" sx={{ mt: 1 }}>
+                        Templates: {Object.values(profile.templates).filter(Boolean).join(', ')}
+                      </Typography>
+                    )}
                   </Box>
                 }
               />
@@ -769,11 +756,20 @@ const TemplateSelectionStep: React.FC<{
       </Grid>
     )}
 
-    {selectedTemplate && (
+    {selectedProfile && selectedProfile.id !== 'start_from_scratch' && (
       <Alert severity="info" sx={{ mt: 2 }}>
-        <strong>Template Selected:</strong> {selectedTemplate.name}
+        <strong>Template Selected:</strong> {selectedProfile.name}
         <br />
-        This church's structure and settings will be copied to your new church.
+        Record tables will be created from global templates in <code>orthodoxmetrics_db.templates</code>.
+        {selectedProfile.templates && Object.keys(selectedProfile.templates).length > 0 && (
+          <>
+            <br />
+            <strong>Templates:</strong> {Object.entries(selectedProfile.templates)
+              .filter(([_, slug]) => slug)
+              .map(([type, slug]) => `${type}: ${slug}`)
+              .join(', ')}
+          </>
+        )}
       </Alert>
     )}
   </Box>
@@ -781,11 +777,11 @@ const TemplateSelectionStep: React.FC<{
 
 const RecordTablesStep: React.FC<{
   formik: any;
-  selectedTemplate: TemplateChurch | null;
+  selectedProfile: TemplateProfile | null;
   onAddCustomField: () => void;
   onEditCustomField: (field: CustomField) => void;
   onDeleteCustomField: (fieldId: string) => void;
-}> = ({ formik, selectedTemplate, onAddCustomField, onEditCustomField, onDeleteCustomField }) => (
+}> = ({ formik, selectedProfile, onAddCustomField, onEditCustomField, onDeleteCustomField }) => (
   <Box>
     <Typography variant="h6" gutterBottom>
       Select Record Tables
@@ -1034,8 +1030,8 @@ const LandingPageStep: React.FC<{ formik: any }> = ({ formik }) => (
 
 const ReviewStep: React.FC<{ 
   formik: any; 
-  selectedTemplate: TemplateChurch | null;
-}> = ({ formik, selectedTemplate }) => (
+  selectedProfile: TemplateProfile | null;
+}> = ({ formik, selectedProfile }) => (
   <Box>
     <Typography variant="h6" gutterBottom>
       Review Church Configuration
@@ -1065,10 +1061,18 @@ const ReviewStep: React.FC<{
             Template & Tables
           </Typography>
           <Typography variant="body2">
-            <strong>Template:</strong> {selectedTemplate ? selectedTemplate.name : 'None (Start from scratch)'}
+            <strong>Template Profile:</strong> {selectedProfile ? selectedProfile.name : 'Start from Scratch'}
           </Typography>
+          {formik.values.selected_templates && Object.keys(formik.values.selected_templates).length > 0 && (
+            <Typography variant="body2">
+              <strong>Templates:</strong> {Object.entries(formik.values.selected_templates)
+                .filter(([_, slug]) => slug)
+                .map(([type, slug]) => `${type}: ${slug}`)
+                .join(', ')}
+            </Typography>
+          )}
           <Typography variant="body2">
-            <strong>Selected Tables:</strong> {formik.values.selected_tables.length}
+            <strong>Selected Tables:</strong> {formik.values.selected_tables?.length || 0}
           </Typography>
           <Typography variant="body2">
             <strong>Custom Fields:</strong> {formik.values.custom_fields.length}
diff --git a/front-end/src/features/cms/settings/ContentSettings.tsx b/front-end/src/features/cms/settings/ContentSettings.tsx
index a2b9b62d..eecb85a4 100755
--- a/front-end/src/features/cms/settings/ContentSettings.tsx
+++ b/front-end/src/features/cms/settings/ContentSettings.tsx
@@ -894,10 +894,43 @@ const ContentSettings: React.FC = () => {
             {/* Success Snackbar */}
             <Snackbar
                 open={snackbarOpen}
-                autoHideDuration={6000}
+                autoHideDuration={12000}
                 onClose={() => setSnackbarOpen(false)}
-                message={snackbarMessage}
-            />
+                anchorOrigin={{ vertical: 'center', horizontal: 'center' }}
+                sx={{
+                  position: 'fixed',
+                  top: '50% !important',
+                  left: '50% !important',
+                  transform: 'translate(-50%, -50%) !important',
+                  zIndex: 10000,
+                  '& .MuiSnackbar-root': {
+                    position: 'fixed',
+                    top: '50% !important',
+                    left: '50% !important',
+                    transform: 'translate(-50%, -50%) !important',
+                  }
+                }}
+            >
+                <Alert 
+                  severity="success" 
+                  onClose={() => setSnackbarOpen(false)}
+                  sx={{ 
+                    minWidth: '400px',
+                    maxWidth: '600px',
+                    fontSize: '1.1rem',
+                    padding: '16px 20px',
+                    '& .MuiAlert-message': {
+                      fontSize: '1.1rem',
+                      fontWeight: 500,
+                    },
+                    '& .MuiAlert-icon': {
+                      fontSize: '28px',
+                    }
+                  }}
+                >
+                    {snackbarMessage}
+                </Alert>
+            </Snackbar>
         </Box>
     );
 };
diff --git a/front-end/src/features/devel-tools/live-table-builder/LiveTableBuilderPage.tsx b/front-end/src/features/devel-tools/live-table-builder/LiveTableBuilderPage.tsx
index d57ec538..b69dea63 100644
--- a/front-end/src/features/devel-tools/live-table-builder/LiveTableBuilderPage.tsx
+++ b/front-end/src/features/devel-tools/live-table-builder/LiveTableBuilderPage.tsx
@@ -47,18 +47,6 @@ import { tableDataToCsv, generateCsvFilename } from './utils/csvExport';
 import { parseCsvTextToTableData } from './utils/csvImport';
 import { HistoryManager } from './utils/history';
 import { normalizeTableData } from './utils/normalize';
-import {
-  getAllTemplates,
-  saveTemplate,
-  getTemplate,
-  deleteTemplate,
-  templateExists,
-  getTemplateNames,
-  exportTemplates,
-  importTemplates,
-  createStandardTemplates,
-  type Template,
-} from '../../../utils/liveTableTemplates';
 import { adminAPI } from '../../../api/admin.api';
 
 const STORAGE_KEY = 'om_live_table_builder_state_v1';
@@ -91,10 +79,10 @@ export const LiveTableBuilderPage: React.FC = () => {
     severity: 'info',
   });
   
-  // Template management state
+  // Template management state (DB-backed)
   const [templateName, setTemplateName] = useState('');
   const [selectedTemplate, setSelectedTemplate] = useState('');
-  const [templateNames, setTemplateNames] = useState<string[]>([]);
+  const [templates, setTemplates] = useState<Array<{ slug: string; name: string; record_type: string; is_global?: boolean }>>([]);
   const [saveTemplateDialogOpen, setSaveTemplateDialogOpen] = useState(false);
   const [overwriteTemplateDialogOpen, setOverwriteTemplateDialogOpen] = useState(false);
   const [deleteTemplateDialogOpen, setDeleteTemplateDialogOpen] = useState(false);
@@ -104,13 +92,39 @@ export const LiveTableBuilderPage: React.FC = () => {
   const [importTemplatesDialogOpen, setImportTemplatesDialogOpen] = useState(false);
   const [importTemplatesJson, setImportTemplatesJson] = useState('');
   const [lastSavedState, setLastSavedState] = useState<string>('');
-  
-  // Database template state
-  const [dbTemplateName, setDbTemplateName] = useState('');
-  const [dbTemplateRecordType, setDbTemplateRecordType] = useState<'baptism' | 'marriage' | 'funeral' | 'custom'>('custom');
-  const [dbTemplateDescription, setDbTemplateDescription] = useState('');
-  const [dbTemplateIsGlobal, setDbTemplateIsGlobal] = useState(false);
-  const [saveDbTemplateDialogOpen, setSaveDbTemplateDialogOpen] = useState(false);
+  const [templateRecordType, setTemplateRecordType] = useState<'baptism' | 'marriage' | 'funeral' | 'custom'>('custom');
+  const [templateDescription, setTemplateDescription] = useState('');
+  const [templateIsGlobal, setTemplateIsGlobal] = useState(false);
+  const [loadingTemplates, setLoadingTemplates] = useState(false);
+
+  // Toast notification handler (defined early to avoid TDZ issues)
+  const showToast = useCallback(
+    (message: string, severity: 'success' | 'error' | 'warning' | 'info' = 'info') => {
+      setToast({ open: true, message, severity });
+    },
+    []
+  );
+
+  // Load templates from database
+  const loadTemplatesFromDb = useCallback(async () => {
+    setLoadingTemplates(true);
+    try {
+      const response = await adminAPI.templates.getAll();
+      if (response.success) {
+        setTemplates(response.templates.map((t: any) => ({
+          slug: t.slug,
+          name: t.name,
+          record_type: t.record_type,
+          is_global: t.is_global,
+        })));
+      }
+    } catch (error) {
+      console.error('Failed to load templates from database:', error);
+      showToast('Failed to load templates', 'error');
+    } finally {
+      setLoadingTemplates(false);
+    }
+  }, [showToast]);
 
   // Load from localStorage on mount
   useEffect(() => {
@@ -151,9 +165,10 @@ export const LiveTableBuilderPage: React.FC = () => {
       setLastSavedState(JSON.stringify(defaultData));
     }
     
-    // Load template names
-    setTemplateNames(getTemplateNames());
-  }, []);
+    // Load templates from database
+    loadTemplatesFromDb();
+    // eslint-disable-next-line react-hooks/exhaustive-deps
+  }, [loadTemplatesFromDb]);
 
   // Update undo/redo button states
   useEffect(() => {
@@ -182,19 +197,13 @@ export const LiveTableBuilderPage: React.FC = () => {
       console.error('Failed to save table data:', e);
       showToast('Failed to save data to localStorage', 'error');
     }
+    // eslint-disable-next-line react-hooks/exhaustive-deps
   }, [tableData]);
   
   // Update template names when templates change
   useEffect(() => {
-    setTemplateNames(getTemplateNames());
-  }, [saveTemplateDialogOpen, deleteTemplateDialogOpen, importTemplatesDialogOpen]);
-
-  const showToast = useCallback(
-    (message: string, severity: 'success' | 'error' | 'warning' | 'info' = 'info') => {
-      setToast({ open: true, message, severity });
-    },
-    []
-  );
+    loadTemplatesFromDb();
+  }, [saveTemplateDialogOpen, deleteTemplateDialogOpen, importTemplatesDialogOpen, loadTemplatesFromDb]);
 
   const handleDataChange = useCallback((data: TableData) => {
     // Normalize data before storing
@@ -526,174 +535,322 @@ export const LiveTableBuilderPage: React.FC = () => {
     const currentState = JSON.stringify(normalized);
     return currentState !== lastSavedState;
   }, [tableData, lastSavedState]);
+
+  // Convert table state to database template format (defined early to avoid TDZ issues)
+  const convertTableToDbTemplate = useCallback((tableData: TableData) => {
+    const fields = tableData.columns.map((col, colIdx) => {
+      // Try to infer type from column name or default to string
+      let type = 'string';
+      const colName = col.label.toLowerCase();
+      if (colName.includes('date') || colName.includes('time')) {
+        type = 'date';
+      } else if (colName.includes('id') || colName.includes('number') || colName.includes('count')) {
+        type = 'number';
+      } else if (colName.includes('email')) {
+        type = 'email';
+      } else if (colName.includes('phone')) {
+        type = 'tel';
+      }
+
+      return {
+        column: col.id, // Use stable column ID
+        label: col.label || `Column ${colIdx + 1}`,
+        type: type,
+        required: false, // Default to optional
+      };
+    });
+
+    return fields;
+  }, []);
   
-  // Template management functions
-  const handleSaveTemplate = useCallback(() => {
+  // Template management functions (DB-backed)
+  const handleSaveTemplate = useCallback(async () => {
     if (!templateName.trim()) {
       showToast('Template name is required', 'warning');
       return;
     }
     
-    if (templateExists(templateName)) {
+    // Check if template exists in DB
+    const existing = templates.find(t => t.name === templateName.trim() || t.slug === templateName.trim().toLowerCase().replace(/[^\w\s-]/g, '').replace(/[\s_-]+/g, '-'));
+    if (existing) {
       setOverwriteTemplateDialogOpen(true);
       return;
     }
     
-    // Save template
-    const normalized = normalizeTableData(
-      tableData,
-      tableData.rows.length,
-      tableData.columns.length
-    );
-    
-    const template: Template = {
-      name: templateName.trim(),
-      tableState: {
-        data: normalized,
-        version: '1',
-      },
-      createdAt: new Date().toISOString(),
-      updatedAt: new Date().toISOString(),
-    };
-    
     try {
-      saveTemplate(template);
-      setTemplateNames(getTemplateNames());
-      setTemplateName('');
-      setSaveTemplateDialogOpen(false);
-      showToast(`Saved template: ${template.name}`, 'success');
-    } catch (e) {
-      showToast('Failed to save template', 'error');
-      console.error(e);
+      const normalized = normalizeTableData(
+        tableData,
+        tableData.rows.length,
+        tableData.columns.length
+      );
+
+      const fields = convertTableToDbTemplate(normalized);
+      const slug = templateName.trim().toLowerCase().replace(/[^\w\s-]/g, '').replace(/[\s_-]+/g, '-').replace(/^-+|-+$/g, '');
+
+      const templateData = {
+        name: templateName.trim(),
+        slug: slug,
+        record_type: templateRecordType,
+        description: templateDescription.trim() || null,
+        fields: fields,
+        grid_type: 'aggrid',
+        theme: 'liturgicalBlueGold',
+        layout_type: 'table',
+        is_editable: true,
+        church_id: null,
+        is_global: templateIsGlobal,
+      };
+
+      const response = await adminAPI.templates.create(templateData);
+      
+      if (response.success) {
+        const savedTemplateName = templateName.trim();
+        await loadTemplatesFromDb();
+        setTemplateName('');
+        setTemplateDescription('');
+        setTemplateRecordType('custom');
+        setTemplateIsGlobal(false);
+        setSaveTemplateDialogOpen(false);
+        showToast(`Saved template: ${savedTemplateName}`, 'success');
+      } else {
+        showToast('Failed to save template', 'error');
+      }
+    } catch (error: any) {
+      console.error('Error saving template:', error);
+      const errorMessage = error?.response?.data?.error || error?.message || 'Unknown error';
+      showToast(`Failed to save template: ${errorMessage}`, 'error');
     }
-  }, [templateName, tableData, showToast]);
+  }, [templateName, templateRecordType, templateDescription, templateIsGlobal, tableData, templates, convertTableToDbTemplate, loadTemplatesFromDb, showToast]);
   
-  const handleOverwriteTemplate = useCallback(() => {
-    const normalized = normalizeTableData(
-      tableData,
-      tableData.rows.length,
-      tableData.columns.length
-    );
-    
-    const existing = getTemplate(templateName.trim());
-    const template: Template = {
-      name: templateName.trim(),
-      tableState: {
-        data: normalized,
-        version: '1',
-      },
-      recordType: existing?.recordType,
-      locale: existing?.locale,
-      createdAt: existing?.createdAt || new Date().toISOString(),
-      updatedAt: new Date().toISOString(),
-    };
-    
+  const handleOverwriteTemplate = useCallback(async () => {
     try {
-      saveTemplate(template);
-      setTemplateNames(getTemplateNames());
-      setTemplateName('');
-      setSaveTemplateDialogOpen(false);
-      setOverwriteTemplateDialogOpen(false);
-      showToast(`Saved template: ${template.name}`, 'success');
-    } catch (e) {
-      showToast('Failed to save template', 'error');
-      console.error(e);
+      const existing = templates.find(t => t.name === templateName.trim() || t.slug === templateName.trim().toLowerCase().replace(/[^\w\s-]/g, '').replace(/[\s_-]+/g, '-'));
+      if (!existing) {
+        showToast('Template not found', 'error');
+        return;
+      }
+
+      const normalized = normalizeTableData(
+        tableData,
+        tableData.rows.length,
+        tableData.columns.length
+      );
+
+      const fields = convertTableToDbTemplate(normalized);
+
+      const templateData = {
+        record_type: templateRecordType,
+        description: templateDescription.trim() || null,
+        fields: fields,
+        is_global: templateIsGlobal,
+      };
+
+      const response = await adminAPI.templates.update(existing.slug, templateData);
+      
+      if (response.success) {
+        const updatedTemplateName = templateName.trim();
+        await loadTemplatesFromDb();
+        setTemplateName('');
+        setTemplateDescription('');
+        setTemplateRecordType('custom');
+        setTemplateIsGlobal(false);
+        setSaveTemplateDialogOpen(false);
+        setOverwriteTemplateDialogOpen(false);
+        showToast(`Updated template: ${updatedTemplateName}`, 'success');
+      } else {
+        showToast('Failed to update template', 'error');
+      }
+    } catch (error: any) {
+      console.error('Error updating template:', error);
+      const errorMessage = error?.response?.data?.error || error?.message || 'Unknown error';
+      showToast(`Failed to update template: ${errorMessage}`, 'error');
     }
-  }, [templateName, tableData, showToast]);
+  }, [templateName, templateRecordType, templateDescription, templateIsGlobal, tableData, templates, convertTableToDbTemplate, loadTemplatesFromDb, showToast]);
   
-  const handleLoadTemplate = useCallback((name: string) => {
+  const handleLoadTemplate = useCallback(async (slug: string) => {
     if (isDirty()) {
-      setTemplateToLoad(name);
+      setTemplateToLoad(slug);
       setLoadTemplateDialogOpen(true);
       return;
     }
     
-    // Load template
-    const template = getTemplate(name);
-    if (!template) {
-      showToast(`Template "${name}" not found`, 'error');
-      return;
+    try {
+      const response = await adminAPI.templates.getBySlug(slug);
+      if (!response.success || !response.template) {
+        showToast(`Template not found`, 'error');
+        return;
+      }
+
+      const template = response.template;
+      
+      // Handle both formats:
+      // 1. Flat array format: [{ column, label, type, required }]
+      // 2. Versioned format: { version, columns: [...], ui: {...} }
+      let fieldsArray: any[] = [];
+      
+      if (Array.isArray(template.fields)) {
+        // Format 1: Already an array
+        fieldsArray = template.fields;
+      } else if (template.fields && typeof template.fields === 'object') {
+        // Format 2: Versioned structure - extract columns array
+        if (template.fields.columns && Array.isArray(template.fields.columns)) {
+          fieldsArray = template.fields.columns.map((col: any) => ({
+            column: col.name || col.column || `col_${fieldsArray.length}`,
+            label: col.label || col.name || col.column,
+            type: col.type || 'string',
+            required: col.required || false
+          }));
+        } else {
+          throw new Error('Template fields format not recognized');
+        }
+      } else {
+        throw new Error('Template fields is missing or invalid');
+      }
+      
+      // Convert DB template fields back to table structure
+      const columns = fieldsArray.map((field: any, index: number) => ({
+        id: field.column || `col_${index}`,
+        label: field.label || field.column || `Column ${index + 1}`,
+      }));
+
+      // Create empty rows (DB templates don't store row data, just structure)
+      const rows = Array.from({ length: DEFAULT_ROWS }, (_, rowIdx) => ({
+        id: `row_${rowIdx}`,
+        cells: columns.reduce((acc: Record<string, string>, col: any) => {
+          acc[col.id] = '';
+          return acc;
+        }, {}),
+      }));
+
+      const tableData: TableData = { columns, rows };
+      const normalized = normalizeTableData(tableData, rows.length, columns.length);
+      
+      setTableData(normalized);
+      historyManagerRef.current.clear();
+      historyManagerRef.current.initialize(normalized);
+      setCanUndo(false);
+      setCanRedo(false);
+      setLastSavedState(JSON.stringify(normalized));
+      setSelectedTemplate(slug);
+      showToast(`Loaded template: ${template.name}`, 'success');
+    } catch (error: any) {
+      console.error('Error loading template:', error);
+      showToast(`Failed to load template: ${error?.message || 'Unknown error'}`, 'error');
     }
-    
-    const normalized = normalizeTableData(
-      template.tableState.data,
-      template.tableState.data.rows.length,
-      template.tableState.data.columns.length
-    );
-    
-    setTableData(normalized);
-    historyManagerRef.current.clear();
-    historyManagerRef.current.initialize(normalized);
-    setCanUndo(false);
-    setCanRedo(false);
-    setLastSavedState(JSON.stringify(normalized));
-    setSelectedTemplate(name);
-    showToast(`Loaded template: ${name}`, 'success');
   }, [isDirty, showToast]);
   
-  const handleConfirmLoadTemplate = useCallback(() => {
-    const name = templateToLoad;
-    const template = getTemplate(name);
-    if (!template) {
-      showToast(`Template "${name}" not found`, 'error');
+  const handleConfirmLoadTemplate = useCallback(async () => {
+    const slug = templateToLoad;
+    try {
+      const response = await adminAPI.templates.getBySlug(slug);
+      if (!response.success || !response.template) {
+        showToast(`Template not found`, 'error');
+        setLoadTemplateDialogOpen(false);
+        return;
+      }
+
+      const template = response.template;
+      
+      // Handle both formats (same logic as handleLoadTemplate)
+      let fieldsArray: any[] = [];
+      
+      if (Array.isArray(template.fields)) {
+        fieldsArray = template.fields;
+      } else if (template.fields && typeof template.fields === 'object') {
+        if (template.fields.columns && Array.isArray(template.fields.columns)) {
+          fieldsArray = template.fields.columns.map((col: any) => ({
+            column: col.name || col.column || `col_${fieldsArray.length}`,
+            label: col.label || col.name || col.column,
+            type: col.type || 'string',
+            required: col.required || false
+          }));
+        } else {
+          throw new Error('Template fields format not recognized');
+        }
+      } else {
+        throw new Error('Template fields is missing or invalid');
+      }
+      
+      const columns = fieldsArray.map((field: any, index: number) => ({
+        id: field.column || `col_${index}`,
+        label: field.label || field.column || `Column ${index + 1}`,
+      }));
+
+      const rows = Array.from({ length: DEFAULT_ROWS }, (_, rowIdx) => ({
+        id: `row_${rowIdx}`,
+        cells: columns.reduce((acc: Record<string, string>, col: any) => {
+          acc[col.id] = '';
+          return acc;
+        }, {}),
+      }));
+
+      const tableData: TableData = { columns, rows };
+      const normalized = normalizeTableData(tableData, rows.length, columns.length);
+      
+      setTableData(normalized);
+      historyManagerRef.current.clear();
+      historyManagerRef.current.initialize(normalized);
+      setCanUndo(false);
+      setCanRedo(false);
+      setLastSavedState(JSON.stringify(normalized));
+      setSelectedTemplate(slug);
+      setLoadTemplateDialogOpen(false);
+      setTemplateToLoad('');
+      showToast(`Loaded template: ${template.name}`, 'success');
+    } catch (error: any) {
+      console.error('Error loading template:', error);
+      showToast(`Failed to load template: ${error?.message || 'Unknown error'}`, 'error');
       setLoadTemplateDialogOpen(false);
-      return;
     }
-    
-    const normalized = normalizeTableData(
-      template.tableState.data,
-      template.tableState.data.rows.length,
-      template.tableState.data.columns.length
-    );
-    
-    setTableData(normalized);
-    historyManagerRef.current.clear();
-    historyManagerRef.current.initialize(normalized);
-    setCanUndo(false);
-    setCanRedo(false);
-    setLastSavedState(JSON.stringify(normalized));
-    setSelectedTemplate(name);
-    setLoadTemplateDialogOpen(false);
-    setTemplateToLoad('');
-    showToast(`Loaded template: ${name}`, 'success');
   }, [templateToLoad, showToast]);
   
-  const handleDeleteTemplate = useCallback((name: string) => {
-    setTemplateToDelete(name);
+  const handleDeleteTemplate = useCallback((slug: string) => {
+    setTemplateToDelete(slug);
     setDeleteTemplateDialogOpen(true);
   }, []);
   
-  const handleConfirmDeleteTemplate = useCallback(() => {
-    const name = templateToDelete;
-    if (deleteTemplate(name)) {
-      setTemplateNames(getTemplateNames());
-      setDeleteTemplateDialogOpen(false);
-      setTemplateToDelete('');
-      if (selectedTemplate === name) {
-        setSelectedTemplate('');
+  const handleConfirmDeleteTemplate = useCallback(async () => {
+    const slug = templateToDelete;
+    try {
+      const response = await adminAPI.templates.delete(slug);
+      if (response.success) {
+        await loadTemplatesFromDb();
+        setDeleteTemplateDialogOpen(false);
+        setTemplateToDelete('');
+        if (selectedTemplate === slug) {
+          setSelectedTemplate('');
+        }
+        showToast('Template deleted successfully', 'success');
+      } else {
+        showToast('Failed to delete template', 'error');
       }
-      showToast(`Deleted template: ${name}`, 'success');
-    } else {
-      showToast(`Failed to delete template: ${name}`, 'error');
+    } catch (error: any) {
+      console.error('Error deleting template:', error);
+      showToast(`Failed to delete template: ${error?.message || 'Unknown error'}`, 'error');
     }
-  }, [templateToDelete, selectedTemplate, showToast]);
+  }, [templateToDelete, selectedTemplate, loadTemplatesFromDb, showToast]);
   
-  const handleExportTemplates = useCallback(() => {
+  const handleExportTemplates = useCallback(async () => {
     try {
-      const json = exportTemplates();
-      const blob = new Blob([json], { type: 'application/json' });
-      const url = URL.createObjectURL(blob);
-      const a = document.createElement('a');
-      a.href = url;
-      const now = new Date();
-      const dateStr = now.toISOString().split('T')[0];
-      const timeStr = now.toTimeString().split(' ')[0].replace(/:/g, '');
-      a.download = `live-table-templates_${dateStr}_${timeStr}.json`;
-      document.body.appendChild(a);
-      a.click();
-      document.body.removeChild(a);
-      URL.revokeObjectURL(url);
-      showToast('Templates exported successfully', 'success');
+      const response = await adminAPI.templates.getAll();
+      if (response.success) {
+        const json = JSON.stringify(response.templates, null, 2);
+        const blob = new Blob([json], { type: 'application/json' });
+        const url = URL.createObjectURL(blob);
+        const a = document.createElement('a');
+        a.href = url;
+        const now = new Date();
+        const dateStr = now.toISOString().split('T')[0];
+        const timeStr = now.toTimeString().split(' ')[0].replace(/:/g, '');
+        a.download = `live-table-templates_${dateStr}_${timeStr}.json`;
+        document.body.appendChild(a);
+        a.click();
+        document.body.removeChild(a);
+        URL.revokeObjectURL(url);
+        showToast('Templates exported successfully', 'success');
+      } else {
+        showToast('Failed to export templates', 'error');
+      }
     } catch (e) {
       showToast('Failed to export templates', 'error');
       console.error(e);
@@ -736,50 +893,108 @@ export const LiveTableBuilderPage: React.FC = () => {
     event.target.value = '';
   }, [showToast]);
   
-  const handleImportTemplatesConfirm = useCallback(() => {
+  const handleImportTemplatesConfirm = useCallback(async () => {
     try {
       if (!importTemplatesJson.trim()) {
         showToast('Please paste template JSON data', 'warning');
         return;
       }
       
-      // For now, use overwrite all conflicts as default
-      // In a more advanced UI, we could show options
-      const result = importTemplates(importTemplatesJson, {
-        overwriteConflicts: true,
-      });
-      
-      setTemplateNames(getTemplateNames());
+      const imported = JSON.parse(importTemplatesJson);
+      if (!Array.isArray(imported)) {
+        showToast('Invalid template format: expected array', 'error');
+        return;
+      }
+
+      let importedCount = 0;
+      let errorCount = 0;
+
+      for (const template of imported) {
+        try {
+          await adminAPI.templates.create({
+            name: template.name,
+            slug: template.slug,
+            record_type: template.record_type || 'custom',
+            description: template.description || null,
+            fields: template.fields || [],
+            grid_type: template.grid_type || 'aggrid',
+            theme: template.theme || 'liturgicalBlueGold',
+            layout_type: template.layout_type || 'table',
+            is_editable: template.is_editable !== false,
+            church_id: template.church_id || null,
+            is_global: template.is_global || false,
+          });
+          importedCount++;
+        } catch (error) {
+          console.error('Failed to import template:', template.name, error);
+          errorCount++;
+        }
+      }
+
+      await loadTemplatesFromDb();
       setImportTemplatesDialogOpen(false);
       setImportTemplatesJson('');
       
-      if (result.errors.length > 0) {
-        showToast(
-          `Imported ${result.imported} templates (${result.overwritten} overwritten, ${result.skipped} skipped). Errors: ${result.errors.length}`,
-          'warning'
-        );
+      if (errorCount > 0) {
+        showToast(`Imported ${importedCount} templates. ${errorCount} failed.`, 'warning');
       } else {
-        showToast(
-          `Imported ${result.imported} templates (${result.overwritten} overwritten, ${result.skipped} skipped)`,
-          'success'
-        );
+        showToast(`Imported ${importedCount} templates successfully`, 'success');
       }
     } catch (e) {
       showToast('Failed to import templates', 'error');
       console.error(e);
     }
-  }, [importTemplatesJson, showToast]);
+  }, [importTemplatesJson, loadTemplatesFromDb, showToast]);
   
-  const handleCreateStandardTemplates = useCallback(() => {
+  const handleCreateStandardTemplates = useCallback(async () => {
     try {
-      createStandardTemplates();
-      setTemplateNames(getTemplateNames());
-      showToast('Created standard record templates', 'success');
+      const standardHeaders = ['id', 'church_id', 'entry_no', 'date', 'first_name', 'last_name', 'father_name', 'mother_name', 'godfather_name', 'godmother_name', 'notes'];
+      const recordTypes: Array<'baptism' | 'marriage' | 'funeral'> = ['baptism', 'marriage', 'funeral'];
+      const locales = ['en', 'gr'];
+
+      let created = 0;
+      for (const locale of locales) {
+        for (const recordType of recordTypes) {
+          const name = `${locale}_${recordType}_records`;
+          const slug = name.toLowerCase().replace(/[^\w\s-]/g, '').replace(/[\s_-]+/g, '-');
+          
+          const fields = standardHeaders.map((header, index) => ({
+            column: `col_${index}`,
+            label: header,
+            type: 'string',
+            required: false,
+          }));
+
+          try {
+            await adminAPI.templates.create({
+              name,
+              slug,
+              record_type: recordType,
+              description: `Standard ${recordType} template for ${locale}`,
+              fields,
+              grid_type: 'aggrid',
+              theme: 'liturgicalBlueGold',
+              layout_type: 'table',
+              is_editable: true,
+              church_id: null,
+              is_global: true,
+            });
+            created++;
+          } catch (error) {
+            // Template might already exist, skip
+            console.log(`Template ${name} may already exist, skipping`);
+          }
+        }
+      }
+
+      await loadTemplatesFromDb();
+      showToast(`Created ${created} standard templates`, 'success');
     } catch (e) {
       showToast('Failed to create standard templates', 'error');
       console.error(e);
     }
-  }, [showToast]);
+  }, [loadTemplatesFromDb, showToast]);
+
 
   return (
     <Container maxWidth="xl" sx={{ py: 4, height: 'calc(100vh - 120px)', display: 'flex', flexDirection: 'column' }}>
@@ -849,20 +1064,23 @@ export const LiveTableBuilderPage: React.FC = () => {
               <Select
                 value={selectedTemplate}
                 label="Load Template"
+                disabled={loadingTemplates}
                 onChange={(e) => {
-                  const name = e.target.value;
-                  setSelectedTemplate(name);
-                  if (name) {
-                    handleLoadTemplate(name);
+                  const slug = e.target.value;
+                  setSelectedTemplate(slug);
+                  if (slug) {
+                    handleLoadTemplate(slug);
                   }
                 }}
               >
-                {templateNames.length === 0 ? (
+                {loadingTemplates ? (
+                  <MenuItem disabled>Loading templates...</MenuItem>
+                ) : templates.length === 0 ? (
                   <MenuItem disabled>No templates available</MenuItem>
                 ) : (
-                  templateNames.map((name) => (
-                    <MenuItem key={name} value={name}>
-                      {name}
+                  templates.map((template) => (
+                    <MenuItem key={template.slug} value={template.slug}>
+                      {template.name} {template.is_global ? '(Global)' : ''}
                     </MenuItem>
                   ))
                 )}
@@ -921,15 +1139,6 @@ export const LiveTableBuilderPage: React.FC = () => {
               Create Standard Templates
             </Button>
             <Divider orientation="vertical" flexItem sx={{ mx: 1 }} />
-            <Button
-              variant="contained"
-              startIcon={<SaveIcon />}
-              onClick={() => setSaveDbTemplateDialogOpen(true)}
-              size="small"
-              color="primary"
-            >
-              Save to Database
-            </Button>
           </Box>
         </Box>
 
@@ -1104,30 +1313,65 @@ export const LiveTableBuilderPage: React.FC = () => {
       </Dialog>
 
       {/* Save Template Dialog */}
-      <Dialog open={saveTemplateDialogOpen} onClose={() => setSaveTemplateDialogOpen(false)}>
-        <DialogTitle>Save Template</DialogTitle>
+      <Dialog open={saveTemplateDialogOpen} onClose={() => setSaveTemplateDialogOpen(false)} maxWidth="sm" fullWidth>
+        <DialogTitle>Save Template to Database</DialogTitle>
         <DialogContent>
-          <TextField
-            autoFocus
-            fullWidth
-            label="Template Name"
-            value={templateName}
-            onChange={(e) => setTemplateName(e.target.value)}
-            placeholder="e.g., en_baptism_records"
-            sx={{ mt: 2 }}
-            onKeyPress={(e) => {
-              if (e.key === 'Enter' && templateName.trim()) {
-                handleSaveTemplate();
+          <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2, pt: 1 }}>
+            <TextField
+              fullWidth
+              label="Template Name"
+              value={templateName}
+              onChange={(e) => setTemplateName(e.target.value)}
+              placeholder="e.g., Standard Baptism Records"
+              required
+              autoFocus
+            />
+            <FormControl fullWidth>
+              <InputLabel>Record Type</InputLabel>
+              <Select
+                value={templateRecordType}
+                label="Record Type"
+                onChange={(e) => setTemplateRecordType(e.target.value as any)}
+              >
+                <MenuItem value="baptism">Baptism</MenuItem>
+                <MenuItem value="marriage">Marriage</MenuItem>
+                <MenuItem value="funeral">Funeral</MenuItem>
+                <MenuItem value="custom">Custom</MenuItem>
+              </Select>
+            </FormControl>
+            <TextField
+              fullWidth
+              label="Description (optional)"
+              value={templateDescription}
+              onChange={(e) => setTemplateDescription(e.target.value)}
+              placeholder="Template description..."
+              multiline
+              rows={2}
+            />
+            <FormControlLabel
+              control={
+                <Checkbox
+                  checked={templateIsGlobal}
+                  onChange={(e) => setTemplateIsGlobal(e.target.checked)}
+                />
               }
-            }}
-          />
+              label="Make this template global (available to all churches)"
+            />
+          </Box>
         </DialogContent>
         <DialogActions>
           <Button onClick={() => {
             setSaveTemplateDialogOpen(false);
             setTemplateName('');
+            setTemplateDescription('');
+            setTemplateRecordType('custom');
+            setTemplateIsGlobal(false);
           }}>Cancel</Button>
-          <Button onClick={handleSaveTemplate} variant="contained" disabled={!templateName.trim()}>
+          <Button 
+            onClick={handleSaveTemplate} 
+            variant="contained" 
+            disabled={!templateName.trim()}
+          >
             Save
           </Button>
         </DialogActions>
@@ -1135,10 +1379,10 @@ export const LiveTableBuilderPage: React.FC = () => {
 
       {/* Overwrite Template Confirmation Dialog */}
       <Dialog open={overwriteTemplateDialogOpen} onClose={() => setOverwriteTemplateDialogOpen(false)}>
-        <DialogTitle>Overwrite Template?</DialogTitle>
+        <DialogTitle>Update Template?</DialogTitle>
         <DialogContent>
           <Typography>
-            Template "{templateName}" already exists. Do you want to overwrite it?
+            Template "{templateName}" already exists in the database. Update it?
           </Typography>
         </DialogContent>
         <DialogActions>
@@ -1148,7 +1392,7 @@ export const LiveTableBuilderPage: React.FC = () => {
             setSaveTemplateDialogOpen(false);
           }}>Cancel</Button>
           <Button onClick={handleOverwriteTemplate} variant="contained" color="warning">
-            Overwrite
+            Update
           </Button>
         </DialogActions>
       </Dialog>
@@ -1158,7 +1402,7 @@ export const LiveTableBuilderPage: React.FC = () => {
         <DialogTitle>Delete Template?</DialogTitle>
         <DialogContent>
           <Typography>
-            Are you sure you want to delete template "{templateToDelete}"? This action cannot be undone.
+            Are you sure you want to delete this template? This action cannot be undone.
           </Typography>
         </DialogContent>
         <DialogActions>
@@ -1177,7 +1421,7 @@ export const LiveTableBuilderPage: React.FC = () => {
         <DialogTitle>Load Template?</DialogTitle>
         <DialogContent>
           <Typography>
-            You have unsaved changes. Loading template "{templateToLoad}" will replace your current table. Continue?
+            You have unsaved changes. Loading this template will replace your current table. Continue?
           </Typography>
         </DialogContent>
         <DialogActions>
@@ -1219,84 +1463,43 @@ export const LiveTableBuilderPage: React.FC = () => {
         </DialogActions>
       </Dialog>
 
-      {/* Save to Database Dialog */}
-      <Dialog open={saveDbTemplateDialogOpen} onClose={() => setSaveDbTemplateDialogOpen(false)} maxWidth="sm" fullWidth>
-        <DialogTitle>Save Template to Database</DialogTitle>
-        <DialogContent>
-          <Box sx={{ display: 'flex', flexDirection: 'column', gap: 2, pt: 1 }}>
-            <TextField
-              label="Template Name"
-              value={dbTemplateName}
-              onChange={(e) => setDbTemplateName(e.target.value)}
-              placeholder="e.g., Standard Baptism Records"
-              required
-              fullWidth
-            />
-            <FormControl fullWidth>
-              <InputLabel>Record Type</InputLabel>
-              <Select
-                value={dbTemplateRecordType}
-                label="Record Type"
-                onChange={(e) => setDbTemplateRecordType(e.target.value as any)}
-              >
-                <MenuItem value="baptism">Baptism</MenuItem>
-                <MenuItem value="marriage">Marriage</MenuItem>
-                <MenuItem value="funeral">Funeral</MenuItem>
-                <MenuItem value="custom">Custom</MenuItem>
-              </Select>
-            </FormControl>
-            <TextField
-              label="Description (optional)"
-              value={dbTemplateDescription}
-              onChange={(e) => setDbTemplateDescription(e.target.value)}
-              placeholder="Template description..."
-              multiline
-              rows={2}
-              fullWidth
-            />
-            <FormControlLabel
-              control={
-                <Checkbox
-                  checked={dbTemplateIsGlobal}
-                  onChange={(e) => setDbTemplateIsGlobal(e.target.checked)}
-                />
-              }
-              label="Make this template global (available to all churches)"
-            />
-            <Typography variant="caption" color="text.secondary">
-              This will convert your current table structure ({tableData.columns.length} columns) into a database template.
-            </Typography>
-          </Box>
-        </DialogContent>
-        <DialogActions>
-          <Button onClick={() => {
-            setSaveDbTemplateDialogOpen(false);
-            setDbTemplateName('');
-            setDbTemplateDescription('');
-            setDbTemplateRecordType('custom');
-            setDbTemplateIsGlobal(false);
-          }}>Cancel</Button>
-          <Button 
-            onClick={handleSaveDbTemplate} 
-            variant="contained" 
-            disabled={!dbTemplateName.trim()}
-          >
-            Save to Database
-          </Button>
-        </DialogActions>
-      </Dialog>
 
       {/* Toast */}
       <Snackbar
         open={toast.open}
-        autoHideDuration={3000}
+        autoHideDuration={12000}
         onClose={() => setToast({ ...toast, open: false })}
-        anchorOrigin={{ vertical: 'bottom', horizontal: 'right' }}
+        anchorOrigin={{ vertical: 'center', horizontal: 'center' }}
+        sx={{
+          position: 'fixed',
+          top: '50% !important',
+          left: '50% !important',
+          transform: 'translate(-50%, -50%) !important',
+          zIndex: 10000,
+          '& .MuiSnackbar-root': {
+            position: 'fixed',
+            top: '50% !important',
+            left: '50% !important',
+            transform: 'translate(-50%, -50%) !important',
+          }
+        }}
       >
         <Alert
           onClose={() => setToast({ ...toast, open: false })}
           severity={toast.severity}
-          sx={{ width: '100%' }}
+          sx={{ 
+            minWidth: '400px',
+            maxWidth: '600px',
+            fontSize: '1.1rem',
+            padding: '16px 20px',
+            '& .MuiAlert-message': {
+              fontSize: '1.1rem',
+              fontWeight: 500,
+            },
+            '& .MuiAlert-icon': {
+              fontSize: '28px',
+            }
+          }}
         >
           {toast.message}
         </Alert>
diff --git a/front-end/src/features/devel-tools/live-table-builder/components/LiveTableBuilder.tsx b/front-end/src/features/devel-tools/live-table-builder/components/LiveTableBuilder.tsx
index f3d32579..42db3c87 100644
--- a/front-end/src/features/devel-tools/live-table-builder/components/LiveTableBuilder.tsx
+++ b/front-end/src/features/devel-tools/live-table-builder/components/LiveTableBuilder.tsx
@@ -575,14 +575,13 @@ export const LiveTableBuilder: React.FC<LiveTableBuilderProps> = ({
               resizable: true,
             }}
             theme="legacy"
-            enableRangeSelection={false}
-            enableFillHandle={false}
-            enableRangeHandle={false}
             suppressClipboardPaste={false}
             animateRows={false}
             getRowId={(params) => params.data.id}
-            suppressRowClickSelection={true}
-            rowSelection="multiple"
+            rowSelection={{
+              mode: 'multiRow',
+              enableClickSelection: true,
+            }}
           />
         </div>
       </Box>
diff --git a/front-end/src/features/devel-tools/om-gallery/Gallery.tsx b/front-end/src/features/devel-tools/om-gallery/Gallery.tsx
index c4cc9b07..3b6fb57b 100755
--- a/front-end/src/features/devel-tools/om-gallery/Gallery.tsx
+++ b/front-end/src/features/devel-tools/om-gallery/Gallery.tsx
@@ -18,6 +18,15 @@ import {
   Paper,
   Stack,
   Chip,
+  FormControl,
+  InputLabel,
+  Select,
+  MenuItem,
+  Tooltip,
+  Checkbox,
+  FormControlLabel,
+  Collapse,
+  Divider,
 } from '@mui/material';
 import {
   IconArrowLeft,
@@ -27,9 +36,18 @@ import {
   IconPhoto,
   IconTrash,
   IconDownload,
+  IconFolder,
+  IconFolderPlus,
+  IconEdit,
+  IconArrowsExchange,
+  IconSparkles,
+  IconCopy,
+  IconChevronDown,
+  IconChevronUp,
 } from '@tabler/icons-react';
 import { styled } from '@mui/material/styles';
 import { DataGrid, GridColDef, GridRowSelectionModel, GridRenderCellParams } from '@mui/x-data-grid';
+import { OMLoading } from '@/components/common/OMLoading';
 
 const GalleryContainer = styled(Container)(({ theme }) => ({
   paddingTop: theme.spacing(4),
@@ -52,10 +70,14 @@ const ThumbnailGrid = styled(Box)(({ theme }) => ({
   },
 }));
 
-const ImageCard = styled(Card)(({ theme }) => ({
+const ImageCard = styled(Card)<{ isUsed?: boolean }>(({ theme, isUsed }) => ({
   position: 'relative',
   cursor: 'pointer',
   transition: 'transform 0.2s ease, box-shadow 0.2s ease',
+  ...(isUsed && {
+    background: 'linear-gradient(90deg, rgba(200, 230, 201, 0.3) 0%, rgba(200, 230, 201, 0.1) 100%)',
+    border: `2px solid rgba(76, 175, 80, 0.5)`,
+  }),
   '&:hover': {
     transform: 'scale(1.05)',
     boxShadow: theme.shadows[8],
@@ -99,7 +121,7 @@ const CarouselButton = styled(IconButton)(({ theme }) => ({
   },
 }));
 
-const CarouselImageContainer = styled(Box)(({ theme }) => ({
+const CarouselImageContainer = styled(Box)<{ isUsed?: boolean }>(({ theme, isUsed }) => ({
   width: '100%',
   height: '400px',
   display: 'flex',
@@ -108,6 +130,10 @@ const CarouselImageContainer = styled(Box)(({ theme }) => ({
   backgroundColor: theme.palette.mode === 'dark' ? '#1a1a1a' : '#f5f5f5',
   borderRadius: '8px',
   overflow: 'hidden',
+  ...(isUsed && {
+    background: 'linear-gradient(90deg, rgba(200, 230, 201, 0.3) 0%, rgba(200, 230, 201, 0.1) 100%)',
+    border: `3px solid rgba(76, 175, 80, 0.6)`,
+  }),
   [theme.breakpoints.down('md')]: {
     height: '300px',
   },
@@ -122,8 +148,11 @@ interface GalleryImage {
   path: string;
   url: string;
   created?: string;
+  modified?: string;
   size?: number;
   type?: string;
+  isUsed?: boolean; // Whether image is used in codebase (true=used, false=not_used, undefined=not_checked)
+  metadataError?: string; // Error message if file stats couldn't be read
 }
 
 const Gallery: React.FC = () => {
@@ -135,72 +164,443 @@ const Gallery: React.FC = () => {
   const [uploading, setUploading] = useState(false);
   const [uploadProgress, setUploadProgress] = useState(0);
   const [uploadError, setUploadError] = useState<string | null>(null);
-  const [selectedFile, setSelectedFile] = useState<File | null>(null);
+  const [selectedFiles, setSelectedFiles] = useState<File[]>([]);
+  const [uploadStatus, setUploadStatus] = useState<{ [key: string]: { progress: number; status: 'pending' | 'uploading' | 'success' | 'error'; error?: string } }>({});
   const [deleting, setDeleting] = useState(false);
   const [selectedRows, setSelectedRows] = useState<GridRowSelectionModel>([]);
   const [currentIndex, setCurrentIndex] = useState(0);
+  const [sortBy, setSortBy] = useState<'date' | 'name' | 'size' | 'type' | 'location'>('date');
+  const [sortOrder, setSortOrder] = useState<'asc' | 'desc'>('desc');
+  const [usageFilter, setUsageFilter] = useState<'all' | 'used' | 'unused' | 'not_checked'>('all');
+  const [checkingUsage, setCheckingUsage] = useState(false);
+  const [exportingUsedImages, setExportingUsedImages] = useState(false);
+  const [selectedDirectory, setSelectedDirectory] = useState<string>(''); // Default to root (empty = all images)
+  const [directoryTree, setDirectoryTree] = useState<any>({ directories: [], files: [] });
+  const [loadingTree, setLoadingTree] = useState(false);
+  const [moveDialogOpen, setMoveDialogOpen] = useState(false);
+  const [renameDialogOpen, setRenameDialogOpen] = useState(false);
+  const [itemToMove, setItemToMove] = useState<GalleryImage | null>(null);
+  const [newName, setNewName] = useState('');
+  const [targetDir, setTargetDir] = useState('review-required');
+  const [suggestions, setSuggestions] = useState<any[]>([]);
+  const [suggestionsDialogOpen, setSuggestionsDialogOpen] = useState(false);
+  const [suggestionStatuses, setSuggestionStatuses] = useState<Record<number, {
+    status: 'pending' | 'valid' | 'invalid' | 'applied' | 'failed';
+    message?: string;
+    code?: string;
+  }>>({});
+  const [showFullSummary, setShowFullSummary] = useState(false);
+  const [summaryExpanded, setSummaryExpanded] = useState(false);
+  const [validating, setValidating] = useState(false);
+  const [applying, setApplying] = useState(false);
   const fileInputRef = useRef<HTMLInputElement>(null);
+  const hasAutoCheckedUsage = useRef(false); // Track if auto-check has been triggered
+  const lastCheckedDirectory = useRef<string>(''); // Track which directory we last checked
+  const lastImageCount = useRef<number>(0); // Track image count when we last checked
 
-  // Carousel navigation handlers
+  // Sort images function
+  const sortImages = (imagesToSort: GalleryImage[]): GalleryImage[] => {
+    const sorted = [...imagesToSort].sort((a, b) => {
+      let comparison = 0;
+      
+      switch (sortBy) {
+        case 'date':
+          // Use modified date (or created if modified not available)
+          const dateA = new Date(a.modified || a.created || 0).getTime();
+          const dateB = new Date(b.modified || b.created || 0).getTime();
+          comparison = dateA - dateB;
+          break;
+        case 'name':
+          comparison = a.name.localeCompare(b.name);
+          break;
+        case 'size':
+          comparison = (a.size || 0) - (b.size || 0);
+          break;
+        case 'type':
+          comparison = (a.type || '').localeCompare(b.type || '');
+          break;
+        case 'location':
+          comparison = (a.path || '').localeCompare(b.path || '');
+          break;
+        default:
+          comparison = 0;
+      }
+      
+      return sortOrder === 'asc' ? comparison : -comparison;
+    });
+    
+    return sorted;
+  };
+
+  // Single source of truth: activeImages combines directory selection, usage filter, and sorting
+  // This ensures grid, table, and carousel all use the same filtered and sorted list
+  const activeImages = React.useMemo(() => {
+    if (!images || images.length === 0) return [];
+    
+    // Apply usage filter
+    let filtered: GalleryImage[] = images;
+    if (usageFilter === 'used') {
+      filtered = images.filter(img => img && img.isUsed === true);
+    } else if (usageFilter === 'unused') {
+      filtered = images.filter(img => img && img.isUsed === false);
+    } else if (usageFilter === 'not_checked') {
+      filtered = images.filter(img => img && img.isUsed === undefined);
+    }
+    // 'all' case: filtered = images (already set)
+    
+    // Apply sorting
+    return sortImages(filtered);
+  }, [images, usageFilter, sortBy, sortOrder]);
+
+  // Reset carousel index when active images change
+  React.useEffect(() => {
+    if (activeImages.length > 0 && currentIndex >= activeImages.length) {
+      setCurrentIndex(0);
+    } else if (activeImages.length === 0) {
+      setCurrentIndex(0);
+    }
+  }, [activeImages.length, currentIndex]);
+
+  // Carousel navigation handlers - use activeImages instead of images
   const handlePrevious = () => {
-    setCurrentIndex((prev) => (prev === 0 ? images.length - 1 : prev - 1));
+    if (activeImages.length === 0) return;
+    setCurrentIndex((prev) => (prev === 0 ? activeImages.length - 1 : prev - 1));
   };
 
   const handleNext = () => {
-    setCurrentIndex((prev) => (prev === images.length - 1 ? 0 : prev + 1));
+    if (activeImages.length === 0) return;
+    setCurrentIndex((prev) => (prev === activeImages.length - 1 ? 0 : prev + 1));
   };
 
-  // Load images from gallery directory
+  // Check image usage in codebase (with batching to handle all images)
+  const checkImageUsage = async (imagesToCheck: GalleryImage[], checkAll = false) => {
+    if (!imagesToCheck || imagesToCheck.length === 0) return;
+    
+    setCheckingUsage(true);
+    
+    try {
+      // Always check all images when explicitly requested
+      // Backend will handle batching internally
+      const imagesToProcess = imagesToCheck.filter(img => img != null);
+      
+      // Process in batches of 500 (backend limit) to avoid timeouts
+      const BATCH_SIZE = 500;
+      const batches: GalleryImage[][] = [];
+      for (let i = 0; i < imagesToProcess.length; i += BATCH_SIZE) {
+        batches.push(imagesToProcess.slice(i, i + BATCH_SIZE));
+      }
+      
+      let allUsage: { [key: string]: boolean } = {};
+      
+      // Process batches sequentially to avoid overwhelming the server
+      for (let batchIndex = 0; batchIndex < batches.length; batchIndex++) {
+        const batch = batches[batchIndex];
+        
+        const response = await fetch('/api/gallery/check-usage', {
+          method: 'POST',
+          headers: {
+            'Content-Type': 'application/json',
+          },
+          credentials: 'include',
+          body: JSON.stringify({
+            images: batch.map(img => ({
+              name: img.name || '',
+              path: img.path || '',
+            }))
+          })
+        });
+
+        if (response.ok) {
+          const data = await response.json();
+          if (data.success && data.usage) {
+            // Merge batch results
+            allUsage = { ...allUsage, ...data.usage };
+          }
+        } else {
+          const errorData = await response.json().catch(() => ({}));
+          console.error(`Usage check failed for batch ${batchIndex + 1}/${batches.length}:`, errorData);
+        }
+      }
+
+      // Update all images with usage information
+      setImages(prevImages => {
+        if (!prevImages || prevImages.length === 0) return prevImages;
+        const updated = prevImages.map(img => {
+          if (!img) return img;
+          // If the image name is in the usage map, use that value
+          // Otherwise, keep as undefined (not checked) if it wasn't in any batch
+          const usageValue = allUsage[img.name];
+          return {
+            ...img,
+            isUsed: usageValue !== undefined ? usageValue : img.isUsed // Preserve undefined if not checked
+          };
+        });
+        return sortImages(updated);
+      });
+    } catch (error: any) {
+      console.error('Error checking image usage:', error);
+      // Don't mark as unknown on error - preserve existing state
+    } finally {
+      setCheckingUsage(false);
+    }
+  };
+
+  // Re-sort images when sort criteria changes
   useEffect(() => {
-    loadImages();
+    if (images.length > 0) {
+      const sorted = sortImages(images);
+      setImages(sorted);
+    }
+    // eslint-disable-next-line react-hooks/exhaustive-deps
+  }, [sortBy, sortOrder]);
+
+  // Auto-check usage when filter changes for the first time (not 'all')
+  useEffect(() => {
+    // Only auto-check once, and only if:
+    // 1. Filter is not 'all' (user wants to see filtered results)
+    // 2. We haven't auto-checked yet
+    // 3. We have images loaded
+    // 4. We're not already checking
+    if (
+      !hasAutoCheckedUsage.current &&
+      usageFilter !== 'all' &&
+      images.length > 0 &&
+      !checkingUsage
+    ) {
+      // Check if there are any images with undefined usage status
+      const hasUncheckedImages = images.some(img => img.isUsed === undefined);
+      
+      if (hasUncheckedImages) {
+        hasAutoCheckedUsage.current = true;
+        console.log(`🔄 Auto-checking usage due to filter change to: ${usageFilter}`);
+        checkImageUsage(images, true);
+      }
+    }
+    // eslint-disable-next-line react-hooks/exhaustive-deps
+  }, [usageFilter]);
+
+  // Reset auto-check flag when directory changes
+  useEffect(() => {
+    hasAutoCheckedUsage.current = false;
+    lastCheckedDirectory.current = '';
+    lastImageCount.current = 0;
+  }, [selectedDirectory]);
+
+  // Load directory tree on mount, then cleanup empty directories
+  useEffect(() => {
+    const initializeGallery = async () => {
+      await loadDirectoryTree();
+      // Clean up empty directories after loading tree
+      await cleanupEmptyDirectories();
+    };
+    initializeGallery();
   }, []);
 
+  // Load images when directory changes
+  useEffect(() => {
+    loadImages();
+  }, [selectedDirectory]);
+
+  // Automatically check usage after images are loaded for a directory
+  // This effect runs when selectedDirectory changes, then waits for images to load
+  useEffect(() => {
+    // Only auto-check if a specific directory is selected (not root)
+    if (selectedDirectory === '') {
+      return;
+    }
+    
+    // Reset flags when directory changes
+    hasAutoCheckedUsage.current = false;
+    lastImageCount.current = 0;
+    
+    // Wait for images to load, then check usage
+    // Use a longer delay to ensure images are fully loaded
+    const checkTimer = setTimeout(() => {
+      // Only check if:
+      // 1. We have images loaded
+      // 2. We haven't already checked this directory (directory is different or never checked)
+      // 3. We're not already checking
+      // 4. Images have actually been loaded (count > 0)
+      const directoryNotChecked = selectedDirectory !== lastCheckedDirectory.current || lastCheckedDirectory.current === '';
+      
+      if (
+        images.length > 0 &&
+        selectedDirectory !== '' &&
+        directoryNotChecked &&
+        !checkingUsage &&
+        !hasAutoCheckedUsage.current
+      ) {
+        lastCheckedDirectory.current = selectedDirectory;
+        lastImageCount.current = images.length;
+        hasAutoCheckedUsage.current = true;
+        checkImageUsage(images, true);
+      }
+    }, 800); // Wait longer to ensure images are loaded after directory change
+    
+    return () => clearTimeout(checkTimer);
+  }, [selectedDirectory]); // Only trigger on directory change, NOT on images change
+
+  const loadDirectoryTree = async () => {
+    setLoadingTree(true);
+    try {
+      const urlParams = new URLSearchParams(window.location.search);
+      const debug = urlParams.get('debug') === '1' ? '&debug=1' : '';
+      const response = await fetch(`/api/gallery/tree?depth=3${debug}`, {
+        credentials: 'include',
+      });
+      if (response.ok) {
+        const data = await response.json();
+        console.log('📁 Directory tree loaded:', {
+          directories: data.directories?.length || 0,
+          files: data.files?.length || 0,
+          path: data.path
+        });
+        setDirectoryTree(data);
+        
+        // Log debug info if available
+        if (data.debug) {
+          console.log('🔍 [DEBUG] Directory tree debug info:', data.debug);
+          if (data.debug.resolvedImagesRoot) {
+            console.log(`🔍 [DEBUG] Images root: ${data.debug.resolvedImagesRoot}`);
+            console.log(`🔍 [DEBUG] Root exists: ${data.debug.rootExists}`);
+            console.log(`🔍 [DEBUG] Root readable: ${data.debug.rootCanRead}`);
+            if (data.debug.firstEntries && data.debug.firstEntries.length > 0) {
+              console.log(`🔍 [DEBUG] First entries:`, data.debug.firstEntries);
+            }
+          }
+        }
+      } else {
+        const errorData = await response.json().catch(() => ({}));
+        console.error('❌ Error loading directory tree:', {
+          status: response.status,
+          error: errorData
+        });
+        // Don't wipe state - keep existing tree if available
+        // Just log the error
+      }
+    } catch (error) {
+      console.error('❌ Error loading directory tree:', error);
+      // Don't wipe state - keep existing tree if available
+    } finally {
+      setLoadingTree(false);
+    }
+  };
+
   const loadImages = async () => {
     try {
-      const response = await fetch('/api/gallery/images', {
+      // Root view (empty string) should load all images recursively
+      // Otherwise use selected directory (no default fallback - use exactly what's selected)
+      const path = selectedDirectory === '' ? '' : selectedDirectory;
+      const url = `/api/gallery/images?path=${encodeURIComponent(path)}&recursive=1`;
+      console.log('Loading images from:', url);
+      
+      const response = await fetch(url, {
         credentials: 'include',
       });
       
       if (response.ok) {
         const data = await response.json();
+        console.log('🖼️ Images loaded:', {
+          count: data.count || 0,
+          path: data.path,
+          recursive: data.recursive
+        });
+        
+        // Log debug info if available
+        if (data.debug) {
+          console.log('🔍 [DEBUG] Images API debug info:', data.debug);
+          if (data.debug.resolvedImagesRoot) {
+            console.log(`🔍 [DEBUG] Images root: ${data.debug.resolvedImagesRoot}`);
+            console.log(`🔍 [DEBUG] Root exists: ${data.debug.rootExists}`);
+            console.log(`🔍 [DEBUG] Root readable: ${data.debug.rootCanRead}`);
+            if (data.debug.sampleImage) {
+              console.log(`🔍 [DEBUG] Sample image:`, data.debug.sampleImage);
+            }
+          }
+        }
+        
         // Gallery API returns { success: true, count: number, images: [...] }
-        // Filter to only show image files (jpg, jpeg, png, gif, tiff)
-        const imageExtensions = ['.jpg', '.jpeg', '.png', '.gif', '.tiff'];
+        // Filter to only show image files (jpg, jpeg, png, gif, tiff, webp, svg)
+        const imageExtensions = ['.jpg', '.jpeg', '.png', '.gif', '.tiff', '.webp', '.svg'];
         const imageFiles = (data.images || []).filter((file: any) => {
           const fileName = file.name || file.path || '';
           const ext = fileName.split('.').pop()?.toLowerCase() || '';
           return imageExtensions.includes(`.${ext}`);
         });
         
+        // Debug: Log API response structure if needed
+        const urlParams = new URLSearchParams(window.location.search);
+        if (urlParams.get('debug') === '1' && imageFiles.length > 0) {
+          console.log('🔍 [DEBUG] Sample image from API:', imageFiles[0]);
+          console.log('🔍 [DEBUG] Sample image keys:', Object.keys(imageFiles[0]));
+        }
+        
         // Transform the data to match GalleryImage interface
         const transformedImages = imageFiles.map((file: any, index: number) => {
+          // Handle metadata - preserve actual values or error status from API
+          let fileSize: number | null = null;
+          let fileModified: string | null = null;
+          let fileCreated: string | null = null;
+          let metadataError: string | null = null;
+          
+          // Check if API returned error status
+          if (file.metadataStatus === 'error') {
+            metadataError = file.statError || 'Failed to read file metadata';
+            // Keep size/modified/created as null when there's an error
+          } else {
+            // Parse size as number - API should always return numeric or null
+            if (file.size !== undefined && file.size !== null && file.size !== '') {
+              fileSize = typeof file.size === 'number' ? file.size : parseInt(String(file.size), 10);
+              if (isNaN(fileSize) || fileSize < 0) {
+                fileSize = null;
+                metadataError = 'Invalid file size value';
+              }
+            }
+            
+            // Preserve date strings exactly as received - API should return ISO strings or null
+            fileModified = (file.modified && file.modified !== '') ? file.modified : null;
+            fileCreated = (file.created && file.created !== '') ? file.created : null;
+          }
+          
           return {
-            id: `img-${index}`,
+            id: `img-${index}-${file.name}`,
             name: file.name || 'Unknown',
-            path: file.path || file.url || file.name, // Use path from API response
-            url: file.url || file.path || `/images/gallery/${file.name}`, // Use URL from API response
-            created: file.created || file.modified || new Date().toISOString(),
-            size: file.size || 0,
-            type: file.type || file.name?.split('.').pop() || 'unknown',
+            path: file.path || file.url || file.name,
+            url: file.url || file.path || `/images/gallery/${file.name}`,
+            created: fileCreated,
+            modified: fileModified,
+            size: fileSize,
+            type: file.type || file.name?.split('.').pop()?.toLowerCase() || 'unknown',
+            isUsed: undefined, // Will be checked separately
+            metadataError: metadataError || undefined, // Store error if present
           };
         });
         
-        // Images are already sorted by the backend (newest first)
-        // But we can sort again just in case
-        transformedImages.sort((a: GalleryImage, b: GalleryImage) => {
-          return new Date(b.created || 0).getTime() - new Date(a.created || 0).getTime();
-        });
-        
+        // Don't sort here - sorting will be applied in activeImages useMemo
+        // This ensures sorting is applied after filtering
         setImages(transformedImages);
+        
+        // Reset carousel index when images change
+        setCurrentIndex(0);
+        
+        // Note: Usage status is not auto-checked on load
+        // Users must click "Refresh Usage" to check which images are used in the codebase
+        // This prevents slow page loads and timeouts for large image collections
       } else {
         // Log the actual error for debugging
         const errorData = await response.json().catch(() => ({}));
         console.error('Gallery API error:', {
           status: response.status,
           statusText: response.statusText,
-          error: errorData
+          error: errorData,
+          url: url
         });
         setImages([]);
+        
+        // Show user-friendly error message
+        if (response.status === 502) {
+          console.error('Backend server may be down or the gallery API endpoint is not available');
+        }
       }
     } catch (error: any) {
       // Log the actual error for debugging
@@ -313,13 +713,14 @@ const Gallery: React.FC = () => {
             <script>
               function deleteImage() {
                 if (confirm('Are you sure you want to delete this image?')) {
-                  fetch('/api/gallery/delete', {
-                    method: 'POST',
+                  const relativePath = '${image.path}'.replace(/^\\/images\\//, '');
+                  fetch('/api/gallery/file', {
+                    method: 'DELETE',
                     headers: {
                       'Content-Type': 'application/json',
                     },
                     credentials: 'include',
-                    body: JSON.stringify({ path: '${image.path}' })
+                    body: JSON.stringify({ path: relativePath })
                   })
                   .then(response => response.json())
                   .then(data => {
@@ -354,72 +755,475 @@ const Gallery: React.FC = () => {
 
     setDeleting(true);
     try {
-      const response = await fetch('/api/gallery/delete', {
-        method: 'POST',
+      // Extract relative path from /images/... path
+      const relativePath = image.path.replace(/^\/images\//, '');
+      console.log('🗑️ [Frontend] Single delete:', {
+        name: image.name,
+        originalPath: image.path,
+        relativePath: relativePath,
+        selectedDirectory: selectedDirectory
+      });
+      
+      const response = await fetch('/api/gallery/file', {
+        method: 'DELETE',
         headers: {
           'Content-Type': 'application/json',
         },
         credentials: 'include',
-        body: JSON.stringify({ path: image.path })
+        body: JSON.stringify({ path: relativePath })
       });
 
+      console.log('🗑️ [Frontend] Delete response status:', response.status, response.statusText);
+
       if (response.ok) {
+        const result = await response.json().catch(() => ({}));
+        console.log('🗑️ [Frontend] Delete successful:', result);
         await loadImages();
+        await loadDirectoryTree();
         setImageDialogOpen(false);
         setSelectedImage(null);
-        // Images will reload automatically via loadImages()
+      } else {
+        let errorMessage = 'Unknown error';
+        try {
+          const errorData = await response.json();
+          console.error('🗑️ [Frontend] Delete error response:', errorData);
+          errorMessage = errorData.error || errorData.message || `HTTP ${response.status}: ${response.statusText}`;
+        } catch (e) {
+          console.error('🗑️ [Frontend] Failed to parse error response:', e);
+          errorMessage = `HTTP ${response.status}: ${response.statusText}`;
+        }
+        alert(`Failed to delete image: ${errorMessage}`);
+      }
+    } catch (error: any) {
+      console.error('🗑️ [Frontend] Error deleting image:', error);
+      alert(`Failed to delete image: ${error.message || 'Network error'}`);
+    } finally {
+      setDeleting(false);
+    }
+  };
+
+  const handleMoveImage = async (image: GalleryImage, targetDir: string) => {
+    try {
+      const relativePath = image.path.replace(/^\/images\//, '');
+      const targetPath = targetDir ? `${targetDir}/${image.name}` : image.name;
+      
+      const response = await fetch('/api/gallery/move', {
+        method: 'POST',
+        headers: { 'Content-Type': 'application/json' },
+        credentials: 'include',
+        body: JSON.stringify({ from: relativePath, to: targetPath, overwrite: false })
+      });
+
+      if (response.ok) {
+        await loadImages();
+        await loadDirectoryTree();
+        setMoveDialogOpen(false);
+        setItemToMove(null);
+      } else {
+        const data = await response.json();
+        alert(data.error || 'Failed to move image');
+      }
+    } catch (error) {
+      console.error('Error moving image:', error);
+      alert('Failed to move image');
+    }
+  };
+
+  const handleRenameImage = async (image: GalleryImage, newName: string) => {
+    try {
+      const relativePath = image.path.replace(/^\/images\//, '');
+      const response = await fetch('/api/gallery/rename', {
+        method: 'POST',
+        headers: { 'Content-Type': 'application/json' },
+        credentials: 'include',
+        body: JSON.stringify({ path: relativePath, newName })
+      });
+
+      if (response.ok) {
+        await loadImages();
+        await loadDirectoryTree();
+        setRenameDialogOpen(false);
+        setItemToMove(null);
+        setNewName('');
       } else {
         const data = await response.json();
-        alert(data.message || 'Failed to delete image');
+        alert(data.error || 'Failed to rename image');
       }
     } catch (error) {
-      console.error('Error deleting image:', error);
-      alert('Failed to delete image');
+      console.error('Error renaming image:', error);
+      alert('Failed to rename image');
+    }
+  };
+
+  const handleCreateDirectory = async (dirName: string) => {
+    if (!dirName || !dirName.trim()) {
+      alert('Directory name cannot be empty');
+      return;
+    }
+    
+    try {
+      const targetPath = selectedDirectory ? `${selectedDirectory}/${dirName.trim()}` : dirName.trim();
+      const response = await fetch('/api/gallery/mkdir', {
+        method: 'POST',
+        headers: { 'Content-Type': 'application/json' },
+        credentials: 'include',
+        body: JSON.stringify({ path: targetPath })
+      });
+
+      if (response.ok) {
+        await loadDirectoryTree();
+        // Show success message
+        const data = await response.json();
+        console.log('Directory created:', data);
+      } else {
+        const data = await response.json();
+        // Show detailed error message
+        const errorMsg = data.error || 'Failed to create directory';
+        const code = data.code ? ` (${data.code})` : '';
+        const pathInfo = data.path ? `\nPath: ${data.path}` : '';
+        alert(`${errorMsg}${code}${pathInfo}`);
+      }
+    } catch (error: any) {
+      console.error('Error creating directory:', error);
+      alert(`Failed to create directory: ${error.message || 'Unknown error'}`);
+    }
+  };
+
+  const handleGetSuggestions = async () => {
+    try {
+      const response = await fetch('/api/gallery/suggest-destination', {
+        method: 'POST',
+        headers: { 'Content-Type': 'application/json' },
+        credentials: 'include',
+        body: JSON.stringify({ images: images.map(img => ({ path: img.path, name: img.name })) })
+      });
+
+      if (response.ok) {
+        const data = await response.json();
+        setSuggestions(data.suggestions || []);
+        setSuggestionStatuses({}); // Reset statuses
+        setSuggestionsDialogOpen(true);
+      } else {
+        const data = await response.json();
+        alert(data.error || 'Failed to get catalog suggestions');
+      }
+    } catch (error) {
+      console.error('Error getting suggestions:', error);
+      alert('Failed to get catalog suggestions');
+    }
+  };
+
+  const normalizePath = (pathStr: string): string => {
+    return pathStr.replace(/^\/images\//, '').replace(/^\/+/, '');
+  };
+
+  const handleDryRun = async () => {
+    if (suggestions.length === 0) return;
+    
+    setValidating(true);
+    try {
+      // Convert suggestions to actions
+      const actions = suggestions.map((suggestion) => {
+        const relativePath = normalizePath(suggestion.path);
+        const targetPath = suggestion.suggestedDir 
+          ? `${suggestion.suggestedDir}/${suggestion.suggestedName}`
+          : suggestion.suggestedName;
+        
+        return {
+          type: 'move',
+          from: relativePath,
+          to: targetPath
+        };
+      });
+
+      const response = await fetch('/api/gallery/validate-actions', {
+        method: 'POST',
+        headers: { 'Content-Type': 'application/json' },
+        credentials: 'include',
+        body: JSON.stringify({ actions })
+      });
+
+      if (response.ok) {
+        const data = await response.json();
+        const newStatuses: Record<number, any> = {};
+        
+        data.results.forEach((result: any, idx: number) => {
+          newStatuses[idx] = {
+            status: result.ok ? 'valid' : 'invalid',
+            message: result.message,
+            code: result.code
+          };
+        });
+        
+        setSuggestionStatuses(newStatuses);
+        setSummaryExpanded(true);
+      } else {
+        const data = await response.json();
+        alert(data.error || 'Failed to validate actions');
+      }
+    } catch (error) {
+      console.error('Error validating actions:', error);
+      alert('Failed to validate actions');
     } finally {
-      setDeleting(false);
+      setValidating(false);
     }
   };
 
+  const handleApplySingle = async (idx: number) => {
+    const suggestion = suggestions[idx];
+    if (!suggestion) return;
+
+    setApplying(true);
+    try {
+      const relativePath = normalizePath(suggestion.path);
+      const targetPath = suggestion.suggestedDir 
+        ? `${suggestion.suggestedDir}/${suggestion.suggestedName}`
+        : suggestion.suggestedName;
+
+      const actions = [{
+        type: 'move',
+        from: relativePath,
+        to: targetPath
+      }];
+
+      const response = await fetch('/api/gallery/apply-actions', {
+        method: 'POST',
+        headers: { 'Content-Type': 'application/json' },
+        credentials: 'include',
+        body: JSON.stringify({ actions, continueOnError: false })
+      });
+
+      if (response.ok) {
+        const data = await response.json();
+        const result = data.results[0];
+        
+        setSuggestionStatuses(prev => ({
+          ...prev,
+          [idx]: {
+            status: result.ok ? 'applied' : 'failed',
+            message: result.message,
+            code: result.code
+          }
+        }));
+
+        if (result.ok) {
+          // Reload images and directory tree
+          await loadImages();
+          await loadDirectoryTree();
+        }
+      } else {
+        const data = await response.json();
+        setSuggestionStatuses(prev => ({
+          ...prev,
+          [idx]: {
+            status: 'failed',
+            message: data.error || 'Failed to apply action',
+            code: 'APPLY_ERROR'
+          }
+        }));
+      }
+    } catch (error) {
+      console.error('Error applying action:', error);
+      setSuggestionStatuses(prev => ({
+        ...prev,
+        [idx]: {
+          status: 'failed',
+          message: 'Failed to apply action',
+          code: 'APPLY_ERROR'
+        }
+      }));
+    } finally {
+      setApplying(false);
+    }
+  };
+
+  const handleApplyAll = async () => {
+    if (suggestions.length === 0) return;
+
+    // Check if any are invalid
+    const invalidCount = Object.values(suggestionStatuses).filter(
+      s => s.status === 'invalid'
+    ).length;
+    
+    if (invalidCount > 0) {
+      const proceed = confirm(
+        `${invalidCount} suggestion(s) are invalid. Do you want to continue anyway? ` +
+        'Only valid suggestions will be applied.'
+      );
+      if (!proceed) return;
+    }
+
+    setApplying(true);
+    try {
+      const actions = suggestions.map((suggestion) => {
+        const relativePath = normalizePath(suggestion.path);
+        const targetPath = suggestion.suggestedDir 
+          ? `${suggestion.suggestedDir}/${suggestion.suggestedName}`
+          : suggestion.suggestedName;
+        
+        return {
+          type: 'move',
+          from: relativePath,
+          to: targetPath
+        };
+      });
+
+      const response = await fetch('/api/gallery/apply-actions', {
+        method: 'POST',
+        headers: { 'Content-Type': 'application/json' },
+        credentials: 'include',
+        body: JSON.stringify({ actions, continueOnError: true })
+      });
+
+      if (response.ok) {
+        const data = await response.json();
+        const newStatuses: Record<number, any> = {};
+        
+        data.results.forEach((result: any, idx: number) => {
+          newStatuses[idx] = {
+            status: result.ok ? 'applied' : 'failed',
+            message: result.message,
+            code: result.code
+          };
+        });
+        
+        setSuggestionStatuses(newStatuses);
+        setSummaryExpanded(true);
+
+        // Reload if any succeeded
+        if (data.summary.ok > 0) {
+          await loadImages();
+          await loadDirectoryTree();
+        }
+      } else {
+        const data = await response.json();
+        alert(data.error || 'Failed to apply actions');
+      }
+    } catch (error) {
+      console.error('Error applying actions:', error);
+      alert('Failed to apply actions');
+    } finally {
+      setApplying(false);
+    }
+  };
+
+  const handleCopySummary = () => {
+    const summary = {
+      total: suggestions.length,
+      statuses: Object.entries(suggestionStatuses).map(([idx, status]) => ({
+        index: parseInt(idx),
+        suggestion: suggestions[parseInt(idx)],
+        status: status.status,
+        message: status.message,
+        code: status.code
+      })),
+      summary: {
+        total: suggestions.length,
+        valid: Object.values(suggestionStatuses).filter(s => s.status === 'valid').length,
+        invalid: Object.values(suggestionStatuses).filter(s => s.status === 'invalid').length,
+        applied: Object.values(suggestionStatuses).filter(s => s.status === 'applied').length,
+        failed: Object.values(suggestionStatuses).filter(s => s.status === 'failed').length,
+        pending: Object.values(suggestionStatuses).filter(s => s.status === 'pending' || !s).length
+      }
+    };
+
+    const text = JSON.stringify(summary, null, 2);
+    navigator.clipboard.writeText(text).then(() => {
+      alert('Summary copied to clipboard');
+    }).catch(err => {
+      console.error('Failed to copy:', err);
+      alert('Failed to copy summary');
+    });
+  };
+
+  const getStatusChip = (idx: number) => {
+    const status = suggestionStatuses[idx];
+    if (!status || status.status === 'pending') {
+      return <Chip label="Pending" size="small" color="default" />;
+    }
+    
+    const colorMap: Record<string, 'success' | 'error' | 'warning' | 'info'> = {
+      'valid': 'success',
+      'invalid': 'error',
+      'applied': 'success',
+      'failed': 'error'
+    };
+    
+    return (
+      <Chip 
+        label={status.status.charAt(0).toUpperCase() + status.status.slice(1)} 
+        size="small" 
+        color={colorMap[status.status] || 'default'}
+        title={status.message}
+      />
+    );
+  };
+
   const handleFileSelect = (event: React.ChangeEvent<HTMLInputElement>) => {
-    const file = event.target.files?.[0];
-    if (file) {
+    const files = Array.from(event.target.files || []);
+    if (files.length === 0) return;
+
+    const validFiles: File[] = [];
+    const errors: string[] = [];
+
+    files.forEach((file) => {
       // Allowed image file extensions
       const allowedExtensions = ['.jpg', '.jpeg', '.tiff', '.gif', '.png'];
       const fileExtension = '.' + file.name.split('.').pop()?.toLowerCase();
       
       if (!allowedExtensions.includes(fileExtension)) {
-        setUploadError(`Invalid file type. Allowed types: ${allowedExtensions.join(', ')}`);
+        errors.push(`${file.name}: Invalid file type. Allowed types: ${allowedExtensions.join(', ')}`);
         return;
       }
       if (file.size > 10 * 1024 * 1024) {
-        setUploadError('File size must be less than 10MB');
+        errors.push(`${file.name}: File size must be less than 10MB`);
         return;
       }
-      setSelectedFile(file);
+      validFiles.push(file);
+    });
+
+    if (errors.length > 0) {
+      setUploadError(errors.join('\n'));
+    } else {
       setUploadError(null);
     }
-  };
 
-  const handleUpload = async () => {
-    if (!selectedFile) {
-      setUploadError('Please select a file');
-      return;
+    if (validFiles.length > 0) {
+      setSelectedFiles(prev => [...prev, ...validFiles]);
     }
+  };
 
-    setUploading(true);
-    setUploadError(null);
-    setUploadProgress(0);
-
-    try {
+  const uploadSingleFile = (file: File, targetDirectory: string = 'review-required'): Promise<void> => {
+    return new Promise((resolve, reject) => {
       const formData = new FormData();
-      formData.append('image', selectedFile); // Gallery API expects field name 'image'
+      formData.append('image', file);
+      formData.append('targetDir', targetDirectory);
       
       const xhr = new XMLHttpRequest();
+      const fileKey = `${file.name}-${file.size}`;
+
+      // Initialize upload status
+      setUploadStatus(prev => ({
+        ...prev,
+        [fileKey]: { progress: 0, status: 'uploading' }
+      }));
 
       xhr.upload.addEventListener('progress', (e) => {
         if (e.lengthComputable) {
           const percentComplete = (e.loaded / e.total) * 100;
-          setUploadProgress(percentComplete);
+          setUploadStatus(prev => {
+            const updated = {
+              ...prev,
+              [fileKey]: { ...prev[fileKey], progress: percentComplete, status: 'uploading' as const }
+            };
+            // Calculate overall progress
+            const allStatuses = Object.values(updated);
+            const totalProgress = allStatuses.length > 0 
+              ? allStatuses.reduce((sum, s) => sum + s.progress, 0) / allStatuses.length 
+              : 0;
+            setUploadProgress(totalProgress);
+            return updated;
+          });
         }
       });
 
@@ -428,34 +1232,32 @@ const Gallery: React.FC = () => {
           try {
             const response = JSON.parse(xhr.responseText);
             if (response.success) {
-              loadImages();
-              setUploadDialogOpen(false);
-              setSelectedFile(null);
-              setUploadProgress(0);
-              if (fileInputRef.current) {
-                fileInputRef.current.value = '';
-              }
+              setUploadStatus(prev => ({
+                ...prev,
+                [fileKey]: { progress: 100, status: 'success' }
+              }));
+              resolve();
             } else {
-              setUploadError(response.error || response.message || 'Upload failed');
+              const errorMsg = response.error || response.message || 'Upload failed';
+              setUploadStatus(prev => ({
+                ...prev,
+                [fileKey]: { progress: 0, status: 'error', error: errorMsg }
+              }));
+              reject(new Error(errorMsg));
             }
           } catch (e) {
-            // If response is not JSON, assume success for 200 status
-            loadImages();
-            setUploadDialogOpen(false);
-            setSelectedFile(null);
-            setUploadProgress(0);
-            if (fileInputRef.current) {
-              fileInputRef.current.value = '';
-            }
+            setUploadStatus(prev => ({
+              ...prev,
+              [fileKey]: { progress: 100, status: 'success' }
+            }));
+            resolve();
           }
         } else {
-          // Try to parse JSON error response
           let errorMessage = 'Upload failed';
           try {
             const errorResponse = JSON.parse(xhr.responseText);
             errorMessage = errorResponse.error || errorResponse.message || errorMessage;
           } catch (e) {
-            // If response is HTML (from nginx), show generic error
             if (xhr.status === 500) {
               errorMessage = 'Internal server error. Please check server logs.';
             } else if (xhr.status === 404) {
@@ -464,44 +1266,125 @@ const Gallery: React.FC = () => {
               errorMessage = `Upload failed with status ${xhr.status}`;
             }
           }
-          setUploadError(errorMessage);
+          setUploadStatus(prev => ({
+            ...prev,
+            [fileKey]: { progress: 0, status: 'error', error: errorMessage }
+          }));
+          reject(new Error(errorMessage));
         }
-        setUploading(false);
       });
 
       xhr.addEventListener('error', () => {
-        setUploadError('Upload failed. Backend endpoint /api/gallery/upload may not be available.');
-        setUploading(false);
+        const errorMsg = 'Upload failed. Backend endpoint /api/gallery/upload may not be available.';
+        setUploadStatus(prev => ({
+          ...prev,
+          [fileKey]: { progress: 0, status: 'error', error: errorMsg }
+        }));
+        reject(new Error(errorMsg));
       });
 
-      xhr.open('POST', '/api/gallery/upload'); // Use gallery upload endpoint
-      xhr.withCredentials = true; // Include cookies for authentication
-      xhr.send(formData);
-    } catch (error) {
-      setUploadError('An error occurred during upload');
+      xhr.open('POST', '/api/gallery/upload');
+      xhr.withCredentials = true;
+      xhr.send(formData);
+    });
+  };
+
+  const handleUpload = async () => {
+    if (selectedFiles.length === 0) {
+      setUploadError('Please select at least one file');
+      return;
+    }
+
+    setUploading(true);
+    setUploadError(null);
+    setUploadProgress(0);
+    
+    // Initialize upload status for all files
+    const initialStatus: { [key: string]: { progress: number; status: 'pending' | 'uploading' | 'success' | 'error'; error?: string } } = {};
+    selectedFiles.forEach(file => {
+      const fileKey = `${file.name}-${file.size}`;
+      initialStatus[fileKey] = { progress: 0, status: 'pending' };
+    });
+    setUploadStatus(initialStatus);
+
+    try {
+      // Upload files sequentially to avoid overwhelming the server
+      const errors: string[] = [];
+      for (const file of selectedFiles) {
+        try {
+          // Always upload to review-required directory
+          await uploadSingleFile(file, 'review-required');
+        } catch (error: any) {
+          errors.push(`${file.name}: ${error.message || 'Upload failed'}`);
+        }
+      }
+
+      if (errors.length > 0) {
+        setUploadError(`Some files failed to upload:\n${errors.join('\n')}`);
+      } else {
+        // All files uploaded successfully
+        setUploadDialogOpen(false);
+        setSelectedFiles([]);
+        setUploadStatus({});
+        setUploadProgress(0);
+        if (fileInputRef.current) {
+          fileInputRef.current.value = '';
+        }
+        // Reload the page to show newly uploaded images
+        window.location.href = '/apps/gallery';
+      }
+    } catch (error: any) {
+      setUploadError(error.message || 'An error occurred during upload');
+    } finally {
       setUploading(false);
     }
   };
 
   const handleOpenUploadDialog = () => {
     setUploadDialogOpen(true);
-    setSelectedFile(null);
+    setSelectedFiles([]);
     setUploadError(null);
     setUploadProgress(0);
+    setUploadStatus({});
   };
 
   const handleCloseUploadDialog = () => {
     if (!uploading) {
       setUploadDialogOpen(false);
-      setSelectedFile(null);
+      setSelectedFiles([]);
       setUploadError(null);
       setUploadProgress(0);
+      setUploadStatus({});
       if (fileInputRef.current) {
         fileInputRef.current.value = '';
       }
     }
   };
 
+  const handleRemoveFile = (index: number) => {
+    setSelectedFiles(prev => prev.filter((_, i) => i !== index));
+  };
+
+  // Filter images based on usage filter (tri-state: used, not_used, not_checked)
+  // Note: This function is kept for backward compatibility but activeImages should be used instead
+  const getFilteredImages = (): GalleryImage[] => {
+    if (!images || images.length === 0) return [];
+    if (usageFilter === 'all') return images;
+    if (usageFilter === 'used') {
+      // Only show images that are explicitly marked as used (true)
+      return images.filter(img => img && img.isUsed === true);
+    }
+    if (usageFilter === 'unused') {
+      // Only show images that are explicitly marked as not used (false)
+      return images.filter(img => img && img.isUsed === false);
+    }
+    if (usageFilter === 'not_checked') {
+      // Only show images that haven't been checked yet (undefined)
+      return images.filter(img => img && img.isUsed === undefined);
+    }
+    return images;
+  };
+
   // MUI DataGrid column definitions
   const columns: GridColDef[] = [
     {
@@ -510,17 +1393,25 @@ const Gallery: React.FC = () => {
       flex: 1,
       minWidth: 200,
       renderCell: (params: GridRenderCellParams) => {
+        if (!params || !params.row) {
+          return <Typography variant="body2">Unknown</Typography>;
+        }
         const image = params.row as GalleryImage;
+        if (!image) {
+          return <Typography variant="body2">Unknown</Typography>;
+        }
         return (
           <Box sx={{ display: 'flex', alignItems: 'center', gap: 1, py: 1 }}>
             <Box
               component="img"
-              src={image.url}
-              alt={image.name}
+              src={image.url || '/images/placeholder.png'}
+              alt={image.name || 'Unknown'}
               sx={{
                 width: 40,
                 height: 40,
                 objectFit: 'cover',
+                imageOrientation: 'from-image', // Respect EXIF orientation
+                transform: 'none', // Let CSS handle rotation
                 borderRadius: 1,
                 cursor: 'pointer',
               }}
@@ -534,7 +1425,7 @@ const Gallery: React.FC = () => {
               sx={{ cursor: 'pointer', color: 'primary.main' }}
               onClick={() => handleImageClick(image)}
             >
-              {params.value}
+              {params.value || 'Unknown'}
             </Typography>
           </Box>
         );
@@ -547,25 +1438,109 @@ const Gallery: React.FC = () => {
       minWidth: 250,
     },
     {
-      field: 'created',
-      headerName: 'Image Created',
+      field: 'modified',
+      headerName: 'Date Modified',
       flex: 1,
       minWidth: 180,
+      valueGetter: (value: any, row: GalleryImage) => {
+        if (!row) return null;
+        // Return the actual date string, not empty string
+        const dateStr = row.modified || row.created;
+        return dateStr || null;
+      },
       valueFormatter: (value: any, row?: any) => {
-        // Handle both direct value and params object
+        // Handle both direct value and params object from valueGetter
         const dateValue = value?.value !== undefined ? value.value : value;
-        if (!dateValue) return 'Unknown';
+        const imageRow = row?.row || row;
+        
+        // Check if there's a metadata error
+        if (imageRow && imageRow.metadataError) {
+          return 'Error';
+        }
+        
+        if (!dateValue || dateValue === '') {
+          return 'Unknown';
+        }
         try {
           const date = new Date(dateValue);
           if (isNaN(date.getTime())) {
-            console.warn('Invalid date value:', dateValue, 'for row:', row);
-            return 'Invalid Date';
+            return 'Unknown';
           }
           return date.toLocaleString();
         } catch (e) {
-          console.warn('Date parsing error:', e, 'value:', dateValue);
-          return 'Invalid Date';
+          return 'Unknown';
+        }
+      },
+      renderCell: (params: GridRenderCellParams) => {
+        if (!params || !params.row) {
+          return <Typography variant="body2">Unknown</Typography>;
+        }
+        const image = params.row as GalleryImage;
+        if (!image) {
+          return <Typography variant="body2">Unknown</Typography>;
+        }
+        
+        // Show error with tooltip if metadata error exists
+        if (image.metadataError) {
+          return (
+            <Tooltip title={image.metadataError} arrow>
+              <Typography variant="body2" color="error">Error</Typography>
+            </Tooltip>
+          );
+        }
+        
+        const dateStr = image.modified || image.created;
+        if (!dateStr) {
+          return <Typography variant="body2">Unknown</Typography>;
+        }
+        
+        try {
+          const date = new Date(dateStr);
+          if (isNaN(date.getTime())) {
+            return <Typography variant="body2">Unknown</Typography>;
+          }
+          return <Typography variant="body2">{date.toLocaleString()}</Typography>;
+        } catch (e) {
+          return <Typography variant="body2">Unknown</Typography>;
+        }
+      },
+    },
+    {
+      field: 'isUsed',
+      headerName: 'Usage Status',
+      width: 130,
+      valueGetter: (value: any, row: GalleryImage) => {
+        if (!row || row.isUsed === undefined) return 'Unknown';
+        return row.isUsed ? 'Used' : 'Not Used';
+      },
+      renderCell: (params: GridRenderCellParams) => {
+        if (!params || !params.row) {
+          return <Chip label="Unknown" size="small" variant="outlined" />;
+        }
+        const image = params.row as GalleryImage;
+        if (!image) {
+          return <Chip label="Unknown" size="small" variant="outlined" />;
+        }
+        // Only show "Checking..." if explicitly undefined (check hasn't run yet)
+        // If false, it means the check completed and the image is not used
+        if (image.isUsed === undefined && checkingUsage) {
+          return <Chip label="Checking..." size="small" variant="outlined" />;
         }
+        // If undefined and not checking, treat as not checked yet
+        if (image.isUsed === undefined) {
+          return <Chip label="Not Checked" size="small" variant="outlined" color="default" />;
+        }
+        return (
+          <Chip
+            label={image.isUsed ? 'Used' : 'Not Used'}
+            size="small"
+            sx={{
+              backgroundColor: image.isUsed ? 'rgba(76, 175, 80, 0.2)' : 'rgba(244, 67, 54, 0.2)',
+              color: image.isUsed ? 'success.main' : 'error.main',
+              fontWeight: 600,
+            }}
+          />
+        );
       },
     },
     {
@@ -577,16 +1552,20 @@ const Gallery: React.FC = () => {
       field: 'size',
       headerName: 'File Size',
       width: 120,
+      valueGetter: (value: any, row: GalleryImage) => {
+        if (!row) return null;
+        // Return the actual size value, preserving 0 if it's actually 0
+        const size = row.size;
+        return size !== undefined && size !== null ? size : null;
+      },
       valueFormatter: (value: any, row?: any) => {
-        // Handle both direct value and params object
+        // Handle both direct value and params object from valueGetter
         const sizeValue = value?.value !== undefined ? value.value : value;
         if (sizeValue === undefined || sizeValue === null) {
-          console.warn('Size is undefined/null for row:', row);
           return 'Unknown';
         }
         const numValue = typeof sizeValue === 'number' ? sizeValue : Number(sizeValue);
-        if (isNaN(numValue)) {
-          console.warn('Size is NaN:', sizeValue, 'for row:', row);
+        if (isNaN(numValue) || numValue < 0) {
           return 'Unknown';
         }
         if (numValue === 0) return '0 KB';
@@ -604,26 +1583,53 @@ const Gallery: React.FC = () => {
       sortable: false,
       filterable: false,
       renderCell: (params: GridRenderCellParams) => {
+        if (!params || !params.row) {
+          return null;
+        }
         const image = params.row as GalleryImage;
+        if (!image) {
+          return null;
+        }
         return (
           <Stack direction="row" spacing={1}>
-            <Button
+            <IconButton
               size="small"
-              variant="contained"
-              color="primary"
-              onClick={() => handleOpenInNewWindow(image)}
+              onClick={() => handleImageClick(image)}
+              title="View"
             >
-              View
-            </Button>
-            <Button
+              <IconPhoto size={18} />
+            </IconButton>
+            <IconButton
+              size="small"
+              onClick={() => {
+                setItemToMove(image);
+                setNewName(image.name);
+                setRenameDialogOpen(true);
+              }}
+              title="Rename"
+            >
+              <IconEdit size={18} />
+            </IconButton>
+            <IconButton
+              size="small"
+              onClick={() => {
+                setItemToMove(image);
+                setTargetDir(selectedDirectory);
+                setMoveDialogOpen(true);
+              }}
+              title="Move"
+            >
+              <IconArrowsExchange size={18} />
+            </IconButton>
+            <IconButton
               size="small"
-              variant="contained"
               color="error"
               onClick={() => handleDeleteImage(image)}
               disabled={deleting}
+              title="Delete"
             >
-              Delete
-            </Button>
+              <IconTrash size={18} />
+            </IconButton>
           </Stack>
         );
       },
@@ -642,32 +1648,71 @@ const Gallery: React.FC = () => {
 
     setDeleting(true);
     try {
-      const selectedImages = images.filter(img => selectedRows.includes(img.id || img.path));
-      const deletePromises = selectedImages.map(image =>
-        fetch('/api/gallery/delete', {
-          method: 'POST',
-          headers: {
-            'Content-Type': 'application/json',
-          },
-          credentials: 'include',
-          body: JSON.stringify({ path: image.path })
-        })
-      );
+      // Use activeImages to ensure we're deleting from the filtered view
+      const selectedImages = activeImages.filter(img => selectedRows.includes(img.id || img.path));
+      const deletePromises = selectedImages.map(async (image) => {
+        const relativePath = image.path.replace(/^\/images\//, '');
+        console.log('🗑️ [Frontend] Attempting to delete:', {
+          name: image.name,
+          originalPath: image.path,
+          relativePath: relativePath,
+          selectedDirectory: selectedDirectory
+        });
+        try {
+          const response = await fetch('/api/gallery/file', {
+            method: 'DELETE',
+            headers: {
+              'Content-Type': 'application/json',
+            },
+            credentials: 'include',
+            body: JSON.stringify({ path: relativePath })
+          });
+
+          if (!response.ok) {
+            let errorMessage = 'Unknown error';
+            try {
+              const errorData = await response.json();
+              errorMessage = errorData.error || errorData.message || `HTTP ${response.status}`;
+            } catch (e) {
+              errorMessage = `HTTP ${response.status}: ${response.statusText}`;
+            }
+            return { success: false, image: image.name, error: errorMessage, path: relativePath };
+          }
+
+          return { success: true, image: image.name, path: relativePath };
+        } catch (error: any) {
+          console.error(`Error deleting ${image.name}:`, error);
+          return { 
+            success: false, 
+            image: image.name, 
+            error: error.message || 'Network error',
+            path: relativePath 
+          };
+        }
+      });
 
-      const results = await Promise.allSettled(deletePromises);
-      const successful = results.filter(r => r.status === 'fulfilled' && (r.value as Response).ok).length;
-      const failed = results.length - successful;
+      const results = await Promise.all(deletePromises);
+      const successful = results.filter(r => r.success);
+      const failed = results.filter(r => !r.success);
 
-      if (successful > 0) {
+      if (successful.length > 0) {
         await loadImages();
+        await loadDirectoryTree();
         setSelectedRows([]);
-        alert(`Successfully deleted ${successful} image(s)`);
+        
+        if (failed.length > 0) {
+          const failedDetails = failed.map(f => `  - ${f.image}: ${f.error}`).join('\n');
+          alert(`Successfully deleted ${successful.length} image(s).\n\nFailed to delete ${failed.length} image(s):\n${failedDetails}`);
+        } else {
+          alert(`Successfully deleted ${successful.length} image(s)`);
+        }
       } else {
-        alert(`Failed to delete ${failed} image(s)`);
+        const failedDetails = failed.map(f => `  - ${f.image}: ${f.error}`).join('\n');
+        alert(`Failed to delete ${failed.length} image(s):\n\n${failedDetails}`);
       }
-    } catch (error) {
+    } catch (error: any) {
       console.error('Error deleting images:', error);
-      alert('An error occurred while deleting images');
+      alert(`An error occurred while deleting images: ${error.message || 'Unknown error'}`);
     } finally {
       setDeleting(false);
     }
@@ -706,14 +1751,243 @@ const Gallery: React.FC = () => {
     document.body.removeChild(link);
   };
 
+  const handleExportUsedImages = async () => {
+    try {
+      // Show loading state
+      setExportingUsedImages(true);
+      setUploadError(null);
+      
+      // Fetch used images list from API with pagination support
+      // Backend will handle batching internally, but we'll fetch all pages if needed
+      let allUsedImages: any[] = [];
+      let offset = 0;
+      const limit = 200; // Backend batch size
+      let hasMore = true;
+      let totalImages = 0;
+      let checkedImages = 0;
+      let limited = false;
+      
+      while (hasMore) {
+        const response = await fetch(`/api/gallery/used-images?format=json&offset=${offset}&limit=${limit}`, {
+          credentials: 'include',
+        });
+
+        if (!response.ok) {
+          const errorText = await response.text();
+          let errorMessage = 'Failed to export used images list';
+          try {
+            const data = JSON.parse(errorText);
+            errorMessage = data.error || data.message || errorMessage;
+          } catch (e) {
+            errorMessage = errorText || errorMessage;
+          }
+          alert(`Error: ${errorMessage}`);
+          setUploadError(errorMessage);
+          setExportingUsedImages(false);
+          return;
+        }
+        
+        const data = await response.json();
+        
+        if (data.success && data.used) {
+          allUsedImages = [...allUsedImages, ...data.used];
+          totalImages = data.total_images || totalImages;
+          checkedImages = data.checked_images || checkedImages;
+          limited = data.limited || limited;
+          
+          // Check if there are more pages
+          if (data.used.length < limit || offset + limit >= checkedImages) {
+            hasMore = false;
+          } else {
+            offset += limit;
+          }
+        } else {
+          hasMore = false;
+        }
+      }
+      
+      // Generate text output
+      let output = `# Images Actively Used in Production\n\n`;
+      output += `Generated: ${new Date().toISOString()}\n`;
+      output += `Total Images: ${totalImages}\n`;
+      output += `Checked Images: ${checkedImages}\n`;
+      output += `Used Images: ${allUsedImages.length}\n`;
+      if (limited) {
+        output += `Note: Scanning was limited due to performance constraints\n`;
+      }
+      output += `\n## Used Images (${allUsedImages.length})\n\n`;
+      
+      allUsedImages.forEach((img, index) => {
+        output += `${index + 1}. **${img.name}**\n`;
+        output += `   - Path: ${img.path}\n`;
+        output += `   - Size: ${img.size ? (img.size / 1024).toFixed(2) + ' KB' : 'Unknown'}\n`;
+        output += `   - Type: ${img.type ? img.type.toUpperCase() : 'Unknown'}\n`;
+        output += `   - Modified: ${img.modified ? new Date(img.modified).toLocaleString() : 'Unknown'}\n`;
+        if (img.referencedIn && img.referencedIn.length > 0) {
+          output += `   - Referenced in:\n`;
+          img.referencedIn.forEach((ref: string) => {
+            output += `     - ${ref}\n`;
+          });
+        }
+        output += `\n`;
+      });
+      
+      // Create and download file
+      const blob = new Blob([output], { type: 'text/plain;charset=utf-8;' });
+      const link = document.createElement('a');
+      const url = URL.createObjectURL(blob);
+      link.setAttribute('href', url);
+      link.setAttribute('download', `used-images-${new Date().toISOString().split('T')[0]}.txt`);
+      link.style.visibility = 'hidden';
+      document.body.appendChild(link);
+      link.click();
+      document.body.removeChild(link);
+      URL.revokeObjectURL(url);
+      
+      // Show success message with counts
+      alert(`Used images list exported successfully!\nTotal: ${totalImages}, Checked: ${checkedImages}, Used: ${allUsedImages.length}${limited ? ' (limited)' : ''}`);
+    } catch (error: any) {
+      console.error('Error exporting used images:', error);
+      const errorMsg = error.message || 'Failed to export used images list. Please check the browser console for details.';
+      alert(`Error: ${errorMsg}`);
+      setUploadError(errorMsg);
+    } finally {
+      setExportingUsedImages(false);
+    }
+  };
+
   // No longer needed - MUI DataGrid uses React components for cell rendering
 
-  // Get first 8 images for thumbnail display
-  const thumbnailImages = images.slice(0, 8);
+  // Get first 8 images for thumbnail display - use activeImages to respect filters
+  const thumbnailImages = activeImages.slice(0, 8);
+
+  // Default directories that should stand out visually
+  const DEFAULT_DIRECTORIES = ['logos', 'backgrounds', 'icons', 'ui', 'records', 'misc'];
+
+  // Check if directory is a default directory
+  const isDefaultDirectory = (dirName: string): boolean => {
+    return DEFAULT_DIRECTORIES.includes(dirName.toLowerCase());
+  };
+
+  // Clean up empty directories
+  const cleanupEmptyDirectories = async () => {
+    try {
+      const response = await fetch('/api/gallery/cleanup-empty-dirs', {
+        method: 'POST',
+        headers: { 'Content-Type': 'application/json' },
+        credentials: 'include',
+      });
+
+      if (response.ok) {
+        const data = await response.json();
+        if (data.deleted && data.deleted.length > 0) {
+          console.log(`🗑️ Cleaned up ${data.deleted.length} empty directory(ies):`, data.deleted);
+          // Reload directory tree after cleanup
+          await loadDirectoryTree();
+        }
+      }
+    } catch (error) {
+      console.error('Error cleaning up empty directories:', error);
+      // Don't show error to user - this is a background operation
+    }
+  };
+
+  // Render directory tree recursively
+  const renderDirectoryTree = (dirs: any[], level: number = 0) => {
+    return dirs.map((dir) => {
+      const isDefault = isDefaultDirectory(dir.name);
+      return (
+        <Box key={dir.path} sx={{ pl: level * 2 }}>
+          <Button
+            fullWidth
+            startIcon={<IconFolder size={16} />}
+            onClick={() => setSelectedDirectory(dir.path)}
+            sx={{
+              justifyContent: 'flex-start',
+              textTransform: 'none',
+              color: selectedDirectory === dir.path ? '#C8A24B' : (isDefault ? '#1976d2' : 'inherit'),
+              fontWeight: selectedDirectory === dir.path ? 'bold' : (isDefault ? '600' : 'normal'),
+              backgroundColor: isDefault && selectedDirectory !== dir.path ? 'rgba(25, 118, 210, 0.08)' : 'transparent',
+              border: isDefault ? '1px solid rgba(25, 118, 210, 0.2)' : 'none',
+              borderRadius: isDefault ? 1 : 0,
+              mb: isDefault ? 0.5 : 0,
+              '&:hover': {
+                backgroundColor: isDefault ? 'rgba(25, 118, 210, 0.12)' : 'rgba(0, 0, 0, 0.04)',
+              },
+            }}
+          >
+            {dir.name} ({dir.childrenCount})
+            {isDefault && (
+              <Chip 
+                label="Default" 
+                size="small" 
+                sx={{ 
+                  ml: 1, 
+                  height: 18, 
+                  fontSize: '0.65rem',
+                  backgroundColor: 'rgba(25, 118, 210, 0.1)',
+                  color: '#1976d2',
+                }} 
+              />
+            )}
+          </Button>
+        </Box>
+      );
+    });
+  };
 
   return (
-    <Box sx={{ minHeight: '100vh', display: 'flex', flexDirection: 'column' }}>
-      <GalleryContainer maxWidth="lg">
+    <Box sx={{ minHeight: '100vh', display: 'flex', flexDirection: 'row' }}>
+      {/* Directory Tree Sidebar */}
+      <Box sx={{ width: 250, minWidth: 250, p: 2, borderRight: 1, borderColor: 'divider', bgcolor: 'background.paper' }}>
+        <Typography variant="h6" sx={{ mb: 2 }}>Directories</Typography>
+        <Button
+          fullWidth
+          startIcon={<IconFolder size={16} />}
+          onClick={() => setSelectedDirectory('')}
+          sx={{
+            justifyContent: 'flex-start',
+            textTransform: 'none',
+            mb: 1,
+            color: selectedDirectory === '' ? '#C8A24B' : 'inherit',
+            fontWeight: selectedDirectory === '' ? 'bold' : 'normal',
+          }}
+        >
+          Root (All Images)
+        </Button>
+        {loadingTree ? (
+          <OMLoading size="sm" label="Loading directories" />
+        ) : directoryTree.directories && directoryTree.directories.length > 0 ? (
+          renderDirectoryTree(directoryTree.directories)
+        ) : (
+          <Typography variant="body2" color="text.secondary" sx={{ mt: 1, p: 1 }}>
+            No directories found. Images may still be available in root.
+          </Typography>
+        )}
+        <Button
+          fullWidth
+          startIcon={<IconFolderPlus size={16} />}
+          onClick={() => {
+            const dirName = prompt('Enter directory name:');
+            if (dirName) handleCreateDirectory(dirName);
+          }}
+          sx={{ mt: 2, justifyContent: 'flex-start', textTransform: 'none' }}
+        >
+          New Folder
+        </Button>
+        <Button
+          fullWidth
+          startIcon={<IconSparkles size={16} />}
+          onClick={handleGetSuggestions}
+          sx={{ mt: 1, justifyContent: 'flex-start', textTransform: 'none' }}
+        >
+          Catalog Suggestions
+        </Button>
+      </Box>
+
+      {/* Main Content */}
+      <Box sx={{ flex: 1, display: 'flex', flexDirection: 'column' }}>
+      <GalleryContainer maxWidth="xl">
         <Box sx={{ textAlign: 'center', mb: 4 }}>
           <Typography
             variant="h2"
@@ -726,11 +2000,30 @@ const Gallery: React.FC = () => {
           >
             Image Gallery
           </Typography>
-          <Typography variant="body1" color="text.secondary" sx={{ mb: 3 }}>
-            Browse through our collection of images
+          <Typography variant="body1" color="text.secondary" sx={{ mb: 2 }}>
+            Browse images from <code>/public/images/**</code> (all directories)
           </Typography>
+          {/* Usage Status Banner */}
+          {images.length > 0 && images.some(img => img.isUsed === undefined) && (
+            <Alert 
+              severity="info" 
+              sx={{ mb: 3 }}
+              action={
+                <Button
+                  color="inherit"
+                  size="small"
+                  onClick={() => checkImageUsage(images, true)}
+                  disabled={checkingUsage}
+                >
+                  {checkingUsage ? 'Checking...' : 'Check Now'}
+                </Button>
+              }
+            >
+              Usage status not computed. Click &quot;Refresh Usage&quot; or &quot;Check Now&quot; to determine which images are used in the codebase.
+            </Alert>
+          )}
           {/* Carousel Navigation Arrows */}
-          {images.length > 0 && (
+          {activeImages.length > 0 && (
             <Box sx={{ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 4, mb: 3 }}>
               <CarouselButton
                 onClick={handlePrevious}
@@ -750,46 +2043,64 @@ const Gallery: React.FC = () => {
             </Box>
           )}
 
-          <Button
-            variant="contained"
-            startIcon={<IconUpload size={20} />}
-            onClick={handleOpenUploadDialog}
-            sx={{
-              backgroundColor: '#C8A24B',
-              color: '#1a1a1a',
-              '&:hover': {
-                backgroundColor: '#B8923A',
-              },
-            }}
-          >
-            Upload Image
-          </Button>
+          <Stack direction="row" spacing={2} justifyContent="center" alignItems="center" sx={{ mb: 3, flexWrap: 'wrap', gap: 2 }}>
+            <Button
+              variant="contained"
+              startIcon={<IconUpload size={20} />}
+              onClick={handleOpenUploadDialog}
+              sx={{
+                backgroundColor: '#C8A24B',
+                color: '#1a1a1a',
+                '&:hover': {
+                  backgroundColor: '#B8923A',
+                },
+              }}
+            >
+              Upload Images
+            </Button>
+
+            {/* Refresh Usage Check */}
+            {images.length > 0 && (
+              <Button
+                variant="outlined"
+                size="small"
+                onClick={() => checkImageUsage(images, true)}
+                disabled={checkingUsage}
+                title={checkingUsage ? 'Checking image usage in codebase...' : 'Check all images for usage in codebase'}
+              >
+                {checkingUsage ? 'Checking...' : 'Refresh Usage'}
+              </Button>
+            )}
+          </Stack>
         </Box>
 
         {/* Carousel - Current Image Display */}
-        {images.length > 0 && (
+        {activeImages.length > 0 && (
           <CarouselContainer>
             <CarouselButton
               onClick={handlePrevious}
               sx={{ left: { xs: -20, sm: -28 } }}
               aria-label="Previous image"
+              disabled={activeImages.length === 0}
             >
               <IconArrowLeft size={28} />
             </CarouselButton>
 
             <CarouselImageContainer
-              onClick={() => images[currentIndex] && handleOpenInNewWindow(images[currentIndex])}
+              onClick={() => activeImages[currentIndex] && handleOpenInNewWindow(activeImages[currentIndex])}
               sx={{ cursor: 'pointer' }}
+              isUsed={activeImages[currentIndex]?.isUsed === true}
             >
-              {images[currentIndex] && (
+              {activeImages[currentIndex] && (
                 <Box
                   component="img"
-                  src={images[currentIndex].url}
-                  alt={images[currentIndex].name}
+                  src={activeImages[currentIndex].url}
+                  alt={activeImages[currentIndex].name}
                   sx={{
                     maxWidth: '100%',
                     maxHeight: '100%',
                     objectFit: 'contain',
+                    imageOrientation: 'from-image', // Respect EXIF orientation
                   }}
                   onError={(e) => {
                     (e.target as HTMLImageElement).src = '/images/placeholder.png';
@@ -802,6 +2113,7 @@ const Gallery: React.FC = () => {
               onClick={handleNext}
               sx={{ right: { xs: -20, sm: -28 } }}
               aria-label="Next image"
+              disabled={activeImages.length === 0}
             >
               <IconArrowRight size={28} />
             </CarouselButton>
@@ -809,26 +2121,35 @@ const Gallery: React.FC = () => {
             {/* Image info below carousel */}
             <Box sx={{ textAlign: 'center', mt: 2 }}>
               <Typography variant="subtitle1" fontWeight={600}>
-                {images[currentIndex]?.name}
+                {activeImages[currentIndex]?.name}
               </Typography>
               <Typography variant="caption" color="text.secondary">
-                {currentIndex + 1} of {images.length}
+                {activeImages.length > 0 ? `${currentIndex + 1} of ${activeImages.length}` : '0 of 0'}
               </Typography>
             </Box>
           </CarouselContainer>
         )}
 
         {/* Thumbnail Grid - 2 rows of 4 images (8 total) */}
-        {images.length > 0 && (
+        {activeImages.length > 0 && (
           <Box>
             <Typography variant="h5" sx={{ mb: 2, fontWeight: 600 }}>
               Recent Images
             </Typography>
             <ThumbnailGrid>
-              {thumbnailImages.map((image, index) => (
+              {thumbnailImages.map((image, index) => {
+                // Find index in activeImages to sync with carousel
+                const activeIndex = activeImages.findIndex(img => img.id === image.id || img.path === image.path);
+                return (
                 <ImageCard
                   key={image.id || index}
-                  onClick={() => handleOpenInNewWindow(image)}
+                  onClick={() => {
+                    if (activeIndex >= 0) {
+                      setCurrentIndex(activeIndex);
+                    }
+                    handleOpenInNewWindow(image);
+                  }}
+                  isUsed={image.isUsed === true}
                 >
                   <ImageThumbnail
                     component="img"
@@ -839,7 +2160,8 @@ const Gallery: React.FC = () => {
                     }}
                   />
                 </ImageCard>
-              ))}
+                );
+              })}
             </ThumbnailGrid>
           </Box>
         )}
@@ -850,7 +2172,51 @@ const Gallery: React.FC = () => {
             <Typography variant="h5" sx={{ fontWeight: 600 }}>
               All Images
             </Typography>
-            <Stack direction="row" spacing={2}>
+            <Stack direction="row" spacing={2} alignItems="center">
+              {/* Sort Controls */}
+              {images.length > 0 && (
+                <>
+                  <FormControl size="small" sx={{ minWidth: 150 }}>
+                    <InputLabel>Sort By</InputLabel>
+                    <Select
+                      value={sortBy}
+                      label="Sort By"
+                      onChange={(e) => setSortBy(e.target.value as 'date' | 'name' | 'size' | 'type' | 'location')}
+                    >
+                      <MenuItem value="date">Date Modified</MenuItem>
+                      <MenuItem value="name">Name</MenuItem>
+                      <MenuItem value="size">Size</MenuItem>
+                      <MenuItem value="type">Type</MenuItem>
+                      <MenuItem value="location">Location</MenuItem>
+                    </Select>
+                  </FormControl>
+                  <IconButton
+                    onClick={() => setSortOrder(sortOrder === 'asc' ? 'desc' : 'asc')}
+                    title={`Sort ${sortOrder === 'asc' ? 'Descending' : 'Ascending'}`}
+                    size="small"
+                  >
+                    {sortOrder === 'asc' ? '↑' : '↓'}
+                  </IconButton>
+                </>
+              )}
+
+              {/* Usage Filter */}
+              {images.length > 0 && (
+                <FormControl size="small" sx={{ minWidth: 150 }}>
+                  <InputLabel>Usage Filter</InputLabel>
+                    <Select
+                      value={usageFilter}
+                      label="Usage Filter"
+                      onChange={(e) => setUsageFilter(e.target.value as 'all' | 'used' | 'unused' | 'not_checked')}
+                    >
+                    <MenuItem value="all">All Images</MenuItem>
+                    <MenuItem value="used">Used in Codebase</MenuItem>
+                    <MenuItem value="unused">Not Used</MenuItem>
+                    <MenuItem value="not_checked">Not Checked</MenuItem>
+                  </Select>
+                </FormControl>
+              )}
+
               <Button
                 variant="outlined"
                 color="error"
@@ -868,11 +2234,20 @@ const Gallery: React.FC = () => {
               >
                 Export CSV
               </Button>
+              <Button
+                variant="outlined"
+                color="success"
+                startIcon={<IconDownload size={18} />}
+                onClick={handleExportUsedImages}
+                disabled={exportingUsedImages}
+              >
+                {exportingUsedImages ? 'Generating...' : 'Export Used Images List'}
+              </Button>
             </Stack>
           </Box>
           <Paper sx={{ height: 600, width: '100%' }}>
             <DataGrid
-              rows={images}
+              rows={activeImages}
               columns={columns}
               getRowId={(row) => row.id || row.path}
               checkboxSelection
@@ -887,12 +2262,32 @@ const Gallery: React.FC = () => {
                   paginationModel: { pageSize: 20 },
                 },
               }}
+              getRowClassName={(params) => {
+                if (!params || !params.row) return '';
+                const image = params.row as GalleryImage;
+                if (!image) return '';
+                if (image.isUsed === true) {
+                  return 'gallery-row-used';
+                } else if (image.isUsed === false) {
+                  return 'gallery-row-unused';
+                }
+                return '';
+              }}
               sx={{
                 '& .MuiDataGrid-cell:focus': {
                   outline: 'none',
                 },
-                '& .MuiDataGrid-row:hover': {
-                  backgroundColor: 'action.hover',
+                '& .gallery-row-used': {
+                  background: 'linear-gradient(90deg, rgba(200, 230, 201, 0.3) 0%, rgba(200, 230, 201, 0.1) 100%)',
+                  '&:hover': {
+                    backgroundColor: 'rgba(200, 230, 201, 0.4) !important',
+                  },
+                },
+                '& .gallery-row-unused': {
+                  background: 'linear-gradient(90deg, rgba(255, 205, 210, 0.3) 0%, rgba(255, 205, 210, 0.1) 100%)',
+                  '&:hover': {
+                    backgroundColor: 'rgba(255, 205, 210, 0.4) !important',
+                  },
                 },
               }}
             />
@@ -918,6 +2313,239 @@ const Gallery: React.FC = () => {
           </Box>
         )}
       </GalleryContainer>
+      </Box>
+
+      {/* Move Dialog */}
+      <Dialog open={moveDialogOpen} onClose={() => setMoveDialogOpen(false)}>
+        <DialogTitle>Move Image</DialogTitle>
+        <DialogContent>
+          <TextField
+            fullWidth
+            label="Target Directory"
+            value={targetDir}
+            onChange={(e) => setTargetDir(e.target.value)}
+            sx={{ mt: 1 }}
+          />
+        </DialogContent>
+        <DialogActions>
+          <Button onClick={() => setMoveDialogOpen(false)}>Cancel</Button>
+          <Button onClick={() => itemToMove && handleMoveImage(itemToMove, targetDir)}>Move</Button>
+        </DialogActions>
+      </Dialog>
+
+      {/* Rename Dialog */}
+      <Dialog open={renameDialogOpen} onClose={() => setRenameDialogOpen(false)}>
+        <DialogTitle>Rename Image</DialogTitle>
+        <DialogContent>
+          <TextField
+            fullWidth
+            label="New Name"
+            value={newName}
+            onChange={(e) => setNewName(e.target.value)}
+            sx={{ mt: 1 }}
+          />
+        </DialogContent>
+        <DialogActions>
+          <Button onClick={() => setRenameDialogOpen(false)}>Cancel</Button>
+          <Button onClick={() => itemToMove && handleRenameImage(itemToMove, newName)}>Rename</Button>
+        </DialogActions>
+      </Dialog>
+
+      {/* Catalog Suggestions Dialog */}
+      <Dialog open={suggestionsDialogOpen} onClose={() => setSuggestionsDialogOpen(false)} maxWidth="md" fullWidth>
+        <DialogTitle>
+          <Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
+            <Typography variant="h6">Catalog Suggestions</Typography>
+            {Object.keys(suggestionStatuses).length > 0 && (
+              <Box sx={{ display: 'flex', gap: 1, alignItems: 'center' }}>
+                <Typography variant="caption" color="text.secondary">
+                  Total: {suggestions.length} | 
+                  Valid: {Object.values(suggestionStatuses).filter(s => s.status === 'valid').length} | 
+                  Invalid: {Object.values(suggestionStatuses).filter(s => s.status === 'invalid').length}
+                </Typography>
+              </Box>
+            )}
+          </Box>
+        </DialogTitle>
+        <DialogContent>
+          <Box sx={{ mb: 2, display: 'flex', gap: 1, flexWrap: 'wrap', alignItems: 'center' }}>
+            <Button
+              variant="outlined"
+              onClick={handleDryRun}
+              disabled={suggestions.length === 0 || validating || applying}
+              startIcon={validating ? <LinearProgress sx={{ width: 16, height: 16 }} /> : null}
+            >
+              {validating ? 'Validating...' : 'Dry Run'}
+            </Button>
+            <Button
+              variant="contained"
+              onClick={handleApplyAll}
+              disabled={suggestions.length === 0 || applying || validating}
+              startIcon={applying ? <LinearProgress sx={{ width: 16, height: 16 }} /> : null}
+            >
+              {applying ? 'Applying...' : 'Apply All'}
+            </Button>
+            <FormControlLabel
+              control={
+                <Checkbox
+                  checked={showFullSummary}
+                  onChange={(e) => setShowFullSummary(e.target.checked)}
+                />
+              }
+              label="Show full summary"
+            />
+          </Box>
+
+          <Box sx={{ maxHeight: 400, overflow: 'auto' }}>
+            {suggestions.map((suggestion, idx) => {
+              const status = suggestionStatuses[idx];
+              const isPending = !status || status.status === 'pending';
+              const isValid = status?.status === 'valid';
+              const isInvalid = status?.status === 'invalid';
+              const isApplied = status?.status === 'applied';
+              const isFailed = status?.status === 'failed';
+
+              return (
+                <Box 
+                  key={idx} 
+                  sx={{ 
+                    mb: 2, 
+                    p: 2, 
+                    border: 1, 
+                    borderColor: isInvalid || isFailed ? 'error.main' : isValid || isApplied ? 'success.main' : 'divider',
+                    borderRadius: 1,
+                    backgroundColor: isValid || isApplied ? 'action.selected' : 'transparent'
+                  }}
+                >
+                  <Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', mb: 1 }}>
+                    <Box sx={{ flex: 1 }}>
+                      <Typography variant="subtitle2">{suggestion.path}</Typography>
+                      <Typography variant="body2" color="text.secondary">
+                        Suggested: {suggestion.suggestedDir}/{suggestion.suggestedName}
+                      </Typography>
+                      <Typography variant="caption" color="text.secondary">
+                        Confidence: {(suggestion.confidence * 100).toFixed(0)}%
+                      </Typography>
+                    </Box>
+                    <Box sx={{ ml: 2 }}>
+                      {getStatusChip(idx)}
+                    </Box>
+                  </Box>
+                  
+                  {status && status.message && (
+                    <Alert 
+                      severity={isInvalid || isFailed ? 'error' : isValid || isApplied ? 'success' : 'info'}
+                      sx={{ mt: 1, mb: 1 }}
+                    >
+                      {status.message}
+                    </Alert>
+                  )}
+
+                  <Box sx={{ display: 'flex', gap: 1, mt: 1 }}>
+                    <Button
+                      size="small"
+                      variant="outlined"
+                      onClick={() => handleApplySingle(idx)}
+                      disabled={isApplied || applying || validating}
+                    >
+                      {isApplied ? 'Applied' : 'Apply'}
+                    </Button>
+                  </Box>
+                </Box>
+              );
+            })}
+          </Box>
+
+          {/* Summary Panel */}
+          {Object.keys(suggestionStatuses).length > 0 && (
+            <>
+              <Divider sx={{ my: 2 }} />
+              <Box>
+                <Box 
+                  sx={{ 
+                    display: 'flex', 
+                    justifyContent: 'space-between', 
+                    alignItems: 'center',
+                    cursor: 'pointer',
+                    p: 1,
+                    '&:hover': { backgroundColor: 'action.hover' }
+                  }}
+                  onClick={() => setSummaryExpanded(!summaryExpanded)}
+                >
+                  <Typography variant="subtitle2">
+                    Summary: Succeeded: {Object.values(suggestionStatuses).filter(s => s.status === 'applied' || s.status === 'valid').length}, 
+                    Failed: {Object.values(suggestionStatuses).filter(s => s.status === 'failed' || s.status === 'invalid').length}
+                  </Typography>
+                  <IconButton size="small">
+                    {summaryExpanded ? <IconChevronUp size={20} /> : <IconChevronDown size={20} />}
+                  </IconButton>
+                </Box>
+                
+                <Collapse in={summaryExpanded}>
+                  <Box sx={{ mt: 1, maxHeight: 300, overflow: 'auto' }}>
+                    {(showFullSummary 
+                      ? Object.entries(suggestionStatuses)
+                      : Object.entries(suggestionStatuses).filter(([_, s]) => s.status === 'invalid' || s.status === 'failed')
+                    ).map(([idxStr, status]) => {
+                      const idx = parseInt(idxStr);
+                      const suggestion = suggestions[idx];
+                      if (!suggestion) return null;
+
+                      return (
+                        <Box 
+                          key={idx}
+                          sx={{ 
+                            p: 1, 
+                            mb: 1, 
+                            border: 1, 
+                            borderColor: 'divider', 
+                            borderRadius: 1,
+                            backgroundColor: status.status === 'invalid' || status.status === 'failed' 
+                              ? 'error.light' 
+                              : 'success.light'
+                          }}
+                        >
+                          <Typography variant="body2" fontWeight="bold">
+                            {suggestion.path} → {suggestion.suggestedDir}/{suggestion.suggestedName}
+                          </Typography>
+                          <Typography variant="caption" color="text.secondary">
+                            Status: {status.status} | Code: {status.code}
+                          </Typography>
+                          {status.message && (
+                            <Typography variant="caption" display="block" sx={{ mt: 0.5 }}>
+                              {status.message}
+                            </Typography>
+                          )}
+                        </Box>
+                      );
+                    })}
+                  </Box>
+                  
+                  <Box sx={{ mt: 1, display: 'flex', justifyContent: 'flex-end' }}>
+                    <Button
+                      size="small"
+                      startIcon={<IconCopy size={16} />}
+                      onClick={handleCopySummary}
+                    >
+                      Copy Summary
+                    </Button>
+                  </Box>
+                </Collapse>
+              </Box>
+            </>
+          )}
+        </DialogContent>
+        <DialogActions>
+          <Button onClick={() => {
+            setSuggestionsDialogOpen(false);
+            setSuggestionStatuses({});
+            setShowFullSummary(false);
+            setSummaryExpanded(false);
+          }}>
+            Close
+          </Button>
+        </DialogActions>
+      </Dialog>
 
       {/* Image Detail Dialog */}
       <Dialog
@@ -1049,6 +2677,7 @@ const Gallery: React.FC = () => {
               onChange={handleFileSelect}
               style={{ display: 'none' }}
               id="image-upload-input"
+              multiple
             />
             <label htmlFor="image-upload-input">
               <Button
@@ -1058,30 +2687,91 @@ const Gallery: React.FC = () => {
                 startIcon={<IconUpload size={20} />}
                 sx={{ mb: 2 }}
               >
-                {selectedFile ? selectedFile.name : 'Select Image'}
+                {selectedFiles.length > 0 ? `Select More Images (${selectedFiles.length} selected)` : 'Select Images'}
               </Button>
             </label>
 
             <Typography variant="caption" color="text.secondary" display="block" sx={{ mb: 2, textAlign: 'center' }}>
-              Allowed formats: .jpg, .jpeg, .tiff, .gif, .png
+              Allowed formats: .jpg, .jpeg, .tiff, .gif, .png (You can select multiple files)
             </Typography>
 
-            {selectedFile && (
-              <Box sx={{ mb: 2, p: 2, bgcolor: 'background.default', borderRadius: 1 }}>
-                <Typography variant="body2" color="text.secondary" sx={{ mb: 1 }}>
-                  <strong>File:</strong> {selectedFile.name}
-                </Typography>
-                <Typography variant="body2" color="text.secondary">
-                  <strong>Size:</strong> {(selectedFile.size / 1024 / 1024).toFixed(2)} MB
+            {selectedFiles.length > 0 && (
+              <Box sx={{ mb: 2 }}>
+                <Typography variant="subtitle2" sx={{ mb: 1, fontWeight: 600 }}>
+                  Selected Images ({selectedFiles.length}):
                 </Typography>
+                <Stack spacing={1}>
+                  {selectedFiles.map((file, index) => {
+                    const fileKey = `${file.name}-${file.size}`;
+                    const status = uploadStatus[fileKey];
+                    return (
+                      <Box
+                        key={index}
+                        sx={{
+                          p: 2,
+                          bgcolor: 'background.default',
+                          borderRadius: 1,
+                          border: '1px solid',
+                          borderColor: status?.status === 'error' ? 'error.main' : status?.status === 'success' ? 'success.main' : 'divider',
+                        }}
+                      >
+                        <Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', mb: 1 }}>
+                          <Box sx={{ flex: 1 }}>
+                            <Typography variant="body2" sx={{ fontWeight: 600, mb: 0.5 }}>
+                              {file.name}
+                            </Typography>
+                            <Typography variant="caption" color="text.secondary" display="block">
+                              Size: {(file.size / 1024 / 1024).toFixed(2)} MB
+                            </Typography>
+                            {status && (
+                              <>
+                                {status.status === 'uploading' && (
+                                  <Box sx={{ mt: 1 }}>
+                                    <LinearProgress variant="determinate" value={status.progress} size="small" />
+                                    <Typography variant="caption" color="text.secondary">
+                                      Uploading... {Math.round(status.progress)}%
+                                    </Typography>
+                                  </Box>
+                                )}
+                                {status.status === 'success' && (
+                                  <Typography variant="caption" color="success.main" sx={{ mt: 0.5, display: 'block' }}>
+                                    ✓ Uploaded successfully
+                                  </Typography>
+                                )}
+                                {status.status === 'error' && (
+                                  <Typography variant="caption" color="error.main" sx={{ mt: 0.5, display: 'block' }}>
+                                    ✗ {status.error || 'Upload failed'}
+                                  </Typography>
+                                )}
+                              </>
+                            )}
+                          </Box>
+                          {!uploading && (
+                            <IconButton
+                              size="small"
+                              onClick={() => handleRemoveFile(index)}
+                              sx={{ ml: 1 }}
+                              color="error"
+                            >
+                              <IconX size={18} />
+                            </IconButton>
+                          )}
+                        </Box>
+                      </Box>
+                    );
+                  })}
+                </Stack>
               </Box>
             )}
 
-            {uploading && (
+            {uploading && selectedFiles.length > 0 && (
               <Box sx={{ mt: 2 }}>
+                <Typography variant="body2" color="text.secondary" sx={{ mb: 1 }}>
+                  Overall Progress: {Math.round(uploadProgress)}%
+                </Typography>
                 <LinearProgress variant="determinate" value={uploadProgress} />
-                <Typography variant="body2" color="text.secondary" sx={{ mt: 1 }}>
-                  Uploading... {Math.round(uploadProgress)}%
+                <Typography variant="caption" color="text.secondary" sx={{ mt: 1, display: 'block' }}>
+                  Uploading {selectedFiles.length} image{selectedFiles.length > 1 ? 's' : ''}...
                 </Typography>
               </Box>
             )}
@@ -1100,7 +2790,7 @@ const Gallery: React.FC = () => {
           <Button
             onClick={handleUpload}
             variant="contained"
-            disabled={!selectedFile || uploading}
+            disabled={selectedFiles.length === 0 || uploading}
             sx={{
               backgroundColor: '#C8A24B',
               color: '#1a1a1a',
@@ -1109,7 +2799,7 @@ const Gallery: React.FC = () => {
               },
             }}
           >
-            Upload
+            {uploading ? `Uploading... (${Math.round(uploadProgress)}%)` : `Upload ${selectedFiles.length} Image${selectedFiles.length !== 1 ? 's' : ''}`}
           </Button>
         </DialogActions>
       </Dialog>
diff --git a/front-end/src/features/devel-tools/om-ocr/EnhancedOCRUploader.tsx b/front-end/src/features/devel-tools/om-ocr/EnhancedOCRUploader.tsx
index acb3d8e3..a2a75e60 100755
--- a/front-end/src/features/devel-tools/om-ocr/EnhancedOCRUploader.tsx
+++ b/front-end/src/features/devel-tools/om-ocr/EnhancedOCRUploader.tsx
@@ -86,6 +86,7 @@ interface UploadFile {
   error?: string;
   thumbnail?: string;
   jobId?: string; // Backend job ID after upload
+  isSimulation?: boolean; // Flag for simulation mode results
 }
 
 interface Church {
@@ -246,8 +247,16 @@ const FileCard: React.FC<{
             {formatFileSize(file.size)}
           </Typography>
           
-          <Stack direction="row" spacing={1} sx={{ mt: 1 }} alignItems="center">
+          <Stack direction="row" spacing={1} sx={{ mt: 1 }} alignItems="center" flexWrap="wrap">
             <RecordTypeBadge type={file.recordType} />
+            {file.isSimulation && (
+              <Chip
+                label="SIMULATION"
+                size="small"
+                color="info"
+                sx={{ fontSize: '0.7rem', height: 20, fontWeight: 600 }}
+              />
+            )}
             <FormControl size="small" sx={{ minWidth: 100 }}>
               <Select
                 value={file.recordType}
@@ -382,7 +391,7 @@ const BatchProgress: React.FC<{
 const EnhancedOCRUploader: React.FC = () => {
   const theme = useTheme();
   const { user, isSuperAdmin } = useAuth();
-  const [searchParams] = useSearchParams();
+  const [searchParams, setSearchParams] = useSearchParams();
   const fileInputRef = useRef<HTMLInputElement>(null);
 
   // Helper to ensure church ID is valid
@@ -394,6 +403,8 @@ const EnhancedOCRUploader: React.FC = () => {
 
   // Get church_id from URL query params
   const urlChurchId = getValidChurchId(searchParams.get('church_id'));
+  // Get ocr_mode from URL query params
+  const urlOcrMode = searchParams.get('ocr_mode');
 
   // State
   const [files, setFiles] = useState<UploadFile[]>([]);
@@ -403,6 +414,9 @@ const EnhancedOCRUploader: React.FC = () => {
   const [isPaused, setIsPaused] = useState(false);
   const [showAdvanced, setShowAdvanced] = useState(false);
   const [dragActive, setDragActive] = useState(false);
+  // Simulation mode state
+  const [simulationMode, setSimulationMode] = useState<boolean>(urlOcrMode === 'simulate');
+  const isSimulationModeAvailable = selectedChurchId === 46;
   // @deprecated - Replaced by Workbench (but still used for backward compatibility)
   const [showInspectionPanel, setShowInspectionPanel] = useState(false);
   const [selectedFileId, setSelectedFileId] = useState<string | null>(null);
@@ -490,6 +504,19 @@ const EnhancedOCRUploader: React.FC = () => {
     return { baptism: false, marriage: false, funeral: false };
   });
 
+  // Update URL when simulation mode changes
+  useEffect(() => {
+    if (isSimulationModeAvailable) {
+      const newParams = new URLSearchParams(searchParams);
+      if (simulationMode) {
+        newParams.set('ocr_mode', 'simulate');
+      } else {
+        newParams.delete('ocr_mode');
+      }
+      setSearchParams(newParams, { replace: true });
+    }
+  }, [simulationMode, isSimulationModeAvailable, searchParams, setSearchParams]);
+
   // Persist sticky defaults to localStorage
   useEffect(() => {
     try {
@@ -720,6 +747,52 @@ const EnhancedOCRUploader: React.FC = () => {
     setFiles(prev => [...prev, ...newFiles]);
   }, [files.length]);
 
+  // Load demo images function (must be after handleFiles)
+  const handleLoadDemoImages = useCallback(async () => {
+    const demoFiles = [
+      'IMG_2025_03_05_10_44_50S.jpg',
+      'IMG_2024_10_25_12_32_04S.jpg',
+      'IMG_2025_03_05_11_04_55S.jpg',
+      'IMG_2024_10_22_11_27_57S.jpg',
+      'IMG_2024_10_22_11_29_28S.jpg',
+      'IMG_2024_10_25_12_28_25S.jpg',
+      'IMG_2024_10_22_11_39_09S.jpg',
+      'IMG_2025_03_12_12_48_44S.jpg',
+    ];
+
+    try {
+      const loadedFiles: File[] = [];
+      
+      for (const filename of demoFiles) {
+        try {
+          const response = await fetch(`/images/misc/demo/${filename}`);
+          if (!response.ok) {
+            console.warn(`Failed to load demo image: ${filename}`);
+            continue;
+          }
+          const blob = await response.blob();
+          const file = new File([blob], filename, { type: blob.type || 'image/jpeg' });
+          loadedFiles.push(file);
+        } catch (error) {
+          console.error(`Error loading demo image ${filename}:`, error);
+        }
+      }
+
+      if (loadedFiles.length > 0) {
+        // Create a FileList-like object
+        const dataTransfer = new DataTransfer();
+        loadedFiles.forEach(file => dataTransfer.items.add(file));
+        handleFiles(dataTransfer.files);
+        showToast(`Loaded ${loadedFiles.length} demo images`, 'success');
+      } else {
+        showToast('Failed to load demo images', 'error');
+      }
+    } catch (error) {
+      console.error('Error loading demo images:', error);
+      showToast('Error loading demo images', 'error');
+    }
+  }, [handleFiles, showToast]);
+
   // Drag and drop handlers
   const handleDrag = useCallback((e: React.DragEvent) => {
     e.preventDefault();
@@ -774,6 +847,11 @@ const EnhancedOCRUploader: React.FC = () => {
           dpi: settings.dpi
         }));
 
+        // Add simulation mode if enabled
+        if (simulationMode && isSimulationModeAvailable) {
+          formData.append('ocr_mode', 'simulate');
+        }
+
         // Simulate progress (real implementation would use XMLHttpRequest for progress)
         const progressInterval = setInterval(() => {
           setFiles(prev => prev.map(f => {
@@ -784,13 +862,26 @@ const EnhancedOCRUploader: React.FC = () => {
           }));
         }, 200);
 
-        const response: any = await apiClient.post('/api/ocr/jobs/upload', formData);
+        // Use different endpoint for simulation mode
+        const endpoint = (simulationMode && isSimulationModeAvailable)
+          ? `/api/church/${selectedChurchId}/ocr/enhanced/process?ocr_mode=simulate`
+          : '/api/ocr/jobs/upload';
+        
+        const response: any = await apiClient.post(endpoint, formData);
 
         clearInterval(progressInterval);
 
         // Extract jobId from response
-        const jobs = response.data?.jobs || [];
+        const jobs = response.data?.jobs || response.jobs || [];
         const jobId = jobs.length > 0 ? jobs[0].id : undefined;
+        
+        // Check if this was a simulation response
+        const isSimulation = response.data?.source === 'simulation' || jobs[0]?.source === 'simulation';
+        
+        // Show appropriate message for unmatched simulation files
+        if (simulationMode && isSimulationModeAvailable && !isSimulation && jobs.length === 0) {
+          showToast('No simulation data for this file', 'warning');
+        }
 
         // Update to processing then complete
         setFiles(prev => prev.map(f => 
@@ -800,14 +891,24 @@ const EnhancedOCRUploader: React.FC = () => {
         await new Promise(resolve => setTimeout(resolve, 500));
 
         setFiles(prev => prev.map(f => 
-          f.id === uploadFile.id ? { ...f, status: 'complete', progress: 100, jobId } : f
+          f.id === uploadFile.id ? { 
+            ...f, 
+            status: 'complete', 
+            progress: 100, 
+            jobId,
+            isSimulation: isSimulation || false
+          } : f
         ));
 
         // Refresh the processed images table to show new job
         refreshOcrJobs();
         
-        // Show success toast (matches spec: "OCR completed")
-        showToast('OCR completed', 'success');
+        // Show success toast with simulation indicator
+        if (simulationMode && isSimulationModeAvailable && response.data?.source === 'simulation') {
+          showToast('Loaded verified demo OCR results', 'success');
+        } else {
+          showToast('OCR completed', 'success');
+        }
 
       } catch (error: any) {
         setFiles(prev => prev.map(f => 
@@ -1213,6 +1314,42 @@ const EnhancedOCRUploader: React.FC = () => {
           <Typography variant="caption" color="text.secondary" sx={{ mt: 1, display: 'block' }}>
             Max 50 files per batch • 10MB per file
           </Typography>
+          
+          {/* Demo Images and Simulation Mode (only for church 46) */}
+          {isSimulationModeAvailable && (
+            <Box sx={{ mt: 2, display: 'flex', gap: 2, justifyContent: 'center', flexWrap: 'wrap' }}>
+              <Button
+                variant="outlined"
+                color="primary"
+                startIcon={<IconPhoto />}
+                onClick={handleLoadDemoImages}
+                disabled={isUploading}
+                sx={{ mt: 1 }}
+              >
+                Load Demo Images
+              </Button>
+              <FormControlLabel
+                control={
+                  <Switch
+                    checked={simulationMode}
+                    onChange={(e) => setSimulationMode(e.target.checked)}
+                    disabled={isUploading}
+                  />
+                }
+                label={
+                  <Box>
+                    <Typography variant="body2" fontWeight={500}>
+                      Simulation Mode
+                    </Typography>
+                    <Typography variant="caption" color="text.secondary">
+                      Use pre-validated demo OCR results
+                    </Typography>
+                  </Box>
+                }
+                sx={{ mt: 1 }}
+              />
+            </Box>
+          )}
         </Paper>
 
         {/* Advanced Options */}
@@ -1429,6 +1566,14 @@ const EnhancedOCRUploader: React.FC = () => {
               }}
             >
               <Stack direction="row" spacing={2} alignItems="center" flexWrap="wrap">
+                {simulationMode && isSimulationModeAvailable && (
+                  <Chip
+                    label="SIMULATION (Verified Demo)"
+                    color="info"
+                    size="small"
+                    sx={{ fontWeight: 600 }}
+                  />
+                )}
                 <Button
                   variant="contained"
                   startIcon={isUploading ? <IconPlayerPause /> : <IconUpload />}
@@ -1545,15 +1690,40 @@ const EnhancedOCRUploader: React.FC = () => {
       {/* Toast Notifications */}
       <Snackbar
         open={toast.open}
-        autoHideDuration={3500}
+        autoHideDuration={12000}
         onClose={() => setToast({ ...toast, open: false })}
-        anchorOrigin={{ vertical: 'bottom', horizontal: 'right' }}
+        anchorOrigin={{ vertical: 'center', horizontal: 'center' }}
+        sx={{
+          position: 'fixed',
+          top: '50% !important',
+          left: '50% !important',
+          transform: 'translate(-50%, -50%) !important',
+          zIndex: 10000,
+          '& .MuiSnackbar-root': {
+            position: 'fixed',
+            top: '50% !important',
+            left: '50% !important',
+            transform: 'translate(-50%, -50%) !important',
+          }
+        }}
       >
         <Alert
           onClose={() => setToast({ ...toast, open: false })}
           severity={toast.severity}
           variant="filled"
-          sx={{ width: '100%' }}
+          sx={{ 
+            minWidth: '400px',
+            maxWidth: '600px',
+            fontSize: '1.1rem',
+            padding: '16px 20px',
+            '& .MuiAlert-message': {
+              fontSize: '1.1rem',
+              fontWeight: 500,
+            },
+            '& .MuiAlert-icon': {
+              fontSize: '28px',
+            }
+          }}
         >
           {toast.message}
         </Alert>
diff --git a/front-end/src/features/devel-tools/system-documentation/om-spec/OMSpecDocumentation.tsx b/front-end/src/features/devel-tools/system-documentation/om-spec/OMSpecDocumentation.tsx
index 2bd572b5..772c1d78 100755
--- a/front-end/src/features/devel-tools/system-documentation/om-spec/OMSpecDocumentation.tsx
+++ b/front-end/src/features/devel-tools/system-documentation/om-spec/OMSpecDocumentation.tsx
@@ -59,6 +59,8 @@ import {
   IconEye,
   IconEdit,
   IconCheck,
+  IconLayoutGrid,
+  IconTable,
 } from '@tabler/icons-react';
 import { styled } from '@mui/material/styles';
 
@@ -166,11 +168,15 @@ const OMSpecDocumentation: React.FC = () => {
   const [activeTab, setActiveTab] = useState(0);
   const [files, setFiles] = useState<DocumentFile[]>([]);
   const [currentIndex, setCurrentIndex] = useState(0);
+  const [viewMode, setViewMode] = useState<'grid' | 'details'>('details');
+  const [sortBy, setSortBy] = useState<'date' | 'name' | 'size' | 'type'>('date');
+  const [sortOrder, setSortOrder] = useState<'asc' | 'desc'>('desc');
   const [uploadDialogOpen, setUploadDialogOpen] = useState(false);
   const [uploading, setUploading] = useState(false);
   const [uploadProgress, setUploadProgress] = useState(0);
   const [uploadError, setUploadError] = useState<string | null>(null);
-  const [selectedFile, setSelectedFile] = useState<File | null>(null);
+  const [selectedFiles, setSelectedFiles] = useState<File[]>([]);
+  const [uploadStatus, setUploadStatus] = useState<{ [key: string]: { progress: number; status: 'pending' | 'uploading' | 'success' | 'error'; error?: string } }>({});
   const fileInputRef = useRef<HTMLInputElement>(null);
   
   // Tasks state
@@ -230,6 +236,34 @@ const OMSpecDocumentation: React.FC = () => {
     }
   }, [activeTab, taskFilters]);
 
+  const sortFiles = (filesToSort: DocumentFile[]): DocumentFile[] => {
+    const sorted = [...filesToSort].sort((a, b) => {
+      let comparison = 0;
+      
+      switch (sortBy) {
+        case 'date':
+          // Use uploadedAt (date modified/created) for sorting
+          comparison = new Date(a.uploadedAt).getTime() - new Date(b.uploadedAt).getTime();
+          break;
+        case 'name':
+          comparison = a.name.localeCompare(b.name);
+          break;
+        case 'size':
+          comparison = a.size - b.size;
+          break;
+        case 'type':
+          comparison = a.type.localeCompare(b.type);
+          break;
+        default:
+          comparison = 0;
+      }
+      
+      return sortOrder === 'asc' ? comparison : -comparison;
+    });
+    
+    return sorted;
+  };
+
   const loadFiles = async () => {
     try {
       const response = await fetch('/api/docs/files', {
@@ -238,10 +272,8 @@ const OMSpecDocumentation: React.FC = () => {
       
       if (response.ok) {
         const data = await response.json();
-        // Sort by timestamp (newest first)
-        const sortedFiles = (data.files || []).sort((a: DocumentFile, b: DocumentFile) => {
-          return new Date(b.timestamp).getTime() - new Date(a.timestamp).getTime();
-        });
+        // Sort by date modified/created (newest first by default)
+        const sortedFiles = sortFiles(data.files || []);
         setFiles(sortedFiles);
       } else {
         console.warn('Could not load files from API. Backend endpoint /api/docs/files may not be implemented.');
@@ -258,6 +290,15 @@ const OMSpecDocumentation: React.FC = () => {
     }
   };
 
+  // Re-sort files when sort criteria changes
+  useEffect(() => {
+    if (files.length > 0) {
+      const sorted = sortFiles(files);
+      setFiles(sorted);
+    }
+    // eslint-disable-next-line react-hooks/exhaustive-deps
+  }, [sortBy, sortOrder]);
+
   const handlePrevious = () => {
     setCurrentIndex((prev) => (prev === 0 ? files.length - 1 : prev - 1));
   };
@@ -271,43 +312,57 @@ const OMSpecDocumentation: React.FC = () => {
   };
 
   const handleFileSelect = (event: React.ChangeEvent<HTMLInputElement>) => {
-    const file = event.target.files?.[0];
-    if (file) {
+    const files = Array.from(event.target.files || []);
+    if (files.length === 0) return;
+
+    const validFiles: File[] = [];
+    const errors: string[] = [];
+
+    files.forEach((file) => {
       // Validate file extension
       const fileExtension = '.' + file.name.split('.').pop()?.toLowerCase();
       if (!allowedTypes.includes(fileExtension)) {
-        setUploadError(`Please select a valid file type: ${allowedTypes.join(', ')}`);
+        errors.push(`${file.name}: Invalid file type. Allowed: ${allowedTypes.join(', ')}`);
         return;
       }
       // Validate file size (max 50MB)
       if (file.size > 50 * 1024 * 1024) {
-        setUploadError('File size must be less than 50MB');
+        errors.push(`${file.name}: File size must be less than 50MB`);
         return;
       }
-      setSelectedFile(file);
+      validFiles.push(file);
+    });
+
+    if (errors.length > 0) {
+      setUploadError(errors.join('\n'));
+    } else {
       setUploadError(null);
     }
+
+    if (validFiles.length > 0) {
+      setSelectedFiles(prev => [...prev, ...validFiles]);
+    }
   };
 
-  const getFileIcon = (type: string) => {
+  const getFileIcon = (type: string, size: number = 64) => {
     switch (type) {
       case 'docx':
-        return <IconFileText size={64} />;
+        return <IconFileText size={size} />;
       case 'xlsx':
-        return <IconFileSpreadsheet size={64} />;
+        return <IconFileSpreadsheet size={size} />;
       case 'md':
       case 'txt':
-        return <IconFileText size={64} />;
+        return <IconFileText size={size} />;
       case 'json':
       case 'tsx':
       case 'ts':
       case 'js':
       case 'html':
-        return <IconCode size={64} />;
+        return <IconCode size={size} />;
       case 'pdf':
-        return <IconFile size={64} />;
+        return <IconFile size={size} />;
       default:
-        return <IconFile size={64} />;
+        return <IconFile size={size} />;
     }
   };
 
@@ -356,30 +411,40 @@ const OMSpecDocumentation: React.FC = () => {
     });
   };
 
-  const handleUpload = async () => {
-    if (!selectedFile) {
-      setUploadError('Please select a file');
-      return;
-    }
-
-    setUploading(true);
-    setUploadError(null);
-    setUploadProgress(0);
-
-    try {
+  const uploadSingleFile = (file: File): Promise<void> => {
+    return new Promise((resolve, reject) => {
       const formData = new FormData();
-      formData.append('file', selectedFile);
+      formData.append('file', file);
       
       // Add timestamp for organization
       const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
       formData.append('timestamp', timestamp);
 
       const xhr = new XMLHttpRequest();
+      const fileKey = `${file.name}-${file.size}`;
+
+      // Initialize upload status
+      setUploadStatus(prev => ({
+        ...prev,
+        [fileKey]: { progress: 0, status: 'uploading' }
+      }));
 
       xhr.upload.addEventListener('progress', (e) => {
         if (e.lengthComputable) {
           const percentComplete = (e.loaded / e.total) * 100;
-          setUploadProgress(percentComplete);
+          setUploadStatus(prev => {
+            const updated = {
+              ...prev,
+              [fileKey]: { ...prev[fileKey], progress: percentComplete, status: 'uploading' as const }
+            };
+            // Calculate overall progress
+            const allStatuses = Object.values(updated);
+            const totalProgress = allStatuses.length > 0 
+              ? allStatuses.reduce((sum, s) => sum + s.progress, 0) / allStatuses.length 
+              : 0;
+            setUploadProgress(totalProgress);
+            return updated;
+          });
         }
       });
 
@@ -388,26 +453,26 @@ const OMSpecDocumentation: React.FC = () => {
           try {
             const response = JSON.parse(xhr.responseText);
             if (response.success) {
-              // Reload files after successful upload
-              loadFiles();
-              setUploadDialogOpen(false);
-              setSelectedFile(null);
-              setUploadProgress(0);
-              if (fileInputRef.current) {
-                fileInputRef.current.value = '';
-              }
+              setUploadStatus(prev => ({
+                ...prev,
+                [fileKey]: { progress: 100, status: 'success' }
+              }));
+              resolve();
             } else {
-              setUploadError(response.error || response.message || 'Upload failed');
+              const errorMsg = response.error || response.message || 'Upload failed';
+              setUploadStatus(prev => ({
+                ...prev,
+                [fileKey]: { progress: 0, status: 'error', error: errorMsg }
+              }));
+              reject(new Error(errorMsg));
             }
           } catch (e) {
             // If response is not JSON, assume success for 200 status
-            loadFiles();
-            setUploadDialogOpen(false);
-            setSelectedFile(null);
-            setUploadProgress(0);
-            if (fileInputRef.current) {
-              fileInputRef.current.value = '';
-            }
+            setUploadStatus(prev => ({
+              ...prev,
+              [fileKey]: { progress: 100, status: 'success' }
+            }));
+            resolve();
           }
         } else {
           // Try to parse JSON error response
@@ -425,21 +490,74 @@ const OMSpecDocumentation: React.FC = () => {
               errorMessage = `Upload failed with status ${xhr.status}`;
             }
           }
-          setUploadError(errorMessage);
+          setUploadStatus(prev => ({
+            ...prev,
+            [fileKey]: { progress: 0, status: 'error', error: errorMessage }
+          }));
+          reject(new Error(errorMessage));
         }
-        setUploading(false);
       });
 
       xhr.addEventListener('error', () => {
-        setUploadError('Upload failed. Backend endpoint /api/docs/upload may not be available. Files should upload to: front-end/public/docs');
-        setUploading(false);
+        const errorMsg = 'Upload failed. Backend endpoint /api/docs/upload may not be available. Files should upload to: front-end/public/docs';
+        setUploadStatus(prev => ({
+          ...prev,
+          [fileKey]: { progress: 0, status: 'error', error: errorMsg }
+        }));
+        reject(new Error(errorMsg));
       });
 
       xhr.open('POST', '/api/docs/upload');
       xhr.withCredentials = true; // Include cookies for authentication
       xhr.send(formData);
-    } catch (error) {
-      setUploadError('An error occurred during upload');
+    });
+  };
+
+  const handleUpload = async () => {
+    if (selectedFiles.length === 0) {
+      setUploadError('Please select at least one file');
+      return;
+    }
+
+    setUploading(true);
+    setUploadError(null);
+    setUploadProgress(0);
+    
+    // Initialize upload status for all files
+    const initialStatus: { [key: string]: { progress: number; status: 'pending' | 'uploading' | 'success' | 'error'; error?: string } } = {};
+    selectedFiles.forEach(file => {
+      const fileKey = `${file.name}-${file.size}`;
+      initialStatus[fileKey] = { progress: 0, status: 'pending' };
+    });
+    setUploadStatus(initialStatus);
+
+    try {
+      // Upload files sequentially to avoid overwhelming the server
+      const errors: string[] = [];
+      for (const file of selectedFiles) {
+        try {
+          await uploadSingleFile(file);
+        } catch (error: any) {
+          errors.push(`${file.name}: ${error.message || 'Upload failed'}`);
+        }
+      }
+
+      if (errors.length > 0) {
+        setUploadError(`Some files failed to upload:\n${errors.join('\n')}`);
+      } else {
+        // All files uploaded successfully
+        loadFiles();
+        setUploadDialogOpen(false);
+        setSelectedFiles([]);
+        setUploadStatus({});
+        setUploadProgress(0);
+        if (fileInputRef.current) {
+          fileInputRef.current.value = '';
+        }
+      }
+    } catch (error: any) {
+      setUploadError(error.message || 'An error occurred during upload');
+    } finally {
       setUploading(false);
     }
   };
@@ -452,23 +570,29 @@ const OMSpecDocumentation: React.FC = () => {
 
   const handleOpenUploadDialog = () => {
     setUploadDialogOpen(true);
-    setSelectedFile(null);
+    setSelectedFiles([]);
     setUploadError(null);
     setUploadProgress(0);
+    setUploadStatus({});
   };
 
   const handleCloseUploadDialog = () => {
     if (!uploading) {
       setUploadDialogOpen(false);
-      setSelectedFile(null);
+      setSelectedFiles([]);
       setUploadError(null);
       setUploadProgress(0);
+      setUploadStatus({});
       if (fileInputRef.current) {
         fileInputRef.current.value = '';
       }
     }
   };
 
+  const handleRemoveFile = (index: number) => {
+    setSelectedFiles(prev => prev.filter((_, i) => i !== index));
+  };
+
   // Tasks functions
   const loadTasks = async () => {
     setTasksLoading(true);
@@ -696,20 +820,66 @@ const OMSpecDocumentation: React.FC = () => {
                 />
               </Box>
 
-              <Button
-                variant="contained"
-                startIcon={<IconUpload size={20} />}
-                onClick={handleOpenUploadDialog}
-                sx={{
-                  backgroundColor: '#C8A24B',
-                  color: '#1a1a1a',
-                  '&:hover': {
-                    backgroundColor: '#B8923A',
-                  },
-                }}
-              >
-                Upload Documentation
-              </Button>
+              <Stack direction="row" spacing={2} justifyContent="center" alignItems="center" sx={{ mb: 2 }}>
+                <Button
+                  variant="contained"
+                  startIcon={<IconUpload size={20} />}
+                  onClick={handleOpenUploadDialog}
+                  sx={{
+                    backgroundColor: '#C8A24B',
+                    color: '#1a1a1a',
+                    '&:hover': {
+                      backgroundColor: '#B8923A',
+                    },
+                  }}
+                >
+                  Upload Documentation
+                </Button>
+                
+                {/* View Mode Toggle */}
+                <Box sx={{ display: 'flex', gap: 1, alignItems: 'center' }}>
+                  <IconButton
+                    onClick={() => setViewMode('grid')}
+                    color={viewMode === 'grid' ? 'primary' : 'default'}
+                    title="Grid View"
+                  >
+                    <IconLayoutGrid size={20} />
+                  </IconButton>
+                  <IconButton
+                    onClick={() => setViewMode('details')}
+                    color={viewMode === 'details' ? 'primary' : 'default'}
+                    title="Details View"
+                  >
+                    <IconTable size={20} />
+                  </IconButton>
+                </Box>
+              </Stack>
+
+              {/* Sort Controls */}
+              {files.length > 0 && (
+                <Box sx={{ display: 'flex', gap: 2, justifyContent: 'center', alignItems: 'center', mb: 3, flexWrap: 'wrap' }}>
+                  <FormControl size="small" sx={{ minWidth: 150 }}>
+                    <InputLabel>Sort By</InputLabel>
+                    <Select
+                      value={sortBy}
+                      label="Sort By"
+                      onChange={(e) => setSortBy(e.target.value as 'date' | 'name' | 'size' | 'type')}
+                    >
+                      <MenuItem value="date">Date Modified</MenuItem>
+                      <MenuItem value="name">Name</MenuItem>
+                      <MenuItem value="size">Size</MenuItem>
+                      <MenuItem value="type">Type</MenuItem>
+                    </Select>
+                  </FormControl>
+                  <IconButton
+                    onClick={() => setSortOrder(sortOrder === 'asc' ? 'desc' : 'asc')}
+                    title={`Sort ${sortOrder === 'asc' ? 'Descending' : 'Ascending'}`}
+                    size="small"
+                  >
+                    {sortOrder === 'asc' ? '↑' : '↓'}
+                  </IconButton>
+                </Box>
+              )}
             </>
           )}
         </Box>
@@ -838,46 +1008,127 @@ const OMSpecDocumentation: React.FC = () => {
               )}
             </CarouselContainer>
 
-            <FileGrid>
-              {files.map((file, index) => (
-                <FileCard
-                  key={index}
-                  onClick={() => handleFileClick(index)}
-                  sx={{
-                    border:
-                      currentIndex === index
-                        ? `3px solid #C8A24B`
-                        : '3px solid transparent',
-                  }}
-                >
-                  <CardContent>
-                    <Box sx={{ textAlign: 'center', mb: 2, color: getFileTypeColor(file.type) }}>
-                      {getFileIcon(file.type)}
-                    </Box>
-                    <Typography variant="subtitle1" sx={{ fontWeight: 600, mb: 1 }} noWrap>
-                      {file.name}
-                    </Typography>
-                    <Stack direction="row" spacing={1} justifyContent="center" sx={{ mb: 1 }}>
-                      <Chip
-                        label={file.type.toUpperCase()}
-                        size="small"
+            {/* Grid View */}
+            {viewMode === 'grid' && (
+              <FileGrid>
+                {files.map((file, index) => (
+                  <FileCard
+                    key={index}
+                    onClick={() => handleFileClick(index)}
+                    sx={{
+                      border:
+                        currentIndex === index
+                          ? `3px solid #C8A24B`
+                          : '3px solid transparent',
+                    }}
+                  >
+                    <CardContent>
+                      <Box sx={{ textAlign: 'center', mb: 2, color: getFileTypeColor(file.type) }}>
+                        {getFileIcon(file.type)}
+                      </Box>
+                      <Typography variant="subtitle1" sx={{ fontWeight: 600, mb: 1 }} noWrap>
+                        {file.name}
+                      </Typography>
+                      <Stack direction="row" spacing={1} justifyContent="center" sx={{ mb: 1 }}>
+                        <Chip
+                          label={file.type.toUpperCase()}
+                          size="small"
+                          sx={{
+                            backgroundColor: getFileTypeColor(file.type),
+                            color: 'white',
+                            fontSize: '0.7rem',
+                          }}
+                        />
+                      </Stack>
+                      <Typography variant="caption" color="text.secondary" display="block" sx={{ mb: 0.5 }}>
+                        {formatFileSize(file.size)}
+                      </Typography>
+                      <Typography variant="caption" color="text.secondary" display="block">
+                        {formatDate(file.uploadedAt)}
+                      </Typography>
+                    </CardContent>
+                  </FileCard>
+                ))}
+              </FileGrid>
+            )}
+
+            {/* Details/Table View */}
+            {viewMode === 'details' && (
+              <TableContainer component={Paper} sx={{ mt: 3 }}>
+                <Table>
+                  <TableHead>
+                    <TableRow>
+                      <TableCell><strong>Name</strong></TableCell>
+                      <TableCell><strong>Type</strong></TableCell>
+                      <TableCell><strong>Size</strong></TableCell>
+                      <TableCell><strong>Date Modified</strong></TableCell>
+                      <TableCell><strong>Actions</strong></TableCell>
+                    </TableRow>
+                  </TableHead>
+                  <TableBody>
+                    {files.map((file, index) => (
+                      <TableRow
+                        key={index}
+                        hover
+                        onClick={() => handleFileClick(index)}
                         sx={{
-                          backgroundColor: getFileTypeColor(file.type),
-                          color: 'white',
-                          fontSize: '0.7rem',
+                          cursor: 'pointer',
+                          backgroundColor: currentIndex === index ? 'action.selected' : 'inherit',
+                          '&:hover': {
+                            backgroundColor: 'action.hover',
+                          },
                         }}
-                      />
-                    </Stack>
-                    <Typography variant="caption" color="text.secondary" display="block" sx={{ mb: 0.5 }}>
-                      {formatFileSize(file.size)}
-                    </Typography>
-                    <Typography variant="caption" color="text.secondary" display="block">
-                      {formatDate(file.uploadedAt)}
-                    </Typography>
-                  </CardContent>
-                </FileCard>
-              ))}
-            </FileGrid>
+                      >
+                        <TableCell>
+                          <Box sx={{ display: 'flex', alignItems: 'center', gap: 1 }}>
+                            <Box sx={{ color: getFileTypeColor(file.type), display: 'flex', alignItems: 'center' }}>
+                              {getFileIcon(file.type, 20)}
+                            </Box>
+                            <Typography variant="body2" sx={{ fontWeight: 600 }}>
+                              {file.name}
+                            </Typography>
+                          </Box>
+                        </TableCell>
+                        <TableCell>
+                          <Chip
+                            label={file.type.toUpperCase()}
+                            size="small"
+                            sx={{
+                              backgroundColor: getFileTypeColor(file.type),
+                              color: 'white',
+                              fontWeight: 600,
+                            }}
+                          />
+                        </TableCell>
+                        <TableCell>
+                          <Typography variant="body2">
+                            {formatFileSize(file.size)}
+                          </Typography>
+                        </TableCell>
+                        <TableCell>
+                          <Typography variant="body2" color="text.secondary">
+                            {formatDate(file.uploadedAt)}
+                          </Typography>
+                        </TableCell>
+                        <TableCell>
+                          <IconButton
+                            size="small"
+                            onClick={(e) => {
+                              e.stopPropagation();
+                              handleDownload(file);
+                            }}
+                            color="primary"
+                            title="Download"
+                          >
+                            <IconDownload size={18} />
+                          </IconButton>
+                        </TableCell>
+                      </TableRow>
+                    ))}
+                  </TableBody>
+                </Table>
+              </TableContainer>
+            )}
           </>
         ))}
 
@@ -1090,7 +1341,7 @@ const OMSpecDocumentation: React.FC = () => {
         fullWidth
       >
         <DialogTitle>
-          Upload Documentation File
+          Upload Documentation Files
           <IconButton
             aria-label="close"
             onClick={handleCloseUploadDialog}
@@ -1112,6 +1363,7 @@ const OMSpecDocumentation: React.FC = () => {
               onChange={handleFileSelect}
               style={{ display: 'none' }}
               id="file-upload-input"
+              multiple
             />
             <label htmlFor="file-upload-input">
               <Button
@@ -1121,33 +1373,91 @@ const OMSpecDocumentation: React.FC = () => {
                 startIcon={<IconUpload size={20} />}
                 sx={{ mb: 2 }}
               >
-                {selectedFile ? selectedFile.name : 'Select File'}
+                {selectedFiles.length > 0 ? `Select More Files (${selectedFiles.length} selected)` : 'Select Files'}
               </Button>
             </label>
 
             <Typography variant="caption" color="text.secondary" display="block" sx={{ mb: 2, textAlign: 'center' }}>
-              Allowed formats: {allowedTypes.join(', ')}
+              Allowed formats: {allowedTypes.join(', ')} (You can select multiple files)
             </Typography>
 
-            {selectedFile && (
-              <Box sx={{ mb: 2, p: 2, bgcolor: 'background.default', borderRadius: 1 }}>
-                <Typography variant="body2" color="text.secondary" sx={{ mb: 1 }}>
-                  <strong>File:</strong> {selectedFile.name}
-                </Typography>
-                <Typography variant="body2" color="text.secondary" sx={{ mb: 1 }}>
-                  <strong>Size:</strong> {formatFileSize(selectedFile.size)}
-                </Typography>
-                <Typography variant="body2" color="text.secondary">
-                  <strong>Type:</strong> {selectedFile.type || 'Unknown'}
+            {selectedFiles.length > 0 && (
+              <Box sx={{ mb: 2 }}>
+                <Typography variant="subtitle2" sx={{ mb: 1, fontWeight: 600 }}>
+                  Selected Files ({selectedFiles.length}):
                 </Typography>
+                <Stack spacing={1}>
+                  {selectedFiles.map((file, index) => {
+                    const fileKey = `${file.name}-${file.size}`;
+                    const status = uploadStatus[fileKey];
+                    return (
+                      <Box
+                        key={index}
+                        sx={{
+                          p: 2,
+                          bgcolor: 'background.default',
+                          borderRadius: 1,
+                          border: '1px solid',
+                          borderColor: status?.status === 'error' ? 'error.main' : status?.status === 'success' ? 'success.main' : 'divider',
+                        }}
+                      >
+                        <Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', mb: 1 }}>
+                          <Box sx={{ flex: 1 }}>
+                            <Typography variant="body2" sx={{ fontWeight: 600, mb: 0.5 }}>
+                              {file.name}
+                            </Typography>
+                            <Typography variant="caption" color="text.secondary" display="block">
+                              Size: {formatFileSize(file.size)} | Type: {file.type || 'Unknown'}
+                            </Typography>
+                            {status && (
+                              <>
+                                {status.status === 'uploading' && (
+                                  <Box sx={{ mt: 1 }}>
+                                    <LinearProgress variant="determinate" value={status.progress} size="small" />
+                                    <Typography variant="caption" color="text.secondary">
+                                      Uploading... {Math.round(status.progress)}%
+                                    </Typography>
+                                  </Box>
+                                )}
+                                {status.status === 'success' && (
+                                  <Typography variant="caption" color="success.main" sx={{ mt: 0.5, display: 'block' }}>
+                                    ✓ Uploaded successfully
+                                  </Typography>
+                                )}
+                                {status.status === 'error' && (
+                                  <Typography variant="caption" color="error.main" sx={{ mt: 0.5, display: 'block' }}>
+                                    ✗ {status.error || 'Upload failed'}
+                                  </Typography>
+                                )}
+                              </>
+                            )}
+                          </Box>
+                          {!uploading && (
+                            <IconButton
+                              size="small"
+                              onClick={() => handleRemoveFile(index)}
+                              sx={{ ml: 1 }}
+                              color="error"
+                            >
+                              <IconX size={18} />
+                            </IconButton>
+                          )}
+                        </Box>
+                      </Box>
+                    );
+                  })}
+                </Stack>
               </Box>
             )}
 
-            {uploading && (
+            {uploading && selectedFiles.length > 0 && (
               <Box sx={{ mt: 2 }}>
+                <Typography variant="body2" color="text.secondary" sx={{ mb: 1 }}>
+                  Overall Progress: {Math.round(uploadProgress)}%
+                </Typography>
                 <LinearProgress variant="determinate" value={uploadProgress} />
-                <Typography variant="body2" color="text.secondary" sx={{ mt: 1 }}>
-                  Uploading... {Math.round(uploadProgress)}%
+                <Typography variant="caption" color="text.secondary" sx={{ mt: 1, display: 'block' }}>
+                  Uploading {selectedFiles.length} file{selectedFiles.length > 1 ? 's' : ''}...
                 </Typography>
               </Box>
             )}
@@ -1166,7 +1476,7 @@ const OMSpecDocumentation: React.FC = () => {
           <Button
             onClick={handleUpload}
             variant="contained"
-            disabled={!selectedFile || uploading}
+            disabled={selectedFiles.length === 0 || uploading}
             sx={{
               backgroundColor: '#C8A24B',
               color: '#1a1a1a',
@@ -1175,7 +1485,7 @@ const OMSpecDocumentation: React.FC = () => {
               },
             }}
           >
-            Upload
+            {uploading ? `Uploading... (${Math.round(uploadProgress)}%)` : `Upload ${selectedFiles.length} File${selectedFiles.length !== 1 ? 's' : ''}`}
           </Button>
         </DialogActions>
       </Dialog>
diff --git a/front-end/src/features/pages/frontend-pages/Samples.tsx b/front-end/src/features/pages/frontend-pages/Samples.tsx
index 40156a75..aac18b77 100755
--- a/front-end/src/features/pages/frontend-pages/Samples.tsx
+++ b/front-end/src/features/pages/frontend-pages/Samples.tsx
@@ -86,8 +86,7 @@ import { keyframes } from '@mui/system';
 import { AgGridReact } from 'ag-grid-react';
 import { ColDef, GridReadyEvent, GridApi, ColumnApi } from 'ag-grid-community';
 import { AllCommunityModule, ModuleRegistry } from 'ag-grid-community';
-import 'ag-grid-community/styles/ag-grid.css';
-import 'ag-grid-community/styles/ag-theme-alpine.css';
+// Note: AG Grid CSS is imported globally in main.tsx to avoid duplicates
 import Header from './Header';
 import LeftSideMenu from './LeftSideMenu';
 import Footer from './Footer';
@@ -1845,8 +1844,9 @@ const Samples: React.FC = () => {
   ];
 
   // Get card title based on language and record type
-  const getCardTitle = (recordId: string) => {
-    if (activeLanguageTab === 1) {
+  const getCardTitle = (recordId: string, languageIndex?: number) => {
+    const langIndex = languageIndex !== undefined ? languageIndex : activeLanguageTab;
+    if (langIndex === 1) {
       // Greek translations
       const greekCardTitles = {
         baptism: 'Μητρώα Βαπτίσεων',
@@ -1854,7 +1854,7 @@ const Samples: React.FC = () => {
         funeral: 'Μητρώα Κηδειών',
       };
       return greekCardTitles[recordId as keyof typeof greekCardTitles] || recordTypes.find(r => r.id === recordId)?.title || '';
-    } else if (activeLanguageTab === 2) {
+    } else if (langIndex === 2) {
       // Russian translations
       const russianCardTitles = {
         baptism: 'Крещальные записи',
@@ -1862,7 +1862,7 @@ const Samples: React.FC = () => {
         funeral: 'Поминальные записи',
       };
       return russianCardTitles[recordId as keyof typeof russianCardTitles] || recordTypes.find(r => r.id === recordId)?.title || '';
-    } else if (activeLanguageTab === 3) {
+    } else if (langIndex === 3) {
       // Romanian translations
       const romanianCardTitles = {
         baptism: 'Înregistrări de Botez',
@@ -1870,7 +1870,7 @@ const Samples: React.FC = () => {
         funeral: 'Înregistrări Funerare',
       };
       return romanianCardTitles[recordId as keyof typeof romanianCardTitles] || recordTypes.find(r => r.id === recordId)?.title || '';
-    } else if (activeLanguageTab === 4) {
+    } else if (langIndex === 4) {
       // Georgian translations
       const georgianCardTitles = {
         baptism: 'ნათლობის ჩანაწერები',
@@ -2051,6 +2051,68 @@ const Samples: React.FC = () => {
         )}
       </Container>
 
+      {/* Dropdown for Record Type and Language Selection */}
+      <Container maxWidth="lg" sx={{ py: 2 }}>
+        <Box sx={{ display: 'flex', justifyContent: 'center', mb: 2 }}>
+          <FormControl 
+            variant="outlined" 
+            sx={{ 
+              minWidth: 300,
+              backgroundColor: theme.palette.background.paper,
+            }}
+          >
+            <InputLabel id="record-language-select-label">
+              Select Record Page & Language
+            </InputLabel>
+            <Select
+              labelId="record-language-select-label"
+              id="record-language-select"
+              value={`${selectedRecordType || 'none'}-${activeLanguageTab}`}
+              onChange={(e) => {
+                const [recordType, languageIndex] = e.target.value.split('-');
+                if (recordType === 'none') {
+                  setSelectedRecordType(null);
+                } else {
+                  setSelectedRecordType(recordType);
+                }
+                setActiveLanguageTab(parseInt(languageIndex, 10));
+                setIsGridReady(false);
+                setSearchTerm(''); // Clear search when changing selection
+              }}
+              label="Select Record Page & Language"
+              sx={{
+                '& .MuiOutlinedInput-notchedOutline': {
+                  borderColor: theme.palette.mode === 'dark' 
+                    ? theme.palette.divider 
+                    : '#1565c0',
+                },
+                '&:hover .MuiOutlinedInput-notchedOutline': {
+                  borderColor: theme.palette.mode === 'dark' 
+                    ? theme.palette.text.primary 
+                    : '#0d47a1',
+                },
+                '&.Mui-focused .MuiOutlinedInput-notchedOutline': {
+                  borderColor: '#1565c0',
+                },
+              }}
+            >
+              <MenuItem value="none-0" sx={{ fontStyle: 'italic', color: 'text.secondary' }}>
+                -- Select to load samples --
+              </MenuItem>
+              {languageTabs.map((langTab, langIndex) => (
+                recordTypes.map((record) => (
+                  <MenuItem 
+                    key={`${record.id}-${langIndex}`} 
+                    value={`${record.id}-${langIndex}`}
+                  >
+                    {langTab.label} - {getCardTitle(record.id, langIndex)}
+                  </MenuItem>
+                ))
+              ))}
+            </Select>
+          </FormControl>
+        </Box>
+      </Container>
 
       {/* Language Selection Tabs */}
       <LanguageTabsContainer>
@@ -2592,7 +2654,7 @@ const Samples: React.FC = () => {
               width: '100%',
               overflow: 'hidden',
               backgroundColor: themeTokens.rowEvenBg,
-              '& .ag-theme-alpine': {
+              '& .ag-theme-quartz': {
                 '& .ag-header': {
                   backgroundColor: themeTokens.headerBg,
                   borderBottom: `1px solid ${themeTokens.border}`,
@@ -2641,7 +2703,7 @@ const Samples: React.FC = () => {
             }}
           >
             <div
-              className="ag-theme-alpine"
+              className="ag-theme-quartz"
               style={{
                 height: '100%',
                 width: '100%',
diff --git a/front-end/src/features/records-centralized/components/baptism/BaptismRecordsPage.tsx b/front-end/src/features/records-centralized/components/baptism/BaptismRecordsPage.tsx
index b0a1fc0d..54ca6979 100755
--- a/front-end/src/features/records-centralized/components/baptism/BaptismRecordsPage.tsx
+++ b/front-end/src/features/records-centralized/components/baptism/BaptismRecordsPage.tsx
@@ -1,4 +1,4 @@
-import React, { useState, useEffect, useMemo, useCallback, startTransition } from 'react';
+import React, { useState, useEffect, useMemo, useCallback, useRef, startTransition } from 'react';
 import { useLocation } from 'react-router-dom';
 import {
   Box,
@@ -37,6 +37,8 @@ import {
   Collapse,
   Menu,
   useTheme,
+  FormControlLabel,
+  Switch,
 } from '@mui/material';
 import {
   Add as AddIcon,
@@ -56,6 +58,7 @@ import {
   ExpandMore as IconChevronDown,
   ChevronLeft as ChevronLeftIcon,
   ChevronRight as ChevronRightIcon,
+  Description as AssignmentIcon,
 } from '@mui/icons-material';
 import { useTableStyleStore } from '@/store/useTableStyleStore';
 import { listRecords, type TableKey, type SortDir } from '@/shared/lib/recordsApi';
@@ -65,10 +68,10 @@ import { AGGridViewOnly } from '@/components/AGGridViewOnly/AGGridViewOnly';
 import { ChurchRecord, RecordType as ChurchRecordType } from '@/types/church-records-advanced.types';
 // import ImportRecordsButton from '@/components/ImportRecordsButton'; // Using BrandButtons instead
 import AdvancedGridDialog from '@/features/tables/AdvancedGridDialog';
+import AdvancedRecordsGrid from '../records/AdvancedRecordsGrid';
 import { AgGridReact } from 'ag-grid-react';
 import { ColDef, GridReadyEvent, GridApi, ModuleRegistry, AllCommunityModule } from 'ag-grid-community';
-import 'ag-grid-community/styles/ag-grid.css';
-import 'ag-grid-community/styles/ag-theme-alpine.css';
+// Note: AG Grid CSS is imported globally in main.tsx to avoid duplicates
 import '@/styles/advanced-grid-themes.css';
 
 // Register AG Grid modules
@@ -81,6 +84,15 @@ import { useNavigate } from 'react-router-dom';
 import adminAPI from '@/api/admin.api';
 import LeftSideMenu from '@/features/pages/frontend-pages/LeftSideMenu';
 import ImageBasedRecordForm from './ImageBasedRecordForm';
+import { normalizeDateForInput } from '@/shared/lib/dateUtils';
+import { getUserPreference, setUserPreference } from '@/shared/lib/userPreferences';
+import { RecordsErrorBoundary } from '../RecordsErrorBoundary';
+import { QuickFactsDrawer } from '../quickFacts/QuickFactsDrawer';
+import { computeQuickFacts } from '../quickFacts/quickFacts';
+import { QuickFactsResult } from '../quickFacts/types';
+import InteractiveReportWizard, { InteractiveReportConfig } from '../interactiveReport/InteractiveReportWizard';
+import { formatRecordDate } from '@/utils/formatDate';
+import { getCached, setCached, safeAdminFetch } from '@/features/records-centralized/utils/adminEndpointCache';
 
 // Types
 interface Church {
@@ -232,9 +244,9 @@ const getSortFields = (recordType: string) => {
 
 // Function to get cell value based on column field and record type
 const getCellValue = (record: any, column: any) => {
+  // Use centralized date formatter - return empty string for empty dates, not 'N/A'
   const formatDate = (dateString: string | null | undefined) => {
-    if (!dateString) return 'N/A';
-    return new Date(dateString).toLocaleDateString();
+    return formatRecordDate(dateString) || '';
   };
 
   if (column.valueGetter) {
@@ -334,6 +346,10 @@ const BaptismRecordsPage: React.FC = () => {
   const [churches, setChurches] = useState<Church[]>([]);
   const [loading, setLoading] = useState<boolean>(false);
   const [error, setError] = useState<string | null>(null);
+  const [quickFactsOpen, setQuickFactsOpen] = useState<boolean>(false);
+  const [quickFacts, setQuickFacts] = useState<QuickFactsResult | null>(null);
+  const [quickFactsLoading, setQuickFactsLoading] = useState<boolean>(false);
+  const [interactiveReportOpen, setInteractiveReportOpen] = useState<boolean>(false);
   const [page, setPage] = useState<number>(0);
   const [rowsPerPage, setRowsPerPage] = useState<number>(10);
   const [searchTerm, setSearchTerm] = useState<string>('');
@@ -345,13 +361,49 @@ const BaptismRecordsPage: React.FC = () => {
   };
   const [selectedChurch, setSelectedChurch] = useState<number>(getValidChurchId(urlChurchId));
   const [selectedRecordType, setSelectedRecordType] = useState<string>(urlRecordType);
-  // Get viewMode from URL or default to 'normal'
+  // Get viewMode from URL or default to 'advanced' (AG Grid)
   const urlViewMode = searchParams.get('view') as 'normal' | 'advanced' | null;
-  const [viewMode, setViewMode] = useState<'normal' | 'advanced'>(urlViewMode || 'normal');
+  const [viewMode, setViewMode] = useState<'normal' | 'advanced'>(urlViewMode || 'advanced');
+  const [autoShrinkEnabled, setAutoShrinkEnabled] = useState<boolean>(() => 
+    getUserPreference<boolean>('normalRecordsTable.autoShrink', true)
+  );
+  const [highlightIncomplete, setHighlightIncomplete] = useState<boolean>(() =>
+    getUserPreference<boolean>('records.highlightIncomplete', false)
+  );
+  
+  // Debug logging for viewMode - Only log when values actually change
+  const prevViewModeRef = useRef(viewMode);
+  const prevUrlViewModeRef = useRef(urlViewMode);
+  
+  useEffect(() => {
+    if (prevViewModeRef.current !== viewMode || prevUrlViewModeRef.current !== urlViewMode) {
+      console.log('🔍 viewMode state changed:', {
+        urlViewMode,
+        currentViewMode: viewMode,
+        urlHasView: !!searchParams.get('view'),
+        urlViewValue: searchParams.get('view'),
+        selectedRecordType,
+        selectedChurch,
+      });
+      prevViewModeRef.current = viewMode;
+      prevUrlViewModeRef.current = urlViewMode;
+    }
+  }, [viewMode, urlViewMode, searchParams, selectedRecordType, selectedChurch]);
   const [activeSection, setActiveSection] = useState<'gallery-of-images' | 'graphical-analysis' | 'om-magic-image' | 'records-systems'>('records-systems');
   const [calendarStartOffset, setCalendarStartOffset] = useState<number>(1); // Start from tomorrow (offset 1)
   const [displayedRecordIndex, setDisplayedRecordIndex] = useState<number>(0); // 0=baptism, 1=marriage, 2=funeral
   const [isRecordTypeHovered, setIsRecordTypeHovered] = useState<boolean>(false);
+  const [viewportHeight, setViewportHeight] = useState<number>(window.innerHeight);
+
+  // Track viewport height for table sizing
+  useEffect(() => {
+    const handleResize = () => {
+      setViewportHeight(window.innerHeight);
+    };
+    
+    window.addEventListener('resize', handleResize);
+    return () => window.removeEventListener('resize', handleResize);
+  }, []);
 
   // Update selectedRecordType and viewMode when URL param or path changes
   useEffect(() => {
@@ -362,7 +414,7 @@ const BaptismRecordsPage: React.FC = () => {
     };
 
     const newType = searchParams.get('type') || getTypeFromPath();
-    const newViewMode = (searchParams.get('view') as 'normal' | 'advanced') || 'normal';
+    const newViewMode = (searchParams.get('view') as 'normal' | 'advanced') || 'advanced';
     
     if (newType !== selectedRecordType) {
       setSelectedRecordType(newType);
@@ -416,6 +468,20 @@ const BaptismRecordsPage: React.FC = () => {
   // AG Grid state for inline view
   const [gridApi, setGridApi] = useState<GridApi | null>(null);
   
+  // Refs to prevent infinite loops and concurrent loads
+  const fetchInFlight = useRef(false);
+  const loadedForChurch = useRef<number | null>(null);
+  const abortControllerRef = useRef<AbortController | null>(null);
+  const datasetsRef = useRef<{
+    baptism: any[];
+    marriage: any[];
+    funeral: any[];
+  }>({
+    baptism: [],
+    marriage: [],
+    funeral: []
+  });
+  
   const [allRecordsDatasets, setAllRecordsDatasets] = useState<{
     baptism: any[];
     marriage: any[];
@@ -425,6 +491,11 @@ const BaptismRecordsPage: React.FC = () => {
     marriage: [],
     funeral: []
   });
+  
+  // Keep ref in sync with state
+  useEffect(() => {
+    datasetsRef.current = allRecordsDatasets;
+  }, [allRecordsDatasets]);
   const [allRecordsCounts, setAllRecordsCounts] = useState<{
     baptism: number;
     marriage: number;
@@ -552,33 +623,28 @@ const BaptismRecordsPage: React.FC = () => {
   // API functions
 
 
-  // Load record settings
+  // Load record settings with graceful 403 handling
   const loadRecordSettings = async () => {
     if (!selectedChurch) return;
     
     try {
-      const response = await fetch(`/api/admin/churches/${selectedChurch}/record-settings`, {
-        credentials: 'include',
-      });
-      if (response.ok) {
-        const data = await response.json();
-        if (data.settings) {
-          setRecordSettings(data.settings);
-        } else {
-          // Use default settings if none exist
-          setRecordSettings({
-            logo: { enabled: true, column: 3, width: 120, height: 'auto', objectFit: 'contain', opacity: 100 },
-            calendar: { enabled: true, column: 2 },
-            omLogo: { enabled: true, column: 4, width: 68, height: 68 },
-            recordImages: { column: 1, width: 60, height: 60 },
-            backgroundImage: { enabled: true },
-            g1Image: { enabled: true },
-          });
-        }
+      const result = await safeAdminFetch(`/api/admin/churches/${selectedChurch}/record-settings`);
+      
+      if (result?.ok && result.data?.settings) {
+        setRecordSettings(result.data.settings);
+      } else {
+        // Use default settings if endpoint unavailable or no settings exist
+        setRecordSettings({
+          logo: { enabled: true, column: 3, width: 120, height: 'auto', objectFit: 'contain', opacity: 100 },
+          calendar: { enabled: true, column: 2 },
+          omLogo: { enabled: true, column: 4, width: 68, height: 68 },
+          recordImages: { column: 1, width: 60, height: 60 },
+          backgroundImage: { enabled: true },
+          g1Image: { enabled: true },
+        });
       }
     } catch (err) {
-      console.error('Error loading record settings:', err);
-      // Use default settings on error
+      // Use default settings on error (don't spam console)
       setRecordSettings({
         logo: { enabled: true, column: 3, width: 120, height: 'auto', objectFit: 'contain', opacity: 100 },
         calendar: { enabled: true, column: 2 },
@@ -608,24 +674,21 @@ const BaptismRecordsPage: React.FC = () => {
     };
   }, [selectedChurch]);
 
-  // Load custom themes from backend when church changes
+  // Load custom themes from backend when church changes (with graceful 403 handling)
   useEffect(() => {
     const loadCustomThemes = async () => {
       if (!selectedChurch) return;
       
       try {
-        const response = await fetch(`/api/admin/churches/${selectedChurch}/themes`, {
-          credentials: 'include',
-        });
-        if (response.ok) {
-          const data = await response.json();
-          if (data.themes && Object.keys(data.themes).length > 0) {
-            // Sync custom themes to enhancedTableStore so they appear in dropdown
-            enhancedTableStore.setCustomThemes(data.themes);
-          }
+        const result = await safeAdminFetch(`/api/admin/churches/${selectedChurch}/themes`);
+        
+        if (result?.ok && result.data?.themes && Object.keys(result.data.themes).length > 0) {
+          // Sync custom themes to enhancedTableStore so they appear in dropdown
+          enhancedTableStore.setCustomThemes(result.data.themes);
         }
+        // If 403/404 or no themes, silently use defaults (no console spam)
       } catch (err) {
-        console.error('Error loading custom themes:', err);
+        // Silently fail (safeAdminFetch already handles logging)
       }
     };
     
@@ -822,10 +885,35 @@ const BaptismRecordsPage: React.FC = () => {
   };
 
   // Fetch all record types for Advanced Grid Dialog
-  const fetchAllRecordTypes = async (churchId?: number) => {
+  const fetchAllRecordTypes = useCallback(async (churchId?: number) => {
     if (!churchId) return;
 
+    // Guard: Prevent concurrent loads for the same church
+    if (fetchInFlight.current && loadedForChurch.current === churchId) {
+      console.log('⏳ Fetch already in progress for church', churchId);
+      return;
+    }
+
+    // Guard: Skip if already loaded for this church (use ref to avoid stale closure)
+    if (loadedForChurch.current === churchId) {
+      const currentDatasets = datasetsRef.current;
+      if (currentDatasets.baptism.length > 0 && 
+          currentDatasets.marriage.length > 0 && 
+          currentDatasets.funeral.length > 0) {
+        console.log('✅ Data already loaded for church', churchId);
+        return;
+      }
+    }
+
+    // Cancel any in-flight request
+    if (abortControllerRef.current) {
+      abortControllerRef.current.abort();
+    }
+
+    fetchInFlight.current = true;
     const ctrl = new AbortController();
+    abortControllerRef.current = ctrl;
+
     const datasets: { baptism: any[]; marriage: any[]; funeral: any[] } = {
       baptism: [],
       marriage: [],
@@ -1006,15 +1094,60 @@ const BaptismRecordsPage: React.FC = () => {
       }
 
       if (!ctrl.signal.aborted) {
-        setAllRecordsDatasets(datasets);
-        setAllRecordsCounts(counts);
+        // Idempotent state update: Only update if data actually changed
+        setAllRecordsDatasets(prev => {
+          const hasChanged = 
+            prev.baptism.length !== datasets.baptism.length ||
+            prev.marriage.length !== datasets.marriage.length ||
+            prev.funeral.length !== datasets.funeral.length;
+          
+          if (!hasChanged) {
+            // Deep check: compare first few items to ensure data is actually the same
+            const deepCheck = 
+              prev.baptism.length > 0 && datasets.baptism.length > 0 &&
+              prev.baptism[0]?.id === datasets.baptism[0]?.id &&
+              prev.marriage.length > 0 && datasets.marriage.length > 0 &&
+              prev.marriage[0]?.id === datasets.marriage[0]?.id &&
+              prev.funeral.length > 0 && datasets.funeral.length > 0 &&
+              prev.funeral[0]?.id === datasets.funeral[0]?.id;
+            
+            if (deepCheck) {
+              console.log('📦 Data unchanged, skipping state update');
+              return prev;
+            }
+          }
+          
+          // Update ref immediately to avoid stale closure issues
+          datasetsRef.current = datasets;
+          return datasets;
+        });
+        
+        setAllRecordsCounts(prev => {
+          const hasChanged = 
+            prev.baptism !== counts.baptism ||
+            prev.marriage !== counts.marriage ||
+            prev.funeral !== counts.funeral;
+          
+          if (!hasChanged) {
+            return prev;
+          }
+          
+          return counts;
+        });
+        
+        loadedForChurch.current = churchId;
       }
     } catch (err) {
-      console.error('Error fetching all record types:', err);
+      if (err instanceof Error && err.name !== 'AbortError') {
+        console.error('Error fetching all record types:', err);
+      }
+    } finally {
+      if (!ctrl.signal.aborted) {
+        fetchInFlight.current = false;
+        abortControllerRef.current = null;
+      }
     }
-
-    return () => ctrl.abort();
-  };
+  }, []); // Empty deps - function is stable, uses refs and state setters
 
   const fetchPriestOptions = (recordType: string, recordsData: any[]) => {
     try {
@@ -1135,7 +1268,7 @@ const BaptismRecordsPage: React.FC = () => {
     }
   }, [selectedRecordType, selectedChurch]); // Fetch when record type or church changes
 
-  // Fetch field mappings from field mapper
+  // Fetch field mappings from field mapper with guards and caching
   const fetchFieldMappings = async (churchId: number, tableName: string) => {
     try {
       const tableMap: Record<string, string> = {
@@ -1145,26 +1278,81 @@ const BaptismRecordsPage: React.FC = () => {
       };
       const dbTableName = tableMap[tableName] || `${tableName}_records`;
 
-      // Get table columns
-      const { columns } = await adminAPI.churches.getTableColumns(churchId, dbTableName);
-      setTableColumns(columns);
-
-      // Try to fetch field mapper settings
-      const mappingCandidates = [
-        `/api/admin/churches/${churchId}/field-mapper?table=${encodeURIComponent(dbTableName)}`,
-        `/admin/churches/${churchId}/field-mapper?table=${encodeURIComponent(dbTableName)}`,
-      ];
+      // Check cache first
+      const cachedColumns = getCached(churchId, tableName, 'columns');
+      let columns: string[] = [];
 
-      let mappingData: any = null;
-      for (const url of mappingCandidates) {
+      if (cachedColumns) {
+        columns = cachedColumns;
+        setTableColumns(columns);
+      } else {
+        // Try to get table columns with graceful fallback
         try {
-          const res = await fetch(url, { credentials: 'include' });
-          if (res.ok) {
-            mappingData = await res.json();
-            break;
+          const columnsResult = await safeAdminFetch(
+            `/api/admin/churches/${churchId}/tables/${dbTableName}/columns`
+          );
+          
+          if (columnsResult?.ok && columnsResult.data?.columns) {
+            columns = columnsResult.data.columns;
+            setCached(churchId, tableName, 'columns', columns);
+            setTableColumns(columns);
+          } else {
+            // Fallback: infer columns from records if available, otherwise use defaults
+            // Use records directly (filteredRecords is computed later, not available here)
+            const currentRecords = records;
+            if (currentRecords.length > 0) {
+              const inferredColumns = Object.keys(currentRecords[0] || {});
+              columns = inferredColumns;
+              setTableColumns(columns);
+              // Log once, not every time
+              if (!getCached(churchId, tableName, 'columns-fallback-logged')) {
+                console.warn(`Using inferred columns for ${tableName} (admin endpoint unavailable)`);
+                setCached(churchId, tableName, 'columns-fallback-logged', true);
+              }
+            } else {
+              // No records yet, use defaults based on record type
+              const defaultColumns: Record<string, string[]> = {
+                'baptism': ['id', 'first_name', 'last_name', 'birth_date', 'reception_date'],
+                'marriage': ['id', 'fname_groom', 'lname_groom', 'fname_bride', 'lname_bride', 'mdate'],
+                'funeral': ['id', 'name', 'lastname', 'deceased_date', 'burial_date'],
+              };
+              columns = defaultColumns[tableName] || defaultColumns['baptism'];
+              setTableColumns(columns);
+            }
           }
-        } catch {
-          // Try next candidate
+        } catch (err) {
+          // Fallback to inferred columns or defaults
+          // Use records directly (filteredRecords is computed later, not available here)
+          const currentRecords = records;
+          if (currentRecords.length > 0) {
+            const inferredColumns = Object.keys(currentRecords[0] || {});
+            columns = inferredColumns;
+            setTableColumns(columns);
+          } else {
+            const defaultColumns: Record<string, string[]> = {
+              'baptism': ['id', 'first_name', 'last_name', 'birth_date', 'reception_date'],
+              'marriage': ['id', 'fname_groom', 'lname_groom', 'fname_bride', 'lname_bride', 'mdate'],
+              'funeral': ['id', 'name', 'lastname', 'deceased_date', 'burial_date'],
+            };
+            columns = defaultColumns[tableName] || defaultColumns['baptism'];
+            setTableColumns(columns);
+          }
+        }
+      }
+
+      // Try to fetch field mapper settings (with cache and graceful fallback)
+      const cachedMappings = getCached(churchId, tableName, 'field-mapper');
+      let mappingData: any = null;
+
+      if (cachedMappings) {
+        mappingData = cachedMappings;
+      } else {
+        const mappingUrl = `/api/admin/churches/${churchId}/field-mapper?table=${encodeURIComponent(dbTableName)}`;
+        const mappingResult = await safeAdminFetch(mappingUrl);
+        
+        if (mappingResult?.ok && mappingResult.data) {
+          mappingData = mappingResult.data;
+          setCached(churchId, tableName, 'field-mapper', mappingData);
         }
       }
 
@@ -1185,11 +1373,12 @@ const BaptismRecordsPage: React.FC = () => {
         setFieldMappings({});
       }
     } catch (err) {
-      console.error('Error fetching field mappings:', err);
-      // Set defaults on error
+      // Set defaults on error - don't spam console (only log once per session)
       const defaultVisibility: Record<string, boolean> = {};
       const defaultSortable: Record<string, boolean> = {};
-      tableColumns.forEach(col => {
+      const currentColumns = tableColumns.length > 0 ? tableColumns : 
+        (records.length > 0 ? Object.keys(records[0] || {}) : ['id', 'first_name', 'last_name']);
+      currentColumns.forEach(col => {
         defaultVisibility[col] = true;
         defaultSortable[col] = true;
       });
@@ -1254,30 +1443,9 @@ const BaptismRecordsPage: React.FC = () => {
         const possibleFieldNames = fieldNameMap[col] || [col];
         const actualFieldName = possibleFieldNames[0]; // Use the database column name first
 
-        // Helper function to format dates
+        // Helper function to format dates - use centralized formatter
         const formatDateValue = (dateValue: any): string => {
-          if (!dateValue) return '';
-          // If already formatted as YYYY-MM-DD, return as-is
-          if (typeof dateValue === 'string' && /^\d{4}-\d{2}-\d{2}$/.test(dateValue)) {
-            return new Date(dateValue).toLocaleDateString();
-          }
-          // Handle ISO datetime strings (e.g., "2025-08-01T04:00:00.000Z")
-          if (typeof dateValue === 'string' && dateValue.includes('T')) {
-            const datePart = dateValue.split('T')[0];
-            if (/^\d{4}-\d{2}-\d{2}$/.test(datePart)) {
-              return new Date(datePart).toLocaleDateString();
-            }
-          }
-          // Try to parse as date
-          try {
-            const date = new Date(dateValue);
-            if (!isNaN(date.getTime())) {
-              return date.toLocaleDateString();
-            }
-          } catch (e) {
-            // If parsing fails, return original value
-          }
-          return String(dateValue);
+          return formatRecordDate(dateValue);
         };
 
         // Check if this is a date field
@@ -1327,11 +1495,13 @@ const BaptismRecordsPage: React.FC = () => {
             }
             
             // Format date if this is a date field
-            if (isDateField && value) {
+            if (isDateField && (value !== null && value !== undefined && value !== '')) {
               return formatDateValue(value);
             }
             
-            return value !== null && value !== undefined ? value : '';
+            // Return value as-is (including empty string, 0, false) - let renderCellValue handle null/undefined
+            // STRICT: Only return null/undefined if truly not found, not empty string
+            return value; // Return null/undefined if not found, actual value otherwise
           },
         };
       });
@@ -1488,7 +1658,7 @@ const BaptismRecordsPage: React.FC = () => {
             fields.push(
               { key: 'firstName', label: 'First Name', value: originalRecord.firstName || '', type: 'text' as const, editable: false },
               { key: 'lastName', label: 'Last Name', value: originalRecord.lastName || '', type: 'text' as const, editable: false },
-              { key: 'dateOfBaptism', label: 'Date', value: originalRecord.dateOfBaptism || '', type: 'text' as const, editable: false },
+              { key: 'dateOfBaptism', label: 'Date', value: formatRecordDate(originalRecord.dateOfBaptism) || '', type: 'text' as const, editable: false },
               { key: 'churchName', label: 'Church', value: originalRecord.churchName || '', type: 'text' as const, editable: false },
               { key: 'priest', label: 'Priest', value: originalRecord.priest || '', type: 'text' as const, editable: false }
             );
@@ -1569,10 +1739,25 @@ const BaptismRecordsPage: React.FC = () => {
 
   // Get records for current record type for AG Grid
   const getRecordsForType = useCallback((type: string): any[] => {
+    // Always prefer allRecordsDatasets when available (for advanced view)
     if (allRecordsDatasets && allRecordsDatasets[type as keyof typeof allRecordsDatasets]) {
-      return allRecordsDatasets[type as keyof typeof allRecordsDatasets] || [];
+      const dataset = allRecordsDatasets[type as keyof typeof allRecordsDatasets];
+      if (dataset && dataset.length > 0) {
+        if (import.meta.env.DEV) {
+          console.log(`✅ getRecordsForType: Using allRecordsDatasets.${type} (${dataset.length} records)`);
+        }
+        return dataset;
+      }
+    }
+    
+    // Fallback to current records (for normal view or when datasets not loaded)
+    if (import.meta.env.DEV) {
+      console.warn(`⚠️ getRecordsForType: Falling back to filteredAndSortedRecords for ${type}`, {
+        allRecordsDatasetsAvailable: !!allRecordsDatasets,
+        allRecordsDatasetsTypeLength: allRecordsDatasets?.[type as keyof typeof allRecordsDatasets]?.length || 0,
+        filteredAndSortedRecordsLength: filteredAndSortedRecords.length,
+      });
     }
-    // Fallback to current records
     return filteredAndSortedRecords;
   }, [allRecordsDatasets, filteredAndSortedRecords]);
 
@@ -1610,10 +1795,8 @@ const BaptismRecordsPage: React.FC = () => {
         fieldName === 'birth_date') {
         config.filter = 'agDateColumnFilter';
         config.valueFormatter = (params) => {
-          if (params.value) {
-            return new Date(params.value).toLocaleDateString();
-          }
-          return '';
+          // Use centralized date formatter to prevent ISO timestamp display
+          return formatRecordDate(params.value);
         };
       }
 
@@ -1821,11 +2004,35 @@ const BaptismRecordsPage: React.FC = () => {
   // Get row data for current record type for AG Grid
   const agGridRowData = useMemo(() => {
     const typeRecords = getRecordsForType(selectedRecordType);
+    
+    // Dev diagnostic logging
+    if (import.meta.env.DEV) {
+      console.log('🔍 agGridRowData calculation:', {
+        selectedRecordType,
+        typeRecordsLength: typeRecords?.length || 0,
+        allRecordsDatasets: {
+          baptism: allRecordsDatasets?.baptism?.length || 0,
+          marriage: allRecordsDatasets?.marriage?.length || 0,
+          funeral: allRecordsDatasets?.funeral?.length || 0,
+        },
+        filteredAndSortedRecordsLength: filteredAndSortedRecords.length,
+        usingAllRecordsDatasets: !!(allRecordsDatasets && allRecordsDatasets[selectedRecordType as keyof typeof allRecordsDatasets]),
+      });
+      
+      // Warning if using fallback instead of allRecordsDatasets
+      if (!allRecordsDatasets || !allRecordsDatasets[selectedRecordType as keyof typeof allRecordsDatasets]) {
+        console.warn('⚠️ agGridRowData: Using filteredAndSortedRecords fallback instead of allRecordsDatasets', {
+          selectedRecordType,
+          filteredAndSortedRecordsLength: filteredAndSortedRecords.length,
+        });
+      }
+    }
+    
     if (!typeRecords || typeRecords.length === 0) {
       return [];
     }
     return typeRecords;
-  }, [selectedRecordType, getRecordsForType]);
+  }, [selectedRecordType, getRecordsForType, allRecordsDatasets, filteredAndSortedRecords.length]);
 
   // Column definitions for AG Grid
   const agGridColumnDefs: ColDef[] = useMemo(() => {
@@ -1848,6 +2055,7 @@ const BaptismRecordsPage: React.FC = () => {
             borderColor: enhancedTableState.tokens.border,
             borderBottomColor: enhancedTableState.tokens.border,
             fontWeight: 'bold',
+            fontSize: '16px',
           };
         } else {
           col.headerStyle = {
@@ -1857,6 +2065,7 @@ const BaptismRecordsPage: React.FC = () => {
             color: enhancedTableState.tokens.headerText,
             borderColor: enhancedTableState.tokens.border,
             borderBottomColor: enhancedTableState.tokens.border,
+            fontSize: '16px',
           };
         }
       });
@@ -1864,6 +2073,32 @@ const BaptismRecordsPage: React.FC = () => {
     return cols;
   }, [selectedRecordType, getAGGridColumnDefinitions, enhancedTableState.tokens]);
 
+  // Log when advanced view should render with data - MOVED HERE to avoid TDZ error
+  // Must be after agGridRowData and agGridColumnDefs declarations
+  const prevAgGridDataRef = useRef({ rowDataLength: 0, columnDefsLength: 0 });
+  useEffect(() => {
+    if (viewMode === 'advanced' && selectedRecordType) {
+      const currentData = {
+        rowDataLength: agGridRowData?.length || 0,
+        columnDefsLength: agGridColumnDefs?.length || 0,
+      };
+      if (
+        prevAgGridDataRef.current.rowDataLength !== currentData.rowDataLength ||
+        prevAgGridDataRef.current.columnDefsLength !== currentData.columnDefsLength
+      ) {
+        console.log('✅ Advanced view ready to render:', {
+          viewMode,
+          selectedRecordType,
+          selectedChurch,
+          agGridRowDataLength: currentData.rowDataLength,
+          agGridColumnDefsLength: currentData.columnDefsLength,
+          loading,
+        });
+        prevAgGridDataRef.current = currentData;
+      }
+    }
+  }, [viewMode, selectedRecordType, selectedChurch, agGridRowData?.length, agGridColumnDefs?.length, loading]);
+
   // Default column properties for AG Grid
   const agGridDefaultColDef: ColDef = useMemo(() => ({
     sortable: true,
@@ -1877,6 +2112,7 @@ const BaptismRecordsPage: React.FC = () => {
       color: enhancedTableState.tokens.headerText,
       borderColor: enhancedTableState.tokens.border,
       fontWeight: 'bold',
+      fontSize: '16px',
     },
     cellStyle: (params: any) => {
       const rowIndex = params.node?.rowIndex ?? 0;
@@ -1886,29 +2122,97 @@ const BaptismRecordsPage: React.FC = () => {
           : enhancedTableState.tokens.rowOddBg,
         color: enhancedTableState.tokens.cellText,
         borderColor: enhancedTableState.tokens.border,
+        fontSize: '15px',
       };
     },
   }), [enhancedTableState.tokens]);
 
   // Grid ready event for AG Grid
   const onAGGridReady = useCallback((params: GridReadyEvent) => {
+    // Check if grid is already destroyed
+    if (params.api.isDestroyed()) {
+      console.warn('AG Grid is already destroyed, skipping initialization');
+      return;
+    }
+    
     setGridApi(params.api);
-    // Apply default sort
-    const defaultSort = DEFAULT_SORT[selectedRecordType] || DEFAULT_SORT.baptism;
-    if (typeof params.api.applyColumnState === 'function') {
-      params.api.applyColumnState({
-        state: [{ colId: defaultSort.field, sort: defaultSort.dir }],
-        defaultState: { sort: null }
-      });
+    
+    // Apply default sort with safety check
+    try {
+      const defaultSort = DEFAULT_SORT[selectedRecordType] || DEFAULT_SORT.baptism;
+      if (typeof params.api.applyColumnState === 'function' && !params.api.isDestroyed()) {
+        params.api.applyColumnState({
+          state: [{ colId: defaultSort.field, sort: defaultSort.dir }],
+          defaultState: { sort: null }
+        });
+      }
+    } catch (error) {
+      console.error('Error applying column state:', error);
     }
   }, [selectedRecordType]);
+  
+  // Cleanup grid API when component unmounts or grid is destroyed
+  useEffect(() => {
+    return () => {
+      if (gridApi && !gridApi.isDestroyed()) {
+        try {
+          // Don't call destroy here as React will handle unmounting
+          // Just clear the reference
+          setGridApi(null);
+        } catch (error) {
+          console.error('Error cleaning up grid API:', error);
+        }
+      }
+    };
+  }, [gridApi]);
 
   // Ensure we have data - fetch if needed when switching to advanced view
   useEffect(() => {
-    if (viewMode === 'advanced' && (!allRecordsDatasets.baptism.length || !allRecordsDatasets.marriage.length || !allRecordsDatasets.funeral.length)) {
+    // Only run when switching to advanced view and church is selected
+    if (viewMode !== 'advanced' || !selectedChurch) {
+      return;
+    }
+
+    // Guard: Skip if already loaded for this church (use ref to avoid re-renders)
+    if (loadedForChurch.current === selectedChurch) {
+      const currentDatasets = datasetsRef.current;
+      if (currentDatasets.baptism.length > 0 && 
+          currentDatasets.marriage.length > 0 && 
+          currentDatasets.funeral.length > 0) {
+        // Data already loaded, no need to log or fetch
+        return;
+      }
+    }
+
+    // Guard: Skip if fetch is already in progress
+    if (fetchInFlight.current) {
+      return;
+    }
+
+    // Only fetch if we actually need data (use ref to check current state)
+    const currentDatasets = datasetsRef.current;
+    const needsData = !currentDatasets.baptism.length || !currentDatasets.marriage.length || !currentDatasets.funeral.length;
+    if (needsData) {
+      console.log('📊 Fetching all record types for advanced view...');
       fetchAllRecordTypes(selectedChurch);
     }
-  }, [viewMode, selectedChurch, allRecordsDatasets, fetchAllRecordTypes]);
+  }, [viewMode, selectedChurch, fetchAllRecordTypes]);
+  
+  // Cleanup: Cancel any in-flight requests when component unmounts or church changes
+  useEffect(() => {
+    // Reset loaded church when church changes to allow reloading
+    if (loadedForChurch.current !== selectedChurch) {
+      loadedForChurch.current = null;
+    }
+    
+    return () => {
+      if (abortControllerRef.current) {
+        abortControllerRef.current.abort();
+        abortControllerRef.current = null;
+      }
+      fetchInFlight.current = false;
+    };
+  }, [selectedChurch]);
 
   // Paginated records
   const paginatedRecords = useMemo(() => {
@@ -1956,6 +2260,85 @@ const BaptismRecordsPage: React.FC = () => {
     setPage(0);
   };
 
+  const handleQuickFactsOpen = useCallback(() => {
+    setQuickFactsOpen(true);
+    setQuickFactsLoading(true);
+    
+    // Compute facts from current filtered records
+    const currentRecords = filteredAndSortedRecords;
+    if (currentRecords.length > 0 && selectedRecordType) {
+      try {
+        const facts = computeQuickFacts(
+          selectedRecordType as 'baptism' | 'marriage' | 'funeral',
+          currentRecords
+        );
+        setQuickFacts(facts);
+      } catch (err) {
+        console.error('Error computing quick facts:', err);
+        setQuickFacts({ sections: [], isEmpty: true });
+      }
+    } else {
+      setQuickFacts({ sections: [], isEmpty: true });
+    }
+    
+    setQuickFactsLoading(false);
+  }, [filteredAndSortedRecords, selectedRecordType]);
+
+  const handleQuickFactsClose = useCallback(() => {
+    setQuickFactsOpen(false);
+  }, []);
+
+  const handleInteractiveReportComplete = useCallback(async (config: InteractiveReportConfig) => {
+    try {
+      const response = await fetch(`/api/records/interactive-reports`, {
+        method: 'POST',
+        headers: {
+          'Content-Type': 'application/json',
+        },
+        credentials: 'include', // Include cookies for session-based auth
+        body: JSON.stringify({
+          churchId: selectedChurch,
+          ...config,
+        }),
+      });
+
+      if (!response.ok) {
+        let errorMessage = 'Failed to create interactive report';
+        try {
+          const error = await response.json();
+          errorMessage = error.message || errorMessage;
+        } catch {
+          // If response isn't JSON, use status text
+          errorMessage = response.statusText || errorMessage;
+        }
+        
+        if (response.status === 404) {
+          errorMessage = 'Interactive reports API endpoint not found. Please ensure the backend routes are mounted.';
+        } else if (response.status === 401 || response.status === 403) {
+          errorMessage = 'You do not have permission to create interactive reports.';
+        }
+        
+        throw new Error(errorMessage);
+      }
+
+      const result = await response.json();
+      
+      // Show success message with link to view jobs
+      showToast(
+        `Report created successfully! ${result.jobId ? 'Click here to view job status.' : ''}`,
+        'success'
+      );
+      
+      // Navigate to review screen (user can access jobs page from menu)
+      navigate(`/apps/records/interactive-reports/${result.id}`);
+    } catch (error: any) {
+      console.error('Error creating interactive report:', error);
+      // Show user-friendly error message
+      alert(`Failed to create report: ${error.message || 'Unknown error'}\n\nIf you see a 404 error, the backend routes may not be mounted. See INTEGRATION_GUIDE.md for setup instructions.`);
+      throw error;
+    }
+  }, [selectedChurch, navigate]);
+
   const handleAddRecord = () => {
     setEditingRecord(null);
     
@@ -2013,11 +2396,32 @@ const BaptismRecordsPage: React.FC = () => {
     setEditingRecord(record);
     setEditingRecords([]); // Clear multiple records array when editing single record
     setCurrentEditingIndex(0);
-    // Map record fields to formData, ensuring entry_type is included
-    setFormData({
-      ...record,
-      entry_type: record.entry_type || record.entryType || 'Baptism',
-    });
+    
+    // Map record fields to formData, ensuring entry_type is included and dates are normalized
+    if (selectedRecordType === 'baptism') {
+      setFormData({
+        ...record,
+        entry_type: record.entry_type || record.entryType || 'Baptism',
+        // Normalize date fields for HTML5 date inputs (requires YYYY-MM-DD format)
+        dateOfBirth: normalizeDateForInput(record.dateOfBirth || record.birth_date || record.birthDate),
+        dateOfBaptism: normalizeDateForInput(record.dateOfBaptism || record.reception_date || record.baptismDate),
+      });
+    } else if (selectedRecordType === 'marriage') {
+      setFormData({
+        ...record,
+        // Normalize date fields for HTML5 date inputs (requires YYYY-MM-DD format)
+        mdate: normalizeDateForInput((record as any).mdate || (record as any).marriageDate),
+      });
+    } else if (selectedRecordType === 'funeral') {
+      setFormData({
+        ...record,
+        // Normalize date fields for HTML5 date inputs (requires YYYY-MM-DD format)
+        dateOfDeath: normalizeDateForInput((record as any).dateOfDeath || (record as any).deceased_date),
+        burialDate: normalizeDateForInput((record as any).burialDate || (record as any).burial_date),
+      });
+    } else {
+      setFormData({ ...record });
+    }
     setDialogOpen(true);
   };
 
@@ -2026,13 +2430,13 @@ const BaptismRecordsPage: React.FC = () => {
     setViewingRecord(record);
     setEditingRecord(null); // Clear editing mode
 
-    // Set form data for display - handle both field name formats
+    // Set form data for display - handle both field name formats and normalize dates
     const formDataToSet: any = {};
     if (selectedRecordType === 'baptism') {
       formDataToSet.firstName = record.firstName || record.first_name || '';
       formDataToSet.lastName = record.lastName || record.last_name || '';
-      formDataToSet.dateOfBirth = record.dateOfBirth || record.birth_date || '';
-      formDataToSet.dateOfBaptism = record.dateOfBaptism || record.reception_date || '';
+      formDataToSet.dateOfBirth = normalizeDateForInput(record.dateOfBirth || record.birth_date || record.birthDate);
+      formDataToSet.dateOfBaptism = normalizeDateForInput(record.dateOfBaptism || record.reception_date || record.baptismDate);
       formDataToSet.placeOfBirth = record.placeOfBirth || record.birthplace || '';
       formDataToSet.entry_type = record.entry_type || record.entryType || '';
       formDataToSet.godparentNames = record.godparentNames || record.sponsors || '';
@@ -2044,7 +2448,7 @@ const BaptismRecordsPage: React.FC = () => {
       formDataToSet.lname_groom = record.lname_groom || '';
       formDataToSet.fname_bride = record.fname_bride || '';
       formDataToSet.lname_bride = record.lname_bride || '';
-      formDataToSet.mdate = record.mdate || record.marriageDate || '';
+      formDataToSet.mdate = normalizeDateForInput(record.mdate || record.marriageDate);
       formDataToSet.parentsg = record.parentsg || record.groomParents || '';
       formDataToSet.parentsb = record.parentsb || record.brideParents || '';
       formDataToSet.witness = record.witness || record.witnesses || '';
@@ -2293,8 +2697,8 @@ const BaptismRecordsPage: React.FC = () => {
       setFormData({
         firstName: (firstRecord as any).firstName || (firstRecord as any).name || '',
         lastName: (firstRecord as any).lastName || (firstRecord as any).lastname || '',
-        dateOfDeath: (firstRecord as any).dateOfDeath || (firstRecord as any).deceased_date || '',
-        burialDate: (firstRecord as any).burialDate || (firstRecord as any).burial_date || '',
+        dateOfDeath: normalizeDateForInput((firstRecord as any).dateOfDeath || (firstRecord as any).deceased_date),
+        burialDate: normalizeDateForInput((firstRecord as any).burialDate || (firstRecord as any).burial_date),
         age: (firstRecord as any).age || '',
         burialLocation: (firstRecord as any).burialLocation || (firstRecord as any).burial_location || '',
         priest: (firstRecord as any).priest || (firstRecord as any).clergy || '',
@@ -2789,9 +3193,10 @@ const BaptismRecordsPage: React.FC = () => {
           customPriest: false,
         });
       }
-    } catch (err) {
+    } catch (err: any) {
       console.error('Save error:', err);
-      showToast('Failed to save record', 'error');
+      const errorMessage = err?.message || err?.response?.data?.message || 'Failed to save record. Please check the console for details.';
+      showToast(errorMessage, 'error');
     } finally {
       setLoading(false);
     }
@@ -2823,13 +3228,13 @@ const BaptismRecordsPage: React.FC = () => {
         const newRecord = editingRecords[newIndex];
         setEditingRecord(newRecord);
         
-        // Populate form with new record data
+        // Populate form with new record data - normalize dates for HTML5 date inputs
         if (selectedRecordType === 'baptism') {
           setFormData({
             firstName: (newRecord as any).firstName || (newRecord as any).first_name || '',
             lastName: (newRecord as any).lastName || (newRecord as any).last_name || '',
-            dateOfBirth: (newRecord as any).dateOfBirth || (newRecord as any).birth_date || '',
-            dateOfBaptism: (newRecord as any).dateOfBaptism || (newRecord as any).reception_date || '',
+            dateOfBirth: normalizeDateForInput((newRecord as any).dateOfBirth || (newRecord as any).birth_date || (newRecord as any).birthDate),
+            dateOfBaptism: normalizeDateForInput((newRecord as any).dateOfBaptism || (newRecord as any).reception_date || (newRecord as any).baptismDate),
             placeOfBirth: (newRecord as any).placeOfBirth || (newRecord as any).birthplace || '',
             entry_type: (newRecord as any).entry_type || (newRecord as any).entryType || '',
             fatherName: (newRecord as any).fatherName || (newRecord as any).father_name || '',
@@ -2844,7 +3249,7 @@ const BaptismRecordsPage: React.FC = () => {
             lname_groom: (newRecord as any).lname_groom || '',
             fname_bride: (newRecord as any).fname_bride || '',
             lname_bride: (newRecord as any).lname_bride || '',
-            mdate: (newRecord as any).mdate || (newRecord as any).marriageDate || '',
+            mdate: normalizeDateForInput((newRecord as any).mdate || (newRecord as any).marriageDate),
             parentsg: (newRecord as any).parentsg || '',
             parentsb: (newRecord as any).parentsb || '',
             witness: (newRecord as any).witness || '',
@@ -2856,8 +3261,8 @@ const BaptismRecordsPage: React.FC = () => {
           setFormData({
             firstName: (newRecord as any).firstName || (newRecord as any).name || '',
             lastName: (newRecord as any).lastName || (newRecord as any).lastname || '',
-            dateOfDeath: (newRecord as any).dateOfDeath || (newRecord as any).deceased_date || '',
-            burialDate: (newRecord as any).burialDate || (newRecord as any).burial_date || '',
+            dateOfDeath: normalizeDateForInput((newRecord as any).dateOfDeath || (newRecord as any).deceased_date),
+            burialDate: normalizeDateForInput((newRecord as any).burialDate || (newRecord as any).burial_date),
             age: (newRecord as any).age || '',
             burialLocation: (newRecord as any).burialLocation || (newRecord as any).burial_location || '',
             priest: (newRecord as any).priest || (newRecord as any).clergy || '',
@@ -2883,13 +3288,13 @@ const BaptismRecordsPage: React.FC = () => {
       const newRecord = filteredAndSortedRecords[newIndex];
       setEditingRecord(newRecord as BaptismRecord);
       
-      // Populate form with new record data
+      // Populate form with new record data - normalize dates for HTML5 date inputs
       if (selectedRecordType === 'baptism') {
         setFormData({
           firstName: (newRecord as any).firstName || (newRecord as any).first_name || '',
           lastName: (newRecord as any).lastName || (newRecord as any).last_name || '',
-          dateOfBirth: (newRecord as any).dateOfBirth || (newRecord as any).birth_date || '',
-          dateOfBaptism: (newRecord as any).dateOfBaptism || (newRecord as any).reception_date || '',
+          dateOfBirth: normalizeDateForInput((newRecord as any).dateOfBirth || (newRecord as any).birth_date || (newRecord as any).birthDate),
+          dateOfBaptism: normalizeDateForInput((newRecord as any).dateOfBaptism || (newRecord as any).reception_date || (newRecord as any).baptismDate),
           placeOfBirth: (newRecord as any).placeOfBirth || (newRecord as any).birthplace || '',
           entry_type: (newRecord as any).entry_type || (newRecord as any).entryType || '',
           fatherName: (newRecord as any).fatherName || (newRecord as any).father_name || '',
@@ -2904,7 +3309,7 @@ const BaptismRecordsPage: React.FC = () => {
           lname_groom: (newRecord as any).lname_groom || '',
           fname_bride: (newRecord as any).fname_bride || '',
           lname_bride: (newRecord as any).lname_bride || '',
-          mdate: (newRecord as any).mdate || (newRecord as any).marriageDate || '',
+          mdate: normalizeDateForInput((newRecord as any).mdate || (newRecord as any).marriageDate),
           parentsg: (newRecord as any).parentsg || '',
           parentsb: (newRecord as any).parentsb || '',
           witness: (newRecord as any).witness || '',
@@ -2916,8 +3321,8 @@ const BaptismRecordsPage: React.FC = () => {
         setFormData({
           firstName: (newRecord as any).firstName || (newRecord as any).name || '',
           lastName: (newRecord as any).lastName || (newRecord as any).lastname || '',
-          dateOfDeath: (newRecord as any).dateOfDeath || (newRecord as any).deceased_date || '',
-          burialDate: (newRecord as any).burialDate || (newRecord as any).burial_date || '',
+          dateOfDeath: normalizeDateForInput((newRecord as any).dateOfDeath || (newRecord as any).deceased_date),
+          burialDate: normalizeDateForInput((newRecord as any).burialDate || (newRecord as any).burial_date),
           age: (newRecord as any).age || '',
           burialLocation: (newRecord as any).burialLocation || (newRecord as any).burial_location || '',
           priest: (newRecord as any).priest || (newRecord as any).clergy || '',
@@ -2932,8 +3337,9 @@ const BaptismRecordsPage: React.FC = () => {
     showToast('Export functionality coming soon', 'info');
   };
 
-  const formatDate = (dateString: string) => {
-    return new Date(dateString).toLocaleDateString();
+  // Use centralized date formatter
+  const formatDate = (dateString: string | null | undefined) => {
+    return formatRecordDate(dateString) || '';
   };
 
   // Effect to update church selection when theme changes
@@ -2981,543 +3387,103 @@ const BaptismRecordsPage: React.FC = () => {
   }
 
   return (
-    <Box sx={{
-      width: '100%',
-      maxWidth: 'none',
-      minHeight: '100vh',
-      flex: 1,
-      pb: 4,
-      backgroundColor: theme.palette.mode === 'dark' 
-        ? theme.palette.background.default 
-        : theme.palette.background.paper,
-      '& .MuiContainer-root': {
-        maxWidth: 'none !important',
-        paddingLeft: 0,
-        paddingRight: 0
-      }
-    }}>
+    <RecordsErrorBoundary
+      recordType={selectedRecordType}
+      viewMode={viewMode}
+      autoShrinkEnabled={autoShrinkEnabled}
+    >
+      <Box sx={{
+        width: '100%',
+        maxWidth: 'none',
+        height: 'calc(100vh - 120px)',
+        minHeight: 0,
+        display: 'flex',
+        flexDirection: 'column',
+        backgroundColor: theme.palette.mode === 'dark' 
+          ? theme.palette.background.default 
+          : theme.palette.background.paper,
+        '& .MuiContainer-root': {
+          maxWidth: 'none !important',
+          paddingLeft: 0,
+          paddingRight: 0
+        }
+      }}>
       {/* Records Table - Only show when record type is selected */}
       <Card sx={{ 
+        flex: 1,
+        display: 'flex',
+        flexDirection: 'column',
         mb: 3,
+        minHeight: 0,
+        minWidth: 0,
+        height: '100%',
+        overflow: 'visible',
         backgroundColor: theme.palette.mode === 'dark' 
           ? theme.palette.background.paper 
           : theme.palette.background.paper,
       }}>
-        <CardContent sx={{ pb: 1 }}>
+        <CardContent sx={{ 
+          flex: 1,
+          display: 'flex',
+          flexDirection: 'column',
+          padding: 0,
+          minHeight: 0,
+          minWidth: 0,
+          height: '100%',
+          overflow: 'visible',
+          '&:last-child': {
+            paddingBottom: 0
+          }
+        }}>
           {/* Collapse/Expand Button */}
 {/* Collapsible Content */}
           <Collapse in={!isFiltersCollapsed}>
-            <Box>
+            <Box sx={{ 
+              flex: 1, 
+              display: 'flex', 
+              flexDirection: 'column', 
+              minHeight: 0,
+              minWidth: 0,
+              height: '100%',
+              overflow: 'visible'
+            }}>
               {/* Removed duplicate Records Management header (Editor integration) */}
 
           
-              <Stack spacing={2}>
-                {/* Removed duplicate selector and action rows (kept single header/banner section below) */}
-
-                {/* Full Header Bar with Images - No Gold Borders */}
-                <Box
-                  sx={{
-                    width: '100%',
-                    mb: 3,
-                    position: 'relative',
-                    display: 'flex',
-                    alignItems: 'stretch',
-                    height: 200,
-                    maxHeight: 200,
-                    backgroundImage: recordSettings?.backgroundImage?.enabled !== false ? `url(/images/records/${selectedChurch}-bg.png)` : 'none',
-                    backgroundRepeat: 'repeat',
-                    backgroundSize: 'auto',
-                    backgroundPosition: 'top left',
-                    overflow: 'hidden',
-                    borderRadius: 2,
-                  }}
-                >
-                  {/* Gradient Overlay using g1.png */}
-                  {recordSettings?.g1Image?.enabled !== false && (
-                    <Box
-                      sx={{
-                        position: 'absolute',
-                        top: 0,
-                        left: 0,
-                        right: 0,
-                        bottom: 0,
-                        backgroundImage: `url(/images/records/g1.png)`,
-                        backgroundSize: 'cover',
-                        backgroundPosition: 'center',
-                        opacity: 0.85,
-                        zIndex: 0,
-                      }}
-                    />
-                  )}
-
-                  {/* Main Header Content Area - 4 Column Layout */}
-                  <Box
-                    sx={{
-                      width: '100%',
-                      p: 2,
-                      display: 'grid',
-                      gridTemplateColumns: '1fr 1fr 1fr 1fr',
-                      gap: 2,
-                      alignItems: 'stretch',
-                      position: 'relative',
-                      zIndex: 1,
-                      height: '100%',
-                    }}
-                  >
-                    {/* Record Type Image Selector - Using recordSettings.recordImages.column */}
-                    <Box
+              <Box sx={{ 
+                flex: 1, 
+                display: 'flex', 
+                flexDirection: 'column', 
+                minHeight: 0,
+                minWidth: 0,
+                height: '100%',
+                overflow: 'visible'
+              }}>
+                {/* Collapsible Actions Section */}
+                {selectedRecordType && (
+                  <Box sx={{ mb: 2, flexShrink: 0 }}>
+                    <Button
+                      onClick={() => setButtonsExpanded(!buttonsExpanded)}
+                      endIcon={buttonsExpanded ? <IconChevronUp /> : <IconChevronDown />}
                       sx={{
-                        display: 'flex',
-                        flexDirection: 'column',
-                        alignItems: 'center',
-                        justifyContent: 'center',
-                        gridColumn: recordSettings?.recordImages?.column || 1,
-                        position: 'relative',
-                        height: '100%',
-                        zIndex: 2,
-                      }}
-                    >
-                      {(() => {
-                          // Determine next record type in cycle: baptism -> marriage -> funeral -> baptism
-                          const getNextRecordType = (current: string) => {
-                            if (current === 'baptism') return 'marriage';
-                            if (current === 'marriage') return 'funeral';
-                            return 'baptism';
-                          };
-                          
-                          const getPathForType = (type: string) => {
-                            if (type === 'baptism') return '/apps/records/baptism';
-                            if (type === 'marriage') return '/apps/records/marriage';
-                            return '/apps/records/funeral';
-                          };
-                          
-                          const getNextPath = (current: string) => {
-                            if (current === 'baptism') return '/apps/records/marriage';
-                            if (current === 'marriage') return '/apps/records/funeral';
-                            return '/apps/records/baptism';
-                          };
-                          
-                          const getNextIndex = (current: string) => {
-                            if (current === 'baptism') return 1;
-                            if (current === 'marriage') return 2;
-                            return 0;
-                          };
-                          
-                          const getAllRecordTypes = () => {
-                            const types = ['baptism', 'marriage', 'funeral'];
-                            const currentIndex = types.indexOf(selectedRecordType);
-                            return {
-                              current: selectedRecordType,
-                              next: types[(currentIndex + 1) % types.length],
-                              prev: types[(currentIndex - 1 + types.length) % types.length],
-                            };
-                          };
-                          
-                          const nextType = getNextRecordType(selectedRecordType);
-                          const nextPath = getNextPath(selectedRecordType);
-                          const nextIndex = getNextIndex(selectedRecordType);
-                          const recordTypes = getAllRecordTypes();
-                          
-                          const imageMap: Record<string, string> = {
-                            baptism: '/images/records/baptism.png',
-                            marriage: '/images/records/marriage.png',
-                            funeral: '/images/records/funeral.png',
-                          };
-                          
-                          const labelMap: Record<string, string> = {
-                            baptism: 'Baptism',
-                            marriage: 'Marriage',
-                            funeral: 'Funeral',
-                          };
-                          
-                          return (
-                            <Box
-                              sx={{
-                                position: 'relative',
-                                display: 'flex',
-                                flexDirection: 'column',
-                                alignItems: 'center',
-                                gap: 1.5,
-                                padding: 2,
-                              }}
-                              onMouseEnter={() => setIsRecordTypeHovered(true)}
-                              onMouseLeave={() => setIsRecordTypeHovered(false)}
-                            >
-                              {/* Hover Preview - Show other record types on sides */}
-                              {isRecordTypeHovered && (
-                                <>
-                                  <Box
-                                    component="img"
-                                    src={imageMap[recordTypes.next]}
-                                    alt={labelMap[recordTypes.next]}
-                                    onClick={(e) => {
-                                      e.stopPropagation();
-                                      setSelectedRecordType(recordTypes.next);
-                                      setDisplayedRecordIndex((recordTypes.next === 'baptism' ? 0 : recordTypes.next === 'marriage' ? 1 : 2));
-                                      const params = new URLSearchParams();
-                                      params.set('church', String(selectedChurch));
-                                      params.set('type', recordTypes.next);
-                                      if (viewMode === 'advanced') {
-                                        params.set('view', 'advanced');
-                                      }
-                                      const nextPath = getPathForType(recordTypes.next);
-                                      navigate(`${nextPath}?${params.toString()}`);
-                                    }}
-                                    sx={{
-                                      position: 'absolute',
-                                      left: '-90px',
-                                      top: '50%',
-                                      transform: 'translateY(-50%)',
-                                      width: '70px',
-                                      height: '70px',
-                                      objectFit: 'contain',
-                                      cursor: 'pointer',
-                                      backgroundColor: '#ffffff',
-                                      border: '3px solid #A78BFA',
-                                      borderRadius: '12px',
-                                      padding: '8px',
-                                      boxShadow: '0 6px 20px rgba(167, 139, 250, 0.35)',
-                                      transition: 'all 0.3s cubic-bezier(0.4, 0, 0.2, 1)',
-                                      opacity: 0,
-                                      animation: 'slideInLeft 0.3s ease forwards',
-                                      zIndex: 5,
-                                      '&:hover': {
-                                        transform: 'translateY(-50%) scale(1.1)',
-                                        borderColor: '#8B5CF6',
-                                        boxShadow: '0 8px 24px rgba(139, 92, 246, 0.5)',
-                                      },
-                                      '@keyframes slideInLeft': {
-                                        from: {
-                                          opacity: 0,
-                                          transform: 'translateY(-50%) translateX(-20px)',
-                                        },
-                                        to: {
-                                          opacity: 1,
-                                          transform: 'translateY(-50%) translateX(0)',
-                                        },
-                                      },
-                                    }}
-                                  />
-                                  <Box
-                                    component="img"
-                                    src={imageMap[recordTypes.prev]}
-                                    alt={labelMap[recordTypes.prev]}
-                                    onClick={(e) => {
-                                      e.stopPropagation();
-                                      setSelectedRecordType(recordTypes.prev);
-                                      setDisplayedRecordIndex((recordTypes.prev === 'baptism' ? 0 : recordTypes.prev === 'marriage' ? 1 : 2));
-                                      const params = new URLSearchParams();
-                                      params.set('church', String(selectedChurch));
-                                      params.set('type', recordTypes.prev);
-                                      if (viewMode === 'advanced') {
-                                        params.set('view', 'advanced');
-                                      }
-                                      const prevPath = getPathForType(recordTypes.prev);
-                                      navigate(`${prevPath}?${params.toString()}`);
-                                    }}
-                                    sx={{
-                                      position: 'absolute',
-                                      right: '-90px',
-                                      top: '50%',
-                                      transform: 'translateY(-50%)',
-                                      width: '70px',
-                                      height: '70px',
-                                      objectFit: 'contain',
-                                      cursor: 'pointer',
-                                      backgroundColor: '#ffffff',
-                                      border: '3px solid #A78BFA',
-                                      borderRadius: '12px',
-                                      padding: '8px',
-                                      boxShadow: '0 6px 20px rgba(167, 139, 250, 0.35)',
-                                      transition: 'all 0.3s cubic-bezier(0.4, 0, 0.2, 1)',
-                                      opacity: 0,
-                                      animation: 'slideInRight 0.3s ease forwards',
-                                      zIndex: 5,
-                                      '&:hover': {
-                                        transform: 'translateY(-50%) scale(1.1)',
-                                        borderColor: '#8B5CF6',
-                                        boxShadow: '0 8px 24px rgba(139, 92, 246, 0.5)',
-                                      },
-                                      '@keyframes slideInRight': {
-                                        from: {
-                                          opacity: 0,
-                                          transform: 'translateY(-50%) translateX(20px)',
-                                        },
-                                        to: {
-                                          opacity: 1,
-                                          transform: 'translateY(-50%) translateX(0)',
-                                        },
-                                      },
-                                    }}
-                                  />
-                                </>
-                              )}
-                              
-                              {/* Main Record Type Image */}
-                              <Box
-                                component="img"
-                                src={imageMap[selectedRecordType]}
-                                alt={`${labelMap[selectedRecordType]} Records`}
-                                onClick={() => {
-                                  setSelectedRecordType(nextType);
-                                  setDisplayedRecordIndex(nextIndex);
-                                  const params = new URLSearchParams();
-                                  params.set('church', String(selectedChurch));
-                                  params.set('type', nextType);
-                                  if (viewMode === 'advanced') {
-                                    params.set('view', 'advanced');
-                                  }
-                                  navigate(`${nextPath}?${params.toString()}`);
-                                }}
-                                sx={{
-                                  width: '100px',
-                                  height: '100px',
-                                  objectFit: 'contain',
-                                  cursor: 'pointer',
-                                  backgroundColor: '#ffffff',
-                                  border: '4px solid #4C1D95',
-                                  borderRadius: '16px',
-                                  padding: '12px',
-                                  transition: 'all 0.4s cubic-bezier(0.4, 0, 0.2, 1)',
-                                  boxShadow: '0 8px 24px rgba(76, 29, 149, 0.25), inset 0 1px 0 rgba(255, 255, 255, 0.5)',
-                                  zIndex: 3,
-                                  position: 'relative',
-                                  '&:hover': {
-                                    transform: 'scale(1.15) rotate(5deg)',
-                                    boxShadow: '0 12px 32px rgba(76, 29, 149, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.6)',
-                                    borderColor: '#6D28D9',
-                                    zIndex: 10,
-                                  },
-                                  '&:active': {
-                                    transform: 'scale(1.05) rotate(0deg)',
-                                  },
-                                }}
-                              />
-                              
-                              {/* Label */}
-                              <Typography
-                                variant="caption"
-                                sx={{
-                                  fontWeight: 600,
-                                  color: '#4C1D95',
-                                  textTransform: 'uppercase',
-                                  letterSpacing: '0.1em',
-                                  fontSize: '0.75rem',
-                                  transition: 'color 0.3s ease',
-                                  position: 'relative',
-                                  zIndex: 3,
-                                }}
-                              >
-                                {labelMap[selectedRecordType]}
-                              </Typography>
-                            </Box>
-                          );
-                        })()}
-                      </Box>
-
-                    {/* Column 2: Calendar Cards - Horizontal Layout */}
-                    {recordSettings?.calendar?.enabled !== false && (
-                      <Box
-                        sx={{
-                          display: 'flex',
-                          flexDirection: 'row',
-                          alignItems: 'center',
-                          justifyContent: 'center',
-                          gridColumn: recordSettings?.calendar?.column || 2,
-                          position: 'relative',
-                          height: '100%',
-                          zIndex: 2,
-                          gap: 1,
-                        }}
-                      >
-                      {(() => {
-                        const today = new Date();
-                        const nextDays: Date[] = [];
-                        
-                        // Find the next 3 days starting from calendarStartOffset that fall within December 2025
-                        let dayOffset = calendarStartOffset;
-                        while (nextDays.length < 3 && dayOffset < 365) {
-                          const nextDate = new Date(today);
-                          nextDate.setDate(today.getDate() + dayOffset);
-                          
-                          // Check if this date is in December 2025
-                          if (nextDate.getMonth() === 11 && nextDate.getFullYear() === 2025) {
-                            nextDays.push(nextDate);
-                          }
-                          dayOffset++;
-                        }
-                        
-                        return nextDays.map((day, index) => (
-                          <Box
-                            key={index}
-                            onClick={() => {
-                              // Advance to show the next 3 days
-                              setCalendarStartOffset(prev => {
-                                const newOffset = prev + 3;
-                                // Check if new offset would still have days in December 2025
-                                const checkDate = new Date(today);
-                                checkDate.setDate(today.getDate() + newOffset);
-                                // If we've gone past December 2025, reset to start
-                                if (checkDate.getMonth() !== 11 || checkDate.getFullYear() !== 2025 || checkDate.getDate() > 31) {
-                                  return 1; // Reset to tomorrow
-                                }
-                                return newOffset;
-                              });
-                            }}
-                            sx={{
-                              width: '75px',
-                              height: '90px',
-                              background: 'linear-gradient(145deg, #ffffff 0%, #f5f0e6 50%, #e8dcc8 100%)',
-                              borderRadius: '12px',
-                              display: 'flex',
-                              flexDirection: 'column',
-                              alignItems: 'center',
-                              justifyContent: 'center',
-                              boxShadow: '0 8px 24px rgba(0,0,0,0.25), 0 4px 8px rgba(0,0,0,0.15)',
-                              border: '3px solid #DAA520',
-                              cursor: 'pointer',
-                              transition: 'all 0.3s ease',
-                              transform: `rotate(${(index - 1) * 3}deg)`,
-                              position: 'relative',
-                              overflow: 'hidden',
-                              '&:hover': {
-                                transform: `rotate(${(index - 1) * 3}deg) scale(1.08) translateY(-5px)`,
-                                boxShadow: '0 12px 32px rgba(0,0,0,0.35), 0 6px 12px rgba(0,0,0,0.2)',
-                              },
-                              '&::before': {
-                                content: '""',
-                                position: 'absolute',
-                                top: 0,
-                                left: 0,
-                                right: 0,
-                                height: '22px',
-                                background: 'linear-gradient(180deg, #8B0000 0%, #5c0000 100%)',
-                                borderRadius: '9px 9px 0 0',
-                              },
-                            }}
-                          >
-                            <Typography
-                              sx={{
-                                fontSize: '9px',
-                                fontWeight: 800,
-                                color: '#ffffff',
-                                textTransform: 'uppercase',
-                                letterSpacing: '0.5px',
-                                position: 'relative',
-                                zIndex: 1,
-                                mt: '-2px',
-                              }}
-                            >
-                              {day.toLocaleDateString('en-US', { month: 'short' })} {day.getFullYear()}
-                            </Typography>
-                            <Typography
-                              sx={{
-                                fontSize: '11px',
-                                fontWeight: 700,
-                                color: '#4C1D95',
-                                textTransform: 'uppercase',
-                                mt: 0.5,
-                              }}
-                            >
-                              {day.toLocaleDateString('en-US', { weekday: 'short' })}
-                            </Typography>
-                            <Typography
-                              sx={{
-                                fontSize: '28px',
-                                fontWeight: 900,
-                                color: '#1a1a2e',
-                                lineHeight: 1,
-                              }}
-                            >
-                              {day.getDate()}
-                            </Typography>
-                          </Box>
-                        ));
-                      })()}
-                      </Box>
-                    )}
-
-                    {/* Column 3: Church Logo */}
-                    {recordSettings && recordSettings.logo && recordSettings.logo.enabled !== false && (
-                      <Box
-                        sx={{
-                          display: 'flex',
-                          flexDirection: 'column',
-                          alignItems: 'center',
-                          justifyContent: 'center',
-                          gridColumn: recordSettings.logo?.column || 3,
-                          position: 'relative',
-                          height: '100%',
-                          zIndex: 2,
-                        }}
-                      >
-                        <Box
-                          component="img"
-                          src={`/images/records/${selectedChurch}-logo.png`}
-                          alt="Church Logo"
-                          onError={(e) => {
-                            (e.target as HTMLImageElement).src = '/images/records/om-logo.png';
-                          }}
-                          sx={{
-                            width: `${recordSettings.logo?.width || 200}px`,
-                            height: recordSettings.logo?.height === 'auto' ? 'auto' : `${recordSettings.logo?.height || 200}px`,
-                            objectFit: (recordSettings.logo?.objectFit || 'contain') as 'contain' | 'cover' | 'fill' | 'none' | 'scale-down',
-                            opacity: ((recordSettings.logo?.opacity || 100) / 100),
-                          }}
-                        />
-                      </Box>
-                    )}
-
-                    {/* Column 4: OM Logo */}
-                    {recordSettings?.omLogo?.enabled !== false && (
-                      <Box
-                        sx={{
-                          display: 'flex',
-                          flexDirection: 'column',
-                          alignItems: 'center',
-                          justifyContent: 'center',
-                          gridColumn: recordSettings?.omLogo?.column || 4,
-                          position: 'relative',
-                          height: '100%',
-                          zIndex: 2,
-                        }}
-                      >
-                        <Box
-                          component="img"
-                          src="/images/records/om-logo.png"
-                          alt="OM Logo"
-                          sx={{
-                            width: `${recordSettings?.omLogo?.width || 68}px`,
-                            height: recordSettings?.omLogo?.height === 'auto' ? 'auto' : `${recordSettings?.omLogo?.height || 68}px`,
-                            objectFit: 'contain',
-                          }}
-                        />
-                      </Box>
-                    )}
-                  </Box>
-                  {/* Main Header Content Area - 4 Column Layout closes above */}
-                </Box>
-                {/* Header Bar closes above */}
-
-                {/* Collapsible Actions Section */}
-                {selectedRecordType && (
-                  <Box sx={{ mb: 2 }}>
-                    <Button
-                      onClick={() => setButtonsExpanded(!buttonsExpanded)}
-                      endIcon={buttonsExpanded ? <IconChevronUp /> : <IconChevronDown />}
-                      sx={{
-                        width: '100%',
-                        justifyContent: 'space-between',
-                        backgroundColor: 'rgba(76, 29, 149, 0.08)',
-                        borderRadius: 2,
-                        px: 2,
-                        py: 1.5,
-                        textTransform: 'none',
-                        fontWeight: 600,
-                        color: '#4C1D95',
-                        '&:hover': {
-                          backgroundColor: 'rgba(76, 29, 149, 0.12)',
-                        },
+                        width: '100%',
+                        justifyContent: 'space-between',
+                        backgroundColor: theme.palette.mode === 'dark'
+                          ? 'rgba(76, 29, 149, 0.2)'
+                          : 'rgba(76, 29, 149, 0.08)',
+                        borderRadius: 2,
+                        px: 2,
+                        py: 1.5,
+                        textTransform: 'none',
+                        fontWeight: 600,
+                        color: theme.palette.mode === 'dark'
+                          ? theme.palette.text.primary
+                          : '#4C1D95',
+                        '&:hover': {
+                          backgroundColor: theme.palette.mode === 'dark'
+                            ? 'rgba(76, 29, 149, 0.3)'
+                            : 'rgba(76, 29, 149, 0.12)',
+                        },
                       }}
                     >
                       Actions
@@ -3567,17 +3533,63 @@ const BaptismRecordsPage: React.FC = () => {
                                   enhancedTableStore.setLiturgicalTheme(newTheme);
                                 }}
                                 disabled={loading}
+                                MenuProps={{
+                                  PaperProps: {
+                                    sx: {
+                                      backgroundColor: theme.palette.mode === 'dark' 
+                                        ? theme.palette.background.paper 
+                                        : theme.palette.background.paper,
+                                      '& .MuiMenuItem-root': {
+                                        color: theme.palette.mode === 'dark' 
+                                          ? theme.palette.text.primary 
+                                          : theme.palette.text.primary,
+                                        '&:hover': {
+                                          backgroundColor: theme.palette.mode === 'dark'
+                                            ? 'rgba(255, 255, 255, 0.08)'
+                                            : 'rgba(0, 0, 0, 0.04)',
+                                        },
+                                        '&.Mui-selected': {
+                                          backgroundColor: theme.palette.mode === 'dark'
+                                            ? 'rgba(76, 29, 149, 0.3)'
+                                            : 'rgba(76, 29, 149, 0.1)',
+                                          color: theme.palette.mode === 'dark'
+                                            ? theme.palette.text.primary
+                                            : '#4C1D95',
+                                          '&:hover': {
+                                            backgroundColor: theme.palette.mode === 'dark'
+                                              ? 'rgba(76, 29, 149, 0.4)'
+                                              : 'rgba(76, 29, 149, 0.15)',
+                                          },
+                                        },
+                                      },
+                                    },
+                                  },
+                                }}
                                 sx={{
                                   borderRadius: 2,
                                   backgroundColor: 'background.paper',
+                                  color: theme.palette.mode === 'dark' 
+                                    ? theme.palette.text.primary 
+                                    : theme.palette.text.primary,
                                   '& .MuiOutlinedInput-notchedOutline': {
-                                    borderColor: 'rgba(76, 29, 149, 0.3)',
+                                    borderColor: theme.palette.mode === 'dark'
+                                      ? 'rgba(255, 255, 255, 0.3)'
+                                      : 'rgba(76, 29, 149, 0.3)',
                                   },
                                   '&:hover .MuiOutlinedInput-notchedOutline': {
-                                    borderColor: 'rgba(76, 29, 149, 0.5)',
+                                    borderColor: theme.palette.mode === 'dark'
+                                      ? 'rgba(255, 255, 255, 0.5)'
+                                      : 'rgba(76, 29, 149, 0.5)',
                                   },
                                   '&.Mui-focused .MuiOutlinedInput-notchedOutline': {
-                                    borderColor: '#4C1D95',
+                                    borderColor: theme.palette.mode === 'dark'
+                                      ? theme.palette.primary.main
+                                      : '#4C1D95',
+                                  },
+                                  '& .MuiSelect-select': {
+                                    color: theme.palette.mode === 'dark' 
+                                      ? theme.palette.text.primary 
+                                      : theme.palette.text.primary,
                                   },
                                 }}
                               >
@@ -3706,8 +3718,285 @@ const BaptismRecordsPage: React.FC = () => {
                       alignItems: 'flex-end',
                       gap: 2,
                       flexWrap: 'wrap',
+                      flex: '0 0 auto',
                     }}
                   >
+                    {/* Record Type Image Selector - Moved to left of Search Records */}
+                    {(() => {
+                      // Determine next record type in cycle: baptism -> marriage -> funeral -> baptism
+                      const getNextRecordType = (current: string) => {
+                        if (current === 'baptism') return 'marriage';
+                        if (current === 'marriage') return 'funeral';
+                        return 'baptism';
+                      };
+                      
+                      const getPathForType = (type: string) => {
+                        if (type === 'baptism') return '/apps/records/baptism';
+                        if (type === 'marriage') return '/apps/records/marriage';
+                        return '/apps/records/funeral';
+                      };
+                      
+                      const getNextPath = (current: string) => {
+                        if (current === 'baptism') return '/apps/records/marriage';
+                        if (current === 'marriage') return '/apps/records/funeral';
+                        return '/apps/records/baptism';
+                      };
+                      
+                      const getNextIndex = (current: string) => {
+                        if (current === 'baptism') return 1;
+                        if (current === 'marriage') return 2;
+                        return 0;
+                      };
+                      
+                      const getAllRecordTypes = () => {
+                        const types = ['baptism', 'marriage', 'funeral'];
+                        const currentIndex = types.indexOf(selectedRecordType);
+                        return {
+                          current: selectedRecordType,
+                          next: types[(currentIndex + 1) % types.length],
+                          prev: types[(currentIndex - 1 + types.length) % types.length],
+                        };
+                      };
+                      
+                      const nextType = getNextRecordType(selectedRecordType);
+                      const nextPath = getNextPath(selectedRecordType);
+                      const nextIndex = getNextIndex(selectedRecordType);
+                      const recordTypes = getAllRecordTypes();
+                      
+                      const imageMap: Record<string, string> = {
+                        baptism: '/images/records/baptism.png',
+                        marriage: '/images/records/marriage.png',
+                        funeral: '/images/records/funeral.png',
+                      };
+                      
+                      // Fallback image for missing assets - use default icon
+                      const fallbackImage = '/images/icons/default.png';
+                      
+                      // Idempotent image error handler - prevents infinite loops
+                      const handleImgError = (e: React.SyntheticEvent<HTMLImageElement, Event>) => {
+                        const img = e.currentTarget;
+                        // Prevent infinite loops: if fallback already applied, do nothing
+                        if (img.dataset.fallbackApplied === '1') {
+                          return;
+                        }
+                        // Mark fallback as applied immediately to prevent retries
+                        img.dataset.fallbackApplied = '1';
+                        
+                        // Get current src (may be absolute URL, extract path)
+                        const currentSrc = img.src;
+                        const currentPath = currentSrc.includes('/images/') 
+                          ? currentSrc.substring(currentSrc.indexOf('/images/'))
+                          : currentSrc;
+                        
+                        // If already showing fallback, don't change
+                        if (currentPath === fallbackImage || currentPath.includes('default.png')) {
+                          return;
+                        }
+                        
+                        // Set fallback image (only once)
+                        try {
+                          img.src = fallbackImage;
+                        } catch (err) {
+                          // If fallback also fails, hide image to prevent further attempts
+                          img.style.display = 'none';
+                        }
+                      };
+                      
+                      const labelMap: Record<string, string> = {
+                        baptism: 'Baptism',
+                        marriage: 'Marriage',
+                        funeral: 'Funeral',
+                      };
+                      
+                      return (
+                        <Box
+                          sx={{
+                            position: 'relative',
+                            display: 'flex',
+                            flexDirection: 'column',
+                            alignItems: 'center',
+                            gap: 0.5,
+                            padding: 1,
+                          }}
+                          onMouseEnter={() => setIsRecordTypeHovered(true)}
+                          onMouseLeave={() => setIsRecordTypeHovered(false)}
+                        >
+                          {/* Hover Preview - Show other record types on sides */}
+                          {isRecordTypeHovered && (
+                            <>
+                              <Box
+                                component="img"
+                                src={imageMap[recordTypes.next] || fallbackImage}
+                                alt={labelMap[recordTypes.next]}
+                                onError={handleImgError}
+                                onClick={(e) => {
+                                  e.stopPropagation();
+                                  setSelectedRecordType(recordTypes.next);
+                                  setDisplayedRecordIndex((recordTypes.next === 'baptism' ? 0 : recordTypes.next === 'marriage' ? 1 : 2));
+                                  const params = new URLSearchParams();
+                                  params.set('church', String(selectedChurch));
+                                  params.set('type', recordTypes.next);
+                                  if (viewMode === 'advanced') {
+                                    params.set('view', 'advanced');
+                                  }
+                                  const nextPath = getPathForType(recordTypes.next);
+                                  navigate(`${nextPath}?${params.toString()}`);
+                                }}
+                                sx={{
+                                  position: 'absolute',
+                                  left: '-90px',
+                                  top: '50%',
+                                  transform: 'translateY(-50%)',
+                                  width: '70px',
+                                  height: '70px',
+                                  objectFit: 'contain',
+                                  cursor: 'pointer',
+                                  backgroundColor: '#ffffff',
+                                  border: '3px solid #A78BFA',
+                                  borderRadius: '12px',
+                                  padding: '8px',
+                                  boxShadow: '0 6px 20px rgba(167, 139, 250, 0.35)',
+                                  transition: 'all 0.3s cubic-bezier(0.4, 0, 0.2, 1)',
+                                  opacity: 0,
+                                  animation: 'slideInLeft 0.3s ease forwards',
+                                  zIndex: 5,
+                                  '&:hover': {
+                                    transform: 'translateY(-50%) scale(1.1)',
+                                    borderColor: '#8B5CF6',
+                                    boxShadow: '0 8px 24px rgba(139, 92, 246, 0.5)',
+                                  },
+                                  '@keyframes slideInLeft': {
+                                    from: {
+                                      opacity: 0,
+                                      transform: 'translateY(-50%) translateX(-20px)',
+                                    },
+                                    to: {
+                                      opacity: 1,
+                                      transform: 'translateY(-50%) translateX(0)',
+                                    },
+                                  },
+                                }}
+                              />
+                              <Box
+                                component="img"
+                                src={imageMap[recordTypes.prev] || fallbackImage}
+                                alt={labelMap[recordTypes.prev]}
+                                onError={handleImgError}
+                                onClick={(e) => {
+                                  e.stopPropagation();
+                                  setSelectedRecordType(recordTypes.prev);
+                                  setDisplayedRecordIndex((recordTypes.prev === 'baptism' ? 0 : recordTypes.prev === 'marriage' ? 1 : 2));
+                                  const params = new URLSearchParams();
+                                  params.set('church', String(selectedChurch));
+                                  params.set('type', recordTypes.prev);
+                                  if (viewMode === 'advanced') {
+                                    params.set('view', 'advanced');
+                                  }
+                                  const prevPath = getPathForType(recordTypes.prev);
+                                  navigate(`${prevPath}?${params.toString()}`);
+                                }}
+                                sx={{
+                                  position: 'absolute',
+                                  right: '-90px',
+                                  top: '50%',
+                                  transform: 'translateY(-50%)',
+                                  width: '70px',
+                                  height: '70px',
+                                  objectFit: 'contain',
+                                  cursor: 'pointer',
+                                  backgroundColor: '#ffffff',
+                                  border: '3px solid #A78BFA',
+                                  borderRadius: '12px',
+                                  padding: '8px',
+                                  boxShadow: '0 6px 20px rgba(167, 139, 250, 0.35)',
+                                  transition: 'all 0.3s cubic-bezier(0.4, 0, 0.2, 1)',
+                                  opacity: 0,
+                                  animation: 'slideInRight 0.3s ease forwards',
+                                  zIndex: 5,
+                                  '&:hover': {
+                                    transform: 'translateY(-50%) scale(1.1)',
+                                    borderColor: '#8B5CF6',
+                                    boxShadow: '0 8px 24px rgba(139, 92, 246, 0.5)',
+                                  },
+                                  '@keyframes slideInRight': {
+                                    from: {
+                                      opacity: 0,
+                                      transform: 'translateY(-50%) translateX(20px)',
+                                    },
+                                    to: {
+                                      opacity: 1,
+                                      transform: 'translateY(-50%) translateX(0)',
+                                    },
+                                  },
+                                }}
+                              />
+                            </>
+                          )}
+                          
+                          {/* Main Record Type Image */}
+                          <Box
+                            component="img"
+                            src={imageMap[selectedRecordType] || fallbackImage}
+                            alt={`${labelMap[selectedRecordType]} Records`}
+                            onError={handleImgError}
+                            onClick={() => {
+                              setSelectedRecordType(nextType);
+                              setDisplayedRecordIndex(nextIndex);
+                              const params = new URLSearchParams();
+                              params.set('church', String(selectedChurch));
+                              params.set('type', nextType);
+                              if (viewMode === 'advanced') {
+                                params.set('view', 'advanced');
+                              }
+                              navigate(`${nextPath}?${params.toString()}`);
+                            }}
+                            sx={{
+                              width: '60px',
+                              height: '60px',
+                              objectFit: 'contain',
+                              cursor: 'pointer',
+                              backgroundColor: '#ffffff',
+                              border: '3px solid #4C1D95',
+                              borderRadius: '12px',
+                              padding: '8px',
+                              transition: 'all 0.3s cubic-bezier(0.4, 0, 0.2, 1)',
+                              boxShadow: '0 4px 12px rgba(76, 29, 149, 0.2)',
+                              zIndex: 3,
+                              position: 'relative',
+                              '&:hover': {
+                                transform: 'scale(1.1)',
+                                boxShadow: '0 6px 16px rgba(76, 29, 149, 0.3)',
+                                borderColor: '#6D28D9',
+                                zIndex: 10,
+                              },
+                              '&:active': {
+                                transform: 'scale(1.05)',
+                              },
+                            }}
+                          />
+                          
+                          {/* Label */}
+                          <Typography
+                            variant="caption"
+                            sx={{
+                              fontWeight: 600,
+                              color: theme.palette.mode === 'dark'
+                                ? theme.palette.text.primary
+                                : '#4C1D95',
+                              textTransform: 'uppercase',
+                              letterSpacing: '0.05em',
+                              fontSize: '0.65rem',
+                              transition: 'color 0.3s ease',
+                              position: 'relative',
+                              zIndex: 3,
+                            }}
+                          >
+                            {labelMap[selectedRecordType]}
+                          </Typography>
+                        </Box>
+                      );
+                    })()}
+
                     {/* Search Records */}
                     <Box sx={{ display: 'flex', flexDirection: 'column', gap: 0.5 }}>
                       <Chip 
@@ -3845,6 +4134,76 @@ const BaptismRecordsPage: React.FC = () => {
                       </>
                     )}
 
+                    {/* Auto-shrink columns toggle - Only show in normal view */}
+                    {viewMode === 'normal' && (
+                      <FormControlLabel
+                        control={
+                          <Switch
+                            checked={autoShrinkEnabled}
+                            onChange={(e) => {
+                              const newValue = e.target.checked;
+                              setAutoShrinkEnabled(newValue);
+                              setUserPreference('normalRecordsTable.autoShrink', newValue);
+                            }}
+                            size="small"
+                            sx={{
+                              '& .MuiSwitch-switchBase.Mui-checked': {
+                                color: enhancedTableState.tokens?.accent || '#1976D2',
+                              },
+                              '& .MuiSwitch-switchBase.Mui-checked + .MuiSwitch-track': {
+                                backgroundColor: enhancedTableState.tokens?.accent || '#1976D2',
+                              },
+                            }}
+                          />
+                        }
+                        label={
+                          <Tooltip title="Fits columns to the available width">
+                            <Typography variant="body2" sx={{ fontSize: '0.75rem', fontWeight: 500 }}>
+                              Auto-shrink columns
+                            </Typography>
+                          </Tooltip>
+                        }
+                        sx={{
+                          marginLeft: 0,
+                          marginRight: 0,
+                        }}
+                      />
+                    )}
+
+                    {/* Highlight incomplete toggle */}
+                    <FormControlLabel
+                      control={
+                        <Switch
+                          checked={highlightIncomplete}
+                          onChange={(e) => {
+                            const newValue = e.target.checked;
+                            setHighlightIncomplete(newValue);
+                            setUserPreference('records.highlightIncomplete', newValue);
+                          }}
+                          size="small"
+                          sx={{
+                            '& .MuiSwitch-switchBase.Mui-checked': {
+                              color: enhancedTableState.tokens?.accent || '#1976D2',
+                            },
+                            '& .MuiSwitch-switchBase.Mui-checked + .MuiSwitch-track': {
+                              backgroundColor: enhancedTableState.tokens?.accent || '#1976D2',
+                            },
+                          }}
+                        />
+                      }
+                      label={
+                        <Tooltip title="Highlight rows with missing important fields">
+                          <Typography variant="body2" sx={{ fontSize: '0.75rem', fontWeight: 500 }}>
+                            Highlight incomplete
+                          </Typography>
+                        </Tooltip>
+                      }
+                      sx={{
+                        marginLeft: viewMode === 'normal' ? 1 : 0,
+                        marginRight: 0,
+                      }}
+                    />
+
                     {/* Switch to AG Button */}
                     <Button
                       key={`switch-to-ag-${enhancedTableState.liturgicalTheme}-${enhancedTableState.tokens?.accent}`}
@@ -3945,25 +4304,104 @@ const BaptismRecordsPage: React.FC = () => {
                         boxShadow: `0 6px 16px -6px ${enhancedTableState.tokens?.accent || '#4C1D95'}59 !important`,
                       }}
                     />
+
+                    {/* Quick Facts Button */}
+                    <Button
+                      variant="outlined"
+                      onClick={handleQuickFactsOpen}
+                      disabled={loading || filteredAndSortedRecords.length === 0}
+                      size={enhancedTableState.actionButtonConfigs?.addRecords?.size || 'small'}
+                      sx={{
+                        ml: 1,
+                        textTransform: 'none',
+                        borderColor: enhancedTableState.tokens?.accent || '#4C1D95',
+                        color: enhancedTableState.tokens?.accent || '#4C1D95',
+                        '&:hover': {
+                          borderColor: enhancedTableState.tokens?.accent || '#4C1D95',
+                          backgroundColor: `${enhancedTableState.tokens?.accent || '#4C1D95'}0A`,
+                        },
+                        '&:disabled': {
+                          borderColor: 'action.disabled',
+                          color: 'action.disabled',
+                        },
+                      }}
+                    >
+                      Quick Facts
+                    </Button>
+
+                    {/* Interactive Report Button */}
+                    <Button
+                      variant="outlined"
+                      onClick={() => setInteractiveReportOpen(true)}
+                      disabled={loading || filteredAndSortedRecords.length === 0}
+                      size={enhancedTableState.actionButtonConfigs?.addRecords?.size || 'small'}
+                      startIcon={<AssignmentIcon />}
+                      sx={{
+                        ml: 1,
+                        textTransform: 'none',
+                        borderColor: enhancedTableState.tokens?.accent || '#4C1D95',
+                        color: enhancedTableState.tokens?.accent || '#4C1D95',
+                        '&:hover': {
+                          borderColor: enhancedTableState.tokens?.accent || '#4C1D95',
+                          backgroundColor: `${enhancedTableState.tokens?.accent || '#4C1D95'}0A`,
+                        },
+                        '&:disabled': {
+                          borderColor: 'action.disabled',
+                          color: 'action.disabled',
+                        },
+                      }}
+                    >
+                      Collaborative Report
+                    </Button>
                   </Box>
                 )}
 
                 {/* Header Text - Above the table */}
-                {selectedRecordType && recordSettings?.headerText && (
+                {selectedRecordType && (
                   <Box
                     sx={{
                       mb: 2,
                       mt: 2,
                       px: 2,
+                      display: 'flex',
+                      alignItems: 'center',
+                      gap: 2,
+                      flex: '0 0 auto',
+                      minHeight: 0,
                     }}
                   >
+                    {viewMode === 'advanced' && (
+                      <Button
+                        variant="outlined"
+                        startIcon={<ChevronLeftIcon />}
+                        onClick={() => {
+                          const params = new URLSearchParams(searchParams);
+                          params.delete('view');
+                          navigate(`${location.pathname}?${params.toString()}`, { replace: true });
+                          setViewMode('normal');
+                        }}
+                        sx={{
+                          flexShrink: 0,
+                        }}
+                      >
+                        Back to Normal View
+                      </Button>
+                    )}
                     <Typography
                       variant="h6"
                       sx={{
                         fontFamily: recordSettings?.headerText?.fontFamily || 'Arial, sans-serif',
                         fontSize: `${recordSettings?.headerText?.fontSize || 16}px`,
                         fontWeight: recordSettings?.headerText?.fontWeight || 700,
-                        color: recordSettings?.headerText?.color || '#4C1D95',
+                        color: recordSettings?.headerText?.color 
+                          ? (theme.palette.mode === 'dark' 
+                              ? (recordSettings.headerText.color === '#4C1D95' || recordSettings.headerText.color === '#4c1d95'
+                                  ? theme.palette.text.primary 
+                                  : recordSettings.headerText.color)
+                              : recordSettings.headerText.color)
+                          : theme.palette.mode === 'dark' 
+                            ? theme.palette.text.primary 
+                            : '#4C1D95',
                         textTransform: 'uppercase',
                       }}
                     >
@@ -3971,7 +4409,9 @@ const BaptismRecordsPage: React.FC = () => {
                         const recordTypeLabel = recordTypes.find(type => type.value === selectedRecordType)?.label || 'Records';
                         const church = churches.find(church => church.id === selectedChurch) || churches[0];
                         const churchName = church?.church_name || church?.name || 'Saints Peter & Paul';
-                        return `${recordTypeLabel} - ${churchName}`;
+                        return viewMode === 'advanced' 
+                          ? `Advanced Grid View - ${recordTypeLabel} - ${churchName}`
+                          : `${recordTypeLabel} - ${churchName}`;
                       })()}
                     </Typography>
                   </Box>
@@ -3980,112 +4420,108 @@ const BaptismRecordsPage: React.FC = () => {
                 {/* Records Table - Only show when record type is selected */}
                 {selectedRecordType && (
                   <Paper className="theme-orthodox-traditional" sx={{
+                    flex: 1,
+                    display: 'flex',
+                    flexDirection: 'column',
                     width: '100%',
                     maxWidth: '100%',
                     margin: 0,
-                    marginLeft: 0,
-                    marginRight: 0,
-                    textAlign: 'left'
+                    padding: 0,
+                    minHeight: 0,
+                    minWidth: 0,
+                    height: '100%',
+                    overflow: 'visible',
+                    // Ensure no transform or position quirks that break AG Grid
+                    transform: 'none',
+                    willChange: 'auto'
                   }}>
-
                     {/* Conditional Table Rendering */}
                     {viewMode === 'advanced' ? (
-                      // AG Grid View - Using same implementation as AdvancedGridDialog
-                      <Box sx={{ width: '100%', height: '600px' }}>
-                        {/* Dynamic style tag to apply theme colors to AG Grid - override CSS variables and direct styles */}
-                        <style id={`ag-grid-theme-${selectedRecordType}-${selectedChurch}`}>
-                          {`#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine{--ag-header-background-color:${enhancedTableState.tokens.headerBg}!important;--ag-header-foreground-color:${enhancedTableState.tokens.headerText}!important;--ag-border-color:${enhancedTableState.tokens.border}!important;--ag-odd-row-background-color:${enhancedTableState.tokens.rowOddBg}!important;--ag-background-color:${enhancedTableState.tokens.rowEvenBg}!important;--ag-data-color:${enhancedTableState.tokens.cellText}!important;--ag-row-hover-color:${enhancedTableState.tokens.accent}15!important;--ag-selected-row-background-color:${enhancedTableState.tokens.accent}20!important;--ag-alpine-active-color:${enhancedTableState.tokens.accent}!important}#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-header,#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-header-row,#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-header-row>div,#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-header-row>div>div{background-color:${enhancedTableState.tokens.headerBg}!important;color:${enhancedTableState.tokens.headerText}!important;border-color:${enhancedTableState.tokens.border}!important}#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-header-cell,#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-header-cell>div,#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-header-cell>div>div{background-color:${enhancedTableState.tokens.headerBg}!important;color:${enhancedTableState.tokens.headerText}!important;border-color:${enhancedTableState.tokens.border}!important;border-bottom-color:${enhancedTableState.tokens.border}!important}#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-header-cell-text,#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-header-cell-label,#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-header-cell-label span,#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-header-cell-label .ag-header-cell-text,#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-header-cell-label>span{color:${enhancedTableState.tokens.headerText}!important}#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-row-odd,#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-row-odd .ag-cell{background-color:${enhancedTableState.tokens.rowOddBg}!important}#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-row-even,#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-row-even .ag-cell{background-color:${enhancedTableState.tokens.rowEvenBg}!important}#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-cell{color:${enhancedTableState.tokens.cellText}!important;border-color:${enhancedTableState.tokens.border}!important}#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-row:hover{background-color:${enhancedTableState.tokens.accent}15!important}#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-row-selected{background-color:${enhancedTableState.tokens.accent}20!important}#ag-grid-container-${selectedRecordType}-${selectedChurch}.ag-theme-alpine .ag-header-cell-resize{background-color:${enhancedTableState.tokens.headerBg}!important}`}
-                          console.log('Current headerBg:', enhancedTableState.tokens.headerBg);
-                          console.log('Current theme:', enhancedTableState.liturgicalTheme);
-                        </style>
-                        <div 
-                          id={`ag-grid-container-${selectedRecordType}-${selectedChurch}`}
-                          className="ag-theme-alpine" 
-                          style={{ 
-                            height: '100%', 
-                            width: '100%',
-                          }}
-                        >
-                          <AgGridReact
-                            key={`ag-grid-${selectedRecordType}-${viewMode}-${enhancedTableState.liturgicalTheme}-${enhancedTableState.tokens.headerBg}`}
-                            theme="legacy"
-                            rowData={agGridRowData}
-                            columnDefs={agGridColumnDefs}
-                            defaultColDef={agGridDefaultColDef}
-                            onGridReady={onAGGridReady}
-                            pagination={false}
-                            animateRows={true}
-                            enableCellTextSelection={true}
-                            enableBrowserTooltips={true}
-                            tooltipShowDelay={500}
-                            rowHeight={40}
-                            headerHeight={45}
-                            maintainColumnOrder={true}
-                            suppressColumnVirtualisation={false}
-                            suppressRowVirtualisation={false}
-                            getRowId={(params) => params.data.id?.toString() || String(params.data.ID || '')}
-                            suppressMenuHide={false}
-                            suppressMovableColumns={false}
-                            rowSelection={{ mode: 'multiRow', enableClickSelection: false }}
-                            getRowStyle={(params) => {
-                              const rowIndex = params.node.rowIndex ?? 0;
-                              return {
-                                backgroundColor: rowIndex % 2 === 0 
-                                  ? enhancedTableState.tokens.rowEvenBg 
-                                  : enhancedTableState.tokens.rowOddBg,
-                                color: enhancedTableState.tokens.cellText,
-                              };
-                            }}
-                          />
-                        </div>
+                      <Box sx={{
+                        flex: 1,
+                        minHeight: 0,
+                        minWidth: 0,
+                        display: 'flex',
+                        flexDirection: 'column',
+                        height: '100%',
+                        width: '100%',
+                        overflow: 'visible'
+                      }}>
+                        <AdvancedRecordsGrid
+                          rowData={agGridRowData || []}
+                          columnDefs={agGridColumnDefs}
+                          recordType={selectedRecordType as 'baptism' | 'marriage' | 'funeral'}
+                          churchId={selectedChurch}
+                          loading={loading}
+                          defaultSort={DEFAULT_SORT[selectedRecordType]}
+                          containerId={`ag-grid-container-${selectedRecordType}-${selectedChurch}`}
+                          highlightIncomplete={highlightIncomplete}
+                        />
                       </Box>
                     ) : (
                       <>
                         {/* Dynamic Records Display - Replaces legacy table implementation */}
                         <DynamicRecordsDisplay
-                          records={filteredAndSortedRecords.map(record => {
-                            // Ensure entry_type is available in the record
-                            const mappedRecord = {
-                              ...record,
-                              entry_type: record.entry_type || record.entryType || record.originalRecord?.entry_type || record.originalRecord?.entryType || '',
-                              _isNew: newlyAddedRecordIds.has(String(record.id)),
-                              _isSelected: selectedRecords.has(String(record.id)),
-                            };
-                            return mappedRecord;
-                          })}
-                          columns={generateColumnsFromFieldMapper.length > 0 ? generateColumnsFromFieldMapper : mapFieldDefinitionsToDynamicColumns(selectedRecordType)}
-                          inferColumns={generateColumnsFromFieldMapper.length === 0}
-                          layout={(new URLSearchParams(location.search).get("layout") as any) || "table"}
-                          initialSort={{ field: "reception_date", direction: "desc" }}
-                          dateFields={["reception_date", "birth_date", "created_at", "updated_at", "deceased_date", "burial_date", "mdate", "marriageDate", "deathDate", "funeralDate"]}
-                          hiddenFields={["church_id", "_isNew", "_isSelected"]}
-                          onSortChange={(model) => console.log("sort", model)}
-                          loading={loading}
-                          onView={handleViewRecord}
-                          onEdit={handleEditRecord}
-                          onDelete={(id) => {
-                            handleDeleteRecord(String(id)).catch(console.error);
-                          }}
-                          onGenerateCertificate={selectedRecordType !== 'funeral' ? handleGenerateCertificate : undefined}
-                          selectedRecords={Array.from(selectedRecords)}
-                          onRecordSelect={handleRecordSelect}
-                          onSelectAll={handleSelectAll}
-                          showActions={true}
-                          maxHeight={600}
-                          themeTokens={enhancedTableState.tokens}
-                          fieldRules={enhancedTableState.fieldRules}
-                          emptyMessage={searchTerm ? "No records match your search" : "No records found. Click \"Add Record\" to create the first record"}
-                          rowStyle={(record: any) => {
-                            if (record._isNew) {
-                              return {
-                                backgroundColor: '#e8f5e9',
-                                borderLeft: '4px solid #4caf50',
-                                animation: 'fadeIn 0.5s ease-in',
-                              };
-                            }
-                            return {};
-                          }}
-                        />
+                              records={(() => {
+                                const mappedRecords = filteredAndSortedRecords.map(record => {
+                                  // Ensure entry_type is available in the record
+                                  const mappedRecord = {
+                                    ...record,
+                                    entry_type: record.entry_type || record.entryType || record.originalRecord?.entry_type || record.originalRecord?.entryType || '',
+                                    _isNew: newlyAddedRecordIds.has(String(record.id)),
+                                    _isSelected: selectedRecords.has(String(record.id)),
+                                  };
+                                  return mappedRecord;
+                                });
+                                
+                                // DEV-only logging: Log final row data structure
+                                if (import.meta.env.DEV && mappedRecords.length > 0) {
+                                  console.log('📊 NORMAL: Final records for table:', {
+                                    count: mappedRecords.length,
+                                    sampleRow: mappedRecords[0],
+                                    sampleRowKeys: Object.keys(mappedRecords[0] || {}),
+                                    recordType: selectedRecordType
+                                  });
+                                }
+                                
+                                return mappedRecords;
+                              })()}
+                              columns={generateColumnsFromFieldMapper.length > 0 ? generateColumnsFromFieldMapper : mapFieldDefinitionsToDynamicColumns(selectedRecordType)}
+                              inferColumns={generateColumnsFromFieldMapper.length === 0}
+                              layout={(new URLSearchParams(location.search).get("layout") as any) || "table"}
+                              initialSort={{ field: "reception_date", direction: "desc" }}
+                              dateFields={["reception_date", "birth_date", "created_at", "updated_at", "deceased_date", "burial_date", "mdate", "marriageDate", "deathDate", "funeralDate"]}
+                              autoShrink={autoShrinkEnabled}
+                              recordType={selectedRecordType as 'baptism' | 'marriage' | 'funeral'}
+                              highlightIncomplete={highlightIncomplete}
+                              hiddenFields={["church_id", "_isNew", "_isSelected"]}
+                              onSortChange={(model) => console.log("sort", model)}
+                              loading={loading}
+                              onView={handleViewRecord}
+                              onEdit={handleEditRecord}
+                              onDelete={(id) => {
+                                handleDeleteRecord(String(id)).catch(console.error);
+                              }}
+                              onGenerateCertificate={selectedRecordType !== 'funeral' ? handleGenerateCertificate : undefined}
+                              selectedRecords={Array.from(selectedRecords)}
+                              onRecordSelect={handleRecordSelect}
+                              onSelectAll={handleSelectAll}
+                              showActions={true}
+                              maxHeight={Math.floor(viewportHeight * 0.85)}
+                              themeTokens={enhancedTableState.tokens}
+                              fieldRules={enhancedTableState.fieldRules}
+                              emptyMessage={searchTerm ? "No records match your search" : "No records found. Click \"Add Record\" to create the first record"}
+                              rowStyle={(record: any) => {
+                                if (record._isNew) {
+                                  return {
+                                    backgroundColor: '#e8f5e9',
+                                    borderLeft: '4px solid #4caf50',
+                                    animation: 'fadeIn 0.5s ease-in',
+                                  };
+                                }
+                                return {};
+                              }}
+                            />
                       </>
                     )}
 
@@ -4120,8 +4556,8 @@ const BaptismRecordsPage: React.FC = () => {
                           : `Edit ${selectedRecordType.charAt(0).toUpperCase() + selectedRecordType.slice(1)} Record`
                         : `Add New ${selectedRecordType.charAt(0).toUpperCase() + selectedRecordType.slice(1)} Record`}
                   </DialogTitle>
-                  <DialogContent>
-                    <Box sx={{ mt: 2, overflow: 'auto' }}>
+                  <DialogContent sx={{ pt: 3, pb: 2 }}>
+                    <Box sx={{ pt: 1, pb: 2 }}>
                       <ImageBasedRecordForm
                         recordType={selectedRecordType as 'baptism' | 'marriage' | 'funeral'}
                         formData={formData}
@@ -4292,22 +4728,36 @@ const BaptismRecordsPage: React.FC = () => {
                           Cancel
                         </Button>
                         <Button
-                          onClick={() => {
+                          onClick={async (e) => {
+                            e.preventDefault();
+                            e.stopPropagation();
                             setSaveAndAddAnother(false);
-                            handleSaveRecord();
+                            try {
+                              await handleSaveRecord();
+                            } catch (error) {
+                              console.error('Error in Save button:', error);
+                            }
                           }}
                           variant="outlined"
                           disabled={loading}
+                          type="button"
                         >
                           {loading ? <CircularProgress size={20} /> : "Save"}
                         </Button>
                         <Button
-                          onClick={() => {
+                          onClick={async (e) => {
+                            e.preventDefault();
+                            e.stopPropagation();
                             setSaveAndAddAnother(true);
-                            handleSaveRecord();
+                            try {
+                              await handleSaveRecord();
+                            } catch (error) {
+                              console.error('Error in Save & Add Another button:', error);
+                            }
                           }}
                           variant="contained"
                           disabled={loading}
+                          type="button"
                         >
                           {loading ? <CircularProgress size={20} /> : "Save & Add Another"}
                         </Button>
@@ -4320,14 +4770,40 @@ const BaptismRecordsPage: React.FC = () => {
                 {/* Toast Snackbar */}
                 <Snackbar
                   open={toastOpen}
-                  autoHideDuration={6000}
+                  autoHideDuration={12000}
                   onClose={() => setToastOpen(false)}
-                  anchorOrigin={{ vertical: 'top', horizontal: 'center' }}
+                  anchorOrigin={{ vertical: 'center', horizontal: 'center' }}
+                  sx={{
+                    position: 'fixed',
+                    top: '50% !important',
+                    left: '50% !important',
+                    transform: 'translate(-50%, -50%) !important',
+                    zIndex: 10000,
+                    '& .MuiSnackbar-root': {
+                      position: 'fixed',
+                      top: '50% !important',
+                      left: '50% !important',
+                      transform: 'translate(-50%, -50%) !important',
+                    }
+                  }}
                 >
                   <Alert
                     onClose={() => setToastOpen(false)}
                     severity={toastSeverity}
-                    sx={{ width: '100%' }}
+                    sx={{ 
+                      width: '100%',
+                      minWidth: '400px',
+                      maxWidth: '600px',
+                      fontSize: '1.1rem',
+                      padding: '16px 20px',
+                      '& .MuiAlert-message': {
+                        fontSize: '1.1rem',
+                        fontWeight: 500,
+                      },
+                      '& .MuiAlert-icon': {
+                        fontSize: '28px',
+                      }
+                    }}
                   >
                     {toastMessage}
                   </Alert>
@@ -4392,6 +4868,15 @@ const BaptismRecordsPage: React.FC = () => {
                           component="img"
                           src={certificatePreviewUrl}
                           alt="Certificate Preview"
+                          onError={(e) => {
+                            // Hide image if certificate preview fails to load (only once)
+                            const img = e.currentTarget as HTMLImageElement;
+                            if (img.dataset.fallbackApplied === '1') {
+                              return; // Already hidden
+                            }
+                            img.dataset.fallbackApplied = '1';
+                            img.style.display = 'none';
+                          }}
                           sx={{
                             maxWidth: '100%',
                             maxHeight: 600,
@@ -4428,12 +4913,34 @@ const BaptismRecordsPage: React.FC = () => {
                     </Button>
                   </DialogActions>
                 </Dialog>
-              </Stack>
+              </Box>
             </Box>
           </Collapse>
         </CardContent>
       </Card>
+
+      {/* Quick Facts Drawer */}
+      <QuickFactsDrawer
+        open={quickFactsOpen}
+        onClose={handleQuickFactsClose}
+        facts={quickFacts}
+        loading={quickFactsLoading}
+        recordType={selectedRecordType as 'baptism' | 'marriage' | 'funeral' | undefined}
+      />
+
+      {/* Interactive Report Wizard */}
+      {selectedRecordType && (
+        <InteractiveReportWizard
+          open={interactiveReportOpen}
+          onClose={() => setInteractiveReportOpen(false)}
+          records={filteredAndSortedRecords}
+          recordType={selectedRecordType as 'baptism' | 'marriage' | 'funeral'}
+          churchId={selectedChurch}
+          onComplete={handleInteractiveReportComplete}
+        />
+      )}
     </Box>
+    </RecordsErrorBoundary>
   );
 };
 
diff --git a/front-end/src/features/records-centralized/components/baptism/ImageBasedRecordForm.tsx b/front-end/src/features/records-centralized/components/baptism/ImageBasedRecordForm.tsx
index 20958272..1f69ace5 100755
--- a/front-end/src/features/records-centralized/components/baptism/ImageBasedRecordForm.tsx
+++ b/front-end/src/features/records-centralized/components/baptism/ImageBasedRecordForm.tsx
@@ -103,6 +103,7 @@ const ImageBasedRecordForm: React.FC<ImageBasedRecordFormProps> = ({
         width: '100%',
         maxWidth: '1200px',
         margin: '0 auto',
+        pt: 1,
       }}
     >
       {/* Form fields in normal grid layout */}
@@ -110,7 +111,15 @@ const ImageBasedRecordForm: React.FC<ImageBasedRecordFormProps> = ({
         sx={{
           display: 'grid',
           gridTemplateColumns: '1fr 1fr',
-          gap: 2,
+          gap: 2.5,
+          '& .MuiTextField-root': {
+            '& .MuiInputLabel-root': {
+              transform: 'translate(14px, 16px) scale(1)',
+              '&.MuiInputLabel-shrink': {
+                transform: 'translate(14px, -9px) scale(0.75)',
+              },
+            },
+          },
         }}
       >
         {/* First Name and Last Name - REQUIRED */}
@@ -139,6 +148,23 @@ const ImageBasedRecordForm: React.FC<ImageBasedRecordFormProps> = ({
           onChange={handleInputChange('dateOfBirth')}
           label="Date of Birth"
           InputLabelProps={{ shrink: true }}
+          inputProps={{
+            max: new Date().toISOString().split('T')[0], // Cannot select future dates
+          }}
+          sx={{
+            '& input[type="date"]::-webkit-calendar-picker-indicator': {
+              opacity: 1,
+              cursor: 'pointer',
+              width: '20px',
+              height: '20px',
+            },
+            '& input[type="date"]::-webkit-inner-spin-button': {
+              display: 'none',
+            },
+            '& input[type="date"]::-webkit-outer-spin-button': {
+              display: 'none',
+            },
+          }}
           fullWidth
           required
         />
@@ -149,6 +175,23 @@ const ImageBasedRecordForm: React.FC<ImageBasedRecordFormProps> = ({
           onChange={handleInputChange('dateOfBaptism')}
           label="Date of Baptism"
           InputLabelProps={{ shrink: true }}
+          inputProps={{
+            max: new Date().toISOString().split('T')[0], // Cannot select future dates
+          }}
+          sx={{
+            '& input[type="date"]::-webkit-calendar-picker-indicator': {
+              opacity: 1,
+              cursor: 'pointer',
+              width: '20px',
+              height: '20px',
+            },
+            '& input[type="date"]::-webkit-inner-spin-button': {
+              display: 'none',
+            },
+            '& input[type="date"]::-webkit-outer-spin-button': {
+              display: 'none',
+            },
+          }}
           fullWidth
           required
         />
@@ -277,6 +320,7 @@ const ImageBasedRecordForm: React.FC<ImageBasedRecordFormProps> = ({
         width: '100%',
         maxWidth: '1200px',
         margin: '0 auto',
+        pt: 1,
       }}
     >
       {/* Form fields in normal grid layout */}
@@ -284,7 +328,15 @@ const ImageBasedRecordForm: React.FC<ImageBasedRecordFormProps> = ({
         sx={{
           display: 'grid',
           gridTemplateColumns: '1fr 1fr',
-          gap: 2,
+          gap: 2.5,
+          '& .MuiTextField-root': {
+            '& .MuiInputLabel-root': {
+              transform: 'translate(14px, 16px) scale(1)',
+              '&.MuiInputLabel-shrink': {
+                transform: 'translate(14px, -9px) scale(0.75)',
+              },
+            },
+          },
         }}
       >
         {/* Groom First Name and Last Name - REQUIRED */}
@@ -331,6 +383,31 @@ const ImageBasedRecordForm: React.FC<ImageBasedRecordFormProps> = ({
           onChange={handleInputChange('mdate')}
           label="Marriage Date"
           InputLabelProps={{ shrink: true }}
+          inputProps={{
+            max: new Date().toISOString().split('T')[0], // Cannot select future dates
+            style: { 
+              WebkitAppearance: 'none',
+              MozAppearance: 'textfield'
+            }
+          }}
+          sx={{
+            '& input[type="date"]::-webkit-calendar-picker-indicator': {
+              opacity: 1,
+              cursor: 'pointer',
+              width: '20px',
+              height: '20px',
+              marginRight: '4px',
+            },
+            '& input[type="date"]::-webkit-inner-spin-button': {
+              display: 'none',
+            },
+            '& input[type="date"]::-webkit-outer-spin-button': {
+              display: 'none',
+            },
+            '& input[type="date"]': {
+              paddingRight: '8px',
+            },
+          }}
           fullWidth
           required
         />
@@ -416,6 +493,7 @@ const ImageBasedRecordForm: React.FC<ImageBasedRecordFormProps> = ({
         width: '100%',
         maxWidth: '1200px',
         margin: '0 auto',
+        pt: 1,
       }}
     >
       {/* Form fields in normal grid layout */}
@@ -423,7 +501,15 @@ const ImageBasedRecordForm: React.FC<ImageBasedRecordFormProps> = ({
         sx={{
           display: 'grid',
           gridTemplateColumns: '1fr 1fr',
-          gap: 2,
+          gap: 2.5,
+          '& .MuiTextField-root': {
+            '& .MuiInputLabel-root': {
+              transform: 'translate(14px, 16px) scale(1)',
+              '&.MuiInputLabel-shrink': {
+                transform: 'translate(14px, -9px) scale(0.75)',
+              },
+            },
+          },
         }}
       >
         {/* First Name and Last Name - REQUIRED */}
@@ -452,6 +538,23 @@ const ImageBasedRecordForm: React.FC<ImageBasedRecordFormProps> = ({
           onChange={handleInputChange('dateOfDeath')}
           label="Date of Death"
           InputLabelProps={{ shrink: true }}
+          inputProps={{
+            max: new Date().toISOString().split('T')[0], // Cannot select future dates
+          }}
+          sx={{
+            '& input[type="date"]::-webkit-calendar-picker-indicator': {
+              opacity: 1,
+              cursor: 'pointer',
+              width: '20px',
+              height: '20px',
+            },
+            '& input[type="date"]::-webkit-inner-spin-button': {
+              display: 'none',
+            },
+            '& input[type="date"]::-webkit-outer-spin-button': {
+              display: 'none',
+            },
+          }}
           fullWidth
         />
         <TextField
@@ -461,6 +564,23 @@ const ImageBasedRecordForm: React.FC<ImageBasedRecordFormProps> = ({
           onChange={handleInputChange('burialDate')}
           label="Burial Date"
           InputLabelProps={{ shrink: true }}
+          inputProps={{
+            max: new Date().toISOString().split('T')[0], // Cannot select future dates
+          }}
+          sx={{
+            '& input[type="date"]::-webkit-calendar-picker-indicator': {
+              opacity: 1,
+              cursor: 'pointer',
+              width: '20px',
+              height: '20px',
+            },
+            '& input[type="date"]::-webkit-inner-spin-button': {
+              display: 'none',
+            },
+            '& input[type="date"]::-webkit-outer-spin-button': {
+              display: 'none',
+            },
+          }}
           fullWidth
           required
         />
diff --git a/front-end/src/features/records-centralized/components/dynamic/DynamicRecordsDisplay.tsx b/front-end/src/features/records-centralized/components/dynamic/DynamicRecordsDisplay.tsx
index 444d26b0..fd198742 100755
--- a/front-end/src/features/records-centralized/components/dynamic/DynamicRecordsDisplay.tsx
+++ b/front-end/src/features/records-centralized/components/dynamic/DynamicRecordsDisplay.tsx
@@ -2,7 +2,7 @@
  * Canonical Dynamic Records Display Component
  */
 
-import React, { useMemo, useState } from 'react';
+import React, { useMemo, useState, useRef, useEffect } from 'react';
 import { Global } from '@emotion/react';
 import {
   Table,
@@ -36,11 +36,17 @@ import {
 } from '@mui/icons-material';
 import { inferColumnsFromRecords, mapFieldDefinitionsToDynamicColumns } from './columnMappers';
 import { renderCellValue } from './cellRenderers';
+import { getNormalColWidth, calculateColumnWidths } from './columnWidthHelper';
+import { buildNormalColGroup } from './buildColGroup';
+import { getCompletenessSeverity } from '../quality/recordCompleteness';
 
 export interface DynamicColumn {
   field: string;
   headerName?: string;
-  valueGetter?: (row: any) => any;
+  // valueGetter supports both signatures for compatibility:
+  // - (row: any) => any (simple form)
+  // - ({ data: row }) => any (AG Grid style)
+  valueGetter?: ((row: any) => any) | ((params: { data: any }) => any);
   cellRenderer?: (value: any, row: any) => React.ReactNode;
   sortable?: boolean;
   width?: number;
@@ -90,6 +96,9 @@ export interface DynamicRecordsDisplayProps {
     color?: string;
     bg?: string;
   }[];
+  autoShrink?: boolean;
+  recordType?: 'baptism' | 'marriage' | 'funeral';
+  highlightIncomplete?: boolean;
 }
 
 export const DynamicRecordsDisplay: React.FC<DynamicRecordsDisplayProps> = ({
@@ -117,6 +126,9 @@ export const DynamicRecordsDisplay: React.FC<DynamicRecordsDisplayProps> = ({
   onRecordSelect,
   onSelectAll,
   rowStyle,
+  autoShrink = false,
+  recordType,
+  highlightIncomplete = false,
 }) => {
   const [sortConfig, setSortConfig] = useState<SortModel | null>(initialSort || null);
   const [anchorEl, setAnchorEl] = useState<null | HTMLElement>(null);
@@ -125,11 +137,12 @@ export const DynamicRecordsDisplay: React.FC<DynamicRecordsDisplayProps> = ({
   // Apply field-specific styling rules
   const getFieldCellStyle = (fieldName: string) => {
     const rule = fieldRules?.find(r => r.field === fieldName);
-    if (!rule && !themeTokens) return {};
+    if (!rule && !themeTokens) return { fontSize: '15px' };
     
     const style: any = {
       color: themeTokens?.cellText,
-      borderBottom: themeTokens ? `1px solid ${themeTokens.border}` : undefined
+      borderBottom: themeTokens ? `1px solid ${themeTokens.border}` : undefined,
+      fontSize: '15px',
     };
     
     if (rule) {
@@ -155,6 +168,85 @@ export const DynamicRecordsDisplay: React.FC<DynamicRecordsDisplayProps> = ({
     return [];
   }, [providedColumns, inferColumns, records, hiddenFields, dateFields, columnOrder]);
 
+  // DEV-only logging: Log row structure and column keys ONCE per page load
+  const hasLoggedRef = useRef(false);
+  useEffect(() => {
+    if (import.meta.env.DEV && records.length > 0 && columns.length > 0 && !hasLoggedRef.current) {
+      const sampleRow = records[0];
+      const rowKeys = Object.keys(sampleRow || {});
+      const columnKeys = columns.map(c => ({ 
+        key: c.field, 
+        header: c.headerName || 'N/A',
+        hasValueGetter: !!c.valueGetter
+      }));
+      
+      console.log('📊 NORMAL: Row structure and columns (first render):', {
+        rowSample: sampleRow,
+        rowKeys,
+        columnKeys,
+        recordCount: records.length
+      });
+      
+      // Check for key mismatches
+      const mismatches = columnKeys.filter(col => 
+        !rowKeys.includes(col.key) && !col.hasValueGetter
+      );
+      if (mismatches.length > 0) {
+        console.warn('⚠️ NORMAL: Column keys not found in row:', mismatches);
+      }
+      
+      // Log sample cell values for first row
+      const sampleCellValues = columnKeys.slice(0, 5).map(col => {
+        let val: any;
+        if (col.hasValueGetter) {
+          const colObj = columns.find(c => c.field === col.key);
+          if (colObj?.valueGetter) {
+            try {
+              if (colObj.valueGetter.length === 1) {
+                val = (colObj.valueGetter as (row: any) => any)(sampleRow);
+              } else {
+                val = colObj.valueGetter({ data: sampleRow });
+              }
+            } catch {
+              val = undefined;
+            }
+          }
+        } else {
+          val = sampleRow[col.key];
+        }
+        return { field: col.key, value: val, type: typeof val };
+      });
+      console.log('📊 NORMAL: Sample cell values (first 5 columns):', sampleCellValues);
+      
+      hasLoggedRef.current = true;
+    }
+  }, [records, columns]); // Only log when records/columns change
+
+  // Memoize completeness severity for all records
+  const completenessMap = useMemo(() => {
+    if (!highlightIncomplete || !recordType) return new Map<string, { severity: 0 | 1 | 2; missing: string[] }>();
+    
+    // Defensive guard: ensure getCompletenessSeverity is available
+    if (typeof getCompletenessSeverity !== 'function') {
+      console.warn('⚠️ getCompletenessSeverity is not available, skipping completeness highlighting');
+      return new Map<string, { severity: 0 | 1 | 2; missing: string[] }>();
+    }
+    
+    const map = new Map<string, { severity: 0 | 1 | 2; missing: string[] }>();
+    records.forEach((record) => {
+      const rowId = String(record.id || record._id || records.indexOf(record));
+      try {
+        const result = getCompletenessSeverity(recordType, record);
+        map.set(rowId, result);
+      } catch (error) {
+        // Defensive: if evaluation fails, default to severity 0 (complete)
+        console.warn('⚠️ Error evaluating completeness for record:', error);
+        map.set(rowId, { severity: 0, missing: [] });
+      }
+    });
+    return map;
+  }, [records, recordType, highlightIncomplete]);
+
   const sortedRecords = useMemo(() => {
     if (!sortConfig || !records.length) return records;
 
@@ -162,8 +254,26 @@ export const DynamicRecordsDisplay: React.FC<DynamicRecordsDisplayProps> = ({
       const column = columns.find((col: DynamicColumn) => col.field === sortConfig.field);
       if (!column) return 0;
 
-      let aVal = column.valueGetter ? column.valueGetter(a) : a[sortConfig.field];
-      let bVal = column.valueGetter ? column.valueGetter(b) : b[sortConfig.field];
+      // Handle valueGetter with both signatures
+      let aVal: any;
+      let bVal: any;
+      if (column.valueGetter) {
+        try {
+          if (column.valueGetter.length === 1) {
+            aVal = (column.valueGetter as (row: any) => any)(a);
+            bVal = (column.valueGetter as (row: any) => any)(b);
+          } else {
+            aVal = column.valueGetter({ data: a });
+            bVal = column.valueGetter({ data: b });
+          }
+        } catch (err) {
+          aVal = a[sortConfig.field];
+          bVal = b[sortConfig.field];
+        }
+      } else {
+        aVal = a[sortConfig.field];
+        bVal = b[sortConfig.field];
+      }
 
       if (aVal == null && bVal == null) return 0;
       if (aVal == null) return 1;
@@ -272,41 +382,193 @@ export const DynamicRecordsDisplay: React.FC<DynamicRecordsDisplayProps> = ({
     //   console.log('✅ Action buttons should be visible. showActions:', showActions, 'onView:', !!onView, 'onEdit:', !!onEdit, 'onDelete:', !!onDelete);
     // }
 
+    const tableClassName = autoShrink 
+      ? `${className} om-normal-table--autoshrink`.trim()
+      : className;
+
+    // Calculate column widths and total fixed width when autoShrink is enabled
+    const { columnWidths, totalFixedWidth } = useMemo(() => {
+      if (!autoShrink) return { columnWidths: null, totalFixedWidth: 0 };
+      const visibleColumns = [...columns];
+      if (showActions) {
+        visibleColumns.push({ field: '__actions' });
+      }
+      const result = calculateColumnWidths(visibleColumns, dateFields, recordType);
+      // Add checkbox column width at the beginning if needed
+      if (showCheckboxes) {
+        const checkboxWidth = 44;
+        return {
+          columnWidths: [{ width: checkboxWidth, minWidth: checkboxWidth }, ...result.widths],
+          totalFixedWidth: result.totalFixedWidth + checkboxWidth,
+        };
+      }
+      return {
+        columnWidths: result.widths,
+        totalFixedWidth: result.totalFixedWidth,
+      };
+    }, [autoShrink, columns, dateFields, showCheckboxes, showActions, recordType]);
+
+    // Build colgroup element using shared helper
+    const colGroupElement = buildNormalColGroup({
+      recordType,
+      columns,
+      autoShrinkEnabled: autoShrink,
+      dateFields,
+      showCheckboxes,
+      showActions,
+    });
+
     return (
-    <TableContainer component={Paper} style={{ maxHeight }} className={className}>
-      <Table stickyHeader size={layout === 'dense' ? 'small' : 'medium'}>
+      <TableContainer 
+        component={Paper} 
+        style={{ 
+          maxHeight,
+        }} 
+        className={tableClassName}
+        sx={autoShrink && totalFixedWidth > 0 ? {
+          minWidth: `${totalFixedWidth}px`,
+          overflowX: 'auto', // Allow horizontal scroll on small screens
+          overflowY: 'auto', // Allow vertical scroll when content exceeds maxHeight
+        } : {
+          overflowX: 'auto', // Allow horizontal scroll when autoShrink is OFF
+          overflowY: 'auto', // Allow vertical scroll when content exceeds maxHeight
+        }}
+      >
+        <Table 
+          stickyHeader 
+          size={autoShrink ? 'small' : (layout === 'dense' ? 'small' : 'medium')} 
+          sx={autoShrink ? { 
+            tableLayout: 'fixed', 
+            width: '100%',
+            minWidth: totalFixedWidth > 0 ? `${totalFixedWidth}px` : undefined,
+            '& .MuiTableCell-root': {
+              padding: '6px 10px',
+              fontSize: '13px',
+              lineHeight: '1.4',
+              whiteSpace: 'nowrap',
+              overflow: 'hidden',
+              textOverflow: 'ellipsis',
+            },
+            '& .MuiTableRow-root': {
+              height: 'auto',
+              minHeight: '32px',
+              '& .MuiTableCell-root': {
+                lineHeight: '1.4',
+              },
+            },
+            '& .MuiTableCell-head': {
+              fontSize: '13px',
+              fontWeight: 600,
+              lineHeight: '1.4',
+              whiteSpace: 'nowrap',
+              overflow: 'hidden',
+              textOverflow: 'ellipsis',
+            },
+            // Date fields: center alignment
+            '& .MuiTableCell-root[data-field-type="date"]': {
+              textAlign: 'center',
+            },
+            // Text fields: left alignment (default)
+            '& .MuiTableCell-root:not([data-field-type="date"])': {
+              textAlign: 'left',
+            },
+          } : {}}
+        >
+          {colGroupElement}
         <TableHead>
           <TableRow sx={themeTokens ? { bgcolor: themeTokens.headerBg, color: themeTokens.headerText } : {}}>
             {showCheckboxes && (
-              <TableCell padding="checkbox" sx={themeTokens ? { bgcolor: themeTokens.headerBg, color: themeTokens.headerText, borderBottom: `1px solid ${themeTokens.border}` } : {}}>
+              <TableCell 
+                padding="checkbox" 
+                sx={themeTokens ? { 
+                  bgcolor: themeTokens.headerBg, 
+                  color: themeTokens.headerText, 
+                  borderBottom: `1px solid ${themeTokens.border}`, 
+                  fontSize: autoShrink ? '13px' : '16px', 
+                  fontWeight: 'bold',
+                  padding: autoShrink ? '6px 10px' : undefined,
+                } : { 
+                  fontSize: autoShrink ? '13px' : '16px', 
+                  fontWeight: 'bold' 
+                }}
+              >
                 <Checkbox
                   indeterminate={someSelected && !allSelected}
                   checked={allSelected}
                   onChange={(e) => onSelectAll?.(e.target.checked)}
+                  size={autoShrink ? 'small' : 'medium'}
                 />
               </TableCell>
             )}
-            {columns.map((column: DynamicColumn) => (
-              <TableCell key={column.field} sx={themeTokens ? { bgcolor: themeTokens.headerBg, color: themeTokens.headerText, borderBottom: `1px solid ${themeTokens.border}` } : {}}>
-                {column.sortable !== false ? (
-                  <TableSortLabel
-                    active={sortConfig?.field === column.field}
-                    direction={sortConfig?.field === column.field ? sortConfig.direction : 'asc'}
-                    onClick={() => handleSort(column.field)}
-                  >
-                    {column.headerName || column.field}
-                  </TableSortLabel>
-                ) : (
-                  column.headerName || column.field
-                )}
-              </TableCell>
-            ))}
+            {columns.map((column: DynamicColumn, colIndex) => {
+              const widthConfig = autoShrink && columnWidths 
+                ? columnWidths[showCheckboxes ? colIndex + 1 : colIndex]
+                : null;
+              
+              // Determine if this is a date field for alignment
+              const fieldLower = column.field.toLowerCase();
+              const isDateField = dateFields.includes(column.field) || 
+                fieldLower.includes('date') || 
+                fieldLower.includes('_date');
+              
+              return (
+                <TableCell 
+                  key={column.field}
+                  data-field-type={isDateField ? 'date' : 'text'}
+                  sx={{
+                    ...(themeTokens ? { 
+                      bgcolor: themeTokens.headerBg, 
+                      color: themeTokens.headerText, 
+                      borderBottom: `1px solid ${themeTokens.border}`, 
+                      fontSize: autoShrink ? '13px' : '16px', 
+                      fontWeight: 'bold',
+                      padding: autoShrink ? '6px 10px' : undefined,
+                      lineHeight: autoShrink ? '1.4' : undefined,
+                    } : { 
+                      fontSize: autoShrink ? '13px' : '16px', 
+                      fontWeight: 'bold',
+                      lineHeight: autoShrink ? '1.4' : undefined,
+                    }),
+                    ...(widthConfig && typeof widthConfig.width === 'number' 
+                      ? { width: `${widthConfig.width}px`, minWidth: `${widthConfig.minWidth || widthConfig.width}px` }
+                      : widthConfig
+                      ? { width: widthConfig.width, minWidth: widthConfig.minWidth ? `${widthConfig.minWidth}px` : undefined }
+                      : {}),
+                    // Alignment: center for dates, left for text
+                    textAlign: isDateField ? 'center' : 'left',
+                  }}
+                >
+                  {column.sortable !== false ? (
+                    <TableSortLabel
+                      active={sortConfig?.field === column.field}
+                      direction={sortConfig?.field === column.field ? sortConfig.direction : 'asc'}
+                      onClick={() => handleSort(column.field)}
+                      sx={{ fontSize: autoShrink ? '13px' : '16px', fontWeight: 'bold' }}
+                    >
+                      {column.headerName || column.field}
+                    </TableSortLabel>
+                  ) : (
+                    column.headerName || column.field
+                  )}
+                </TableCell>
+              );
+            })}
             {showActions && (
               <TableCell 
                 sx={{ 
-                  ...(themeTokens ? { bgcolor: themeTokens.headerBg, color: themeTokens.headerText, borderBottom: `1px solid ${themeTokens.border}` } : {}),
-                  width: 60,
-                  minWidth: 60,
+                  ...(themeTokens ? { 
+                    bgcolor: themeTokens.headerBg, 
+                    color: themeTokens.headerText, 
+                    borderBottom: `1px solid ${themeTokens.border}`, 
+                    fontSize: autoShrink ? '13px' : '16px', 
+                    fontWeight: 'bold',
+                    padding: autoShrink ? '6px 10px' : undefined,
+                  } : { 
+                    fontSize: autoShrink ? '13px' : '16px', 
+                    fontWeight: 'bold' 
+                  }),
+                  width: autoShrink ? 50 : 60,
+                  minWidth: autoShrink ? 50 : 60,
                   position: 'sticky',
                   right: 0,
                   backgroundColor: themeTokens?.headerBg || 'background.paper',
@@ -325,55 +587,265 @@ export const DynamicRecordsDisplay: React.FC<DynamicRecordsDisplayProps> = ({
             const isNew = row._isNew;
             const rowStyleProps = rowStyle ? rowStyle(row) : {};
             
+            // Get completeness severity for this row
+            const completeness = highlightIncomplete ? completenessMap.get(rowId) : null;
+            const severity = completeness?.severity || 0;
+            const missingFields = completeness?.missing || [];
+            
             return (
-              <TableRow 
-                key={rowId} 
-                hover 
-                selected={isSelected}
-                className={isNew ? 'new-record-row' : ''}
-                sx={{
-                  ...(themeTokens ? { 
-                    bgcolor: index % 2 === 0 ? themeTokens.rowEvenBg : themeTokens.rowOddBg, 
-                    borderBottom: `1px solid ${themeTokens.border}` 
-                  } : {}),
-                  ...(isNew ? {
-                    backgroundColor: '#e8f5e9 !important',
-                    borderLeft: '4px solid #4caf50 !important',
-                    '& .MuiTableCell-root': {
-                      backgroundColor: 'transparent',
-                    },
-                  } : {}),
-                  ...rowStyleProps,
-                }}
+              <Tooltip
+                title={highlightIncomplete && severity > 0 ? `Missing: ${missingFields.join(', ')}` : ''}
+                arrow
+                placement="top"
               >
+                <TableRow 
+                  key={rowId} 
+                  hover 
+                  selected={isSelected}
+                  className={isNew ? 'new-record-row' : ''}
+                  sx={{
+                    ...(themeTokens ? { 
+                      bgcolor: index % 2 === 0 ? themeTokens.rowEvenBg : themeTokens.rowOddBg, 
+                      borderBottom: `1px solid ${themeTokens.border}` 
+                    } : {}),
+                    ...(isNew ? {
+                      backgroundColor: '#e8f5e9 !important',
+                      borderLeft: '4px solid #4caf50 !important',
+                      '& .MuiTableCell-root': {
+                        backgroundColor: 'transparent',
+                      },
+                    } : {}),
+                    // Completeness highlighting
+                    ...(highlightIncomplete && severity === 1 ? {
+                      backgroundColor: (theme: any) => 
+                        theme.palette.mode === 'dark' 
+                          ? 'rgba(255, 193, 7, 0.15)' // Yellow tint for dark mode
+                          : 'rgba(255, 193, 7, 0.1)', // Yellow tint for light mode
+                      '&:hover': {
+                        backgroundColor: (theme: any) => 
+                          theme.palette.mode === 'dark' 
+                            ? 'rgba(255, 193, 7, 0.25)'
+                            : 'rgba(255, 193, 7, 0.15)',
+                      },
+                    } : {}),
+                    ...(highlightIncomplete && severity === 2 ? {
+                      backgroundColor: (theme: any) => 
+                        theme.palette.mode === 'dark' 
+                          ? 'rgba(244, 67, 54, 0.15)' // Red tint for dark mode
+                          : 'rgba(244, 67, 54, 0.1)', // Red tint for light mode
+                      '&:hover': {
+                        backgroundColor: (theme: any) => 
+                          theme.palette.mode === 'dark' 
+                            ? 'rgba(244, 67, 54, 0.25)'
+                            : 'rgba(244, 67, 54, 0.15)',
+                      },
+                    } : {}),
+                    ...rowStyleProps,
+                  }}
+                >
                 {showCheckboxes && (
-                  <TableCell padding="checkbox">
+                  <TableCell 
+                    padding="checkbox"
+                    sx={autoShrink ? { padding: '6px 10px' } : {}}
+                  >
                     <Checkbox
                       checked={isSelected}
                       onChange={(e) => onRecordSelect?.(rowId, e.target.checked)}
+                      size={autoShrink ? 'small' : 'medium'}
                     />
                   </TableCell>
                 )}
-                {columns.map((column: DynamicColumn) => {
-                  const value = column.valueGetter ? column.valueGetter(row) : row[column.field];
-                  return (
-                    <TableCell key={column.field} sx={getFieldCellStyle(column.field)}>
-                      {column.cellRenderer 
-                        ? column.cellRenderer(value, row) 
-                        : renderCellValue(value, column.field, dateFields)
+                {columns.map((column: DynamicColumn, colIndex) => {
+                  const widthConfig = autoShrink && columnWidths 
+                    ? columnWidths[showCheckboxes ? colIndex + 1 : colIndex]
+                    : null;
+                  
+                  // Get cell value with proper key matching
+                  let cellValue: any;
+                  if (column.valueGetter) {
+                    // Use valueGetter if provided (this handles field name variations)
+                    try {
+                      // valueGetter can be either (row) => value or ({ data: row }) => value
+                      // Try both signatures for compatibility
+                      if (column.valueGetter.length === 1) {
+                        // Signature: (row) => value
+                        cellValue = (column.valueGetter as (row: any) => any)(row);
+                      } else {
+                        // Signature: ({ data: row }) => value
+                        cellValue = column.valueGetter({ data: row });
+                      }
+                    } catch (err) {
+                      if (import.meta.env.DEV) {
+                        console.warn('⚠️ valueGetter error for field:', column.field, err);
+                      }
+                      cellValue = undefined;
+                    }
+                  } else {
+                    // No valueGetter: try direct field access, then try common variations
+                    cellValue = row[column.field];
+                    
+                    // If not found, try common key variations (snake_case <-> camelCase)
+                    if (cellValue === undefined || cellValue === null) {
+                      const field = column.field;
+                      // Try camelCase if field is snake_case
+                      if (field.includes('_')) {
+                        const camelCase = field.replace(/_([a-z])/g, (_, letter) => letter.toUpperCase());
+                        if (row[camelCase] !== undefined && row[camelCase] !== null) {
+                          cellValue = row[camelCase];
+                        }
+                      }
+                      // Try snake_case if field is camelCase
+                      if ((cellValue === undefined || cellValue === null) && /[A-Z]/.test(field)) {
+                        const snakeCase = field.replace(/([A-Z])/g, '_$1').toLowerCase();
+                        if (row[snakeCase] !== undefined && row[snakeCase] !== null) {
+                          cellValue = row[snakeCase];
+                        }
+                      }
+                      // Try lowercase version
+                      if ((cellValue === undefined || cellValue === null)) {
+                        const lowerField = field.toLowerCase();
+                        if (row[lowerField] !== undefined && row[lowerField] !== null) {
+                          cellValue = row[lowerField];
+                        }
                       }
+                    }
+                  }
+                  
+                  // DEV-only: Log if cell value is still undefined for first few cells
+                  if (import.meta.env.DEV && colIndex < 3 && row === sortedRecords[0]) {
+                    if (cellValue === undefined || cellValue === null) {
+                      console.warn('⚠️ NORMAL: Cell value undefined for field:', column.field, {
+                        rowHasField: column.field in row,
+                        rowKeys: Object.keys(row).slice(0, 10),
+                        columnField: column.field,
+                        hasValueGetter: !!column.valueGetter
+                      });
+                    }
+                  }
+                  
+                  // Pass cellValue directly to renderCellValue - it handles null/undefined/objects/arrays
+                  // Do NOT pre-convert objects/arrays here - let renderCellValue handle it properly
+                  let displayValue = column.cellRenderer
+                    ? column.cellRenderer(cellValue, row)
+                    : renderCellValue(cellValue, column.field, dateFields);
+                  
+                  // Ensure displayValue is always renderable (prevent React error #300)
+                  // STRICT null check only
+                  if (displayValue === null || displayValue === undefined) {
+                    if (import.meta.env.DEV) {
+                      console.warn('⚠️ DEV: displayValue is null/undefined for field:', column.field, {
+                        cellValue,
+                        rowKeys: Object.keys(row).slice(0, 10),
+                        columnField: column.field
+                      });
+                    }
+                    // Fallback to safe string representation (only for null/undefined)
+                    displayValue = <Typography variant="body2" color="textSecondary">—</Typography>;
+                  }
+                  
+                  // DEV-only assertion: Check if >80% of cells are showing "—" (blank table)
+                  if (import.meta.env.DEV && colIndex === 0 && row === sortedRecords[0]) {
+                    // Check first row's first few cells
+                    const firstRowCells = columns.slice(0, Math.min(10, columns.length)).map((col: DynamicColumn) => {
+                      let val: any;
+                      if (col.valueGetter) {
+                        try {
+                          if (col.valueGetter.length === 1) {
+                            val = (col.valueGetter as (row: any) => any)(row);
+                          } else {
+                            val = col.valueGetter({ data: row });
+                          }
+                        } catch {
+                          val = undefined;
+                        }
+                      } else {
+                        val = row[col.field];
+                      }
+                      return { field: col.field, value: val };
+                    });
+                    
+                    const blankCount = firstRowCells.filter(cell => 
+                      cell.value === null || cell.value === undefined || cell.value === ''
+                    ).length;
+                    const blankPercentage = (blankCount / firstRowCells.length) * 100;
+                    
+                    if (blankPercentage > 80 && columns.length > 0 && sortedRecords.length > 0) {
+                      console.warn('⚠️ NORMAL: >80% of cells showing blank. Diagnostic:', {
+                        blankCount,
+                        totalCells: firstRowCells.length,
+                        blankPercentage: `${blankPercentage.toFixed(1)}%`,
+                        rowKeys: Object.keys(row),
+                        columnKeys: columns.map(c => c.field),
+                        firstRowCells: firstRowCells.slice(0, 5),
+                        sampleRow: row
+                      });
+                    }
+                  }
+                  
+                  // Get cell text for tooltip (STRICT null check)
+                  const cellText = typeof displayValue === 'string' 
+                    ? displayValue 
+                    : (cellValue === null || cellValue === undefined)
+                      ? ''
+                      : String(cellValue);
+                  // Check if field is a long text field that might be truncated (sponsors, parents, long text)
+                  const fieldLower = column.field.toLowerCase();
+                  const isLongTextField = fieldLower.includes('location') ||
+                    fieldLower.includes('notes') ||
+                    fieldLower.includes('address') ||
+                    fieldLower.includes('comment') ||
+                    fieldLower.includes('description') ||
+                    fieldLower.includes('sponsor') ||
+                    fieldLower.includes('parent') ||
+                    fieldLower.includes('witness');
+                  const isTruncated = autoShrink && isLongTextField && cellText && cellText.length > 0;
+                  
+                  // Determine if this is a date field for alignment
+                  const isDateField = dateFields.includes(column.field) || 
+                    fieldLower.includes('date') || 
+                    fieldLower.includes('_date');
+                  
+                  const cellContent = (
+                    <TableCell
+                      key={column.field}
+                      data-field-type={isDateField ? 'date' : 'text'}
+                      sx={{
+                        ...getFieldCellStyle(column.field),
+                        ...(widthConfig && typeof widthConfig.width === 'number' 
+                          ? { width: `${widthConfig.width}px`, minWidth: `${widthConfig.minWidth || widthConfig.width}px` }
+                          : widthConfig
+                          ? { width: widthConfig.width, minWidth: widthConfig.minWidth ? `${widthConfig.minWidth}px` : undefined }
+                          : {}),
+                        // Alignment: center for dates, left for text
+                        textAlign: isDateField ? 'center' : 'left',
+                      }}
+                      title={isTruncated ? cellText : undefined}
+                    >
+                      {displayValue}
                     </TableCell>
                   );
+
+                  // Wrap in Tooltip if truncated (for better UX)
+                  if (isTruncated && cellText) {
+                    return (
+                      <Tooltip key={column.field} title={cellText} arrow placement="top">
+                        {cellContent}
+                      </Tooltip>
+                    );
+                  }
+                  
+                  return cellContent;
                 })}
                 {showActions && (
                   <TableCell 
                     sx={{ 
-                      width: 60,
-                      minWidth: 60,
+                      width: autoShrink ? 72 : 60,
+                      minWidth: autoShrink ? 72 : 60,
                       position: 'sticky',
                       right: 0,
                       backgroundColor: isNew ? '#e8f5e9' : (themeTokens ? (index % 2 === 0 ? themeTokens.rowEvenBg : themeTokens.rowOddBg) : 'background.paper'),
-                      zIndex: 5
+                      zIndex: 5,
+                      padding: autoShrink ? '6px 10px' : undefined,
                     }}
                   >
                     <Tooltip title="Actions">
@@ -391,14 +863,15 @@ export const DynamicRecordsDisplay: React.FC<DynamicRecordsDisplayProps> = ({
                     </Tooltip>
                   </TableCell>
                 )}
-              </TableRow>
+                </TableRow>
+              </Tooltip>
             );
           })}
         </TableBody>
       </Table>
-    </TableContainer>
+        </TableContainer>
     );
-  };
+    };
 
   const renderCardsLayout = () => (
     <Box>
@@ -499,6 +972,8 @@ export const DynamicRecordsDisplay: React.FC<DynamicRecordsDisplayProps> = ({
             borderLeft: '4px solid #4caf50 !important',
             animation: 'fadeIn 0.5s ease-in',
           },
+          ...(autoShrink ? {
+          } : {}),
         }}
       />
       {layout === 'cards' ? renderCardsLayout() : renderTableLayout()}
diff --git a/front-end/src/features/records-centralized/components/dynamic/cellRenderers.tsx b/front-end/src/features/records-centralized/components/dynamic/cellRenderers.tsx
index ae89802e..a751f820 100755
--- a/front-end/src/features/records-centralized/components/dynamic/cellRenderers.tsx
+++ b/front-end/src/features/records-centralized/components/dynamic/cellRenderers.tsx
@@ -5,17 +5,14 @@
 import React from 'react';
 import { Typography } from '@mui/material';
 import { detectFieldType } from './columnMappers';
+import { formatRecordDate } from '@/utils/formatDate';
 
+/**
+ * @deprecated Use formatRecordDate from utils/formatDate instead
+ * Kept for backward compatibility
+ */
 export function formatDate(value: any): string {
-  if (value == null || value === '') return '';
-  
-  try {
-    const date = new Date(value);
-    if (isNaN(date.getTime())) return String(value);
-    return date.toLocaleDateString();
-  } catch {
-    return String(value);
-  }
+  return formatRecordDate(value) || '';
 }
 
 export function renderCellValue(
@@ -23,14 +20,81 @@ export function renderCellValue(
   fieldName: string,
   dateFields: string[] = []
 ): React.ReactNode {
-  if (value == null || value === '') {
+  // Only show "—" for null/undefined, not for empty strings or falsy values
+  // Allow 0, false, '' to display as-is (or '' as empty but not '-')
+  if (value === null || value === undefined) {
     return <Typography variant="body2" color="textSecondary">—</Typography>;
   }
+  
+  // Empty string should render as empty, not as "—"
+  if (value === '') {
+    return <Typography variant="body2" color="textSecondary"></Typography>;
+  }
+
+  // Handle arrays: empty → '-', else join(', ')
+  if (Array.isArray(value)) {
+    if (value.length === 0) {
+      return <Typography variant="body2" color="textSecondary">—</Typography>;
+    }
+    // Join array items as strings (for primitives)
+    const arrayStrings = value.map(item => {
+      // STRICT null check - allow 0, false, ''
+      if (item === null || item === undefined) return '';
+      if (typeof item === 'object' && !(item instanceof Date)) {
+        // Try common object fields first
+        if ('label' in item && item.label != null) return String(item.label);
+        if ('name' in item && item.name != null) return String(item.name);
+        if ('value' in item && item.value != null) return String(item.value);
+        // Fallback to JSON in DEV only
+        if (import.meta.env.DEV) {
+          return JSON.stringify(item);
+        }
+        return '[Object]';
+      }
+      return String(item);
+    }).filter(item => item !== ''); // Filter out empty strings but keep '0', 'false'
+    const stringValue = arrayStrings.join(', ');
+    return (
+      <Typography variant="body2">
+        {stringValue.length > 50 ? `${stringValue.substring(0, 50)}...` : stringValue}
+      </Typography>
+    );
+  }
+
+  // Handle objects: try common fields (label, name, value), else stringify
+  if (typeof value === 'object' && value !== null && !(value instanceof Date)) {
+    // Try common fields in order: label, name, value
+    if ('label' in value && value.label != null && value.label !== undefined) {
+      return <Typography variant="body2">{String(value.label)}</Typography>;
+    }
+    if ('name' in value && value.name != null && value.name !== undefined) {
+      return <Typography variant="body2">{String(value.name)}</Typography>;
+    }
+    if ('value' in value && value.value != null && value.value !== undefined) {
+      return <Typography variant="body2">{String(value.value)}</Typography>;
+    }
+    // Fallback: stringify in DEV only, show [Object] in prod
+    if (import.meta.env.DEV) {
+      try {
+        const stringValue = JSON.stringify(value);
+        return (
+          <Typography variant="body2">
+            {stringValue.length > 50 ? `${stringValue.substring(0, 50)}...` : stringValue}
+          </Typography>
+        );
+      } catch {
+        return <Typography variant="body2" color="textSecondary">[Object]</Typography>;
+      }
+    }
+    return <Typography variant="body2" color="textSecondary">[Object]</Typography>;
+  }
 
   if (dateFields.includes(fieldName)) {
-    return <Typography variant="body2">{formatDate(value)}</Typography>;
+    const formatted = formatRecordDate(value);
+    return <Typography variant="body2">{formatted || '—'}</Typography>;
   }
 
+  // Ensure we always return a string
   const stringValue = String(value);
   return (
     <Typography variant="body2">
diff --git a/front-end/src/features/records-centralized/components/dynamic/columnMappers.ts b/front-end/src/features/records-centralized/components/dynamic/columnMappers.ts
index a10bc1f7..3fd75cd6 100755
--- a/front-end/src/features/records-centralized/components/dynamic/columnMappers.ts
+++ b/front-end/src/features/records-centralized/components/dynamic/columnMappers.ts
@@ -32,7 +32,12 @@ export function inferColumnsFromRecords(records: any[], options: ColumnInference
   const allFields = new Set<string>();
   records.forEach((record: any) => {
     if (record && typeof record === 'object') {
-      Object.keys(record).forEach(key => allFields.add(key));
+      Object.keys(record).forEach(key => {
+        // Skip internal/metadata fields
+        if (!key.startsWith('_') && !key.startsWith('originalRecord')) {
+          allFields.add(key);
+        }
+      });
     }
   });
 
@@ -40,12 +45,43 @@ export function inferColumnsFromRecords(records: any[], options: ColumnInference
     field => !hiddenFields.includes(field)
   );
 
-  const fieldsToUse = maxColumns > 0 ? availableFields.slice(0, maxColumns) : availableFields;
+  // Respect columnOrder if provided
+  const orderedFields = columnOrder.length > 0
+    ? [...columnOrder.filter(f => availableFields.includes(f)), ...availableFields.filter(f => !columnOrder.includes(f))]
+    : availableFields;
+
+  const fieldsToUse = maxColumns > 0 ? orderedFields.slice(0, maxColumns) : orderedFields;
 
   return fieldsToUse.map((fieldName: string) => ({
     field: fieldName,
     headerName: humanizeFieldName(fieldName),
     sortable: true,
+    // Add valueGetter to handle key variations for inferred columns
+    // Support both signatures: (row) => value and ({ data: row }) => value
+    valueGetter: (params: any) => {
+      // Handle both signatures
+      const row = params?.data || params;
+      
+      // Try direct access first
+      if (row[fieldName] !== undefined && row[fieldName] !== null) {
+        return row[fieldName];
+      }
+      // Try camelCase if field is snake_case
+      if (fieldName.includes('_')) {
+        const camelCase = fieldName.replace(/_([a-z])/g, (_, letter) => letter.toUpperCase());
+        if (row[camelCase] !== undefined && row[camelCase] !== null) {
+          return row[camelCase];
+        }
+      }
+      // Try snake_case if field is camelCase
+      if (/[A-Z]/.test(fieldName)) {
+        const snakeCase = fieldName.replace(/([A-Z])/g, '_$1').toLowerCase();
+        if (row[snakeCase] !== undefined && row[snakeCase] !== null) {
+          return row[snakeCase];
+        }
+      }
+      return null;
+    }
   }));
 }
 
diff --git a/front-end/src/features/records-centralized/components/records/AgGridRecordsTable.tsx b/front-end/src/features/records-centralized/components/records/AgGridRecordsTable.tsx
index e8082345..c656914d 100755
--- a/front-end/src/features/records-centralized/components/records/AgGridRecordsTable.tsx
+++ b/front-end/src/features/records-centralized/components/records/AgGridRecordsTable.tsx
@@ -178,7 +178,9 @@ export function AgGridRecordsTable({
               return <Chip label={value ? 'Yes' : 'No'} color={value ? 'success' : 'default'} size="small" />;
             }
             if (field.field_type === 'date') {
-              return new Date(value).toLocaleDateString();
+              // Use centralized date formatter
+              const { formatRecordDate } = require('@/utils/formatDate');
+              return formatRecordDate(value);
             }
             if (typeof value === 'string' && value.length > 50) {
               return value.substring(0, 50) + '...';
@@ -394,7 +396,7 @@ export function AgGridRecordsTable({
       </Box>
 
       {/* AG Grid */}
-      <div className="ag-theme-alpine" style={{ height: 'calc(100% - 80px)', width: '100%' }}>
+      <div className="ag-theme-quartz" style={{ height: 'calc(100% - 80px)', width: '100%' }}>
         <AgGridReact
           ref={gridRef}
           {...gridOptions}
diff --git a/front-end/src/features/records-centralized/components/records/useDynamicRecords.ts b/front-end/src/features/records-centralized/components/records/useDynamicRecords.ts
index b414a3ca..5b735068 100755
--- a/front-end/src/features/records-centralized/components/records/useDynamicRecords.ts
+++ b/front-end/src/features/records-centralized/components/records/useDynamicRecords.ts
@@ -429,7 +429,9 @@ function formatDisplayValue(value: any, column: any): string {
 
   switch (column.type) {
     case 'date':
-      return new Date(value).toLocaleDateString();
+      // Use centralized date formatter
+      const { formatRecordDate } = require('@/utils/formatDate');
+      return formatRecordDate(value);
     case 'number':
       return value.toString();
     case 'boolean':
diff --git a/front-end/src/features/records/EnhancedRecordsGrid.tsx b/front-end/src/features/records/EnhancedRecordsGrid.tsx
index b7f0707d..a4bbd0b5 100755
--- a/front-end/src/features/records/EnhancedRecordsGrid.tsx
+++ b/front-end/src/features/records/EnhancedRecordsGrid.tsx
@@ -23,8 +23,7 @@ import {
 import { AgGridReact } from 'ag-grid-react';
 import { ColDef, GridReadyEvent, ModuleRegistry } from 'ag-grid-community';
 import { AllCommunityModule } from 'ag-grid-community';
-import 'ag-grid-community/styles/ag-grid.css';
-import 'ag-grid-community/styles/ag-theme-alpine.css';
+// Note: AG Grid CSS is imported globally in main.tsx to avoid duplicates
 import { fetchTables, fetchRecords } from './api.ts';
 
 // Register AG Grid modules
@@ -330,7 +329,7 @@ const EnhancedRecordsGrid: React.FC<EnhancedRecordsGridProps> = ({ defaultChurch
       {/* AG Grid */}
       {!loading && columnDefs.length > 0 && recordsData.length > 0 && (
         <Paper sx={{ height: 600, width: '100%', overflow: 'hidden' }}>
-          <div className="ag-theme-alpine" style={{ height: '100%', width: '100%' }}>
+          <div className="ag-theme-quartz" style={{ height: '100%', width: '100%' }}>
             <AgGridReact
               theme="legacy"
               rowData={recordsData}
diff --git a/front-end/src/features/records/apps/records/RecordsUIPage.tsx b/front-end/src/features/records/apps/records/RecordsUIPage.tsx
index b82f23dd..4831b0ed 100755
--- a/front-end/src/features/records/apps/records/RecordsUIPage.tsx
+++ b/front-end/src/features/records/apps/records/RecordsUIPage.tsx
@@ -34,8 +34,7 @@ import {
 } from '@tabler/icons-react';
 import { AgGridReact } from 'ag-grid-react';
 import { ColDef, GridReadyEvent, GridApi, ColumnApi, ModuleRegistry, AllCommunityModule } from 'ag-grid-community';
-import 'ag-grid-community/styles/ag-grid.css';
-import 'ag-grid-community/styles/ag-theme-alpine.css';
+// Note: AG Grid CSS is imported globally in main.tsx to avoid duplicates
 import { listRecords, type TableKey, type SortDir } from '@/shared/lib/recordsApi';
 import { AddRecordModal, ImportRecordsModal } from './components';
 import { useNavigate } from 'react-router-dom';
@@ -1184,15 +1183,15 @@ export default function RecordsUIPage() {
             }}
           >
             <div
-              className="ag-theme-alpine"
+              className="ag-theme-quartz"
               style={{
                 height: '100%',
                 width: '100%',
               }}
             >
               <AgGridReact
-                theme="legacy"
                 key={tab}
+                theme="legacy"
                 rowData={rows}
                 columnDefs={columnDefs}
                 onGridReady={handleGridReady}
diff --git a/front-end/src/features/records/apps/records/components/AdvancedGrid.tsx b/front-end/src/features/records/apps/records/components/AdvancedGrid.tsx
index f199e05f..f16ed229 100755
--- a/front-end/src/features/records/apps/records/components/AdvancedGrid.tsx
+++ b/front-end/src/features/records/apps/records/components/AdvancedGrid.tsx
@@ -4,9 +4,7 @@ import { ColDef, GridReadyEvent, SortChangedEvent } from 'ag-grid-community';
 import { Box, Typography, CircularProgress, Alert } from '@mui/material';
 import { listRecords, type TableKey, type SortDir } from "@/shared/lib/recordsApi";
 
-// Import AG Grid styles
-import 'ag-grid-community/styles/ag-grid.css';
-import 'ag-grid-community/styles/ag-theme-alpine.css';
+// Note: AG Grid CSS is imported globally in main.tsx to avoid duplicates
 
 interface AdvancedGridProps {
   table: TableKey;
@@ -384,7 +382,7 @@ export default function AdvancedGrid({ table, churchId, search }: AdvancedGridPr
       </Typography>
 
       <Box
-        className="ag-theme-alpine"
+        className="ag-theme-quartz"
         sx={{
           width: '100%',
           height: '600px',
diff --git a/front-end/src/features/records/baptism/BaptismRecordEntryPage.tsx b/front-end/src/features/records/baptism/BaptismRecordEntryPage.tsx
index f8d14da2..39578f11 100644
--- a/front-end/src/features/records/baptism/BaptismRecordEntryPage.tsx
+++ b/front-end/src/features/records/baptism/BaptismRecordEntryPage.tsx
@@ -1,58 +1,206 @@
-import React, { useState } from 'react';
+import React, { useState, useEffect, useRef } from 'react';
 import { Snackbar, Alert } from '@mui/material';
 import { useNavigate } from 'react-router-dom';
+import { Formik, FormikProps, Form } from 'formik';
+import * as Yup from 'yup';
 import { BaptismRecordForm, BaptismRecordFormData } from './BaptismRecordForm';
 import { RecordFormShell } from '@/components/records/RecordFormShell';
+import { createRecord } from '@/records/api';
+import { RECORD_TYPES } from '@/records/constants';
 
 const initialFormData: BaptismRecordFormData = {
-  date_of_birth: '',
-  date_of_baptism: '',
-  record_type: 'Baptism',
   child_first_name: '',
   child_last_name: '',
+  date_of_birth: '',
   place_of_birth: '',
+  date_of_baptism: '',
+  church: '',
+  officiating_priest: '',
   father_name: '',
   mother_name: '',
   godparents: '',
-  officiating_priest: '',
-  church: '',
   register_number: '',
   notes: '',
 };
 
+// Validation schema matching the form requirements
+const validationSchema = Yup.object().shape({
+  child_first_name: Yup.string().required('First name is required'),
+  child_last_name: Yup.string().required('Last name is required'),
+  date_of_birth: Yup.string().required('Date of birth is required'),
+  date_of_baptism: Yup.string().required('Date of baptism is required'),
+  church: Yup.string().required('Church is required'),
+});
+
 export const BaptismRecordEntryPage: React.FC = () => {
   const navigate = useNavigate();
   const [formData, setFormData] = useState<BaptismRecordFormData>(initialFormData);
-  const [snackbar, setSnackbar] = useState({ open: false, message: '', severity: 'success' as 'success' | 'error' });
+  const [snackbar, setSnackbar] = useState({ 
+    open: false, 
+    message: '', 
+    severity: 'success' as 'success' | 'error' 
+  });
+  const [isSubmitting, setIsSubmitting] = useState(false);
+  const [hasUnsavedChanges, setHasUnsavedChanges] = useState(false);
+  const formikRef = useRef<FormikProps<BaptismRecordFormData>>(null);
+
+  // Mock data - replace with actual API calls or context
+  const [priestOptions, setPriestOptions] = useState<Array<{ id: number | string; name: string }>>([]);
+  const [churchOptions, setChurchOptions] = useState<Array<{ id: number | string; name: string }>>([]);
+  const [currentChurch, setCurrentChurch] = useState<string>('');
+
+  // Load options on mount (placeholder - replace with actual API calls)
+  useEffect(() => {
+    // TODO: Replace with actual API calls
+    // Example:
+    // fetchPriests().then(setPriestOptions);
+    // fetchChurches().then(setChurchOptions);
+    // getCurrentChurch().then(setCurrentChurch);
+    
+    // Mock data for now
+    setPriestOptions([
+      { id: 1, name: 'Fr. John Smith' },
+      { id: 2, name: 'Fr. Michael Johnson' },
+    ]);
+    setChurchOptions([
+      { id: 1, name: 'St. Peter and Paul Orthodox Church' },
+      { id: 2, name: 'Holy Trinity Orthodox Church' },
+    ]);
+  }, []);
 
   const handleChange = (field: keyof BaptismRecordFormData, value: string) => {
-    setFormData(prev => ({ ...prev, [field]: value }));
+    setFormData(prev => {
+      const newData = { ...prev, [field]: value };
+      // Check if there are unsaved changes
+      const hasChanges = JSON.stringify(newData) !== JSON.stringify(initialFormData);
+      setHasUnsavedChanges(hasChanges);
+      return newData;
+    });
+    
+    // Also update Formik if it exists
+    if (formikRef.current) {
+      formikRef.current.setFieldValue(field, value, false);
+    }
+  };
+
+  // Transform form data to backend payload shape
+  const transformToBackendPayload = (data: BaptismRecordFormData) => {
+    // Map to backend field names based on constants.js
+    // Backend expects: first_name, last_name, birth_date, reception_date, birthplace, 
+    //                  entry_type, sponsors, parents, clergy
+    return {
+      first_name: data.child_first_name,
+      last_name: data.child_last_name,
+      birth_date: data.date_of_birth,
+      reception_date: data.date_of_baptism,
+      birthplace: data.place_of_birth,
+      entry_type: 'Baptism', // Default to Baptism
+      sponsors: data.godparents, // Already serialized as "; " separated string
+      parents: data.father_name && data.mother_name 
+        ? `${data.father_name} and ${data.mother_name}`
+        : data.father_name || data.mother_name || '',
+      clergy: data.officiating_priest,
+      church: data.church,
+      register_number: data.register_number,
+      notes: data.notes,
+    };
   };
 
   const handleSubmit = async () => {
+    // Validate form
+    if (formikRef.current) {
+      const errors = await formikRef.current.validateForm();
+      if (Object.keys(errors).length > 0) {
+        formikRef.current.setTouched(
+          Object.keys(errors).reduce((acc, key) => {
+            acc[key] = true;
+            return acc;
+          }, {} as Record<string, boolean>)
+        );
+        setSnackbar({ 
+          open: true, 
+          message: 'Please fix the validation errors before submitting', 
+          severity: 'error' 
+        });
+        return;
+      }
+    }
+
+    setIsSubmitting(true);
     try {
-      // TODO: Implement API call to save record
-      console.log('Submitting baptism record:', formData);
-      setSnackbar({ open: true, message: 'Baptism record saved successfully', severity: 'success' });
+      const payload = transformToBackendPayload(formData);
+      await createRecord(RECORD_TYPES.BAPTISM, payload);
+      
+      setSnackbar({ 
+        open: true, 
+        message: 'Record saved successfully.', 
+        severity: 'success' 
+      });
+      setHasUnsavedChanges(false);
+      
       setTimeout(() => {
         navigate('/apps/records/baptism');
       }, 1500);
-    } catch (error) {
-      setSnackbar({ open: true, message: 'Failed to save record', severity: 'error' });
+    } catch (error: any) {
+      const errorMessage = error?.response?.data?.message || error?.message || 'Failed to save record';
+      setSnackbar({ 
+        open: true, 
+        message: errorMessage, 
+        severity: 'error' 
+      });
+    } finally {
+      setIsSubmitting(false);
     }
   };
 
   const handleSaveAndAddAnother = async () => {
+    // Validate form
+    if (formikRef.current) {
+      const errors = await formikRef.current.validateForm();
+      if (Object.keys(errors).length > 0) {
+        formikRef.current.setTouched(
+          Object.keys(errors).reduce((acc, key) => {
+            acc[key] = true;
+            return acc;
+          }, {} as Record<string, boolean>)
+        );
+        setSnackbar({ 
+          open: true, 
+          message: 'Please fix the validation errors before submitting', 
+          severity: 'error' 
+        });
+        return;
+      }
+    }
+
+    setIsSubmitting(true);
     try {
-      // TODO: Implement API call to save record
-      console.log('Submitting baptism record:', formData);
-      setSnackbar({ open: true, message: 'Baptism record saved successfully', severity: 'success' });
+      const payload = transformToBackendPayload(formData);
+      await createRecord(RECORD_TYPES.BAPTISM, payload);
+      
+      setSnackbar({ 
+        open: true, 
+        message: 'Record saved successfully.', 
+        severity: 'success' 
+      });
+      
       // Reset form for another entry
       setTimeout(() => {
         setFormData(initialFormData);
+        setHasUnsavedChanges(false);
+        if (formikRef.current) {
+          formikRef.current.resetForm({ values: initialFormData });
+        }
       }, 500);
-    } catch (error) {
-      setSnackbar({ open: true, message: 'Failed to save record', severity: 'error' });
+    } catch (error: any) {
+      const errorMessage = error?.response?.data?.message || error?.message || 'Failed to save record';
+      setSnackbar({ 
+        open: true, 
+        message: errorMessage, 
+        severity: 'error' 
+      });
+    } finally {
+      setIsSubmitting(false);
     }
   };
 
@@ -62,32 +210,93 @@ export const BaptismRecordEntryPage: React.FC = () => {
 
   return (
     <>
-      <RecordFormShell
-        title="Baptism Record Entry"
-        subtitle="Enter the details of the baptism ceremony"
-        showTitle={false}
-        footerActions={{
-          onCancel: handleCancel,
-          onSave: handleSubmit,
-          onSaveAndAddAnother: handleSaveAndAddAnother,
-          cancelLabel: 'Cancel',
-          saveLabel: 'Save Record',
-          saveAndAddLabel: 'Save & Add Another',
+      <Formik
+        innerRef={formikRef}
+        initialValues={formData}
+        validationSchema={validationSchema}
+        enableReinitialize
+        onSubmit={() => {
+          // Submission handled by handleSubmit
         }}
       >
-        <BaptismRecordForm
-          formData={formData}
-          onChange={handleChange}
-        />
-      </RecordFormShell>
+        {({ setFieldValue }) => {
+          // Enhanced onChange that also updates Formik
+          const enhancedOnChange = (field: keyof BaptismRecordFormData, value: string) => {
+            handleChange(field, value);
+            setFieldValue(field, value, false);
+          };
+
+          return (
+            <RecordFormShell
+              title="Add New Baptism Record"
+              subtitle="Enter the details of the baptism ceremony [NEW FORM SYSTEM ACTIVE]"
+              showTitle={true}
+              footerActions={{
+                onCancel: handleCancel,
+                onSave: handleSubmit,
+                onSaveAndAddAnother: handleSaveAndAddAnother,
+                cancelLabel: 'Cancel',
+                saveLabel: 'Save Record',
+                saveAndAddLabel: 'Save & Add Another',
+                loading: isSubmitting,
+                hasUnsavedChanges: hasUnsavedChanges,
+              }}
+            >
+              <Form>
+                <BaptismRecordForm
+                  formData={formData}
+                  onChange={enhancedOnChange}
+                  priestOptions={priestOptions}
+                  churchOptions={churchOptions}
+                  currentChurch={currentChurch}
+                />
+              </Form>
+            </RecordFormShell>
+          );
+        }}
+      </Formik>
 
       <Snackbar
         open={snackbar.open}
-        autoHideDuration={6000}
+        autoHideDuration={12000}
         onClose={() => setSnackbar(prev => ({ ...prev, open: false }))}
+        anchorOrigin={{ vertical: 'center', horizontal: 'center' }}
+        sx={{
+          position: 'fixed',
+          top: '50% !important',
+          left: '50% !important',
+          transform: 'translate(-50%, -50%) !important',
+          zIndex: 10000,
+          '& .MuiSnackbar-root': {
+            position: 'fixed',
+            top: '50% !important',
+            left: '50% !important',
+            transform: 'translate(-50%, -50%) !important',
+          }
+        }}
       >
-        <Alert severity={snackbar.severity}>{snackbar.message}</Alert>
+        <Alert 
+          severity={snackbar.severity}
+          onClose={() => setSnackbar(prev => ({ ...prev, open: false }))}
+          sx={{ 
+            minWidth: '400px',
+            maxWidth: '600px',
+            fontSize: '1.1rem',
+            padding: '16px 20px',
+            '& .MuiAlert-message': {
+              fontSize: '1.1rem',
+              fontWeight: 500,
+            },
+            '& .MuiAlert-icon': {
+              fontSize: '28px',
+            }
+          }}
+        >
+          {snackbar.message}
+        </Alert>
       </Snackbar>
     </>
   );
 };
+
+export default BaptismRecordEntryPage;
diff --git a/front-end/src/features/records/baptism/BaptismRecordForm.tsx b/front-end/src/features/records/baptism/BaptismRecordForm.tsx
index f79643f6..08ea0062 100644
--- a/front-end/src/features/records/baptism/BaptismRecordForm.tsx
+++ b/front-end/src/features/records/baptism/BaptismRecordForm.tsx
@@ -1,27 +1,33 @@
 import React from 'react';
+import { Box } from '@mui/material';
 import {
-  TextField,
-  Autocomplete,
-  MenuItem,
-  Select,
-  FormControl,
-  InputLabel,
-  Box,
-} from '@mui/material';
-import { RecordFormGrid } from '@/components/records/RecordFormGrid';
+  FormTextField,
+  FormDateField,
+  FormSelectSearch,
+  FormChipsField,
+  RecordSectionCard,
+} from '@/features/forms';
 
 export interface BaptismRecordFormData {
-  date_of_birth: string;
-  date_of_baptism: string;
-  record_type: string;
+  // Person section
   child_first_name: string;
   child_last_name: string;
+  date_of_birth: string;
   place_of_birth: string;
+  
+  // Baptism Event section
+  date_of_baptism: string;
+  church: string;
+  officiating_priest: string;
+  
+  // Family section
   father_name: string;
   mother_name: string;
+  
+  // Sponsors section (stored as string, managed as chips)
   godparents: string;
-  officiating_priest: string;
-  church: string;
+  
+  // Registry & Notes section
   register_number: string;
   notes: string;
 }
@@ -29,186 +35,152 @@ export interface BaptismRecordFormData {
 interface BaptismRecordFormProps {
   formData: BaptismRecordFormData;
   onChange: (field: keyof BaptismRecordFormData, value: string) => void;
-  priestOptions?: string[];
+  priestOptions?: Array<{ id: number | string; name: string }> | string[];
   churchOptions?: Array<{ id: number | string; name: string }>;
+  currentChurch?: string; // Pre-selected church from app context
   readOnly?: boolean;
 }
 
+
 export const BaptismRecordForm: React.FC<BaptismRecordFormProps> = ({
   formData,
   onChange,
   priestOptions = [],
   churchOptions = [],
+  currentChurch,
   readOnly = false,
 }) => {
-  const handleChange = (field: keyof BaptismRecordFormData) => (
-    event: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement> | { target: { value: string } }
-  ) => {
-    onChange(field, event.target.value);
-  };
-
-  const handleAutocompleteChange = (field: keyof BaptismRecordFormData) => (
-    _event: React.SyntheticEvent,
-    value: string | null
-  ) => {
-    onChange(field, value || '');
-  };
+  // Normalize priest options
+  const normalizedPriestOptions = React.useMemo(() => {
+    if (priestOptions.length === 0) return [];
+    return priestOptions.map((opt) =>
+      typeof opt === 'string' ? { id: opt, name: opt } : opt
+    );
+  }, [priestOptions]);
+
+  // Initialize church value with currentChurch if provided
+  const initialChurchValue = React.useMemo(() => {
+    return currentChurch || formData.church || '';
+  }, [currentChurch, formData.church]);
 
   return (
-    <RecordFormGrid>
-        <TextField
+    <>
+      {/* Person Section */}
+      <RecordSectionCard
+        title="Person"
+        helperText="Enter the personal information of the person being baptized"
+      >
+        <FormTextField
+          name="child_first_name"
           label="First Name"
-          value={formData.child_first_name}
-          onChange={handleChange('child_first_name')}
-          fullWidth
-          disabled={readOnly}
           required
+          disabled={readOnly}
+          placeholder="e.g., John"
         />
-
-        <TextField
+        <FormTextField
+          name="child_last_name"
           label="Last Name"
-          value={formData.child_last_name}
-          onChange={handleChange('child_last_name')}
-          fullWidth
-          disabled={readOnly}
           required
-        />
-
-        <TextField
-          label="mm / dd / yyyy"
-          type="date"
-          value={formData.date_of_birth}
-          onChange={handleChange('date_of_birth')}
-          InputLabelProps={{ shrink: true }}
-          fullWidth
           disabled={readOnly}
+          placeholder="e.g., Smith"
+        />
+        <FormDateField
+          name="date_of_birth"
+          label="Date of Birth"
           required
+          disabled={readOnly}
+          placeholder="MM/DD/YYYY"
         />
-
-        <TextField
-          label="mm / dd / yyyy"
-          type="date"
-          value={formData.date_of_baptism}
-          onChange={handleChange('date_of_baptism')}
-          InputLabelProps={{ shrink: true }}
-          fullWidth
+        <FormTextField
+          name="place_of_birth"
+          label="Place of Birth"
           disabled={readOnly}
+          placeholder="e.g., New York, NY"
+        />
+      </RecordSectionCard>
+
+      {/* Baptism Event Section */}
+      <RecordSectionCard
+        title="Baptism Event"
+        helperText="Enter the details of the baptism ceremony"
+      >
+        <FormDateField
+          name="date_of_baptism"
+          label="Date of Baptism"
           required
+          disabled={readOnly}
+          placeholder="MM/DD/YYYY"
         />
-
-        <TextField
-          label="Baptism"
-          value={formData.record_type}
-          onChange={handleChange('record_type')}
-          fullWidth
+        <FormSelectSearch
+          name="church"
+          label="Church"
+          options={churchOptions}
+          required
           disabled={readOnly}
-          placeholder="Baptism"
+          placeholder="Search or select a church..."
+          defaultValue={initialChurchValue}
         />
-
-        <TextField
-          label="Place of Birth"
-          value={formData.place_of_birth}
-          onChange={handleChange('place_of_birth')}
-          fullWidth
+        <FormSelectSearch
+          name="officiating_priest"
+          label="Priest"
+          options={normalizedPriestOptions}
+          required
           disabled={readOnly}
+          placeholder="Search or select a priest..."
         />
-
-        <TextField
+      </RecordSectionCard>
+
+      {/* Family Section */}
+      <RecordSectionCard
+        title="Family"
+        helperText="Enter the names of the parents"
+      >
+        <FormTextField
+          name="father_name"
           label="Father's Name"
-          value={formData.father_name}
-          onChange={handleChange('father_name')}
-          fullWidth
           disabled={readOnly}
+          placeholder="e.g., John Smith"
         />
-
-        <TextField
+        <FormTextField
+          name="mother_name"
           label="Mother's Name"
-          value={formData.mother_name}
-          onChange={handleChange('mother_name')}
-          fullWidth
           disabled={readOnly}
+          placeholder="e.g., Jane Smith"
         />
-
-        <TextField
+      </RecordSectionCard>
+
+      {/* Sponsors Section */}
+      <RecordSectionCard
+        title="Sponsors"
+        helperText="Enter the names of the godparents (press Enter or comma to add each name)"
+      >
+        <FormChipsField
+          name="godparents"
           label="Godparent Names"
-          value={formData.godparents}
-          onChange={handleChange('godparents')}
-          fullWidth
           disabled={readOnly}
-          placeholder="Enter godparent names, separated by commas"
+          placeholder="Type a name and press Enter or comma"
+          serializeFormat={(chips) => chips.join('; ')}
         />
-
-        <TextField
+      </RecordSectionCard>
+
+      {/* Registry & Notes Section */}
+      <RecordSectionCard
+        title="Registry & Notes"
+        helperText="Enter registry information and any additional notes"
+      >
+        <FormTextField
+          name="register_number"
           label="Registry Number"
-          value={formData.register_number}
-          onChange={handleChange('register_number')}
-          fullWidth
           disabled={readOnly}
+          placeholder="e.g., 2024-001"
         />
-
-        {priestOptions.length > 0 ? (
-          <Autocomplete
-            options={priestOptions}
-            value={formData.officiating_priest || null}
-            onChange={handleAutocompleteChange('officiating_priest')}
-            renderInput={(params) => (
-              <TextField
-                {...params}
-                label="Select a priest..."
-                disabled={readOnly}
-                required
-              />
-            )}
-            freeSolo
-            fullWidth
-            disabled={readOnly}
-          />
-        ) : (
-          <TextField
-            label="Select a priest..."
-            value={formData.officiating_priest}
-            onChange={handleChange('officiating_priest')}
-            fullWidth
-            disabled={readOnly}
-            required
-          />
-        )}
-
-        {churchOptions.length > 0 ? (
-          <Autocomplete
-            options={churchOptions.map(church => church.name)}
-            value={formData.church || null}
-            onChange={handleAutocompleteChange('church')}
-            renderInput={(params) => (
-              <TextField
-                {...params}
-                label="Church"
-                disabled={readOnly}
-              />
-            )}
-            freeSolo
-            fullWidth
-            disabled={readOnly}
-          />
-        ) : (
-          <TextField
-            label="Church"
-            value={formData.church}
-            onChange={handleChange('church')}
-            fullWidth
-            disabled={readOnly}
-          />
-        )}
-
         <Box sx={{ gridColumn: '1 / -1' }}>
-          <TextField
+          <FormTextField
+            name="notes"
             label="Notes"
-            value={formData.notes}
-            onChange={handleChange('notes')}
-            fullWidth
+            disabled={readOnly}
             multiline
             rows={5}
-            disabled={readOnly}
             placeholder="Additional notes or comments"
             sx={{
               '& .MuiOutlinedInput-root': {
@@ -223,6 +195,7 @@ export const BaptismRecordForm: React.FC<BaptismRecordFormProps> = ({
             }}
           />
         </Box>
-      </RecordFormGrid>
+      </RecordSectionCard>
+    </>
   );
 };
diff --git a/front-end/src/features/records/funeral/FuneralRecordEntryPage.tsx b/front-end/src/features/records/funeral/FuneralRecordEntryPage.tsx
index ef658be0..45916212 100644
--- a/front-end/src/features/records/funeral/FuneralRecordEntryPage.tsx
+++ b/front-end/src/features/records/funeral/FuneralRecordEntryPage.tsx
@@ -60,10 +60,42 @@ export const FuneralRecordEntryPage: React.FC = () => {
 
       <Snackbar
         open={snackbar.open}
-        autoHideDuration={6000}
+        autoHideDuration={12000}
         onClose={() => setSnackbar(prev => ({ ...prev, open: false }))}
+        anchorOrigin={{ vertical: 'center', horizontal: 'center' }}
+        sx={{
+          position: 'fixed',
+          top: '50% !important',
+          left: '50% !important',
+          transform: 'translate(-50%, -50%) !important',
+          zIndex: 10000,
+          '& .MuiSnackbar-root': {
+            position: 'fixed',
+            top: '50% !important',
+            left: '50% !important',
+            transform: 'translate(-50%, -50%) !important',
+          }
+        }}
       >
-        <Alert severity={snackbar.severity}>{snackbar.message}</Alert>
+        <Alert 
+          severity={snackbar.severity}
+          onClose={() => setSnackbar(prev => ({ ...prev, open: false }))}
+          sx={{ 
+            minWidth: '400px',
+            maxWidth: '600px',
+            fontSize: '1.1rem',
+            padding: '16px 20px',
+            '& .MuiAlert-message': {
+              fontSize: '1.1rem',
+              fontWeight: 500,
+            },
+            '& .MuiAlert-icon': {
+              fontSize: '28px',
+            }
+          }}
+        >
+          {snackbar.message}
+        </Alert>
       </Snackbar>
     </Container>
   );
diff --git a/front-end/src/features/records/marriage/MarriageRecordEntryPage.tsx b/front-end/src/features/records/marriage/MarriageRecordEntryPage.tsx
index ca3db342..9ad02b7d 100644
--- a/front-end/src/features/records/marriage/MarriageRecordEntryPage.tsx
+++ b/front-end/src/features/records/marriage/MarriageRecordEntryPage.tsx
@@ -61,10 +61,42 @@ export const MarriageRecordEntryPage: React.FC = () => {
 
       <Snackbar
         open={snackbar.open}
-        autoHideDuration={6000}
+        autoHideDuration={12000}
         onClose={() => setSnackbar(prev => ({ ...prev, open: false }))}
+        anchorOrigin={{ vertical: 'center', horizontal: 'center' }}
+        sx={{
+          position: 'fixed',
+          top: '50% !important',
+          left: '50% !important',
+          transform: 'translate(-50%, -50%) !important',
+          zIndex: 10000,
+          '& .MuiSnackbar-root': {
+            position: 'fixed',
+            top: '50% !important',
+            left: '50% !important',
+            transform: 'translate(-50%, -50%) !important',
+          }
+        }}
       >
-        <Alert severity={snackbar.severity}>{snackbar.message}</Alert>
+        <Alert 
+          severity={snackbar.severity}
+          onClose={() => setSnackbar(prev => ({ ...prev, open: false }))}
+          sx={{ 
+            minWidth: '400px',
+            maxWidth: '600px',
+            fontSize: '1.1rem',
+            padding: '16px 20px',
+            '& .MuiAlert-message': {
+              fontSize: '1.1rem',
+              fontWeight: 500,
+            },
+            '& .MuiAlert-icon': {
+              fontSize: '28px',
+            }
+          }}
+        >
+          {snackbar.message}
+        </Alert>
       </Snackbar>
     </Container>
   );
diff --git a/front-end/src/features/settings/ServiceManagement.tsx b/front-end/src/features/settings/ServiceManagement.tsx
index 94dbc839..a64a1c91 100755
--- a/front-end/src/features/settings/ServiceManagement.tsx
+++ b/front-end/src/features/settings/ServiceManagement.tsx
@@ -2128,11 +2128,40 @@ const ServiceManagement: React.FC = () => {
             {/* Success Snackbar */}
             <Snackbar
                 open={!!success}
-                autoHideDuration={6000}
+                autoHideDuration={12000}
                 onClose={() => setSuccess(null)}
-                anchorOrigin={{ vertical: 'bottom', horizontal: 'right' }}
+                anchorOrigin={{ vertical: 'center', horizontal: 'center' }}
+                sx={{
+                  position: 'fixed',
+                  top: '50% !important',
+                  left: '50% !important',
+                  transform: 'translate(-50%, -50%) !important',
+                  zIndex: 10000,
+                  '& .MuiSnackbar-root': {
+                    position: 'fixed',
+                    top: '50% !important',
+                    left: '50% !important',
+                    transform: 'translate(-50%, -50%) !important',
+                  }
+                }}
             >
-                <Alert severity="success" onClose={() => setSuccess(null)}>
+                <Alert 
+                  severity="success" 
+                  onClose={() => setSuccess(null)}
+                  sx={{ 
+                    minWidth: '400px',
+                    maxWidth: '600px',
+                    fontSize: '1.1rem',
+                    padding: '16px 20px',
+                    '& .MuiAlert-message': {
+                      fontSize: '1.1rem',
+                      fontWeight: 500,
+                    },
+                    '& .MuiAlert-icon': {
+                      fontSize: '28px',
+                    }
+                  }}
+                >
                     {success}
                 </Alert>
             </Snackbar>
diff --git a/front-end/src/features/tables/AGGridViewOnly/AGGridViewOnly.tsx b/front-end/src/features/tables/AGGridViewOnly/AGGridViewOnly.tsx
index 0f2a3e6a..9bcfe07d 100755
--- a/front-end/src/features/tables/AGGridViewOnly/AGGridViewOnly.tsx
+++ b/front-end/src/features/tables/AGGridViewOnly/AGGridViewOnly.tsx
@@ -32,8 +32,7 @@ import {
 import { devLogStateChange } from '@/utils/devLogger';
 
 // AG Grid CSS imports
-import 'ag-grid-community/styles/ag-grid.css';
-import 'ag-grid-community/styles/ag-theme-material.css';
+// Note: AG Grid CSS is imported globally in main.tsx to avoid duplicates
 
 /**
  * Custom cell renderer for colored fields
@@ -303,6 +302,7 @@ export const AGGridViewOnly: React.FC<AGGridViewOnlyProps> = ({
         }}
       >
         <AgGridReact
+          theme="legacy"
           rowData={rowData}
           columnDefs={columnDefs}
           defaultColDef={{
diff --git a/front-end/src/features/tables/AdvancedGridDialog.tsx b/front-end/src/features/tables/AdvancedGridDialog.tsx
index 5c5b5748..bc3d67e0 100755
--- a/front-end/src/features/tables/AdvancedGridDialog.tsx
+++ b/front-end/src/features/tables/AdvancedGridDialog.tsx
@@ -33,15 +33,7 @@ import { ColDef, GridReadyEvent, GridApi, ModuleRegistry, AllCommunityModule } f
 // For community version, we only register AllCommunityModule
 ModuleRegistry.registerModules([AllCommunityModule]);
 
-// Import AG Grid base styles (required for legacy theme)
-// Using side-effect imports that Vite can handle properly
-// Note: This import is needed when using theme="legacy"
-import 'ag-grid-community/styles/ag-grid.css';
-
-// Import AG Grid alpine theme (used as fallback, custom themes are in advanced-grid-themes.css)
-// Note: Only importing alpine as it's the most commonly used base theme
-import 'ag-grid-community/styles/ag-theme-alpine.css';
-
+// Note: AG Grid CSS is imported globally in main.tsx to avoid duplicates
 // Import custom themes (contains ag-theme-ocean-blue and other custom themes)
 import '../../styles/advanced-grid-themes.css';
 
@@ -149,10 +141,24 @@ export const AdvancedGridDialog: React.FC<AdvancedGridDialogProps> = ({
 
   // Get records for the active tab
   const getRecordsForTab = (tab: string): any[] => {
+    // Always prefer datasets when available (full dataset)
     if (datasets && datasets[tab as keyof GridDatasets]) {
-      return datasets[tab as keyof GridDatasets] || [];
+      const dataset = datasets[tab as keyof GridDatasets];
+      if (dataset && dataset.length > 0) {
+        if (import.meta.env.DEV) {
+          console.log(`✅ getRecordsForTab: Using datasets.${tab} (${dataset.length} records)`);
+        }
+        return dataset;
+      }
     }
     // Fallback to current records if datasets not provided
+    if (import.meta.env.DEV) {
+      console.warn(`⚠️ getRecordsForTab: Falling back to records prop for ${tab}`, {
+        datasetsAvailable: !!datasets,
+        datasetsTabLength: datasets?.[tab as keyof GridDatasets]?.length || 0,
+        recordsLength: records?.length || 0,
+      });
+    }
     return records || [];
   };
 
@@ -167,15 +173,25 @@ export const AdvancedGridDialog: React.FC<AdvancedGridDialogProps> = ({
   };
 
   // Prepare row data with fallback - MOVED UP to fix circular dependency
+  // Only calculate when dialog is open to avoid unnecessary computation
   const rowData = useMemo(() => {
+    // Skip calculation if dialog is not open (performance optimization)
+    if (!open) {
+      return [];
+    }
+    
     const tabRecords = getRecordsForTab(activeTab);
     if (!tabRecords || tabRecords.length === 0) {
-      console.log(`⚠️ No ${activeTab} records available for AG Grid`);
+      if (import.meta.env.DEV) {
+        console.log(`⚠️ AdvancedGridDialog: No ${activeTab} records available for AG Grid (open=${open})`);
+      }
       return [];
     }
-    console.log(`✅ Preparing ${tabRecords.length} ${activeTab} records for AG Grid`);
+    if (import.meta.env.DEV) {
+      console.log(`✅ AdvancedGridDialog: Preparing ${tabRecords.length} ${activeTab} records for AG Grid (open=${open})`);
+    }
     return tabRecords;
-  }, [activeTab, datasets, records]); // Include both datasets and records
+  }, [activeTab, datasets, records, open]); // Include open in dependencies
 
   // Handle tab change
   const handleTabChange = (event: React.SyntheticEvent, newValue: 'baptism' | 'marriage' | 'funeral') => {
@@ -197,14 +213,21 @@ export const AdvancedGridDialog: React.FC<AdvancedGridDialogProps> = ({
     }
   };
 
-  // Debug logging for records
+  // Debug logging for records - only when dialog is open
   React.useEffect(() => {
-    console.log('🔍 AdvancedGridDialog - Records received:', {
-      recordsLength: records?.length || 0,
-      records: records?.slice(0, 3), // Log first 3 records for debugging
-      open
-    });
-  }, [records, open]);
+    if (import.meta.env.DEV && open) {
+      console.log('🔍 AdvancedGridDialog - Records received:', {
+        recordsLength: records?.length || 0,
+        records: records?.slice(0, 3), // Log first 3 records for debugging
+        open,
+        datasets: {
+          baptism: datasets?.baptism?.length || 0,
+          marriage: datasets?.marriage?.length || 0,
+          funeral: datasets?.funeral?.length || 0,
+        }
+      });
+    }
+  }, [records, open, datasets]);
 
   // Update active tab when recordType prop changes
   React.useEffect(() => {
@@ -596,11 +619,11 @@ export const AdvancedGridDialog: React.FC<AdvancedGridDialogProps> = ({
         <Box sx={{ height: 'calc(100% - 80px)', width: '100%' }}>
           {/* Dynamic style tag to apply theme colors to AG Grid */}
           <style id={`ag-grid-theme-advanced-${activeTab}`}>
-            {`#ag-grid-container-advanced-${activeTab}.ag-theme-alpine{--ag-header-background-color:${enhancedTableState.tokens.headerBg}!important;--ag-header-foreground-color:${enhancedTableState.tokens.headerText}!important;--ag-border-color:${enhancedTableState.tokens.border}!important;--ag-odd-row-background-color:${enhancedTableState.tokens.rowOddBg}!important;--ag-background-color:${enhancedTableState.tokens.rowEvenBg}!important;--ag-data-color:${enhancedTableState.tokens.cellText}!important;--ag-row-hover-color:${enhancedTableState.tokens.accent}15!important;--ag-selected-row-background-color:${enhancedTableState.tokens.accent}20!important;--ag-alpine-active-color:${enhancedTableState.tokens.accent}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-header,#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-header-row,#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-header-row>div,#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-header-row>div>div{background-color:${enhancedTableState.tokens.headerBg}!important;color:${enhancedTableState.tokens.headerText}!important;border-color:${enhancedTableState.tokens.border}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-header-cell,#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-header-cell>div,#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-header-cell>div>div{background-color:${enhancedTableState.tokens.headerBg}!important;color:${enhancedTableState.tokens.headerText}!important;border-color:${enhancedTableState.tokens.border}!important;border-bottom-color:${enhancedTableState.tokens.border}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-header-cell-text,#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-header-cell-label,#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-header-cell-label span,#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-header-cell-label .ag-header-cell-text,#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-header-cell-label>span{color:${enhancedTableState.tokens.headerText}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-row-odd,#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-row-odd .ag-cell{background-color:${enhancedTableState.tokens.rowOddBg}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-row-even,#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-row-even .ag-cell{background-color:${enhancedTableState.tokens.rowEvenBg}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-cell{color:${enhancedTableState.tokens.cellText}!important;border-color:${enhancedTableState.tokens.border}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-row:hover{background-color:${enhancedTableState.tokens.accent}15!important}#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-row-selected{background-color:${enhancedTableState.tokens.accent}20!important}#ag-grid-container-advanced-${activeTab}.ag-theme-alpine .ag-header-cell-resize{background-color:${enhancedTableState.tokens.headerBg}!important}`}
+            {`#ag-grid-container-advanced-${activeTab}.ag-theme-quartz{--ag-header-background-color:${enhancedTableState.tokens.headerBg}!important;--ag-header-foreground-color:${enhancedTableState.tokens.headerText}!important;--ag-border-color:${enhancedTableState.tokens.border}!important;--ag-odd-row-background-color:${enhancedTableState.tokens.rowOddBg}!important;--ag-background-color:${enhancedTableState.tokens.rowEvenBg}!important;--ag-data-color:${enhancedTableState.tokens.cellText}!important;--ag-row-hover-color:${enhancedTableState.tokens.accent}15!important;--ag-selected-row-background-color:${enhancedTableState.tokens.accent}20!important;--ag-quartz-active-color:${enhancedTableState.tokens.accent}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-header,#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-header-row,#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-header-row>div,#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-header-row>div>div{background-color:${enhancedTableState.tokens.headerBg}!important;color:${enhancedTableState.tokens.headerText}!important;border-color:${enhancedTableState.tokens.border}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-header-cell,#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-header-cell>div,#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-header-cell>div>div{background-color:${enhancedTableState.tokens.headerBg}!important;color:${enhancedTableState.tokens.headerText}!important;border-color:${enhancedTableState.tokens.border}!important;border-bottom-color:${enhancedTableState.tokens.border}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-header-cell-text,#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-header-cell-label,#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-header-cell-label span,#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-header-cell-label .ag-header-cell-text,#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-header-cell-label>span{color:${enhancedTableState.tokens.headerText}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-row-odd,#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-row-odd .ag-cell{background-color:${enhancedTableState.tokens.rowOddBg}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-row-even,#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-row-even .ag-cell{background-color:${enhancedTableState.tokens.rowEvenBg}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-cell{color:${enhancedTableState.tokens.cellText}!important;border-color:${enhancedTableState.tokens.border}!important}#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-row:hover{background-color:${enhancedTableState.tokens.accent}15!important}#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-row-selected{background-color:${enhancedTableState.tokens.accent}20!important}#ag-grid-container-advanced-${activeTab}.ag-theme-quartz .ag-header-cell-resize{background-color:${enhancedTableState.tokens.headerBg}!important}`}
           </style>
           <div
             id={`ag-grid-container-advanced-${activeTab}`}
-            className="ag-theme-alpine"
+            className="ag-theme-quartz"
             style={{
               height: '100%',
               width: '100%'
diff --git a/front-end/src/layouts/full/vertical/header/Header.tsx b/front-end/src/layouts/full/vertical/header/Header.tsx
index 426f5b9b..bb3210cf 100755
--- a/front-end/src/layouts/full/vertical/header/Header.tsx
+++ b/front-end/src/layouts/full/vertical/header/Header.tsx
@@ -27,11 +27,14 @@ const Header = () => {
   // Default background for authenticated users is bgtiled.png
   const getBackground = () => {
     if (headerBackground) {
-      return `url(/images/bgtiled${headerBackground}.png) repeat`;
+      // Check if file exists in backgrounds folder, otherwise fallback to gradient
+      // Files are at /images/backgrounds/bgtiled{number}.png
+      return `url(/images/backgrounds/bgtiled${headerBackground}.png) repeat`;
     }
-    // For authenticated users, default to bgtiled.png
+    // For authenticated users, default to bgtiled.png (if exists) or gradient
     if (authenticated) {
-      return `url(/images/bgtiled.png) repeat`;
+      // Try default bgtiled.png, but fallback to gradient if not found
+      return `url(/images/backgrounds/bgtiled.png) repeat`;
     }
     // For unauthenticated users, use gradient
     return 'linear-gradient(135deg, #1a0d2e 0%, #2d1b4e 50%, #3d1f5d 100%)';
diff --git a/front-end/src/layouts/full/vertical/sidebar/MenuItems.ts b/front-end/src/layouts/full/vertical/sidebar/MenuItems.ts
index 19d87580..d0f7d21e 100755
--- a/front-end/src/layouts/full/vertical/sidebar/MenuItems.ts
+++ b/front-end/src/layouts/full/vertical/sidebar/MenuItems.ts
@@ -194,9 +194,9 @@ const Menuitems: MenuitemsType[] = [
   },
   {
     id: uniqueId(),
-    title: 'Enhanced Modern Dashboard',
+    title: 'User Dashboard',
     icon: IconLayoutDashboard,
-    href: '/dashboards/modern',
+    href: '/dashboards/user',
   },
   {
     id: uniqueId(),
@@ -446,6 +446,12 @@ const Menuitems: MenuitemsType[] = [
         icon: IconBorderAll,
         href: '/devel-tools/live-table-builder',
       },
+      {
+        id: uniqueId(),
+        title: 'Loading Demo',
+        icon: IconComponents,
+        href: '/apps/devel/loading-demo',
+      },
       {
         id: uniqueId(),
         title: 'OCR Studio',
@@ -470,6 +476,18 @@ const Menuitems: MenuitemsType[] = [
         icon: IconShield,
         href: '/devel-tools/om-permission-center',
       },
+      {
+        id: uniqueId(),
+        title: 'Interactive Report Jobs',
+        icon: IconFileDescription,
+        href: '/devel-tools/interactive-reports/jobs',
+      },
+      {
+        id: uniqueId(),
+        title: 'Build Info',
+        icon: IconGitBranch,
+        href: '/devel-tools/build-info',
+      },
       {
         id: uniqueId(),
         title: 'Component Library',
@@ -640,7 +658,90 @@ export const getMenuItems = (user: any) => {
     return dynamicMenuItems;
   }
   
-  // For non-admin users, show simplified menu with basic functionality
+  // For priest role, show User Dashboard menu
+  if (user && user.role === 'priest') {
+    return [
+      {
+        navlabel: true,
+        subheader: '📊 Dashboards',
+      },
+      {
+        id: uniqueId(),
+        title: 'User Dashboard',
+        icon: IconLayoutDashboard,
+        href: '/dashboards/user',
+      },
+      {
+        navlabel: true,
+        subheader: '⛪ Church',
+      },
+      {
+        id: uniqueId(),
+        title: 'Notes',
+        icon: IconNotes,
+        href: '/apps/notes',
+      },
+      {
+        id: uniqueId(),
+        title: 'Orthodox Calendar',
+        icon: IconCalendar,
+        href: '/apps/liturgical-calendar',
+      },
+      {
+        id: uniqueId(),
+        title: 'Records System',
+        icon: IconFileDescription,
+        href: '#',
+        children: [
+          // Modern Enhanced Records
+          {
+            id: uniqueId(),
+            title: 'Records',
+            icon: IconRocket,
+            href: '/apps/records/baptism',
+            chip: 'NEW',
+            chipColor: 'primary',
+          },
+          {
+            id: uniqueId(),
+            title: 'OCR Uploads',
+            icon: IconFileDescription,
+            href: `/devel/enhanced-ocr-uploader?church_id=${churchId}`,
+          },
+        ],
+      },
+      {
+        navlabel: true,
+        subheader: '💬 Social',
+      },
+      {
+        id: uniqueId(),
+        title: 'Email',
+        icon: IconMessage,
+        href: '/apps/email',
+      },
+      {
+        id: uniqueId(),
+        title: 'Friends',
+        icon: IconUserPlus,
+        href: '/social/friends',
+      },
+      {
+        id: uniqueId(),
+        title: 'Chat',
+        icon: IconMessage,
+        href: '/social/chat',
+      },
+      {
+        id: uniqueId(),
+        title: 'Notifications',
+        icon: IconBell,
+        href: '/social/notifications',
+      },
+    ];
+  }
+  
+  // For other non-admin users, show simplified menu with basic functionality
   return [
     {
       navlabel: true,
diff --git a/front-end/src/legacy/features/pages/pricing/Pricing.tsx b/front-end/src/legacy/features/pages/pricing/Pricing.tsx
index 69ed8fa0..0f8840a2 100755
--- a/front-end/src/legacy/features/pages/pricing/Pricing.tsx
+++ b/front-end/src/legacy/features/pages/pricing/Pricing.tsx
@@ -74,7 +74,7 @@ const pricing: PricingPlan[] = [
   {
     id: 2,
     package: 'Bronze',
-    monthlyplan: 10.99,
+    monthlyplan: ,
     avatar: pck2,
     badge: true,
     btntext: 'Choose Bronze',
@@ -104,7 +104,7 @@ const pricing: PricingPlan[] = [
   {
     id: 3,
     package: 'Gold',
-    monthlyplan: 22.99,
+    monthlyplan: ,
     avatar: pck3,
     badge: false,
     btntext: 'Choose Gold',
diff --git a/front-end/src/main.tsx b/front-end/src/main.tsx
index 1f60a1f9..086b5b17 100755
--- a/front-end/src/main.tsx
+++ b/front-end/src/main.tsx
@@ -11,9 +11,17 @@ import './utils/i18n';
 import './index.css';
 // Temporarily commented out to avoid conflicts with Tailwind CSS
 // import 'bootstrap/dist/css/bootstrap.min.css';
+
+// Import AG Grid styles globally (required for CSS theme approach)
+// This must be imported exactly once to avoid error #239 (Theming API and CSS both used)
+// Using ag-theme-quartz as the base theme (legacy CSS approach only)
+import 'ag-grid-community/styles/ag-grid.css';
+import 'ag-grid-community/styles/ag-theme-quartz.css';
+
 import { setupGlobalErrorHandlers } from './shared/lib/globalErrorHandler';
 import './shared/lib/debugLogger'; // Initialize debug logger
 import { registerAgGridModulesOnce } from './agGridModules';
+import { setupDevErrorHandlers } from './shared/lib/devErrorHandler';
 
 // Register AG Grid modules before React renders
 // This prevents error #272: "No AG Grid modules are registered"
@@ -22,6 +30,11 @@ registerAgGridModulesOnce();
 // Initialize global error handlers for OMAI
 setupGlobalErrorHandlers();
 
+// Initialize DEV-only error handlers for debugging React errors
+if (import.meta.env.DEV) {
+  setupDevErrorHandlers();
+}
+
 // Auto-refresh functionality disabled - removed to prevent refresh loops
 // Users can manually refresh when needed
 
diff --git a/front-end/src/routes/Router.tsx b/front-end/src/routes/Router.tsx
index 048b7a38..18ea1cd2 100755
--- a/front-end/src/routes/Router.tsx
+++ b/front-end/src/routes/Router.tsx
@@ -8,6 +8,7 @@ import AdminErrorBoundary from '../components/ErrorBoundary/AdminErrorBoundary';
 import HeadlineSourcePicker from '../components/headlines/HeadlineSourcePicker';
 import SmartRedirect from '../components/routing/SmartRedirect';
 import AppErrorBoundary from '@/shared/ui/AppErrorBoundary';
+import { RecordsRouteErrorBoundary } from '@/shared/ui/RecordsRouteErrorBoundary';
 import Loadable from '../layouts/full/shared/loadable/Loadable';
 import OMDeps from '../tools/om-deps/OM-deps';
 
@@ -29,6 +30,7 @@ const ImageAI = Loadable(lazy(() => import('../features/apps/image-ai/ImageAI'))
 
 /* ****Developer Tools***** */
 const SiteStructureVisualizer = Loadable(lazy(() => import('../tools/SiteStructureVisualizer')));
+const LoadingDemo = Loadable(lazy(() => import('../pages/devel/LoadingDemo')));
 const OmtraceConsole = Loadable(lazy(() => import('../components/ui-tools/omtrace/OmtraceConsole')));
 const Kanban = Loadable(lazy(() => import('../features/apps/kanban/Kanban')));
 const InvoiceList = Loadable(lazy(() => import('../features/apps/invoice/List')));
@@ -104,6 +106,11 @@ const AdminLogs = Loadable(lazy(() => import('../features/admin/admin/AdminLogs'
 const ActivityLogs = Loadable(lazy(() => import('../features/admin/admin/ActivityLogs')));
 const MenuPermissions = Loadable(lazy(() => import('../features/admin/admin/MenuPermissions')));
 const MenuManagement = Loadable(lazy(() => import('../features/admin/admin/MenuManagement')));
+const ChurchPublishingGuide = Loadable(lazy(() => import('../features/admin/components/ChurchPublishingGuide')));
+const InteractiveReportReview = Loadable(lazy(() => import('../features/records-centralized/components/interactiveReport/InteractiveReportReview')));
+const RecipientSubmissionPage = Loadable(lazy(() => import('../features/records-centralized/components/interactiveReport/RecipientSubmissionPage')));
+const InteractiveReportJobsPage = Loadable(lazy(() => import('../features/devel-tools/interactive-reports/InteractiveReportJobsPage')));
+const BuildInfoPage = Loadable(lazy(() => import('../features/devel-tools/build-info/BuildInfoPage')));
 const OrthodMetricsAdmin = Loadable(lazy(() => import('../features/admin/admin/OrthodoxMetricsAdmin')));
 const AIAdminPanel = Loadable(lazy(() => import('../features/admin/ai/AIAdminPanel')));
 const OMAIUltimateLogger = Loadable(lazy(() => import('../features/devel-tools/om-ultimatelogger/LoggerDashboard')));
@@ -111,7 +118,7 @@ const ScriptRunner = Loadable(lazy(() => import('../features/admin/admin/ScriptR
 const SuperDashboard = Loadable(lazy(() => import('../features/devel-tools/users-customized-landing/SuperDashboard')));
 const SuperDashboardCustomizer = Loadable(lazy(() => import('../features/devel-tools/users-customized-landing/SuperDashboardCustomizer')));
 const UserDashboard = Loadable(lazy(() => import('../features/devel-tools/users-customized-landing/UserDashboard')));
-const RouterMenuStudio = Loadable(lazy(() => import('../features/router-menu-studio/RouterMenuStudioPage')));
+const RouterMenuStudio = Loadable(lazy(() => import('../features/devel-tools/RouterMenuStudio/RouterMenuStudioPage')));
 const DynamicRecordsInspector = Loadable(lazy(() => import('../features/records-centralized/components/dynamic/DynamicRecordsInspector')));
 const RefactorConsole = Loadable(lazy(() => import('../features/devel-tools/refactor-console/RefactorConsole')));
 const OMSpecDocumentation = Loadable(lazy(() => import('../features/devel-tools/system-documentation/om-spec/OMSpecDocumentation')));
@@ -149,7 +156,7 @@ const OMLearn = Loadable(lazy(() => import('../modules/OMLearn/OMLearn')));
 const BuildConsole = Loadable(lazy(() => import('../features/devel-tools/om-build-console/BuildConsole')));
 
 // Records Pages
-import { RECORDS_LEGACY_ENABLED } from '../config/featureFlags';
+import { RECORDS_LEGACY_ENABLED, isInteractiveReportRecipientsEnabled } from '../config/featureFlags';
 
 const ChurchAdminList = Loadable(lazy(() => import('../features/admin/admin/ChurchAdminList')));
 const ChurchAdminPanel = Loadable(lazy(() => import('../features/admin/admin/ChurchAdminPanelWorking')));
@@ -272,7 +279,7 @@ const Router = [
         )
       },
       {
-        path: '/dashboards/orthodmetrics',
+        path: '/dashboards/orthodoxmetrics',
         exact: true,
         element: (
           <ProtectedRoute requiredPermission="admin_dashboard">
@@ -284,7 +291,7 @@ const Router = [
         path: '/dashboards/user',
         exact: true,
         element: (
-          <ProtectedRoute>
+          <ProtectedRoute requiredRole={['priest']}>
             <AppErrorBoundary>
               <UserDashboard />
             </AppErrorBoundary>
@@ -627,6 +634,14 @@ const Router = [
           </ProtectedRoute>
         )
       },
+      {
+        path: '/admin/publishing-guide',
+        element: (
+          <ProtectedRoute requiredRole={['admin', 'super_admin', 'church_admin']}>
+            <ChurchPublishingGuide />
+          </ProtectedRoute>
+        )
+      },
       {
         path: '/admin/tools/survey',
         element: (
@@ -820,6 +835,16 @@ const Router = [
           </ProtectedRoute>
         )
       },
+      {
+        path: '/apps/devel/loading-demo',
+        element: (
+          <ProtectedRoute requiredRole={['super_admin', 'admin']}>
+            <AdminErrorBoundary>
+              <LoadingDemo />
+            </AdminErrorBoundary>
+          </ProtectedRoute>
+        )
+      },
       {
         path: '/devel-tools/live-table-builder',
         element: (
@@ -840,6 +865,22 @@ const Router = [
           </ProtectedRoute>
         )
       },
+      {
+        path: '/devel-tools/interactive-reports/jobs',
+        element: (
+          <ProtectedRoute requiredRole={['admin', 'super_admin', 'church_admin', 'priest']}>
+            <InteractiveReportJobsPage />
+          </ProtectedRoute>
+        )
+      },
+      {
+        path: '/devel-tools/build-info',
+        element: (
+          <ProtectedRoute requiredRole={['admin', 'super_admin', 'church_admin', 'priest']}>
+            <BuildInfoPage />
+          </ProtectedRoute>
+        )
+      },
       {
         path: '/apps/ocr-upload',
         element: (
@@ -1043,7 +1084,9 @@ const Router = [
         path: '/apps/records/baptism',
         element: (
           <ProtectedRoute requiredRole={['admin', 'super_admin', 'church_admin', 'priest', 'deacon', 'editor']}>
-            <BaptismRecordsPage />
+            <RecordsRouteErrorBoundary>
+              <BaptismRecordsPage />
+            </RecordsRouteErrorBoundary>
           </ProtectedRoute>
         )
       },
@@ -1051,7 +1094,9 @@ const Router = [
         path: '/apps/records/marriage',
         element: (
           <ProtectedRoute requiredRole={['admin', 'super_admin', 'church_admin', 'priest', 'deacon', 'editor']}>
-            <MarriageRecordsPage />
+            <RecordsRouteErrorBoundary>
+              <MarriageRecordsPage />
+            </RecordsRouteErrorBoundary>
           </ProtectedRoute>
         )
       },
@@ -1059,7 +1104,9 @@ const Router = [
         path: '/apps/records/funeral',
         element: (
           <ProtectedRoute requiredRole={['admin', 'super_admin', 'church_admin', 'priest', 'deacon', 'editor']}>
-            <FuneralRecordsPage />
+            <RecordsRouteErrorBoundary>
+              <FuneralRecordsPage />
+            </RecordsRouteErrorBoundary>
           </ProtectedRoute>
         )
       },
@@ -1095,6 +1142,14 @@ const Router = [
          </ProtectedRoute>
        )
      },
+     {
+       path: '/apps/records/interactive-reports/:reportId',
+       element: (
+         <ProtectedRoute requiredRole={['admin', 'super_admin', 'church_admin', 'priest']}>
+           <InteractiveReportReview />
+         </ProtectedRoute>
+       )
+     },
      // { path: '/apps/records/manager', element: <DynamicRecordsManager /> },
      // { path: '/apps/records/modern-manager', element: <ModernDynamicRecordsManager /> },
      // { path: '/apps/records/editable', element: <EditableRecordPage /> },
@@ -1145,6 +1200,11 @@ const Router = [
       // Public Task Pages (no authentication required)
       { path: '/tasks', element: <PublicTasksListPage /> },
       { path: '/tasks/:id', element: <PublicTaskDetailPage /> },
+      // Interactive Report Recipient Page (public, token-based)
+      // Feature flag: interactiveReports.enableRecipientPages
+      ...(isInteractiveReportRecipientsEnabled() ? [
+        { path: '/r/interactive/:token', element: <RecipientSubmissionPage /> },
+      ] : []),
       { path: '*', element: <Navigate to="/auth/404" /> },
     ],
   },
diff --git a/front-end/src/store/enhancedTableStore.ts b/front-end/src/store/enhancedTableStore.ts
index 0c014996..96188535 100755
--- a/front-end/src/store/enhancedTableStore.ts
+++ b/front-end/src/store/enhancedTableStore.ts
@@ -66,119 +66,119 @@ export interface EnhancedTableState {
   actionButtonConfigs?: ActionButtonConfigs; // Button configurations
 }
 
-// Light mode themes
+// Light mode themes - Neutral colors
 export const THEME_MAP: Record<LiturgicalThemeKey, ThemeTokens> = {
   orthodox_traditional: {
-    headerBg: '#bd56fa',
+    headerBg: '#6b7280',
     headerText: '#ffffff',
-    rowOddBg: '#fafafa',
+    rowOddBg: '#f9fafb',
     rowEvenBg: '#ffffff',
-    border: '#e0e0e0',
-    accent: '#bd56fa',
-    cellText: '#212121',
+    border: '#e5e7eb',
+    accent: '#6b7280',
+    cellText: '#1f2937',
   },
   great_lent: {
-    headerBg: '#4a148c',
+    headerBg: '#6b7280',
     headerText: '#ffffff',
-    rowOddBg: '#f3e5f5',
+    rowOddBg: '#f9fafb',
     rowEvenBg: '#ffffff',
-    border: '#ce93d8',
-    accent: '#7b1fa2',
-    cellText: '#4a148c',
+    border: '#e5e7eb',
+    accent: '#6b7280',
+    cellText: '#1f2937',
   },
   pascha: {
-    headerBg: '#d32f2f',
+    headerBg: '#6b7280',
     headerText: '#ffffff',
-    rowOddBg: '#ffebee',
+    rowOddBg: '#f9fafb',
     rowEvenBg: '#ffffff',
-    border: '#ffcdd2',
-    accent: '#c62828',
-    cellText: '#b71c1c',
+    border: '#e5e7eb',
+    accent: '#6b7280',
+    cellText: '#1f2937',
   },
   nativity: {
-    headerBg: '#2e7d32',
+    headerBg: '#6b7280',
     headerText: '#ffffff',
-    rowOddBg: '#e8f5e8',
+    rowOddBg: '#f9fafb',
     rowEvenBg: '#ffffff',
-    border: '#c8e6c9',
-    accent: '#388e3c',
-    cellText: '#1b5e20',
+    border: '#e5e7eb',
+    accent: '#6b7280',
+    cellText: '#1f2937',
   },
   palm_sunday: {
-    headerBg: '#558b2f',
+    headerBg: '#6b7280',
     headerText: '#ffffff',
-    rowOddBg: '#f1f8e9',
+    rowOddBg: '#f9fafb',
     rowEvenBg: '#ffffff',
-    border: '#dcedc8',
-    accent: '#689f38',
-    cellText: '#33691e',
+    border: '#e5e7eb',
+    accent: '#6b7280',
+    cellText: '#1f2937',
   },
   theotokos_feasts: {
-    headerBg: '#1565c0',
+    headerBg: '#6b7280',
     headerText: '#ffffff',
-    rowOddBg: '#e3f2fd',
+    rowOddBg: '#f9fafb',
     rowEvenBg: '#ffffff',
-    border: '#bbdefb',
-    accent: '#1976d2',
-    cellText: '#0d47a1',
+    border: '#e5e7eb',
+    accent: '#6b7280',
+    cellText: '#1f2937',
   },
 };
 
-// Dark mode themes - same accent colors but dark backgrounds
+// Dark mode themes - Neutral colors
 export const THEME_MAP_DARK: Record<LiturgicalThemeKey, ThemeTokens> = {
   orthodox_traditional: {
-    headerBg: '#bd56fa',
+    headerBg: '#4b5563',
     headerText: '#ffffff',
-    rowOddBg: '#1e1e1e',
-    rowEvenBg: '#2d2d2d',
-    border: '#424242',
-    accent: '#bd56fa',
-    cellText: '#e0e0e0',
+    rowOddBg: '#1f2937',
+    rowEvenBg: '#111827',
+    border: '#374151',
+    accent: '#4b5563',
+    cellText: '#f3f4f6',
   },
   great_lent: {
-    headerBg: '#4a148c',
+    headerBg: '#4b5563',
     headerText: '#ffffff',
-    rowOddBg: '#1a1a2e',
-    rowEvenBg: '#252538',
-    border: '#6a1b9a',
-    accent: '#7b1fa2',
-    cellText: '#ce93d8',
+    rowOddBg: '#1f2937',
+    rowEvenBg: '#111827',
+    border: '#374151',
+    accent: '#4b5563',
+    cellText: '#f3f4f6',
   },
   pascha: {
-    headerBg: '#d32f2f',
+    headerBg: '#4b5563',
     headerText: '#ffffff',
-    rowOddBg: '#2d1a1a',
-    rowEvenBg: '#3d2525',
-    border: '#c62828',
-    accent: '#c62828',
-    cellText: '#ffcdd2',
+    rowOddBg: '#1f2937',
+    rowEvenBg: '#111827',
+    border: '#374151',
+    accent: '#4b5563',
+    cellText: '#f3f4f6',
   },
   nativity: {
-    headerBg: '#2e7d32',
+    headerBg: '#4b5563',
     headerText: '#ffffff',
-    rowOddBg: '#1a2d1a',
-    rowEvenBg: '#253d25',
-    border: '#388e3c',
-    accent: '#388e3c',
-    cellText: '#c8e6c9',
+    rowOddBg: '#1f2937',
+    rowEvenBg: '#111827',
+    border: '#374151',
+    accent: '#4b5563',
+    cellText: '#f3f4f6',
   },
   palm_sunday: {
-    headerBg: '#558b2f',
+    headerBg: '#4b5563',
     headerText: '#ffffff',
-    rowOddBg: '#1f2d1a',
-    rowEvenBg: '#2a3d25',
-    border: '#689f38',
-    accent: '#689f38',
-    cellText: '#dcedc8',
+    rowOddBg: '#1f2937',
+    rowEvenBg: '#111827',
+    border: '#374151',
+    accent: '#4b5563',
+    cellText: '#f3f4f6',
   },
   theotokos_feasts: {
-    headerBg: '#1565c0',
+    headerBg: '#4b5563',
     headerText: '#ffffff',
-    rowOddBg: '#1a1f2d',
-    rowEvenBg: '#252a3d',
-    border: '#1976d2',
-    accent: '#1976d2',
-    cellText: '#bbdefb',
+    rowOddBg: '#1f2937',
+    rowEvenBg: '#111827',
+    border: '#374151',
+    accent: '#4b5563',
+    cellText: '#f3f4f6',
   },
 };
 
diff --git a/front-end/src/styles/advanced-grid-themes.css b/front-end/src/styles/advanced-grid-themes.css
index f0f91588..12e75f3b 100755
--- a/front-end/src/styles/advanced-grid-themes.css
+++ b/front-end/src/styles/advanced-grid-themes.css
@@ -1,5 +1,23 @@
 /* Custom Advanced Grid Themes */
 
+/* AG Grid Container Sizing Guardrails - Critical for proper grid rendering */
+/* These rules ensure AG Grid containers have explicit height/width and can shrink in flex layouts */
+.ag-theme-alpine,
+.ag-theme-alpine-dark,
+.ag-theme-quartz,
+.ag-theme-quartz-dark {
+  height: 100%;
+  width: 100%;
+  min-height: 0;
+  min-width: 0;
+}
+
+.ag-root-wrapper {
+  height: 100%;
+  min-height: 0;
+  min-width: 0;
+}
+
 /* Theme 2: Forest Green - Natural and Calming */
 .ag-theme-forest-green {
   --ag-background-color: #ffffff;
diff --git a/front-end/src/utils/liveTableTemplates.ts b/front-end/src/utils/liveTableTemplates.ts
deleted file mode 100644
index dcd99f95..00000000
--- a/front-end/src/utils/liveTableTemplates.ts
+++ /dev/null
@@ -1,263 +0,0 @@
-/**
- * Live Table Builder - Template Storage Utilities
- * Manages reusable table templates in localStorage
- */
-
-import type { TableState } from '../features/devel-tools/live-table-builder/types';
-
-export type RecordType = 'baptism' | 'marriage' | 'funeral';
-export type Locale = 'en' | 'gr' | 'ru' | 'ro' | 'ka' | string;
-
-export interface Template {
-  name: string;
-  tableState: TableState;
-  recordType?: RecordType;
-  locale?: Locale;
-  createdAt: string; // ISO string
-  updatedAt: string; // ISO string
-}
-
-export interface TemplatesStore {
-  [name: string]: Template;
-}
-
-const STORAGE_KEY = 'om.liveTableBuilder.templates.v1';
-
-/**
- * Get all templates from localStorage
- */
-export function getAllTemplates(): TemplatesStore {
-  try {
-    const stored = localStorage.getItem(STORAGE_KEY);
-    if (!stored) return {};
-    
-    const parsed = JSON.parse(stored) as TemplatesStore;
-    // Validate structure
-    if (typeof parsed !== 'object' || parsed === null) return {};
-    
-    return parsed;
-  } catch (error) {
-    console.error('Failed to load templates:', error);
-    return {};
-  }
-}
-
-/**
- * Save a template
- */
-export function saveTemplate(template: Template): void {
-  const templates = getAllTemplates();
-  templates[template.name] = {
-    ...template,
-    updatedAt: new Date().toISOString(),
-  };
-  
-  try {
-    localStorage.setItem(STORAGE_KEY, JSON.stringify(templates));
-  } catch (error) {
-    console.error('Failed to save template:', error);
-    throw new Error('Failed to save template to localStorage');
-  }
-}
-
-/**
- * Get a template by name
- */
-export function getTemplate(name: string): Template | null {
-  const templates = getAllTemplates();
-  return templates[name] || null;
-}
-
-/**
- * Delete a template
- */
-export function deleteTemplate(name: string): boolean {
-  const templates = getAllTemplates();
-  if (!(name in templates)) return false;
-  
-  delete templates[name];
-  
-  try {
-    localStorage.setItem(STORAGE_KEY, JSON.stringify(templates));
-    return true;
-  } catch (error) {
-    console.error('Failed to delete template:', error);
-    return false;
-  }
-}
-
-/**
- * Check if a template exists
- */
-export function templateExists(name: string): boolean {
-  const templates = getAllTemplates();
-  return name in templates;
-}
-
-/**
- * Get all template names
- */
-export function getTemplateNames(): string[] {
-  const templates = getAllTemplates();
-  return Object.keys(templates).sort();
-}
-
-/**
- * Export all templates as JSON
- */
-export function exportTemplates(): string {
-  const templates = getAllTemplates();
-  return JSON.stringify(templates, null, 2);
-}
-
-/**
- * Import templates from JSON
- */
-export interface ImportResult {
-  imported: number;
-  overwritten: number;
-  skipped: number;
-  errors: string[];
-}
-
-export function importTemplates(
-  jsonText: string,
-  options: {
-    overwriteConflicts?: boolean;
-    skipConflicts?: boolean;
-  } = {}
-): ImportResult {
-  const result: ImportResult = {
-    imported: 0,
-    overwritten: 0,
-    skipped: 0,
-    errors: [],
-  };
-
-  try {
-    const imported = JSON.parse(jsonText) as TemplatesStore;
-    
-    if (typeof imported !== 'object' || imported === null || Array.isArray(imported)) {
-      result.errors.push('Invalid template format: expected object');
-      return result;
-    }
-
-    const existing = getAllTemplates();
-    const merged: TemplatesStore = { ...existing };
-
-    for (const [name, template] of Object.entries(imported)) {
-      // Validate template structure
-      if (!template || typeof template !== 'object') {
-        result.errors.push(`Invalid template structure for "${name}"`);
-        continue;
-      }
-
-      if (!template.name || !template.tableState) {
-        result.errors.push(`Missing required fields in template "${name}"`);
-        continue;
-      }
-
-      // Ensure name matches key
-      template.name = name;
-
-      // Ensure timestamps
-      if (!template.createdAt) {
-        template.createdAt = new Date().toISOString();
-      }
-      if (!template.updatedAt) {
-        template.updatedAt = new Date().toISOString();
-      }
-
-      // Handle conflicts
-      if (name in existing) {
-        if (options.skipConflicts) {
-          result.skipped++;
-          continue;
-        }
-        if (options.overwriteConflicts) {
-          result.overwritten++;
-        } else {
-          // Default: skip if not explicitly overwriting
-          result.skipped++;
-          continue;
-        }
-      }
-
-      merged[name] = template;
-      result.imported++;
-    }
-
-    // Save merged templates
-    try {
-      localStorage.setItem(STORAGE_KEY, JSON.stringify(merged));
-    } catch (error) {
-      result.errors.push('Failed to save imported templates to localStorage');
-      console.error('Failed to save templates:', error);
-    }
-  } catch (error) {
-    result.errors.push(`Failed to parse JSON: ${error instanceof Error ? error.message : 'Unknown error'}`);
-  }
-
-  return result;
-}
-
-/**
- * Create standard record templates
- */
-export function createStandardTemplates(): void {
-  const standardHeaders = [
-    'id',
-    'church_id',
-    'entry_no',
-    'date',
-    'first_name',
-    'last_name',
-    'father_name',
-    'mother_name',
-    'godfather_name',
-    'godmother_name',
-    'notes',
-  ];
-
-  const recordTypes: RecordType[] = ['baptism', 'marriage', 'funeral'];
-  const locales: Locale[] = ['en', 'gr'];
-
-  const now = new Date().toISOString();
-
-  for (const locale of locales) {
-    for (const recordType of recordTypes) {
-      const name = `${locale}_${recordType}_records`;
-      
-      // Create columns from standard headers
-      const columns = standardHeaders.map((header, index) => ({
-        id: `col_${index}`,
-        label: header,
-      }));
-
-      // Create one empty row
-      const rows = [
-        {
-          id: 'row_0',
-          cells: columns.reduce((acc, col) => {
-            acc[col.id] = '';
-            return acc;
-          }, {} as Record<string, string>),
-        },
-      ];
-
-      const template: Template = {
-        name,
-        tableState: {
-          data: { columns, rows },
-          version: '1',
-        },
-        recordType,
-        locale,
-        createdAt: now,
-        updatedAt: now,
-      };
-
-      saveTemplate(template);
-    }
-  }
-}
diff --git a/front-end/vite.config.ts b/front-end/vite.config.ts
index 3ec9d3bf..9f953bd8 100755
--- a/front-end/vite.config.ts
+++ b/front-end/vite.config.ts
@@ -5,7 +5,14 @@ import fs from 'fs/promises';
 import svgr from '@svgr/rollup';
 
 // https://vitejs.dev/config/
-export default defineConfig(({ mode }) => ({
+export default defineConfig(({ mode }) => {
+  // Enable sourcemaps for debug builds (staging/debug environment)
+  // This helps map minified React errors to actual TSX lines
+  // Note: In vite.config.ts, we can use process.env because it runs in Node.js
+  // But in browser code, we must use import.meta.env
+  const isDebugBuild = mode === 'development' || process.env.VITE_DEBUG_BUILD === 'true';
+  
+  return {
     base: '/', // Ensure assets are loaded from root
     cacheDir: 'node_modules/.vite', // Enable build cache
     resolve: {
@@ -81,11 +88,12 @@ export default defineConfig(({ mode }) => ({
     plugins: [svgr(), react()],
     build: {
         minify: mode === 'production', // Only minify in production
-        sourcemap: true, // Enable sourcemaps for debugging circular dependencies
+        sourcemap: isDebugBuild || mode === 'development', // Enable sourcemaps for debug builds and dev
         target: mode === 'production' ? 'es2015' : 'esnext', // Use modern JS in dev
         cssCodeSplit: true, // Enable CSS code splitting to handle AG Grid CSS properly
-        // Enable incremental builds - only rebuild changed files
-        emptyOutDir: false, // Don't empty dist on each build (allows incremental)
+        // CRITICAL: Always empty dist directory to prevent stale assets
+        // This ensures users always get the latest build, not cached old files
+        emptyOutDir: true, // Always clean dist before build to prevent stale assets
         commonjsOptions: {
             include: [/node_modules/],
             transformMixedEsModules: true,
@@ -121,6 +129,7 @@ export default defineConfig(({ mode }) => ({
             interval: 100, // Faster polling for changes
         },
         proxy: {
+            // Proxy /api/* to backend
             '/api': {
                 target: 'http://127.0.0.1:3001',
                 changeOrigin: true,
@@ -130,27 +139,32 @@ export default defineConfig(({ mode }) => ({
                         console.log('Proxy error:', err);
                     });
                     proxy.on('proxyReq', (proxyReq, req, _res) => {
-                        console.log('Proxying request:', req.method, req.url, '→', proxyReq.getHeader('host'));
+                        // Only log API requests, not static assets
+                        if (req.url?.startsWith('/api')) {
+                            console.log('Proxying request:', req.method, req.url, '→', proxyReq.getHeader('host'));
+                        }
                     });
                 }
             },
-            '/images': {
-                target: 'http://localhost:3001',
+            // Proxy /r/interactive/* to backend (public recipient routes)
+            '/r/interactive': {
+                target: 'http://127.0.0.1:3001',
                 changeOrigin: true,
                 secure: false,
                 configure: (proxy) => {
                     proxy.on('error', (err, _req, _res) => {
                         console.log('Proxy error:', err);
                     });
-                    proxy.on('proxyReq', (proxyReq, req, _res) => {
-                        console.log('Proxying image request:', req.method, req.url, '→', proxyReq.getHeader('host'));
-                    });
                 }
-            }
+            },
+            // /images/*, /assets/*, and all other routes are served directly from Vite public/ directory
+            // Vite automatically serves files from public/ at the root path - NO PROXY NEEDED
+            // Explicitly do NOT proxy /images, /assets, or any other static paths
         }
     },
     preview: {
         port: 5174,
         host: '0.0.0.0'
     },
-}));
+  };
+});
diff --git a/om_church_46 - activity_log.png b/om_church_46 - activity_log.png
deleted file mode 100755
index 33114263..00000000
Binary files a/om_church_46 - activity_log.png and /dev/null differ
diff --git a/om_church_46 - baptism_records.png b/om_church_46 - baptism_records.png
deleted file mode 100755
index 78c33369..00000000
Binary files a/om_church_46 - baptism_records.png and /dev/null differ
diff --git a/om_church_46 - change_log.png b/om_church_46 - change_log.png
deleted file mode 100755
index ebf1d734..00000000
Binary files a/om_church_46 - change_log.png and /dev/null differ
diff --git a/om_church_46 - funeral_records.png b/om_church_46 - funeral_records.png
deleted file mode 100755
index 881f4f31..00000000
Binary files a/om_church_46 - funeral_records.png and /dev/null differ
diff --git a/om_church_46 - marriage_records.png b/om_church_46 - marriage_records.png
deleted file mode 100755
index fe87cdc8..00000000
Binary files a/om_church_46 - marriage_records.png and /dev/null differ
diff --git a/om_church_46 - ocr_draft_records.png b/om_church_46 - ocr_draft_records.png
deleted file mode 100755
index 04245168..00000000
Binary files a/om_church_46 - ocr_draft_records.png and /dev/null differ
diff --git a/om_church_46 - ocr_finalize_history.png b/om_church_46 - ocr_finalize_history.png
deleted file mode 100755
index d7fa350e..00000000
Binary files a/om_church_46 - ocr_finalize_history.png and /dev/null differ
diff --git a/om_church_46 - ocr_fused_drafts.png b/om_church_46 - ocr_fused_drafts.png
deleted file mode 100755
index dbf2d179..00000000
Binary files a/om_church_46 - ocr_fused_drafts.png and /dev/null differ
diff --git a/om_church_46 - ocr_jobs.png b/om_church_46 - ocr_jobs.png
deleted file mode 100755
index bfb01410..00000000
Binary files a/om_church_46 - ocr_jobs.png and /dev/null differ
diff --git a/om_church_46 - ocr_mappings.png b/om_church_46 - ocr_mappings.png
deleted file mode 100755
index 5632bb04..00000000
Binary files a/om_church_46 - ocr_mappings.png and /dev/null differ
diff --git a/om_church_46 - ocr_settings.png b/om_church_46 - ocr_settings.png
deleted file mode 100755
index e681f61c..00000000
Binary files a/om_church_46 - ocr_settings.png and /dev/null differ
diff --git a/om_church_46 - record_table.png b/om_church_46 - record_table.png
deleted file mode 100755
index 5fdb143d..00000000
Binary files a/om_church_46 - record_table.png and /dev/null differ
diff --git a/om_church_46.png b/om_church_46.png
deleted file mode 100755
index 61ae772d..00000000
Binary files a/om_church_46.png and /dev/null differ
diff --git a/orthodoxmetrics_db - churches.png b/orthodoxmetrics_db - churches.png
deleted file mode 100755
index d8d382b2..00000000
Binary files a/orthodoxmetrics_db - churches.png and /dev/null differ
diff --git a/server/routes/admin/churches.js b/server/routes/admin/churches.js
index 0f0b8cd4..e85b5433 100755
--- a/server/routes/admin/churches.js
+++ b/server/routes/admin/churches.js
@@ -2339,6 +2339,84 @@ router.get('/:id/dynamic-records-config', async (req, res) => {
   }
 });
 
+/**
+ * GET /api/admin/churches/wizard/template-profiles
+ * Get available template profiles for wizard (not church-based)
+ */
+router.get('/wizard/template-profiles', async (req, res) => {
+  try {
+    // Get global templates from orthodoxmetrics_db.templates
+    const [templates] = await getAppPool().query(`
+      SELECT slug, name, record_type, description, fields, is_global
+      FROM \`${APP_DB}\`.templates
+      WHERE is_global = 1
+      ORDER BY record_type, name
+    `);
+
+    // Parse JSON fields
+    const parsedTemplates = templates.map(t => ({
+      ...t,
+      fields: typeof t.fields === 'string' ? JSON.parse(t.fields) : t.fields
+    }));
+
+    // Build template profiles
+    const profiles = [
+      {
+        id: 'start_from_scratch',
+        name: 'Start from Scratch',
+        description: 'Create a new church without using any template',
+        templates: {}
+      },
+      {
+        id: 'standard_en',
+        name: 'Standard English (Default)',
+        description: 'Creates baptism/marriage/funeral record tables from global English templates',
+        templates: {
+          baptism: parsedTemplates.find(t => t.record_type === 'baptism' && t.slug.includes('en'))?.slug || 'en_baptism_records',
+          marriage: parsedTemplates.find(t => t.record_type === 'marriage' && t.slug.includes('en'))?.slug || 'en_marriage_records',
+          funeral: parsedTemplates.find(t => t.record_type === 'funeral' && t.slug.includes('en'))?.slug || 'en_funeral_records'
+        }
+      }
+    ];
+
+    // Optionally add other language profiles if templates exist
+    const languages = ['gr', 'ru', 'ro', 'sr'];
+    for (const lang of languages) {
+      const langTemplates = {
+        baptism: parsedTemplates.find(t => t.record_type === 'baptism' && t.slug.includes(lang))?.slug,
+        marriage: parsedTemplates.find(t => t.record_type === 'marriage' && t.slug.includes(lang))?.slug,
+        funeral: parsedTemplates.find(t => t.record_type === 'funeral' && t.slug.includes(lang))?.slug
+      };
+      
+      if (langTemplates.baptism || langTemplates.marriage || langTemplates.funeral) {
+        profiles.push({
+          id: `standard_${lang}`,
+          name: `Standard ${lang.toUpperCase()}`,
+          description: `Creates record tables from global ${lang.toUpperCase()} templates`,
+          templates: langTemplates
+        });
+      }
+    }
+
+    res.json({
+      success: true,
+      profiles,
+      available_templates: parsedTemplates.map(t => ({
+        slug: t.slug,
+        name: t.name,
+        record_type: t.record_type
+      }))
+    });
+  } catch (error) {
+    console.error('❌ Error fetching template profiles:', error);
+    res.status(500).json({
+      success: false,
+      error: 'Failed to fetch template profiles',
+      details: process.env.NODE_ENV === 'development' ? error.message : undefined
+    });
+  }
+});
+
 // POST /api/admin/churches/wizard - Create church via wizard interface
 router.post('/wizard', upload.single('logo'), async (req, res) => {
   const requestId = Math.random().toString(36).substring(7);
@@ -2367,7 +2445,10 @@ router.post('/wizard', upload.single('logo'), async (req, res) => {
       auto_setup_standard = false,
       generate_components = false,
       record_types = ['baptism', 'marriage', 'funeral'],
-      template_style = 'orthodox_traditional'
+      template_style = 'orthodox_traditional',
+      // New: Template profile selection (replaces template_church_id)
+      template_profile_id = null,
+      selected_templates = null // Object: { baptism: 'en_baptism_records', marriage: 'en_marriage_records', funeral: 'en_funeral_records' }
     } = req.body;
 
     const finalChurchName = church_name || name;
@@ -2441,6 +2522,25 @@ router.post('/wizard', upload.single('logo'), async (req, res) => {
     };
 
     // Prepare template options
+    // If template_profile_id is provided, expand it to selected_templates
+    let finalSelectedTemplates = selected_templates;
+    if (template_profile_id && !selected_templates) {
+      if (template_profile_id === 'standard_en') {
+        finalSelectedTemplates = {
+          baptism: 'en_baptism_records',
+          marriage: 'en_marriage_records',
+          funeral: 'en_funeral_records'
+        };
+      } else if (template_profile_id.startsWith('standard_')) {
+        const lang = template_profile_id.replace('standard_', '');
+        finalSelectedTemplates = {
+          baptism: `${lang}_baptism_records`,
+          marriage: `${lang}_marriage_records`,
+          funeral: `${lang}_funeral_records`
+        };
+      }
+    }
+
     const templateOptions = {
       setupTemplates: setup_templates,
       autoSetupStandard: auto_setup_standard,
@@ -2448,7 +2548,8 @@ router.post('/wizard', upload.single('logo'), async (req, res) => {
       recordTypes: record_types,
       templateStyle: template_style,
       includeGlobalTemplates: true,
-      createCustomTemplates: false
+      createCustomTemplates: false,
+      selectedTemplates: finalSelectedTemplates // Pass template slugs for table provisioning
     };
 
     // Use enhanced church setup service
@@ -2485,5 +2586,307 @@ router.post('/wizard', upload.single('logo'), async (req, res) => {
     });
   }
 });
+
+/**
+ * Export table schema and field mapper config to global template
+ * POST /api/admin/churches/:id/export-template
+ */
+router.post('/:id/export-template', async (req, res) => {
+  try {
+    const churchId = parseInt(req.params.id);
+    const { table, language = 'en', template_slug, template_name, overwrite = false } = req.body;
+
+    if (!table) {
+      return res.status(400).json({
+        success: false,
+        error: 'Table name is required'
+      });
+    }
+
+    // Validate church exists and user has access
+    const [churches] = await getAppPool().query(
+      'SELECT id, church_name, database_name, preferred_language FROM churches WHERE id = ?',
+      [churchId]
+    );
+
+    if (churches.length === 0) {
+      return res.status(404).json({
+        success: false,
+        error: 'Church not found'
+      });
+    }
+
+    const church = churches[0];
+    if (!church.database_name) {
+      return res.status(400).json({
+        success: false,
+        error: 'Church database not configured'
+      });
+    }
+
+    // Determine record type from table name
+    let recordType = 'custom';
+    if (table === 'baptism_records') recordType = 'baptism';
+    else if (table === 'marriage_records') recordType = 'marriage';
+    else if (table === 'funeral_records') recordType = 'funeral';
+
+    // Generate slug if not provided
+    const slug = template_slug || `${language}_${table}`;
+    const name = template_name || `${language.charAt(0).toUpperCase() + language.slice(1)} ${recordType.charAt(0).toUpperCase() + recordType.slice(1)} Records`;
+
+    // Get church database connection
+    const churchDb = await getChurchDbConnection(church.database_name);
+    const connection = await churchDb.getConnection();
+
+    try {
+      // 1. Fetch table schema from INFORMATION_SCHEMA
+      const [columns] = await connection.execute(`
+        SELECT 
+          COLUMN_NAME as column_name,
+          DATA_TYPE as data_type,
+          COLUMN_TYPE as column_type,
+          IS_NULLABLE as is_nullable,
+          COLUMN_DEFAULT as column_default,
+          COLUMN_KEY as column_key,
+          EXTRA as extra,
+          ORDINAL_POSITION as ordinal_position
+        FROM INFORMATION_SCHEMA.COLUMNS
+        WHERE TABLE_SCHEMA = ?
+          AND TABLE_NAME = ?
+        ORDER BY ORDINAL_POSITION
+      `, [church.database_name, table]);
+
+      if (columns.length === 0) {
+        return res.status(404).json({
+          success: false,
+          error: `Table ${table} not found in church database`
+        });
+      }
+
+      // 2. Fetch field mapper config from orthodoxmetrics_db
+      let mappings = {};
+      let fieldSettings = {
+        visibility: {},
+        sortable: {},
+        default_sort_field: null,
+        default_sort_direction: 'asc'
+      };
+
+      try {
+        const [mappingRows] = await getAppPool().query(`
+          SELECT mapping_json, field_settings_json
+          FROM church_field_mappings
+          WHERE church_id = ? AND table_name = ?
+        `, [churchId, table]);
+
+        if (mappingRows.length > 0) {
+          const row = mappingRows[0];
+          if (row.mapping_json) {
+            mappings = typeof row.mapping_json === 'string' 
+              ? JSON.parse(row.mapping_json) 
+              : row.mapping_json;
+          }
+          if (row.field_settings_json) {
+            fieldSettings = typeof row.field_settings_json === 'string'
+              ? JSON.parse(row.field_settings_json)
+              : row.field_settings_json;
+          }
+        }
+      } catch (err) {
+        console.warn('⚠️ Could not fetch field mapper config:', err.message);
+        // Continue with defaults
+      }
+
+      // 3. Build template fields JSON (versioned schema)
+      const templateFields = {
+        version: 1,
+        source: {
+          church_id: churchId,
+          table: table,
+          exported_at: new Date().toISOString()
+        },
+        columns: columns
+          .filter(col => col.column_name !== 'id' && col.column_name !== 'church_id') // Skip auto-generated fields
+          .map(col => {
+            // Map SQL types to template field types
+            let sqlType = col.column_type || col.data_type;
+            let nullable = col.is_nullable === 'YES';
+            
+            // Infer field type from column name/type
+            let type = 'string';
+            const colName = col.column_name.toLowerCase();
+            if (colName.includes('date') || colName.includes('time')) {
+              type = 'date';
+            } else if (colName.includes('id') || colName.includes('count') || colName.includes('number')) {
+              type = 'number';
+            } else if (sqlType && (sqlType.includes('INT') || sqlType.includes('DECIMAL') || sqlType.includes('FLOAT'))) {
+              type = 'number';
+            } else if (sqlType && sqlType.includes('TEXT')) {
+              type = 'text';
+            } else if (sqlType && sqlType.includes('BOOLEAN') || sqlType.includes('TINYINT(1)')) {
+              type = 'boolean';
+            }
+
+            return {
+              name: col.column_name,
+              label: mappings[col.column_name] || col.column_name.replace(/_/g, ' ').replace(/\b\w/g, l => l.toUpperCase()),
+              sqlType: sqlType,
+              nullable: nullable,
+              sqlTypeInferred: false, // We have actual schema
+              type: type,
+              required: !nullable,
+              default: col.column_default || null
+            };
+          }),
+        ui: {
+          visibility: fieldSettings.visibility || {},
+          sortable: fieldSettings.sortable || {},
+          default_sort_field: fieldSettings.default_sort_field || null,
+          default_sort_direction: fieldSettings.default_sort_direction || 'asc'
+        }
+      };
+
+      // 4. Check if template already exists
+      const [existing] = await getAppPool().query(
+        'SELECT slug FROM templates WHERE slug = ?',
+        [slug]
+      );
+
+      if (existing.length > 0 && !overwrite) {
+        return res.status(409).json({
+          success: false,
+          error: 'Template already exists',
+          slug: slug,
+          message: 'Set overwrite=true to update existing template'
+        });
+      }
+
+      // 5. Convert columns to Live Table Builder format (flat array)
+      // Live Table Builder expects fields to be an array of { column, label, type, required }
+      const fieldsArray = templateFields.columns.map((col, index) => ({
+        column: col.name || `col_${index}`, // Use column name as stable ID
+        label: col.label || col.name,
+        type: col.type || 'string',
+        required: col.required || false
+      }));
+
+      // Store both formats:
+      // - fields: flat array for Live Table Builder compatibility
+      // - templateFields: full versioned structure for provisioning (store in description or separate field)
+      // For now, store the flat array as fields (Live Table Builder format)
+      // The full versioned structure can be reconstructed from fields if needed
+
+      // 6. Upsert template
+      const templateData = {
+        name: name,
+        slug: slug,
+        record_type: recordType,
+        description: `Exported from church ${churchId} (${church.church_name}) table ${table}. Full schema: ${JSON.stringify(templateFields)}`,
+        fields: JSON.stringify(fieldsArray), // Store as flat array for Live Table Builder
+        grid_type: 'aggrid',
+        layout_type: 'table',
+        theme: null,
+        is_global: 1,
+        church_id: null,
+        is_editable: true
+      };
+
+      if (existing.length > 0) {
+        // Update existing
+        await getAppPool().query(`
+          UPDATE templates
+          SET name = ?, record_type = ?, description = ?, fields = ?, updated_at = NOW()
+          WHERE slug = ?
+        `, [templateData.name, templateData.record_type, templateData.description, templateData.fields, slug]);
+      } else {
+        // Insert new
+        await getAppPool().query(`
+          INSERT INTO templates (name, slug, record_type, description, fields, grid_type, layout_type, theme, is_global, church_id, is_editable, created_at, updated_at)
+          VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, NOW(), NOW())
+        `, [
+          templateData.name,
+          templateData.slug,
+          templateData.record_type,
+          templateData.description,
+          templateData.fields,
+          templateData.grid_type,
+          templateData.layout_type,
+          templateData.theme,
+          templateData.is_global,
+          templateData.church_id,
+          templateData.is_editable
+        ]);
+      }
+
+      console.log(`✅ Exported template: ${slug} with ${fieldsArray.length} fields`);
+
+      console.log(`✅ Exported template: ${slug} from church ${churchId} table ${table}`);
+
+      res.json({
+        success: true,
+        slug: slug,
+        template: {
+          name: name,
+          slug: slug,
+          record_type: recordType,
+          fields_count: fieldsArray.length
+        },
+        message: existing.length > 0 ? 'Template updated' : 'Template created'
+      });
+
+    } finally {
+      connection.release();
+    }
+
+  } catch (error) {
+    console.error('❌ Error exporting template:', error);
+    res.status(500).json({
+      success: false,
+      error: 'Failed to export template',
+      details: process.env.NODE_ENV === 'development' ? error.message : undefined
+    });
+  }
+});
+
+/**
+ * Verify history tables exist for all record tables
+ * GET /api/admin/churches/:id/verify-history-tables
+ */
+router.get('/:id/verify-history-tables', async (req, res) => {
+  try {
+    const churchId = parseInt(req.params.id);
+
+    // Validate church exists
+    const [churches] = await getAppPool().query(
+      'SELECT id, database_name FROM churches WHERE id = ?',
+      [churchId]
+    );
+
+    if (churches.length === 0) {
+      return res.status(404).json({
+        success: false,
+        error: 'Church not found'
+      });
+    }
+
+    const { verifyHistoryTables } = require('../services/recordHistoryLogger');
+    const results = await verifyHistoryTables(churchId);
+
+    res.json({
+      success: true,
+      ...results,
+      all_present: results.missing.length === 0
+    });
+
+  } catch (error) {
+    console.error('❌ Error verifying history tables:', error);
+    res.status(500).json({
+      success: false,
+      error: 'Failed to verify history tables',
+      details: process.env.NODE_ENV === 'development' ? error.message : undefined
+    });
+  }
+});
+
 module.exports = router;
 
diff --git a/test-image.png b/test-image.png
deleted file mode 100755
index 1ecece5e..00000000
Binary files a/test-image.png and /dev/null differ
